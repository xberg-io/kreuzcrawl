// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ae26a5a0bf8af26ab622b06ae2b51bdbbcc6d8a700492f01240d5e8c57727402
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.kreuzcrawl;

import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import java.util.Optional;

/**
 * A single page interaction action.
 *
 * Actions are serialized with a {@code type} tag using camelCase naming,
 * except {@code ExecuteJs} which is explicitly renamed to {@code "executeJs"}.
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = false)
@JsonSubTypes({
    @JsonSubTypes.Type(value = PageAction.Click.class, name = "click"),
    @JsonSubTypes.Type(value = PageAction.TypeText.class, name = "type"),
    @JsonSubTypes.Type(value = PageAction.Press.class, name = "press"),
    @JsonSubTypes.Type(value = PageAction.Scroll.class, name = "scroll"),
    @JsonSubTypes.Type(value = PageAction.Wait.class, name = "wait"),
    @JsonSubTypes.Type(value = PageAction.Screenshot.class, name = "screenshot"),
    @JsonSubTypes.Type(value = PageAction.ExecuteJs.class, name = "executeJs"),
    @JsonSubTypes.Type(value = PageAction.Scrape.class, name = "scrape")
})
@com.fasterxml.jackson.annotation.JsonIgnoreProperties(ignoreUnknown = true)
public sealed interface PageAction {

    /** Click on an element matching the given CSS selector. */
    record Click(@JsonProperty("selector") String selector) implements PageAction { }

    /** Type text into an element matching the given CSS selector. */
    record TypeText(
        @JsonProperty("selector") String selector,
        @JsonProperty("text") String text
    ) implements PageAction {
    }

    /** Press a keyboard key (e.g. "Enter", "Tab", "Escape"). */
    record Press(@JsonProperty("key") String key) implements PageAction { }

    /** Scroll the page or a specific element. */
    record Scroll(
        @JsonProperty("direction") ScrollDirection direction,
        @JsonProperty("selector") Optional<String> selector,
        @JsonProperty("amount") Optional<Long> amount
    ) implements PageAction {
    }

    /** Wait for a duration or for an element to appear. */
    record Wait(
        @JsonProperty("milliseconds") Optional<Long> milliseconds,
        @JsonProperty("selector") Optional<String> selector
    ) implements PageAction {
    }

    /** Take a screenshot of the current page. */
    record Screenshot(@JsonProperty("full_page") Optional<Boolean> fullPage) implements PageAction { }

    /** Execute arbitrary JavaScript in the page context. */
    record ExecuteJs(@JsonProperty("script") String script) implements PageAction { }

    /** Scrape the current page HTML. */
    record Scrape() implements PageAction {
    }
}
