// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:75e7429577b1fa0fbaf5063764aee06ce7a0d1fc37f1bc2109b4736fb0b09e90
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
package dev.kreuzberg.kreuzcrawl;

import java.util.List;
import java.util.Optional;
import com.fasterxml.jackson.annotation.JsonProperty;

/**
 * Content extraction and conversion configuration.
 *
 * Controls how HTML is converted to the output format. Uses html-to-markdown-rs
 * as the conversion engine for all formats (markdown, plain text, djot).
 */
public record ContentConfig(
		/**
		 * Output format: {@code "markdown"} (default), {@code "plain"}, {@code "djot"}.
		 */
		@JsonProperty("output_format") String outputFormat,
		/**
		 * Preprocessing aggressiveness: {@code "minimal"}, {@code "standard"}
		 * (default), {@code "aggressive"}.
		 */
		@JsonProperty("preprocessing_preset") String preprocessingPreset,
		/**
		 * Remove navigation elements (nav, breadcrumbs, menus). Default: {@code true}.
		 */
		@JsonProperty("remove_navigation") boolean removeNavigation,
		/** Remove form elements. Default: {@code true}. */
		@JsonProperty("remove_forms") boolean removeForms,
		/** HTML tag names to strip (render children only, remove the tag wrapper). */
		@JsonProperty("strip_tags") List<String> stripTags,
		/** HTML tag names to preserve as raw HTML in output. */
		@JsonProperty("preserve_tags") List<String> preserveTags,
		/** CSS selectors for elements to exclude entirely (element + all content). */
		@JsonProperty("exclude_selectors") List<String> excludeSelectors,
		/** Skip image elements in output. Default: {@code false}. */
		@JsonProperty("skip_images") boolean skipImages,
		/** Max DOM traversal depth. Prevents stack overflow on deeply nested HTML. */
		@JsonProperty("max_depth") Optional<Long> maxDepth,
		/** Enable line wrapping. Default: {@code false}. */
		boolean wrap,
		/** Wrap width when {@code wrap} is enabled. Default: {@code 80}. */
		@JsonProperty("wrap_width") long wrapWidth,
		/** Include document structure tree in output. Default: {@code true}. */
		@JsonProperty("include_document_structure") boolean includeDocumentStructure) {
	public static ContentConfigBuilder builder() {
		return new ContentConfigBuilder();
	}
}
