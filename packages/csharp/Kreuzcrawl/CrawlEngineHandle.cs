// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:088d4d001703f4451d0d6d988742109d7a33bc2f233502af8ae4373f9460362b
#nullable enable

using System;
using Microsoft.Win32.SafeHandles;
using System.Runtime.InteropServices;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Collections.Generic;
using System.Threading.Tasks;
using System.Threading;
using System.Runtime.CompilerServices;

namespace Kreuzcrawl;

internal sealed class CrawlEngineHandleSafeHandle : SafeHandle
{
    internal CrawlEngineHandleSafeHandle(IntPtr handle) : base(IntPtr.Zero, true)
    {
        SetHandle(handle);
    }

    public override bool IsInvalid => handle == IntPtr.Zero;

    protected override bool ReleaseHandle()
    {
        NativeMethods.CrawlEngineHandleFree(handle);
        return true;
    }
}
/// <summary>/// Opaque handle to a configured crawl engine./// /// Constructed via `create_engine` with an optional `CrawlConfig`./// Default implementations for all pluggable components are used internally./// </summary>
public sealed class CrawlEngineHandle : IDisposable
{
    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault
    };

    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) },
    };
    private readonly CrawlEngineHandleSafeHandle _safeHandle;

    internal CrawlEngineHandle(IntPtr handle)
    {
        _safeHandle = new CrawlEngineHandleSafeHandle(handle);
    }

    internal IntPtr Handle => _safeHandle.DangerousGetHandle();

    public void Dispose() => _safeHandle.Dispose();


    /// <summary>
    /// Stream a single-URL crawl, yielding `CrawlEvent`s as pages are processed.
    ///
    /// Returns an async stream that emits one event per crawled page, plus a
    /// terminal `Complete` event. On per-URL failure during the crawl, emits an
    /// `Error` event followed by `Complete`. The stream item type is wrapped in
    /// a `Result` to surface transport-level errors; today every emit is `Ok`.
    /// </summary>
    public async IAsyncEnumerable<CrawlEvent> CrawlStreamAsync(
        CrawlStreamRequest req,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonSerializationOptions);
        var reqHandle = NativeMethods.CrawlStreamRequestFromJson(reqJson);
        var streamHandle = NativeMethods.CrawlEngineHandleCrawlStreamStart(Handle, reqHandle);
        if (streamHandle == IntPtr.Zero)
        {
            NativeMethods.CrawlStreamRequestFree(reqHandle);
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error";
            throw new KreuzcrawlException(ec, msg);
        }
        try
        {
            while (true)
            {
                cancellationToken.ThrowIfCancellationRequested();
                var chunkPtr = NativeMethods.CrawlEngineHandleCrawlStreamNext(streamHandle);
                if (chunkPtr == IntPtr.Zero)
                {
                    var ec = NativeMethods.LastErrorCode();
                    if (ec != 0)
                    {
                        var ctxPtr = NativeMethods.LastErrorContext();
                        var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error";
                        throw new KreuzcrawlException(ec, msg);
                    }
                    yield break;
                }
                var jsonPtr = NativeMethods.CrawlEventToJson(chunkPtr);
                var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
                NativeMethods.FreeString(jsonPtr);
                NativeMethods.CrawlEventFree(chunkPtr);
                var chunk = JsonSerializer.Deserialize<CrawlEvent>(json ?? "null", JsonOptions)!;
                yield return chunk;
                await Task.Yield();
            }
        }
        finally
        {
            NativeMethods.CrawlEngineHandleCrawlStreamFree(streamHandle);
            NativeMethods.CrawlStreamRequestFree(reqHandle);
        }
    }

    /// <summary>
    /// Stream a multi-URL crawl, yielding `CrawlEvent`s across all seeds.
    ///
    /// Returns an async stream that emits one event per crawled page across all
    /// seeds, plus terminal `Complete` and `Error` events as appropriate. The
    /// stream item type is wrapped in a `Result` to surface transport-level
    /// errors; today every emit is `Ok`.
    /// </summary>
    public async IAsyncEnumerable<CrawlEvent> BatchCrawlStreamAsync(
        BatchCrawlStreamRequest req,
        [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var reqJson = JsonSerializer.Serialize(req, JsonSerializationOptions);
        var reqHandle = NativeMethods.BatchCrawlStreamRequestFromJson(reqJson);
        var streamHandle = NativeMethods.CrawlEngineHandleBatchCrawlStreamStart(Handle, reqHandle);
        if (streamHandle == IntPtr.Zero)
        {
            NativeMethods.BatchCrawlStreamRequestFree(reqHandle);
            var ec = NativeMethods.LastErrorCode();
            var ctxPtr = NativeMethods.LastErrorContext();
            var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error";
            throw new KreuzcrawlException(ec, msg);
        }
        try
        {
            while (true)
            {
                cancellationToken.ThrowIfCancellationRequested();
                var chunkPtr = NativeMethods.CrawlEngineHandleBatchCrawlStreamNext(streamHandle);
                if (chunkPtr == IntPtr.Zero)
                {
                    var ec = NativeMethods.LastErrorCode();
                    if (ec != 0)
                    {
                        var ctxPtr = NativeMethods.LastErrorContext();
                        var msg = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(ctxPtr) ?? "Unknown error";
                        throw new KreuzcrawlException(ec, msg);
                    }
                    yield break;
                }
                var jsonPtr = NativeMethods.CrawlEventToJson(chunkPtr);
                var json = global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
                NativeMethods.FreeString(jsonPtr);
                NativeMethods.CrawlEventFree(chunkPtr);
                var chunk = JsonSerializer.Deserialize<CrawlEvent>(json ?? "null", JsonOptions)!;
                yield return chunk;
                await Task.Yield();
            }
        }
        finally
        {
            NativeMethods.CrawlEngineHandleBatchCrawlStreamFree(streamHandle);
            NativeMethods.BatchCrawlStreamRequestFree(reqHandle);
        }
    }
}
