// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e612b4208fa954019809c4b976d3981c63fb39b46568924a375cb4b186adac65
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
#nullable enable

using System;
using System.Collections.Generic;
using System.IO;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Kreuzcrawl;

/// <summary>
/// A single page interaction action.
///
/// Actions are serialized with a `type` tag using camelCase naming,
/// except `ExecuteJs` which is explicitly renamed to `"executeJs"`.
/// </summary>
[JsonConverter(typeof(PageActionJsonConverter))]
public abstract record PageAction
{
    /// <summary>
    /// Click on an element matching the given CSS selector.
    /// </summary>
    public sealed record Click(
        [property: JsonPropertyName("selector")] string Selector
    ) : PageAction;

    /// <summary>
    /// Type text into an element matching the given CSS selector.
    /// </summary>
    public sealed record TypeText(
        [property: JsonPropertyName("selector")] string Selector,
        [property: JsonPropertyName("text")] string Text
    ) : PageAction;

    /// <summary>
    /// Press a keyboard key (e.g. "Enter", "Tab", "Escape").
    /// </summary>
    public sealed record Press(
        [property: JsonPropertyName("key")] string Key
    ) : PageAction;

    /// <summary>
    /// Scroll the page or a specific element.
    /// </summary>
    public sealed record Scroll(
        [property: JsonPropertyName("direction")] ScrollDirection Direction,
        [property: JsonPropertyName("selector")] string? Selector,
        [property: JsonPropertyName("amount")] long? Amount
    ) : PageAction;

    /// <summary>
    /// Wait for a duration or for an element to appear.
    /// </summary>
    public sealed record Wait(
        [property: JsonPropertyName("milliseconds")] long? Milliseconds,
        [property: JsonPropertyName("selector")] string? Selector
    ) : PageAction;

    /// <summary>
    /// Take a screenshot of the current page.
    /// </summary>
    public sealed record Screenshot(
        [property: JsonPropertyName("full_page")] bool? FullPage
    ) : PageAction;

    /// <summary>
    /// Execute arbitrary JavaScript in the page context.
    ///
    /// # Safety
    ///
    /// The script runs with full page privileges in the browser context.
    /// Only execute scripts from trusted sources.
    /// </summary>
    public sealed record ExecuteJs(
        [property: JsonPropertyName("script")] string Script
    ) : PageAction;

    /// <summary>
    /// Scrape the current page HTML.
    /// </summary>
    public sealed record Scrape() : PageAction;

}

/// <summary>
/// Custom converter for PageAction sealed union with flattened variant fields.
/// </summary>
/// <remarks>
/// Handles JSON objects with a discriminator field (type) and variant-specific
/// fields at the same level. System.Text.Json's [JsonPolymorphic] cannot handle
/// this layout, so we manually deserialize here.
/// </remarks>
public sealed class PageActionJsonConverter : JsonConverter<PageAction>
{
    public override PageAction Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        if (reader.TokenType != JsonTokenType.StartObject)
        {
            throw new JsonException($"Expected JSON object, got {reader.TokenType}");
        }

        using var doc = JsonDocument.ParseValue(ref reader);
        var root = doc.RootElement;

        if (!root.TryGetProperty("type", out var tagElement))
        {
            throw new JsonException($"Missing discriminator field: type");
        }

        var tagValue = tagElement.GetString();
        if (tagValue == null)
        {
            throw new JsonException("Discriminator field is null");
        }

        // Tuple-variant records (`Variant(InnerStruct value)`) expect a single
        // "Value" field holding the inner struct's JSON, so wrap the remaining
        // fields under "Value". Struct-variant records (`Variant { field1,
        // field2 }`) have positional record components annotated with
        // [JsonPropertyName(...)] for each named field, so pass the remaining
        // fields through directly without the wrap.
        using var ms = new MemoryStream();
        using var writer = new Utf8JsonWriter(ms);
        writer.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "type")
            {
                writer.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writer);
            }
        }
        writer.WriteEndObject();
        writer.Flush();
        ms.Position = 0;
        var flatJson = ms.ToArray();

        using var msWrapped = new MemoryStream();
        using var writerWrapped = new Utf8JsonWriter(msWrapped);
        writerWrapped.WriteStartObject();
        writerWrapped.WritePropertyName("Value");
        writerWrapped.WriteStartObject();
        foreach (var prop in root.EnumerateObject())
        {
            if (prop.Name != "type")
            {
                writerWrapped.WritePropertyName(prop.Name);
                prop.Value.WriteTo(writerWrapped);
            }
        }
        writerWrapped.WriteEndObject();
        writerWrapped.WriteEndObject();
        writerWrapped.Flush();
        msWrapped.Position = 0;
        var wrappedJson = msWrapped.ToArray();

        return tagValue switch
        {
            "click" => JsonSerializer.Deserialize<PageAction.Click>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),
            "type" => JsonSerializer.Deserialize<PageAction.TypeText>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),
            "press" => JsonSerializer.Deserialize<PageAction.Press>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),
            "scroll" => JsonSerializer.Deserialize<PageAction.Scroll>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),
            "wait" => JsonSerializer.Deserialize<PageAction.Wait>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),
            "screenshot" => JsonSerializer.Deserialize<PageAction.Screenshot>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),
            "executeJs" => JsonSerializer.Deserialize<PageAction.ExecuteJs>(flatJson, options) ?? throw new JsonException("Failed to deserialize variant"),
            "scrape" => new PageAction.Scrape(),
            _ => throw new JsonException($"Unknown PageAction discriminator: {tagValue}")
        };
    }

    public override void Write(Utf8JsonWriter writer, PageAction value, JsonSerializerOptions options)
    {
        // Emit the discriminator tag plus the inner variant's fields flattened at
        // the same level — mirrors the Java sealed-union serializer pattern. Turn
        // `Message.User(UserMessage value)` into `{"type":"user","content":...}`
        // not `{"value":{...}}`. Without this, sending a chat request to FFI fails
        // with "missing field type" inside Rust serde.
        string tag;
        object? inner;
        switch (value)
        {
            case PageAction.Click v_click:
                tag = "click"; inner = v_click; break;
            case PageAction.TypeText v_typetext:
                tag = "type"; inner = v_typetext; break;
            case PageAction.Press v_press:
                tag = "press"; inner = v_press; break;
            case PageAction.Scroll v_scroll:
                tag = "scroll"; inner = v_scroll; break;
            case PageAction.Wait v_wait:
                tag = "wait"; inner = v_wait; break;
            case PageAction.Screenshot v_screenshot:
                tag = "screenshot"; inner = v_screenshot; break;
            case PageAction.ExecuteJs v_executejs:
                tag = "executeJs"; inner = v_executejs; break;
            case PageAction.Scrape _:
                tag = "scrape";
                inner = null;
                break;
            default:
                throw new JsonException($"Unknown PageAction variant: {value.GetType().Name}");
        }

        writer.WriteStartObject();
        writer.WriteString("type", tag);
        if (inner != null)
        {
            using var doc = JsonSerializer.SerializeToDocument(inner, inner.GetType(), options);
            if (doc.RootElement.ValueKind == JsonValueKind.Object)
            {
                foreach (var prop in doc.RootElement.EnumerateObject())
                {
                    writer.WritePropertyName(prop.Name);
                    prop.Value.WriteTo(writer);
                }
            }
        }
        writer.WriteEndObject();
    }
}
