// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:72558e3430fe432c0d196559cdd4f37d88e83d0ce5f9df21ac48756b891a02cb
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.kreuzcrawl.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.kreuzcrawl.android.Kreuzcrawl

/** E2e tests for category: metadata. */
class MetadataTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("kcrawl_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load kcrawl_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
    }
    @Test
    fun testMetadataArticleTimes() = runBlocking {
        // Extracts article:published_time, modified_time, author, section, and tags
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_article_times"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode)
        // skipped: field 'article.published_time' not available on result type
        // skipped: field 'article.modified_time' not available on result type
        // skipped: field 'article.author' not available on result type
        // skipped: field 'article.section' not available on result type
        // skipped: field 'article.tags.length' not available on result type
    }

    @Test
    fun testMetadataFavicons() = runBlocking {
        // Extracts favicon link tags including apple-touch-icon
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_favicons"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode)
        // skipped: field 'favicons.length' not available on result type
        // skipped: field 'favicons[].apple_touch' not available on result type
    }

    @Test
    fun testMetadataHeadings() = runBlocking {
        // Extracts heading hierarchy (h1-h6) from HTML page
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_headings"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode)
        // skipped: field 'headings.h1.length' not available on result type
        // skipped: field 'headings.h1[0].text' not available on result type
        // skipped: field 'headings.length' not available on result type
    }

    @Test
    fun testMetadataHreflang() = runBlocking {
        // Extracts hreflang alternate link tags
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_hreflang"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode)
        // skipped: field 'hreflang.length' not available on result type
        // skipped: field 'hreflang[].lang' not available on result type
    }

    @Test
    fun testMetadataKeywordsAuthor() = runBlocking {
        // Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_keywords_author"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals("Comprehensive Metadata Test Page", result.metadata.title.orEmpty().trim())
        assertTrue(result.metadata.canonicalUrl != null, "expected non-empty value")
        assertTrue(result.metadata.keywords != null, "expected non-empty value")
        assertTrue(result.metadata.keywords.orEmpty().contains("rust"), "expected to contain: " + "rust")
        assertEquals("Jane Developer", result.metadata.author.orEmpty().trim())
        assertTrue(result.metadata.viewport != null, "expected non-empty value")
        assertEquals("kreuzcrawl/1.0", result.metadata.generator.orEmpty().trim())
        assertEquals("#ff6600", result.metadata.themeColor.orEmpty().trim())
        assertEquals("index, follow", result.metadata.robots.orEmpty().trim())
        assertEquals("en", result.metadata.htmlLang.orEmpty().trim())
        assertEquals("ltr", result.metadata.htmlDir.orEmpty().trim())
    }

    @Test
    fun testMetadataOgVideoAudio() = runBlocking {
        // Extracts og:video, og:audio, and og:locale:alternate metadata
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_og_video_audio"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals("https://example.com/video.mp4", result.metadata.ogVideo.orEmpty().trim())
        assertEquals("https://example.com/audio.mp3", result.metadata.ogAudio.orEmpty().trim())
        // skipped: field 'og.locale_alternate.length' not available on result type
    }

    @Test
    fun testMetadataResponseHeaders() = runBlocking {
        // Extracts response metadata from HTTP headers (etag, server, content-language)
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_response_headers"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode)
        // skipped: field 'response_headers.etag' not available on result type
        // skipped: field 'response_headers.last_modified' not available on result type
        // skipped: field 'response_headers.server' not available on result type
        // skipped: field 'response_headers.content_language' not available on result type
    }

    @Test
    fun testMetadataWordCount() = runBlocking {
        // Computes word count from visible page text
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_word_count"
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertTrue(result.metadata.wordCount!! > 99, "expected > 99")
        assertTrue(result.metadata.wordCount!! < 301, "expected < 301")
    }

}
