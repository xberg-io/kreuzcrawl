// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:72558e3430fe432c0d196559cdd4f37d88e83d0ce5f9df21ac48756b891a02cb
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzcrawl
import RustBridge

/// E2e tests for category: metadata.
final class MetadataTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "KREUZCRAWL_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testMetadataArticleTimes() async throws {
        // Extracts article:published_time, modified_time, author, section, and tags
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_article_times"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        // skipped: field 'article.published_time' not available on result type
        // skipped: field 'article.modified_time' not available on result type
        // skipped: field 'article.author' not available on result type
        // skipped: field 'article.section' not available on result type
        // skipped: field 'article.tags.length' not available on result type
    }

    func testMetadataFavicons() async throws {
        // Extracts favicon link tags including apple-touch-icon
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_favicons"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        // skipped: field 'favicons.length' not available on result type
        // skipped: field 'favicons[].apple_touch' not available on result type
    }

    func testMetadataHeadings() async throws {
        // Extracts heading hierarchy (h1-h6) from HTML page
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_headings"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        // skipped: field 'headings.h1.length' not available on result type
        // skipped: field 'headings.h1[0].text' not available on result type
        // skipped: field 'headings.length' not available on result type
    }

    func testMetadataHreflang() async throws {
        // Extracts hreflang alternate link tags
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_hreflang"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        // skipped: field 'hreflang.length' not available on result type
        // skipped: field 'hreflang[].lang' not available on result type
    }

    func testMetadataKeywordsAuthor() async throws {
        // Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_keywords_author"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertEqual((result.metadata().title()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Comprehensive Metadata Test Page")
        XCTAssertNotNil(result.metadata().canonicalUrl(), "expected non-nil value")
        XCTAssertNotNil(result.metadata().keywords(), "expected non-nil value")
        XCTAssertTrue("rust".isEmpty || (result.metadata().keywords()?.toString() ?? "").contains("rust"), "expected to contain: \("rust")")
        XCTAssertEqual((result.metadata().author()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Jane Developer")
        XCTAssertNotNil(result.metadata().viewport(), "expected non-nil value")
        XCTAssertEqual((result.metadata().generator()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "kreuzcrawl/1.0")
        XCTAssertEqual((result.metadata().themeColor()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "#ff6600")
        XCTAssertEqual((result.metadata().robots()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "index, follow")
        XCTAssertEqual((result.metadata().htmlLang()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "en")
        XCTAssertEqual((result.metadata().htmlDir()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "ltr")
    }

    func testMetadataOgVideoAudio() async throws {
        // Extracts og:video, og:audio, and og:locale:alternate metadata
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_og_video_audio"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertEqual((result.metadata().ogVideo()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "https://example.com/video.mp4")
        XCTAssertEqual((result.metadata().ogAudio()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "https://example.com/audio.mp3")
        // skipped: field 'og.locale_alternate.length' not available on result type
    }

    func testMetadataResponseHeaders() async throws {
        // Extracts response metadata from HTTP headers (etag, server, content-language)
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_response_headers"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        // skipped: field 'response_headers.etag' not available on result type
        // skipped: field 'response_headers.last_modified' not available on result type
        // skipped: field 'response_headers.server' not available on result type
        // skipped: field 'response_headers.content_language' not available on result type
    }

    func testMetadataWordCount() async throws {
        // Computes word count from visible page text
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_word_count"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertGreaterThan((result.metadata().wordCount() ?? 0), 99)
        XCTAssertLessThan((result.metadata().wordCount() ?? 0), 301)
    }

}
