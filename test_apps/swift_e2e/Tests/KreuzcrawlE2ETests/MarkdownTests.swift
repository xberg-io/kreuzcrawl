// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:72558e3430fe432c0d196559cdd4f37d88e83d0ce5f9df21ac48756b891a02cb
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzcrawl
import RustBridge

/// E2e tests for category: markdown.
final class MarkdownTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "KREUZCRAWL_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testCitationsBalancedParens() async throws {
        // Citations correctly handle links inside parentheses with balanced rendering
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/citations_balanced_parens"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue("(".isEmpty || (result.markdown()?.content().toString() ?? "").contains("("), "expected to contain: \("(")")
    }

    func testCitationsDuplicateUrls() async throws {
        // Citations deduplicates multiple links to the same URL
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/citations_duplicate_urls"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue((result.markdown()?.citations() ?? false))
    }

    func testMarkdownBasicConversion() async throws {
        // HTML is always converted to markdown alongside raw HTML
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/markdown_basic_conversion"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertEqual((result.metadata().title()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Test")
        XCTAssertGreaterThan(result.html().toString().count, 0, "expected non-empty value")
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue("Hello World".isEmpty || (result.markdown()?.content().toString() ?? "").contains("Hello World"), "expected to contain: \("Hello World")")
    }

    func testMarkdownCrawlAllPages() async throws {
        // All crawled pages have markdown field populated
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_MARKDOWN_CRAWL_ALL_PAGES"] ?? (AlefE2EMockServer.baseURL + "/fixtures/markdown_crawl_all_pages")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        // skipped: field 'crawl.pages_crawled' not available on result type
    }

    func testMarkdownFitContent() async throws {
        // Fit markdown removes navigation and boilerplate content
        let engineObj = try createEngine(config: nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_MARKDOWN_FIT_CONTENT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/markdown_fit_content")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
    }

    func testMarkdownHeadingsAndParagraphs() async throws {
        // Markdown conversion preserves heading hierarchy and paragraph text
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/markdown_headings_and_paragraphs"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue("Main Title".isEmpty || (result.markdown()?.content().toString() ?? "").contains("Main Title"), "expected to contain: \("Main Title")")
    }

    func testMarkdownLinksConverted() async throws {
        // HTML links are converted to markdown link syntax
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/markdown_links_converted"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertGreaterThan(result.html().toString().count, 0, "expected non-empty value")
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue("Example".isEmpty || (result.markdown()?.content().toString() ?? "").contains("Example"), "expected to contain: \("Example")")
    }

    func testMarkdownWithCitations() async throws {
        // Markdown includes citation conversion with numbered references
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/markdown_with_citations"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
    }

}
