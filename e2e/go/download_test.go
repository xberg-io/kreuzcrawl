// Code generated by alef. DO NOT EDIT.

// E2e tests for category: download
package e2e_test

import (
	"encoding/json"
	"os"
	"strings"
	"testing"

	pkg "github.com/kreuzberg-dev/kreuzcrawl/packages/go"
)

func Test_DownloadBasicPdf(t *testing.T) {
	// Download a basic PDF document with download_documents enabled
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"download_documents":true,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/download_basic_pdf"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.DownloadedDocument != nil {
		if strings.TrimSpace(result.DownloadedDocument.MimeType) != `application/pdf` {
			t.Errorf("equals mismatch: got %v", result.DownloadedDocument.MimeType)
		}
	}
}

func Test_DownloadFilenameExtraction(t *testing.T) {
	// Extract filename from Content-Disposition header
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"download_documents":true,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/download_filename_extraction"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.DownloadedDocument != nil {
		if strings.TrimSpace(result.DownloadedDocument.MimeType) != `application/pdf` {
			t.Errorf("equals mismatch: got %v", result.DownloadedDocument.MimeType)
		}
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
}

func Test_DownloadMimeFilter(t *testing.T) {
	// Only download documents matching specified MIME types (PDF only, not DOCX)
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"document_mime_types":["application/pdf"],"download_documents":true,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/download_mime_filter"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.DownloadedDocument != nil {
		if strings.TrimSpace(result.DownloadedDocument.MimeType) != `application/pdf` {
			t.Errorf("equals mismatch: got %v", result.DownloadedDocument.MimeType)
		}
	}
}

func Test_DownloadNoDocument(t *testing.T) {
	// HTML pages are not downloaded as documents even when download_documents is enabled
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"download_documents":true,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/download_no_document"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
}

func Test_DownloadSizeLimit(t *testing.T) {
	// Reject documents exceeding the configured size limit
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"document_max_size":100,"download_documents":true,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/download_size_limit"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
}
