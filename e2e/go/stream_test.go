// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9560f08f3c9fa0fc75741beb741f5dd3820369e4a877408df98cad5b3efe0544
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: stream
package e2e_test

import (
	"encoding/json"
	"os"
	"testing"

	pkg "github.com/kreuzberg-dev/kreuzcrawl/packages/go"
)

func Test_BatchCrawlStreamEvents(t *testing.T) {
	// Batch crawl stream produces expected Page and Complete events
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_concurrent":2}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	stream, err := pkg.BatchCrawlStream(engine, nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var chunks []pkg.ChatCompletionChunk
	for chunk := range stream {
		chunks = append(chunks, chunk)
	}
	assert.GreaterOrEqual(t, len(chunks), 4, "expected >= 4")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventPage); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventComplete); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
}

func Test_BatchCrawlStreamPartialFailure(t *testing.T) {
	// Batch crawl stream emits both Page and Error events for mixed success/failure URLs
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_concurrent":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	stream, err := pkg.BatchCrawlStream(engine, nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var chunks []pkg.ChatCompletionChunk
	for chunk := range stream {
		chunks = append(chunks, chunk)
	}
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventPage); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventError); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventComplete); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
}

func Test_CrawlStreamEvents(t *testing.T) {
	// Crawl stream produces page and complete events
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/crawl_stream_events"
	stream, err := pkg.CrawlStream(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var chunks []pkg.ChatCompletionChunk
	for chunk := range stream {
		chunks = append(chunks, chunk)
	}
	assert.GreaterOrEqual(t, len(chunks), 4, "expected >= 4")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventPage); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventComplete); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
}

func Test_StreamDepthCrawl(t *testing.T) {
	// Stream produces events for multi-depth crawl with link following
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_concurrent":1,"max_depth":2}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/stream_depth_crawl"
	stream, err := pkg.CrawlStream(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var chunks []pkg.ChatCompletionChunk
	for chunk := range stream {
		chunks = append(chunks, chunk)
	}
	assert.GreaterOrEqual(t, len(chunks), 5, "expected >= 5")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventPage); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventComplete); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
}

func Test_StreamErrorEventMidCrawl(t *testing.T) {
	// Stream emits error event when a page fails mid-crawl, but other pages succeed
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_concurrent":1,"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/stream_error_event_mid_crawl"
	stream, err := pkg.CrawlStream(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var chunks []pkg.ChatCompletionChunk
	for chunk := range stream {
		chunks = append(chunks, chunk)
	}
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventPage); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventError); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventComplete); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
}

func Test_StreamEventOrdering(t *testing.T) {
	// Stream ensures complete event arrives after all page events
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_concurrent":1,"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/stream_event_ordering"
	stream, err := pkg.CrawlStream(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var chunks []pkg.ChatCompletionChunk
	for chunk := range stream {
		chunks = append(chunks, chunk)
	}
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventComplete); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventPage); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.GreaterOrEqual(t, len(chunks), 3, "expected >= 3")
}

func Test_StreamLargeCrawl(t *testing.T) {
	// Stream handles crawl of 5+ pages with multiple events
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/stream_large_crawl"
	stream, err := pkg.CrawlStream(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var chunks []pkg.ChatCompletionChunk
	for chunk := range stream {
		chunks = append(chunks, chunk)
	}
	assert.GreaterOrEqual(t, len(chunks), 6, "expected >= 6")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventPage); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventComplete); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
}

func Test_StreamWithErrorEvent(t *testing.T) {
	// Stream emits page and complete events even when some pages fail
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_concurrent":1,"max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/stream_with_error_event"
	stream, err := pkg.CrawlStream(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var chunks []pkg.ChatCompletionChunk
	for chunk := range stream {
		chunks = append(chunks, chunk)
	}
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventPage); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventComplete); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.GreaterOrEqual(t, len(chunks), 2, "expected >= 2")
}
