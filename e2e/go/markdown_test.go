// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:519ded16db86cbafe7ff148df1d103d077f9dd57462bb28dd3199870e98d3f5b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: markdown
package e2e_test

import (
	"encoding/json"
	"fmt"
	"os"
	"strings"
	"testing"

	pkg "github.com/kreuzberg-dev/kreuzcrawl/packages/go"
)

func Test_MarkdownBasicConversion(t *testing.T) {
	// HTML is always converted to markdown alongside raw HTML
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/markdown_basic_conversion"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var metadataTitle string
	if result.Metadata.Title != nil {
		metadataTitle = string(*result.Metadata.Title)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if strings.TrimSpace(string(metadataTitle)) != `Test` {
		t.Errorf("equals mismatch: got %v", metadataTitle)
	}
	if len(result.HTML) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Markdown != nil {
		if len(result.Markdown.Content) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Markdown != nil {
		if !strings.Contains(fmt.Sprint(result.Markdown.Content), `Hello World`) {
			t.Errorf("expected to contain %s, got %v", `Hello World`, result.Markdown.Content)
		}
	}
}

func Test_MarkdownCrawlAllPages(t *testing.T) {
	// All crawled pages have markdown field populated
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/markdown_crawl_all_pages"
	_, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	// skipped: field 'crawl.pages_crawled' not available on result type
}

func Test_MarkdownFitContent(t *testing.T) {
	// Fit markdown removes navigation and boilerplate content
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/markdown_fit_content"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if result.Markdown != nil {
		if len(result.Markdown.Content) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
}

func Test_MarkdownHeadingsAndParagraphs(t *testing.T) {
	// Markdown conversion preserves heading hierarchy and paragraph text
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/markdown_headings_and_paragraphs"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.Markdown != nil {
		if len(result.Markdown.Content) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Markdown != nil {
		if !strings.Contains(fmt.Sprint(result.Markdown.Content), `Main Title`) {
			t.Errorf("expected to contain %s, got %v", `Main Title`, result.Markdown.Content)
		}
	}
}

func Test_MarkdownLinksConverted(t *testing.T) {
	// HTML links are converted to markdown link syntax
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/markdown_links_converted"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if len(result.HTML) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Markdown != nil {
		if len(result.Markdown.Content) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
	if result.Markdown != nil {
		if !strings.Contains(fmt.Sprint(result.Markdown.Content), `Example`) {
			t.Errorf("expected to contain %s, got %v", `Example`, result.Markdown.Content)
		}
	}
}

func Test_MarkdownWithCitations(t *testing.T) {
	// Markdown includes citation conversion with numbered references
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/markdown_with_citations"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if result.Markdown != nil {
		if len(result.Markdown.Content) == 0 {
			t.Errorf("expected non-empty value")
		}
	}
}
