// Code generated by alef. DO NOT EDIT.

// E2e tests for category: content
package e2e_test

import (
	"encoding/json"
	"os"
	"strings"
	"testing"

	pkg "github.com/kreuzberg-dev/kreuzcrawl/packages/go"
)

func Test_Content204NoContent(t *testing.T) {
	// Handles 204 No Content response gracefully
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_204_no_content"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 204 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if len(result.Html) != 0 {
		t.Errorf("expected empty value, got %v", result.Html)
	}
}

func Test_ContentCharsetIso8859(t *testing.T) {
	// Handles ISO-8859-1 encoded page correctly
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_charset_iso8859"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var detectedCharset string
	if result.DetectedCharset != nil {
		detectedCharset = *result.DetectedCharset
	}
	if strings.TrimSpace(detectedCharset) != `iso-8859-1` {
		t.Errorf("equals mismatch: got %v", detectedCharset)
	}
}

func Test_ContentEmptyBody(t *testing.T) {
	// Handles 200 response with empty body gracefully
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_empty_body"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
}

func Test_ContentGzipCompressed(t *testing.T) {
	// Handles response with Accept-Encoding gzip negotiation
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_gzip_compressed"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Html) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
}

func Test_ContentLargePageLimit(t *testing.T) {
	// Respects max body size limit and truncates or skips oversized pages
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_body_size":1024,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_large_page_limit"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.BodySize >= 1025 {
		t.Errorf("expected < 1025, got %v", result.BodySize)
	}
}

func Test_ContentMainOnly(t *testing.T) {
	// Extracts content with aggressive preprocessing, excluding nav, sidebar, footer
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"content":{"preprocessing_preset":"aggressive"},"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_main_only"
	_, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
}

func Test_ContentPdfNoExtension(t *testing.T) {
	// Detects PDF content by Content-Type header when URL has no .pdf extension
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_pdf_no_extension"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.IsPdf != true {
		t.Errorf("equals mismatch: got %v", result.IsPdf)
	}
}

func Test_ContentRemoveTags(t *testing.T) {
	// Removes specified HTML elements by CSS selector before processing
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"remove_tags":["nav","aside","footer"],"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_remove_tags"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Html) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_ContentUtf8Bom(t *testing.T) {
	// Handles UTF-8 content with BOM marker correctly
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_utf8_bom"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var detectedCharset string
	if result.DetectedCharset != nil {
		detectedCharset = *result.DetectedCharset
	}
	if strings.TrimSpace(detectedCharset) != `utf-8` {
		t.Errorf("equals mismatch: got %v", detectedCharset)
	}
	if len(result.Html) == 0 {
		t.Errorf("expected non-empty value")
	}
}
