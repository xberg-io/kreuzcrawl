// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a4a7ee13a4704d794ac931a9082c0e65ba8635cbaac284ebcfa47f64add9f65d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

// E2e tests for category: cookies
package e2e_test

import (
	"encoding/json"
	"os"
	"strings"
	"testing"

	pkg "github.com/kreuzberg-dev/kreuzcrawl/packages/go"
)

func Test_CookiesPerDomain(t *testing.T) {
	// Isolates cookies per domain during crawl
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"cookies_enabled":true,"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_COOKIES_PER_DOMAIN")
	if url == "" { url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/cookies_per_domain" }
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Cookies) != 1 {
		t.Errorf("equals mismatch: got %v", len(result.Cookies))
	}
	if !strings.Contains(jsonString(result.Cookies), `domain_cookie`) {
		t.Errorf("expected to contain %s, got %v", `domain_cookie`, result.Cookies)
	}
}

func Test_CookiesPersistence(t *testing.T) {
	// Maintains cookies across multiple crawl requests
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"cookies_enabled":true,"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_COOKIES_PERSISTENCE")
	if url == "" { url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/cookies_persistence" }
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(jsonString(result.Cookies), `session`) {
		t.Errorf("expected to contain %s, got %v", `session`, result.Cookies)
	}
}

func Test_CookiesSetCookieResponse(t *testing.T) {
	// Respects Set-Cookie header from server responses
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"cookies_enabled":true,"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_COOKIES_SET_COOKIE_RESPONSE")
	if url == "" { url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/cookies_set_cookie_response" }
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(jsonString(result.Cookies), `tracking`) {
		t.Errorf("expected to contain %s, got %v", `tracking`, result.Cookies)
	}
}
