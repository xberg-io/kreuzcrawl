// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8a1d5fc762aa04eb5ea6bec7f449171fb9ee63bdcaf0dd741ca3684caf6a8735
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

// E2e tests for category: batch
package e2e_test

import (
	"encoding/json"
	"os"
	"strings"
	"testing"

	pkg "github.com/kreuzberg-dev/kreuzcrawl/packages/go"
)

func Test_BatchCrawlBasic(t *testing.T) {
	// Batch crawl of 2 seed URLs with links to discover
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	urlsBase := os.Getenv("MOCK_SERVER_BATCH_CRAWL_BASIC")
	if urlsBase == "" {
		urlsBase = os.Getenv("MOCK_SERVER_URL") + "/fixtures/batch_crawl_basic"
	}
	var urls []string
	for _, p := range []string{`/seed1`, `/seed2`} {
		if strings.HasPrefix(p, "http") {
			urls = append(urls, p)
		} else {
			urls = append(urls, urlsBase+p)
		}
	}
	result, err := pkg.BatchCrawl(engine, urls)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.CompletedCount != 2 {
		t.Errorf("equals mismatch: got %v", result.CompletedCount)
	}
	if result.FailedCount != 0 {
		t.Errorf("equals mismatch: got %v", result.FailedCount)
	}
	if result.TotalCount != 2 {
		t.Errorf("equals mismatch: got %v", result.TotalCount)
	}
}

func Test_BatchCrawlPartialFailure(t *testing.T) {
	// Batch crawl where one seed URL returns 404 error
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	urlsBase := os.Getenv("MOCK_SERVER_BATCH_CRAWL_PARTIAL_FAILURE")
	if urlsBase == "" {
		urlsBase = os.Getenv("MOCK_SERVER_URL") + "/fixtures/batch_crawl_partial_failure"
	}
	var urls []string
	for _, p := range []string{`/good_seed`, `/bad_seed`} {
		if strings.HasPrefix(p, "http") {
			urls = append(urls, p)
		} else {
			urls = append(urls, urlsBase+p)
		}
	}
	result, err := pkg.BatchCrawl(engine, urls)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.CompletedCount != 1 {
		t.Errorf("equals mismatch: got %v", result.CompletedCount)
	}
	if result.FailedCount != 1 {
		t.Errorf("equals mismatch: got %v", result.FailedCount)
	}
	if result.TotalCount != 2 {
		t.Errorf("equals mismatch: got %v", result.TotalCount)
	}
}

func Test_BatchCrawlWithConfig(t *testing.T) {
	// Batch crawl with max_depth=1 config verifying pages are discovered
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_depth":1,"respect_robots_txt":false}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	urlsBase := os.Getenv("MOCK_SERVER_BATCH_CRAWL_WITH_CONFIG")
	if urlsBase == "" {
		urlsBase = os.Getenv("MOCK_SERVER_URL") + "/fixtures/batch_crawl_with_config"
	}
	var urls []string
	for _, p := range []string{`/seed1`, `/seed2`} {
		if strings.HasPrefix(p, "http") {
			urls = append(urls, p)
		} else {
			urls = append(urls, urlsBase+p)
		}
	}
	result, err := pkg.BatchCrawl(engine, urls)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.CompletedCount != 2 {
		t.Errorf("equals mismatch: got %v", result.CompletedCount)
	}
	if result.FailedCount != 0 {
		t.Errorf("equals mismatch: got %v", result.FailedCount)
	}
}

func Test_BatchScrapeEmptyUrlsError(t *testing.T) {
	// Batch scrape with empty batch_urls array returns error
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	urlsBase := os.Getenv("MOCK_SERVER_BATCH_SCRAPE_EMPTY_URLS_ERROR")
	if urlsBase == "" {
		urlsBase = os.Getenv("MOCK_SERVER_URL") + "/fixtures/batch_scrape_empty_urls_error"
	}
	var urls []string
	for _, p := range []string{} {
		if strings.HasPrefix(p, "http") {
			urls = append(urls, p)
		} else {
			urls = append(urls, urlsBase+p)
		}
	}
	_, err := pkg.BatchScrape(engine, urls)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_BatchScrapeWithConfig(t *testing.T) {
	// Batch scrape with aggressive preprocessing configuration
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"content":{"preprocessing_preset":"aggressive"}}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	urlsBase := os.Getenv("MOCK_SERVER_BATCH_SCRAPE_WITH_CONFIG")
	if urlsBase == "" {
		urlsBase = os.Getenv("MOCK_SERVER_URL") + "/fixtures/batch_scrape_with_config"
	}
	var urls []string
	for _, p := range []string{`/article1`, `/article2`} {
		if strings.HasPrefix(p, "http") {
			urls = append(urls, p)
		} else {
			urls = append(urls, urlsBase+p)
		}
	}
	result, err := pkg.BatchScrape(engine, urls)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.CompletedCount != 2 {
		t.Errorf("equals mismatch: got %v", result.CompletedCount)
	}
	if result.FailedCount != 0 {
		t.Errorf("equals mismatch: got %v", result.FailedCount)
	}
	if result.TotalCount != 2 {
		t.Errorf("equals mismatch: got %v", result.TotalCount)
	}
}

func Test_ScrapeBatchBasic(t *testing.T) {
	// Batch scrape of multiple URLs all succeeding
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	urlsBase := os.Getenv("MOCK_SERVER_SCRAPE_BATCH_BASIC")
	if urlsBase == "" {
		urlsBase = os.Getenv("MOCK_SERVER_URL") + "/fixtures/scrape_batch_basic"
	}
	var urls []string
	for _, p := range []string{`/page1`, `/page2`, `/page3`} {
		if strings.HasPrefix(p, "http") {
			urls = append(urls, p)
		} else {
			urls = append(urls, urlsBase+p)
		}
	}
	result, err := pkg.BatchScrape(engine, urls)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.CompletedCount != 3 {
		t.Errorf("equals mismatch: got %v", result.CompletedCount)
	}
	if result.FailedCount != 0 {
		t.Errorf("equals mismatch: got %v", result.FailedCount)
	}
	if result.TotalCount != 3 {
		t.Errorf("equals mismatch: got %v", result.TotalCount)
	}
}

func Test_ScrapeBatchPartialFailure(t *testing.T) {
	// Batch scrape with one URL failing returns partial results
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	urlsBase := os.Getenv("MOCK_SERVER_SCRAPE_BATCH_PARTIAL_FAILURE")
	if urlsBase == "" {
		urlsBase = os.Getenv("MOCK_SERVER_URL") + "/fixtures/scrape_batch_partial_failure"
	}
	var urls []string
	for _, p := range []string{`/good1`, `/bad`, `/good2`} {
		if strings.HasPrefix(p, "http") {
			urls = append(urls, p)
		} else {
			urls = append(urls, urlsBase+p)
		}
	}
	result, err := pkg.BatchScrape(engine, urls)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.CompletedCount != 2 {
		t.Errorf("equals mismatch: got %v", result.CompletedCount)
	}
	if result.FailedCount != 1 {
		t.Errorf("equals mismatch: got %v", result.FailedCount)
	}
	if result.TotalCount != 3 {
		t.Errorf("equals mismatch: got %v", result.TotalCount)
	}
}

func Test_ScrapeBatchProgress(t *testing.T) {
	// Batch scrape of 2 URLs completes with 2 results
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	urlsBase := os.Getenv("MOCK_SERVER_SCRAPE_BATCH_PROGRESS")
	if urlsBase == "" {
		urlsBase = os.Getenv("MOCK_SERVER_URL") + "/fixtures/scrape_batch_progress"
	}
	var urls []string
	for _, p := range []string{`/target`, `/other`} {
		if strings.HasPrefix(p, "http") {
			urls = append(urls, p)
		} else {
			urls = append(urls, urlsBase+p)
		}
	}
	result, err := pkg.BatchScrape(engine, urls)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.TotalCount != 2 {
		t.Errorf("equals mismatch: got %v", result.TotalCount)
	}
	if result.CompletedCount != 2 {
		t.Errorf("equals mismatch: got %v", result.CompletedCount)
	}
}
