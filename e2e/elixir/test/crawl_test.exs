# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:bfa929f33988c23d0e49959e33b2b22bf91b9ec0a7d3dafc2a8e955706b45893
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: crawl
defmodule E2e.CrawlTest do
  use ExUnit.Case, async: false

  describe "content_binary_skip" do
    test "content_binary_skip" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/content_binary_skip"
      {:ok, result} = Kreuzcrawl.scrape_async(engine, url)
      assert result.was_skipped == true
    end
  end

  describe "content_pdf_link_skip" do
    test "content_pdf_link_skip" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/content_pdf_link_skip"
      {:ok, result} = Kreuzcrawl.scrape_async(engine, url)
      assert result.was_skipped == true
    end
  end

  describe "crawl_concurrent_depth" do
    test "crawl_concurrent_depth" do
      engine_config = "{\"max_concurrent\":3,\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_CONCURRENT_DEPTH") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_concurrent_depth"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 3
      assert result.stayed_on_domain == true
    end
  end

  describe "crawl_concurrent_limit" do
    test "crawl_concurrent_limit" do
      engine_config = "{\"max_concurrent\":2,\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_CONCURRENT_LIMIT") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_concurrent_limit"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 5
    end
  end

  describe "crawl_concurrent_max_pages" do
    test "crawl_concurrent_max_pages" do
      engine_config = "{\"max_concurrent\":4,\"max_depth\":1,\"max_pages\":3,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_CONCURRENT_MAX_PAGES") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_concurrent_max_pages"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) <= 3
    end
  end

  describe "crawl_custom_headers" do
    test "crawl_custom_headers" do
      engine_config = "{\"custom_headers\":{\"Accept-Language\":\"en-US\",\"X-Custom-Header\":\"test-value\"},\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_CUSTOM_HEADERS") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_custom_headers"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "crawl_depth_one" do
    test "crawl_depth_one" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_DEPTH_ONE") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_depth_one"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 3
      assert result.stayed_on_domain == true
    end
  end

  describe "crawl_depth_priority" do
    test "crawl_depth_priority" do
      engine_config = "{\"max_depth\":2,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_DEPTH_PRIORITY") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_depth_priority"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 4
    end
  end

  describe "crawl_depth_two" do
    test "crawl_depth_two" do
      engine_config = "{\"max_depth\":2,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_DEPTH_TWO") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_depth_two"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 3
      assert length(result.pages) >= 3
    end
  end

  describe "crawl_depth_two_chain" do
    test "crawl_depth_two_chain" do
      engine_config = "{\"max_concurrent\":1,\"max_depth\":2}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_DEPTH_TWO_CHAIN") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_depth_two_chain"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 3
    end
  end

  describe "crawl_double_slash_normalization" do
    test "crawl_double_slash_normalization" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_DOUBLE_SLASH_NORMALIZATION") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_double_slash_normalization"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "crawl_empty_page_no_links" do
    test "crawl_empty_page_no_links" do
      engine_config = "{\"max_concurrent\":1,\"max_depth\":2}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_EMPTY_PAGE_NO_LINKS") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_empty_page_no_links"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "crawl_exclude_path_pattern" do
    test "crawl_exclude_path_pattern" do
      engine_config = "{\"exclude_paths\":[\"/admin/.*\"],\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_EXCLUDE_PATH_PATTERN") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_exclude_path_pattern"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "crawl_external_links_ignored" do
    test "crawl_external_links_ignored" do
      engine_config = "{\"max_concurrent\":1,\"max_depth\":1,\"stay_on_domain\":true}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_EXTERNAL_LINKS_IGNORED") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_external_links_ignored"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
      assert result.stayed_on_domain == true
    end
  end

  describe "crawl_fragment_stripping" do
    test "crawl_fragment_stripping" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_FRAGMENT_STRIPPING") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_fragment_stripping"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "crawl_include_path_pattern" do
    test "crawl_include_path_pattern" do
      engine_config = "{\"include_paths\":[\"/blog/.*\"],\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_INCLUDE_PATH_PATTERN") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_include_path_pattern"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "crawl_max_depth_zero" do
    test "crawl_max_depth_zero" do
      engine_config = "{\"max_depth\":0}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_MAX_DEPTH_ZERO") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_max_depth_zero"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 1
      assert length(result.pages) <= 1
    end
  end

  describe "crawl_max_pages" do
    test "crawl_max_pages" do
      engine_config = "{\"max_pages\":3,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_MAX_PAGES") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_max_pages"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) <= 3
    end
  end

  describe "crawl_mixed_content_types" do
    test "crawl_mixed_content_types" do
      engine_config = "{\"max_concurrent\":1,\"max_depth\":1}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_MIXED_CONTENT_TYPES") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_mixed_content_types"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) >= 2
    end
  end

  describe "crawl_multiple_redirects_in_traversal" do
    test "crawl_multiple_redirects_in_traversal" do
      engine_config = "{\"max_concurrent\":1,\"max_depth\":1}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_MULTIPLE_REDIRECTS_IN_TRAVERSAL") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_multiple_redirects_in_traversal"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) >= 1
    end
  end

  describe "crawl_query_param_dedup" do
    test "crawl_query_param_dedup" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_QUERY_PARAM_DEDUP") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_query_param_dedup"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "crawl_redirect_in_traversal" do
    test "crawl_redirect_in_traversal" do
      engine_config = "{\"max_concurrent\":1,\"max_depth\":1}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_REDIRECT_IN_TRAVERSAL") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_redirect_in_traversal"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) >= 1
    end
  end

  describe "crawl_self_link_no_loop" do
    test "crawl_self_link_no_loop" do
      engine_config = "{\"max_concurrent\":1,\"max_depth\":1}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_SELF_LINK_NO_LOOP") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_self_link_no_loop"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "crawl_single_page_no_links" do
    test "crawl_single_page_no_links" do
      engine_config = "{\"max_depth\":2}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_single_page_no_links"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 1
    end
  end

  describe "crawl_stay_on_domain" do
    test "crawl_stay_on_domain" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":false,\"stay_on_domain\":true}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_STAY_ON_DOMAIN") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_stay_on_domain"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
      assert result.stayed_on_domain == true
    end
  end

  describe "crawl_subdomain_exclusion" do
    test "crawl_subdomain_exclusion" do
      engine_config = "{\"allow_subdomains\":false,\"max_depth\":1,\"respect_robots_txt\":false,\"stay_on_domain\":true}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_SUBDOMAIN_EXCLUSION") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_subdomain_exclusion"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
      assert result.stayed_on_domain == true
    end
  end

  describe "crawl_subdomain_inclusion" do
    test "crawl_subdomain_inclusion" do
      engine_config = "{\"allow_subdomains\":true,\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_SUBDOMAIN_INCLUSION") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_subdomain_inclusion"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) >= 2
    end
  end

  describe "crawl_trailing_slash_dedup" do
    test "crawl_trailing_slash_dedup" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_TRAILING_SLASH_DEDUP") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_trailing_slash_dedup"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "crawl_url_deduplication" do
    test "crawl_url_deduplication" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Kreuzcrawl.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_CRAWL_URL_DEDUPLICATION") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/crawl_url_deduplication"
      {:ok, result} = Kreuzcrawl.crawl_async(engine, url)
      assert length(result.pages) <= 2
    end
  end
end
