// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a879462787f220fa30fc216e0ed795ae8b6d6235e23b0c8bdecc73a5a58d712f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://example.invalid/alef
package com.github.kreuzberg_dev.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import com.github.kreuzberg_dev.Kreuzcrawl
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.github.kreuzberg_dev.CrawlConfig

/** E2e tests for category: error. */
class ErrorTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testError401Unauthorized() {
        // Handles 401 Unauthorized response correctly
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_401_unauthorized"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testError403Forbidden() {
        // Handles 403 Forbidden response correctly
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_403_forbidden"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testError404Page() {
        // Handles 404 response correctly
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_404_page"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testError408RequestTimeout() {
        // Handles 408 Request Timeout response correctly
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_408_request_timeout"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testError410Gone() {
        // Handles 410 Gone response correctly
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_410_gone"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testError500Server() {
        // Handles 500 server error
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_500_server"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testError502BadGateway() {
        // Handles 502 Bad Gateway response correctly
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_502_bad_gateway"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorConnectionRefused() {
        // Handles connection refused error gracefully
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_connection_refused"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorDnsResolution() {
        // Handles DNS resolution failure gracefully
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_dns_resolution"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorEmptyResponse() {
        // Handles 200 with completely empty body gracefully
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_empty_response"
        val result = Kreuzcrawl.scrape(engine, url)
        // skipped: field 'html_not_empty' not available on result type
        // skipped: field 'error.is_error' not available on result type
    }

    @Test
    fun testErrorInvalidProxy() {
        // Proxy pointing to unreachable address causes connection error during scrape
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"proxy\":{\"url\":\"http://127.0.0.1:1\"}}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_invalid_proxy"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorPartialResponse() {
        // Handles incomplete or truncated HTTP response
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_partial_response"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorRateLimited() {
        // Handles 429 rate limiting with Retry-After
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_rate_limited"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorRetry503() {
        // Retries request on 503 Service Unavailable response
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_retry_503"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorRetryBackoff() {
        // Implements exponential backoff when retrying failed requests
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_retry_backoff"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorSslInvalidCert() {
        // Handles SSL certificate validation error
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_ssl_invalid_cert"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorTimeout() {
        // Mock server delays response longer than request_timeout, surfacing a timeout error
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"request_timeout\":500}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_timeout"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorWafAkamai() {
        // Akamai WAF detection returns WafBlocked error
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_waf_akamai"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorWafFalse403() {
        // Detects WAF/bot protection false 403 (Cloudflare challenge page)
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_waf_false_403"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testErrorWafImperva() {
        // Imperva/Incapsula WAF detection
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/error_waf_imperva"
            Kreuzcrawl.scrape(engine, url)
        }
    }

}
