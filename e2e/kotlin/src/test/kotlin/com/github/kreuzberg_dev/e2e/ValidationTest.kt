// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b54cbd6d5428b78588399b1724585768e70927af2a012bcf4c2d6283fa7945b4
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://example.invalid/alef
package com.github.kreuzberg_dev.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import com.github.kreuzberg_dev.Kreuzcrawl
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.github.kreuzberg_dev.CrawlConfig

/** E2e tests for category: validation. */
class ValidationTest {

    companion object {
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testValidationBrowserEndpointInvalid() {
        // Browser endpoint must be a valid ws:// or wss:// URL, not http://
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"browser\":{\"endpoint\":\"http://not-websocket:3000\",\"mode\":\"always\"}}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_browser_endpoint_invalid"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationEmptyBatchUrls() {
        // batch operation with empty batch_urls array is rejected
        assertFailsWith<Exception> {
            val engine = Kreuzcrawl.createEngine(null)
            Kreuzcrawl.batchScrape(engine, listOf())
        }
    }

    @Test
    fun testValidationInvalidAuthConfig() {
        // auth object with empty username in basic auth is rejected
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"auth\":{\"password\":\"secret\",\"type\":\"basic\",\"username\":\"\"}}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_invalid_auth_config"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationInvalidExcludeRegex() {
        // Invalid regex in exclude_paths is rejected
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"exclude_paths\":[\"(unclosed\"]}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_invalid_exclude_regex"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationInvalidIncludeRegex() {
        // Invalid regex in include_paths is rejected
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"include_paths\":[\"[invalid\"]}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_invalid_include_regex"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationInvalidProxyUrl() {
        // proxy with invalid URL like 'not-a-url' is rejected
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"proxy\":{\"url\":\"not-a-url\"}}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_invalid_proxy_url"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationInvalidRetryCode() {
        // Retry code outside 100-599 is rejected
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"retry_codes\":[999]}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_invalid_retry_code"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationMaxConcurrentZero() {
        // max_concurrent=0 is rejected as invalid config (minimum is 1)
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"max_concurrent\":0}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_max_concurrent_zero"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationMaxDepthTooHigh() {
        // max_depth=200 exceeds limit of 100
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"max_depth\":200}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_max_depth_too_high"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationMaxPagesZero() {
        // max_pages=0 is rejected as invalid config
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"max_pages\":0}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_max_pages_zero"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationMaxRedirectsTooHigh() {
        // max_redirects > 100 is rejected as invalid config
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"max_redirects\":200}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_max_redirects_too_high"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationNegativeBodySize() {
        // max_body_size set to -1 is rejected as invalid config
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"max_body_size\":0}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_negative_body_size"
            Kreuzcrawl.scrape(engine, url)
        }
    }

    @Test
    fun testValidationTimeoutZero() {
        // Zero request timeout is rejected as invalid config
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"request_timeout\":0}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/validation_timeout_zero"
            Kreuzcrawl.scrape(engine, url)
        }
    }

}
