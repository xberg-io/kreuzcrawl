/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:cc01a1c094d3c52a5b52b4a912620f9a716410d5ef08dea3520ea2a80cd328ad
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 */
/* E2e tests for category: markdown */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_citations_balanced_parens(void) {
    /* Citations correctly handle links inside parentheses with balanced rendering */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_CITATIONS_BALANCED_PARENS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/citations_balanced_parens", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGMarkdownResult* markdown_handle = cberg_scrape_result_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = cberg_markdown_result_content(markdown_handle);
    assert(status_code == 200 && "equals assertion failed");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    assert(markdown_content != NULL && strstr(markdown_content, "(") != NULL && "expected to contain substring");
    cberg_free_string(markdown_content);
    cberg_markdown_result_free(markdown_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_citations_duplicate_urls(void) {
    /* Citations deduplicates multiple links to the same URL */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_CITATIONS_DUPLICATE_URLS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/citations_duplicate_urls", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGMarkdownResult* markdown_handle = cberg_scrape_result_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = cberg_markdown_result_content(markdown_handle);
    int32_t markdown_citations = cberg_markdown_result_citations(markdown_handle);
    assert(status_code == 200 && "equals assertion failed");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    assert(markdown_citations);
    cberg_free_string(markdown_content);
    cberg_markdown_result_free(markdown_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_markdown_basic_conversion(void) {
    /* HTML is always converted to markdown alongside raw HTML */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_BASIC_CONVERSION");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/markdown_basic_conversion", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGPageMetadata* metadata_handle = cberg_scrape_result_metadata(result);
    assert(metadata_handle != NULL);
    char* metadata_title = cberg_page_metadata_title(metadata_handle);
    char* html = cberg_scrape_result_html(result);
    CBERGMarkdownResult* markdown_handle = cberg_scrape_result_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = cberg_markdown_result_content(markdown_handle);
    assert(status_code == 200 && "equals assertion failed");
    assert(str_trim_eq(metadata_title, "Test") == 0 && "equals assertion failed");
    assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    assert(markdown_content != NULL && strstr(markdown_content, "Hello World") != NULL && "expected to contain substring");
    cberg_free_string(metadata_title);
    cberg_free_string(html);
    cberg_free_string(markdown_content);
    cberg_markdown_result_free(markdown_handle);
    cberg_page_metadata_free(metadata_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_markdown_crawl_all_pages(void) {
    /* All crawled pages have markdown field populated */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"max_depth\":1}");
    assert(config_handle != NULL && "failed to parse config");
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_CRAWL_ALL_PAGES");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/markdown_crawl_all_pages", mock_base);
    }
    CBERGCrawlResult* result = cberg_crawl(engine, url);
    if (result != NULL) cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_markdown_fit_content(void) {
    /* Fit markdown removes navigation and boilerplate content */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_FIT_CONTENT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/markdown_fit_content", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGMarkdownResult* markdown_handle = cberg_scrape_result_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = cberg_markdown_result_content(markdown_handle);
    assert(status_code == 200 && "equals assertion failed");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    cberg_free_string(markdown_content);
    cberg_markdown_result_free(markdown_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_markdown_headings_and_paragraphs(void) {
    /* Markdown conversion preserves heading hierarchy and paragraph text */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_HEADINGS_AND_PARAGRAPHS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/markdown_headings_and_paragraphs", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    CBERGMarkdownResult* markdown_handle = cberg_scrape_result_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = cberg_markdown_result_content(markdown_handle);
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    assert(markdown_content != NULL && strstr(markdown_content, "Main Title") != NULL && "expected to contain substring");
    cberg_free_string(markdown_content);
    cberg_markdown_result_free(markdown_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_markdown_links_converted(void) {
    /* HTML links are converted to markdown link syntax */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_LINKS_CONVERTED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/markdown_links_converted", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    char* html = cberg_scrape_result_html(result);
    CBERGMarkdownResult* markdown_handle = cberg_scrape_result_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = cberg_markdown_result_content(markdown_handle);
    assert(status_code == 200 && "equals assertion failed");
    assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    assert(markdown_content != NULL && strstr(markdown_content, "Example") != NULL && "expected to contain substring");
    cberg_free_string(html);
    cberg_free_string(markdown_content);
    cberg_markdown_result_free(markdown_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_markdown_with_citations(void) {
    /* Markdown includes citation conversion with numbered references */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_WITH_CITATIONS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/markdown_with_citations", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGMarkdownResult* markdown_handle = cberg_scrape_result_markdown(result);
    assert(markdown_handle != NULL);
    char* markdown_content = cberg_markdown_result_content(markdown_handle);
    assert(status_code == 200 && "equals assertion failed");
    assert(markdown_content != NULL && strlen(markdown_content) > 0 && "expected non-empty value");
    cberg_free_string(markdown_content);
    cberg_markdown_result_free(markdown_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}
