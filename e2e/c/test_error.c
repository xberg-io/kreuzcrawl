/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:cc01a1c094d3c52a5b52b4a912620f9a716410d5ef08dea3520ea2a80cd328ad
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 */
/* E2e tests for category: error */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_error_401_unauthorized(void) {
    /* Handles 401 Unauthorized response correctly */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_401_UNAUTHORIZED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_401_unauthorized", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_403_forbidden(void) {
    /* Handles 403 Forbidden response correctly */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_403_FORBIDDEN");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_403_forbidden", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_404_page(void) {
    /* Handles 404 response correctly */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_404_PAGE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_404_page", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_408_request_timeout(void) {
    /* Handles 408 Request Timeout response correctly */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_408_REQUEST_TIMEOUT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_408_request_timeout", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_410_gone(void) {
    /* Handles 410 Gone response correctly */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_410_GONE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_410_gone", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_500_server(void) {
    /* Handles 500 server error */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_500_SERVER");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_500_server", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_502_bad_gateway(void) {
    /* Handles 502 Bad Gateway response correctly */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_502_BAD_GATEWAY");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_502_bad_gateway", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_browser_launch_failure(void) {
    /* Browser launch fails when browser mode is always but browser is unavailable */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"browser\":{\"mode\":\"always\",\"timeout\":1}}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_BROWSER_LAUNCH_FAILURE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_browser_launch_failure", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_browser_page_timeout(void) {
    /* Browser page load times out */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"browser\":{\"mode\":\"always\",\"timeout\":1}}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_BROWSER_PAGE_TIMEOUT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_browser_page_timeout", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_connection_refused(void) {
    /* Handles connection refused error gracefully */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_CONNECTION_REFUSED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_connection_refused", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_data_loss_truncated(void) {
    /* Content-Length mismatch causes data loss error */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_DATA_LOSS_TRUNCATED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_data_loss_truncated", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_dns_resolution(void) {
    /* Handles DNS resolution failure gracefully */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_DNS_RESOLUTION");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_dns_resolution", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_empty_batch_urls(void) {
    /* Scraping a URL that cannot be found returns an error containing the URL path */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_EMPTY_BATCH_URLS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_empty_batch_urls", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_empty_response(void) {
    /* Handles 200 with completely empty body gracefully */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_EMPTY_RESPONSE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_empty_response", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    char* html = cberg_scrape_result_html(result);
    assert((html == NULL || strlen(html) == 0) && "expected empty value");
    cberg_free_string(html);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_invalid_proxy(void) {
    /* Proxy pointing to unreachable address causes connection error during scrape */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"proxy\":{\"url\":\"http://127.0.0.1:1\"}}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_INVALID_PROXY");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_invalid_proxy", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_partial_response(void) {
    /* Handles incomplete or truncated HTTP response */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_PARTIAL_RESPONSE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_partial_response", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_rate_limited(void) {
    /* Handles 429 rate limiting with Retry-After */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_RATE_LIMITED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_rate_limited", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_retry_503(void) {
    /* Retries request on 503 Service Unavailable response */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_RETRY_503");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_retry_503", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_retry_backoff(void) {
    /* Implements exponential backoff when retrying failed requests */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_RETRY_BACKOFF");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_retry_backoff", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_ssl_invalid_cert(void) {
    /* Handles SSL certificate validation error */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_SSL_INVALID_CERT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_ssl_invalid_cert", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_timeout(void) {
    /* Mock server delays response longer than request_timeout, surfacing a timeout error */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"request_timeout\":500}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_TIMEOUT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_timeout", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_unsupported_scheme(void) {
    /* Unsupported URL scheme (gopher) is rejected */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_UNSUPPORTED_SCHEME");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_unsupported_scheme", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_waf_akamai(void) {
    /* Akamai WAF detection returns WafBlocked error */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_WAF_AKAMAI");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_waf_akamai", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_waf_blocked(void) {
    /* WAF challenge/block detection returns WafBlocked error */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_WAF_BLOCKED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_waf_blocked", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_waf_false_403(void) {
    /* Detects WAF/bot protection false 403 (Cloudflare challenge page) */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_WAF_FALSE_403");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_waf_false_403", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_error_waf_imperva(void) {
    /* Imperva/Incapsula WAF detection */
    CBERGCrawlConfig* config_handle = cberg_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    if (config_handle == NULL) { return; }
    CBERGCrawlEngineHandle* engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == NULL) { return; }
    const char* mock_per_fixture = getenv("MOCK_SERVER_ERROR_WAF_IMPERVA");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/error_waf_imperva", mock_base);
    }
    CBERGScrapeResult* result = cberg_scrape(engine, url);
    if (result != NULL) cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}
