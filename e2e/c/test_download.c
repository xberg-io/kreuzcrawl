/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:694559ac704779b9c31a84ee56962209c41be79de7938a1686a148a16b88fab4
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 * Issues & docs: https://github.com/kreuzberg-dev/alef
 */
/* E2e tests for category: download */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "kreuzcrawl.h"
#include "test_runner.h"

void test_download_basic_pdf(void) {
    /* Download a basic PDF document with download_documents enabled */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_BASIC_PDF");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_basic_pdf", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    KCRAWLDownloadedDocument* downloaded_document_handle = kcrawl_scrape_result_downloaded_document(result);
    assert(downloaded_document_handle != NULL);
    char* downloaded_document_mime_type = kcrawl_downloaded_document_mime_type(downloaded_document_handle);
    assert(str_trim_eq(downloaded_document_mime_type, "application/pdf") == 0 && "equals assertion failed");
    kcrawl_free_string(downloaded_document_mime_type);
    kcrawl_downloaded_document_free(downloaded_document_handle);
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_download_filename_extraction(void) {
    /* Extract filename from Content-Disposition header */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_FILENAME_EXTRACTION");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_filename_extraction", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    KCRAWLDownloadedDocument* downloaded_document_handle = kcrawl_scrape_result_downloaded_document(result);
    assert(downloaded_document_handle != NULL);
    char* downloaded_document_mime_type = kcrawl_downloaded_document_mime_type(downloaded_document_handle);
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    assert(str_trim_eq(downloaded_document_mime_type, "application/pdf") == 0 && "equals assertion failed");
    assert(status_code == 200 && "equals assertion failed");
    kcrawl_free_string(downloaded_document_mime_type);
    kcrawl_downloaded_document_free(downloaded_document_handle);
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_download_mime_filter(void) {
    /* Only download documents matching specified MIME types (PDF only, not DOCX) */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"document_mime_types\":[\"application/pdf\"],\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_MIME_FILTER");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_mime_filter", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    KCRAWLDownloadedDocument* downloaded_document_handle = kcrawl_scrape_result_downloaded_document(result);
    assert(downloaded_document_handle != NULL);
    char* downloaded_document_mime_type = kcrawl_downloaded_document_mime_type(downloaded_document_handle);
    assert(str_trim_eq(downloaded_document_mime_type, "application/pdf") == 0 && "equals assertion failed");
    kcrawl_free_string(downloaded_document_mime_type);
    kcrawl_downloaded_document_free(downloaded_document_handle);
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_download_no_document(void) {
    /* HTML pages are not downloaded as documents even when download_documents is enabled */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_NO_DOCUMENT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_no_document", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    assert(status_code == 200 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_download_size_limit(void) {
    /* Reject documents exceeding the configured size limit */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"document_max_size\":100,\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_SIZE_LIMIT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_size_limit", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    assert(status_code == 200 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}
