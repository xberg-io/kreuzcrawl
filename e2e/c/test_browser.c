/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:4f71164c01eba3dc1c171b313092bde2b6745791f9e3e4c239faead7916eadb0
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 * Issues & docs: https://github.com/kreuzberg-dev/alef
 */
/* E2e tests for category: browser */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "kreuzcrawl.h"
#include "test_runner.h"

void test_browser_config_auto_no_feature(void) {
    /* Browser mode 'never' prevents browser use even when JS render hint is set */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_CONFIG_AUTO_NO_FEATURE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_config_auto_no_feature", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(status_code == 200 && "equals assertion failed");
    assert(js_render_hint == 1 && "equals assertion failed");
    assert(browser_used == 0 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_config_never_mode(void) {
    /* Browser mode 'never' prevents browser fallback even for SPA shell content */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_CONFIG_NEVER_MODE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_config_never_mode", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(status_code == 200 && "equals assertion failed");
    assert(js_render_hint == 1 && "equals assertion failed");
    assert(browser_used == 0 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_crawl_mode_always(void) {
    /* Crawl with browser mode 'always' follows links using browser rendering */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"always\"},\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_CRAWL_MODE_ALWAYS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_crawl_mode_always", mock_base);
    }
    KCRAWLCrawlResult* result = kcrawl_crawl(engine, url);
    assert(result != NULL && "expected call to succeed");
    char* pages_json = kcrawl_crawl_result_pages(result);
    int pages_length = alef_json_array_count(pages_json);
    assert(pages_length >= 2 && "expected greater than or equal");
    // skipped: field 'browser_used' not available on result type
    kcrawl_free_string(pages_json);
    kcrawl_crawl_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_crawl_waf_fallback(void) {
    /* Crawl with browser mode 'auto' falls back to browser when encountering WAF 403 */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"auto\"},\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_CRAWL_WAF_FALLBACK");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_crawl_waf_fallback", mock_base);
    }
    KCRAWLCrawlResult* result = kcrawl_crawl(engine, url);
    assert(result != NULL && "expected call to succeed");
    char* pages_json = kcrawl_crawl_result_pages(result);
    int pages_length = alef_json_array_count(pages_json);
    assert(pages_length >= 1 && "expected greater than or equal");
    kcrawl_free_string(pages_json);
    kcrawl_crawl_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_detect_minimal_page(void) {
    /* Does NOT flag a short but real content page as needing JS rendering */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_DETECT_MINIMAL_PAGE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_detect_minimal_page", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(status_code == 200 && "equals assertion failed");
    assert(js_render_hint == 0 && "equals assertion failed");
    assert(browser_used == 0 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_detect_next_empty(void) {
    /* Detects Next.js page with __NEXT_DATA__ but no rendered content as needing JS rendering */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_DETECT_NEXT_EMPTY");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_detect_next_empty", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(status_code == 200 && "equals assertion failed");
    assert(js_render_hint == 1 && "equals assertion failed");
    assert(browser_used == 0 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_detect_next_rendered(void) {
    /* Does NOT flag Next.js page with full SSR content as needing JS rendering */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_DETECT_NEXT_RENDERED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_detect_next_rendered", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    char* html = kcrawl_scrape_result_html(result);
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(status_code == 200 && "equals assertion failed");
    assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
    assert(js_render_hint == 0 && "equals assertion failed");
    assert(browser_used == 0 && "equals assertion failed");
    kcrawl_free_string(html);
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_detect_normal_page(void) {
    /* Does NOT flag a normal server-rendered page as needing JS rendering */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_DETECT_NORMAL_PAGE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_detect_normal_page", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(status_code == 200 && "equals assertion failed");
    assert(js_render_hint == 0 && "equals assertion failed");
    assert(browser_used == 0 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_detect_nuxt_shell(void) {
    /* Detects Nuxt SPA shell with empty #__nuxt div as needing JS rendering */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_DETECT_NUXT_SHELL");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_detect_nuxt_shell", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(status_code == 200 && "equals assertion failed");
    assert(js_render_hint == 1 && "equals assertion failed");
    assert(browser_used == 0 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_detect_react_shell(void) {
    /* Detects React SPA shell with empty #root div as needing JS rendering */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_DETECT_REACT_SHELL");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_detect_react_shell", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    char* html = kcrawl_scrape_result_html(result);
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(status_code == 200 && "equals assertion failed");
    assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
    assert(js_render_hint == 1 && "equals assertion failed");
    assert(browser_used == 0 && "equals assertion failed");
    kcrawl_free_string(html);
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_detect_vue_shell(void) {
    /* Detects Vue SPA shell with empty #app div as needing JS rendering */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"never\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_DETECT_VUE_SHELL");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_detect_vue_shell", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(status_code == 200 && "equals assertion failed");
    assert(js_render_hint == 1 && "equals assertion failed");
    assert(browser_used == 0 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_extra_wait(void) {
    /* Browser extra_wait adds additional time after network_idle to ensure all async operations complete */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"extra_wait\":200,\"mode\":\"always\",\"wait\":\"network_idle\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_EXTRA_WAIT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_extra_wait", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(browser_used == 1 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_fallback_spa_render(void) {
    /* Browser auto re-fetches SPA shell when JS rendering is detected */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"always\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_FALLBACK_SPA_RENDER");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_fallback_spa_render", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    int32_t js_render_hint = kcrawl_scrape_result_js_render_hint(result);
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(js_render_hint == 1 && "equals assertion failed");
    assert(browser_used == 1 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_fallback_waf_blocked(void) {
    /* Browser fallback is used when browser mode is always, simulating WAF-blocked scenario */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"always\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_FALLBACK_WAF_BLOCKED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_fallback_waf_blocked", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(browser_used == 1 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_mode_always(void) {
    /* Browser mode 'always' uses browser even for normal server-rendered pages */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"always\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_MODE_ALWAYS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_mode_always", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    assert(browser_used == 1 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_profile_basic(void) {
    /* Browser profile configuration persists and reuses browser state across crawl sessions */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"always\"},\"browser_profile\":\"test-profile\"}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_PROFILE_BASIC");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_profile_basic", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    assert(browser_used == 1 && "equals assertion failed");
    assert(status_code == 200 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_wait_fixed(void) {
    /* Browser wait strategy 'fixed' waits for a specific duration after page navigation */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"extra_wait\":100,\"mode\":\"always\",\"wait\":\"fixed\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_WAIT_FIXED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_wait_fixed", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    assert(browser_used == 1 && "equals assertion failed");
    assert(status_code == 200 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_browser_wait_selector(void) {
    /* Browser wait strategy 'selector' waits for specific CSS selector before considering page loaded */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"browser\":{\"mode\":\"always\",\"wait\":\"selector\",\"wait_selector\":\"#content\"}}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BROWSER_WAIT_SELECTOR");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/browser_wait_selector", mock_base);
    }
    KCRAWLScrapeResult* result = kcrawl_scrape(engine, url);
    assert(result != NULL && "expected call to succeed");
    int32_t browser_used = kcrawl_scrape_result_browser_used(result);
    uint16_t status_code = kcrawl_scrape_result_status_code(result);
    assert(browser_used == 1 && "equals assertion failed");
    assert(status_code == 200 && "equals assertion failed");
    kcrawl_scrape_result_free(result);
    kcrawl_crawl_engine_handle_free(engine);
}
