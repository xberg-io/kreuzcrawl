/*
 * This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:5fe82d147a981d0c7b65d455cfbab56be32e7dbeba9e8d63bf0c44de627a35d4
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 * Issues & docs: https://github.com/kreuzberg-dev/alef
 */
/* E2e tests for category: stream */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "kreuzcrawl.h"
#include "test_runner.h"

void test_batch_crawl_stream_events(void) {
    /* Batch crawl stream produces expected Page and Complete events */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"max_concurrent\":2}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BATCH_CRAWL_STREAM_EVENTS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/batch_crawl_stream_events", mock_base);
    }
    char* result = kcrawl_batch_crawl_stream(engine, url);
    if (result != NULL) kcrawl_free_string(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_batch_crawl_stream_partial_failure(void) {
    /* Batch crawl stream emits both Page and Error events for mixed success/failure URLs */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"max_concurrent\":1}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BATCH_CRAWL_STREAM_PARTIAL_FAILURE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/batch_crawl_stream_partial_failure", mock_base);
    }
    char* result = kcrawl_batch_crawl_stream(engine, url);
    if (result != NULL) kcrawl_free_string(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_crawl_stream_events(void) {
    /* Crawl stream produces page and complete events */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_CRAWL_STREAM_EVENTS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/crawl_stream_events", mock_base);
    }
    char* result = kcrawl_crawl_stream(engine, url);
    if (result != NULL) kcrawl_free_string(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_stream_depth_crawl(void) {
    /* Stream produces events for multi-depth crawl with link following */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"max_concurrent\":1,\"max_depth\":2}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STREAM_DEPTH_CRAWL");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/stream_depth_crawl", mock_base);
    }
    char* result = kcrawl_crawl_stream(engine, url);
    if (result != NULL) kcrawl_free_string(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_stream_error_event_mid_crawl(void) {
    /* Stream emits error event when a page fails mid-crawl, but other pages succeed */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STREAM_ERROR_EVENT_MID_CRAWL");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/stream_error_event_mid_crawl", mock_base);
    }
    char* result = kcrawl_crawl_stream(engine, url);
    if (result != NULL) kcrawl_free_string(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_stream_event_ordering(void) {
    /* Stream ensures complete event arrives after all page events */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STREAM_EVENT_ORDERING");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/stream_event_ordering", mock_base);
    }
    char* result = kcrawl_crawl_stream(engine, url);
    if (result != NULL) kcrawl_free_string(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_stream_large_crawl(void) {
    /* Stream handles crawl of 5+ pages with multiple events */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STREAM_LARGE_CRAWL");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/stream_large_crawl", mock_base);
    }
    char* result = kcrawl_crawl_stream(engine, url);
    if (result != NULL) kcrawl_free_string(result);
    kcrawl_crawl_engine_handle_free(engine);
}

void test_stream_with_error_event(void) {
    /* Stream emits page and complete events even when some pages fail */
    KCRAWLCrawlConfig* config_handle = kcrawl_crawl_config_from_json("{\"max_concurrent\":1,\"max_depth\":1}");
    assert(config_handle != NULL && "failed to parse config");
    KCRAWLCrawlEngineHandle* engine = kcrawl_create_engine(config_handle);
    kcrawl_crawl_config_free(config_handle);
    assert(engine != NULL && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STREAM_WITH_ERROR_EVENT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/stream_with_error_event", mock_base);
    }
    char* result = kcrawl_crawl_stream(engine, url);
    if (result != NULL) kcrawl_free_string(result);
    kcrawl_crawl_engine_handle_free(engine);
}
