// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7eb06114cc332b186e8e14174ef133d77ef37d51d2eaac1d274d935e32d70a9b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import Kreuzcrawl
import RustBridge

/// E2e tests for category: error.
final class ErrorTests: XCTestCase {
    func testError401Unauthorized() async throws {
        // Handles 401 Unauthorized response correctly
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_401_unauthorized"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testError403Forbidden() async throws {
        // Handles 403 Forbidden response correctly
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_403_forbidden"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testError404Page() async throws {
        // Handles 404 response correctly
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_404_page"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testError408RequestTimeout() async throws {
        // Handles 408 Request Timeout response correctly
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_408_request_timeout"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testError410Gone() async throws {
        // Handles 410 Gone response correctly
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_410_gone"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testError500Server() async throws {
        // Handles 500 server error
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_500_server"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testError502BadGateway() async throws {
        // Handles 502 Bad Gateway response correctly
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_502_bad_gateway"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorConnectionRefused() async throws {
        // Handles connection refused error gracefully
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_connection_refused"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorDnsResolution() async throws {
        // Handles DNS resolution failure gracefully
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_dns_resolution"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorEmptyResponse() async throws {
        // Handles 200 with completely empty body gracefully
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_empty_response"
        let result = try await scrape(engineObj, url)
        // skipped: field 'html_not_empty' not available on result type
        // skipped: field 'error.is_error' not available on result type
    }

    func testErrorInvalidProxy() async throws {
        // Proxy pointing to unreachable address causes connection error during scrape
        do {
            let engineConfig = try crawlConfigFromJson("{\"proxy\":{\"url\":\"http://127.0.0.1:1\"}}")
            let engineObj = try createEngine(engineConfig)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_invalid_proxy"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorPartialResponse() async throws {
        // Handles incomplete or truncated HTTP response
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_partial_response"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorRateLimited() async throws {
        // Handles 429 rate limiting with Retry-After
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_rate_limited"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorRetry503() async throws {
        // Retries request on 503 Service Unavailable response
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_retry_503"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorRetryBackoff() async throws {
        // Implements exponential backoff when retrying failed requests
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_retry_backoff"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorSslInvalidCert() async throws {
        // Handles SSL certificate validation error
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_ssl_invalid_cert"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorTimeout() async throws {
        // Mock server delays response longer than request_timeout, surfacing a timeout error
        do {
            let engineConfig = try crawlConfigFromJson("{\"request_timeout\":500}")
            let engineObj = try createEngine(engineConfig)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_timeout"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorWafAkamai() async throws {
        // Akamai WAF detection returns WafBlocked error
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_waf_akamai"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorWafFalse403() async throws {
        // Detects WAF/bot protection false 403 (Cloudflare challenge page)
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_waf_false_403"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorWafImperva() async throws {
        // Imperva/Incapsula WAF detection
        do {
            let engineObj = try createEngine(nil)
            let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/error_waf_imperva"
            _ = try await scrape(engineObj, url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

}
