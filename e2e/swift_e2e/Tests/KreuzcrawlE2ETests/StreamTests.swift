// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:74af25224250f90e9704f16320cb08e4e469845ae2bdc25a2bc66c9e3a4f2304
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
import Kreuzcrawl

/// E2e tests for category: stream.
final class StreamTests: XCTestCase {
    func testCrawlStreamEvents() async throws {
        // Crawl stream produces page and complete events
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_CRAWL_STREAM_EVENTS"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_stream_events")
        let result = try await Kreuzcrawl.crawlStream(engine: engineObj, url: url)
        var chunks: [Kreuzcrawl.ChatCompletionChunk] = []
                for try await _chunk in result { chunks.append(_chunk) }
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
        XCTAssertTrue(chunks.contains(where: { e in if case .page = e { return true } else { return false } }))
        XCTAssertTrue(chunks.contains(where: { e in if case .complete = e { return true } else { return false } }))
    }

    func testStreamDepthCrawl() async throws {
        // Stream produces events for multi-depth crawl with link following
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":2}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_DEPTH_CRAWL"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_depth_crawl")
        let result = try await Kreuzcrawl.crawlStream(engine: engineObj, url: url)
        var chunks: [Kreuzcrawl.ChatCompletionChunk] = []
                for try await _chunk in result { chunks.append(_chunk) }
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
        XCTAssertTrue(chunks.contains(where: { e in if case .page = e { return true } else { return false } }))
        XCTAssertTrue(chunks.contains(where: { e in if case .complete = e { return true } else { return false } }))
    }

    func testStreamErrorEventMidCrawl() async throws {
        // Stream emits error event when a page fails mid-crawl, but other pages succeed
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_ERROR_EVENT_MID_CRAWL"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_error_event_mid_crawl")
        let result = try await Kreuzcrawl.crawlStream(engine: engineObj, url: url)
        var chunks: [Kreuzcrawl.ChatCompletionChunk] = []
                for try await _chunk in result { chunks.append(_chunk) }
        XCTAssertTrue(chunks.contains(where: { e in if case .page = e { return true } else { return false } }))
        XCTAssertTrue(chunks.contains(where: { e in if case .error = e { return true } else { return false } }))
        XCTAssertTrue(chunks.contains(where: { e in if case .complete = e { return true } else { return false } }))
    }

    func testStreamEventOrdering() async throws {
        // Stream ensures complete event arrives after all page events
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_EVENT_ORDERING"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_event_ordering")
        let result = try await Kreuzcrawl.crawlStream(engine: engineObj, url: url)
        var chunks: [Kreuzcrawl.ChatCompletionChunk] = []
                for try await _chunk in result { chunks.append(_chunk) }
        XCTAssertTrue(chunks.contains(where: { e in if case .complete = e { return true } else { return false } }))
        XCTAssertTrue(chunks.contains(where: { e in if case .page = e { return true } else { return false } }))
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
    }

    func testStreamLargeCrawl() async throws {
        // Stream handles crawl of 5+ pages with multiple events
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_LARGE_CRAWL"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_large_crawl")
        let result = try await Kreuzcrawl.crawlStream(engine: engineObj, url: url)
        var chunks: [Kreuzcrawl.ChatCompletionChunk] = []
                for try await _chunk in result { chunks.append(_chunk) }
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
        XCTAssertTrue(chunks.contains(where: { e in if case .page = e { return true } else { return false } }))
        XCTAssertTrue(chunks.contains(where: { e in if case .complete = e { return true } else { return false } }))
    }

    func testStreamWithErrorEvent() async throws {
        // Stream emits page and complete events even when some pages fail
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_WITH_ERROR_EVENT"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/stream_with_error_event")
        let result = try await Kreuzcrawl.crawlStream(engine: engineObj, url: url)
        var chunks: [Kreuzcrawl.ChatCompletionChunk] = []
                for try await _chunk in result { chunks.append(_chunk) }
        XCTAssertTrue(chunks.contains(where: { e in if case .page = e { return true } else { return false } }))
        XCTAssertTrue(chunks.contains(where: { e in if case .complete = e { return true } else { return false } }))
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
    }

}
