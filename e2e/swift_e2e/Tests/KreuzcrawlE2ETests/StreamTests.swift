// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8eae86c36839b50d908ee9293bb951de8f2586fa943ded8ef7529c875438c31a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzcrawl
import RustBridge

/// E2e tests for category: stream.
final class StreamTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testCrawlStreamEvents() async throws {
        // Crawl stream produces page and complete events
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_CRAWL_STREAM_EVENTS"] ?? (AlefE2EMockServer.baseURL + "/fixtures/crawl_stream_events")
        let urlRequest = CrawlStreamRequest(url: url)
        let result = try await Kreuzcrawl.crawlStream(engineObj, urlRequest)
        var chunks: [Kreuzcrawl.CrawlEvent] = []
                for try await _chunk in result { chunks.append(_chunk) }
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
    }

    func testStreamDepthCrawl() async throws {
        // Stream produces events for multi-depth crawl with link following
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":2}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_DEPTH_CRAWL"] ?? (AlefE2EMockServer.baseURL + "/fixtures/stream_depth_crawl")
        let urlRequest = CrawlStreamRequest(url: url)
        let result = try await Kreuzcrawl.crawlStream(engineObj, urlRequest)
        var chunks: [Kreuzcrawl.CrawlEvent] = []
                for try await _chunk in result { chunks.append(_chunk) }
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
    }

    func testStreamErrorEventMidCrawl() async throws {
        // Stream emits error event when a page fails mid-crawl, but other pages succeed
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_ERROR_EVENT_MID_CRAWL"] ?? (AlefE2EMockServer.baseURL + "/fixtures/stream_error_event_mid_crawl")
        let urlRequest = CrawlStreamRequest(url: url)
        let result = try await Kreuzcrawl.crawlStream(engineObj, urlRequest)
        var chunks: [Kreuzcrawl.CrawlEvent] = []
                for try await _chunk in result { chunks.append(_chunk) }
    }

    func testStreamEventOrdering() async throws {
        // Stream ensures complete event arrives after all page events
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_EVENT_ORDERING"] ?? (AlefE2EMockServer.baseURL + "/fixtures/stream_event_ordering")
        let urlRequest = CrawlStreamRequest(url: url)
        let result = try await Kreuzcrawl.crawlStream(engineObj, urlRequest)
        var chunks: [Kreuzcrawl.CrawlEvent] = []
                for try await _chunk in result { chunks.append(_chunk) }
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
    }

    func testStreamLargeCrawl() async throws {
        // Stream handles crawl of 5+ pages with multiple events
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_LARGE_CRAWL"] ?? (AlefE2EMockServer.baseURL + "/fixtures/stream_large_crawl")
        let urlRequest = CrawlStreamRequest(url: url)
        let result = try await Kreuzcrawl.crawlStream(engineObj, urlRequest)
        var chunks: [Kreuzcrawl.CrawlEvent] = []
                for try await _chunk in result { chunks.append(_chunk) }
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
    }

    func testStreamWithErrorEvent() async throws {
        // Stream emits page and complete events even when some pages fail
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STREAM_WITH_ERROR_EVENT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/stream_with_error_event")
        let urlRequest = CrawlStreamRequest(url: url)
        let result = try await Kreuzcrawl.crawlStream(engineObj, urlRequest)
        var chunks: [Kreuzcrawl.CrawlEvent] = []
                for try await _chunk in result { chunks.append(_chunk) }
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
    }

}
