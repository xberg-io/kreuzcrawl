// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c5fe270870f081f7f905fb5d890c97170a6cf9f01f7a2706b293031c7dcc8d23
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzcrawl
import RustBridge

/// E2e tests for category: robots.
final class RobotsTests: XCTestCase {
    func testRobotsAllowAll() async throws {
        // Permissive robots.txt allows all paths
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_ALLOW_ALL"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_allow_all")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isAllowed(), true)
    }

    func testRobotsAllowOverride() async throws {
        // Allow directive overrides Disallow for specific paths
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_ALLOW_OVERRIDE"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_allow_override")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isAllowed(), true)
    }

    func testRobotsCommentsHandling() async throws {
        // Correctly parses robots.txt with inline and line comments
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"kreuzcrawl\"}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_COMMENTS_HANDLING"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_comments_handling")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isAllowed(), true)
    }

    func testRobotsCrawlDelay() async throws {
        // Respects crawl-delay directive from robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"kreuzcrawl\"}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_CRAWL_DELAY"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_crawl_delay")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.crawlDelay(), 2)
    }

    func testRobotsDisallowPath() async throws {
        // Robots.txt disallows specific paths
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_DISALLOW_PATH"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_disallow_path")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isAllowed(), false)
    }

    func testRobotsMetaNofollow() async throws {
        // Detects nofollow meta robots tag and skips link extraction
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_META_NOFOLLOW"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_meta_nofollow")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.nofollowDetected(), true)
    }

    func testRobotsMetaNoindex() async throws {
        // Detects noindex meta robots tag in HTML page
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_META_NOINDEX"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_meta_noindex")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.noindexDetected(), true)
    }

    func testRobotsMissing404() async throws {
        // Missing robots.txt (404) allows all crawling
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_MISSING_404"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_missing_404")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isAllowed(), true)
    }

    func testRobotsMultipleUserAgents() async throws {
        // Picks the most specific user-agent block from robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"SpecificBot\"}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_MULTIPLE_USER_AGENTS"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_multiple_user_agents")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isAllowed(), true)
    }

    func testRobotsRequestRate() async throws {
        // Parses request-rate directive from robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"kreuzcrawl\"}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_REQUEST_RATE"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_request_rate")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.crawlDelay(), 5)
        XCTAssertEqual(result.isAllowed(), true)
    }

    func testRobotsSitemapDirective() async throws {
        // Discovers sitemap URL from Sitemap directive in robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_SITEMAP_DIRECTIVE"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_sitemap_directive")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isAllowed(), true)
    }

    func testRobotsUserAgentSpecific() async throws {
        // Matches user-agent specific rules in robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"KreuzcrawlBot\"}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_USER_AGENT_SPECIFIC"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_user_agent_specific")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isAllowed(), false)
    }

    func testRobotsWildcardPaths() async throws {
        // Handles wildcard Disallow patterns in robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_WILDCARD_PATHS"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_wildcard_paths")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isAllowed(), false)
    }

    func testRobotsXRobotsTag() async throws {
        // Respects X-Robots-Tag HTTP header directives
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_X_ROBOTS_TAG"] ?? (AlefE2EMockServer.baseURL + "/fixtures/robots_x_robots_tag")
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual((result.xRobotsTag()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "noindex, nofollow")
        XCTAssertEqual(result.noindexDetected(), true)
        XCTAssertEqual(result.nofollowDetected(), true)
    }

}
