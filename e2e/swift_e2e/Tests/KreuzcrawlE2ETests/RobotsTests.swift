// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a299151385bf65d1e76a70d5247419cb8d171fff0c9da8ca534daa89c25160a8
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import Kreuzcrawl
import RustBridge

/// E2e tests for category: robots.
final class RobotsTests: XCTestCase {
    func testRobotsAllowAll() async throws {
        // Permissive robots.txt allows all paths
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_ALLOW_ALL"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_allow_all")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_allowed(), true)
    }

    func testRobotsAllowOverride() async throws {
        // Allow directive overrides Disallow for specific paths
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_ALLOW_OVERRIDE"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_allow_override")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_allowed(), true)
    }

    func testRobotsCommentsHandling() async throws {
        // Correctly parses robots.txt with inline and line comments
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"kreuzcrawl\"}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_COMMENTS_HANDLING"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_comments_handling")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_allowed(), true)
    }

    func testRobotsCrawlDelay() async throws {
        // Respects crawl-delay directive from robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"kreuzcrawl\"}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_CRAWL_DELAY"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_crawl_delay")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.crawl_delay(), 2)
    }

    func testRobotsDisallowPath() async throws {
        // Robots.txt disallows specific paths
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_DISALLOW_PATH"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_disallow_path")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_allowed(), false)
    }

    func testRobotsMetaNofollow() async throws {
        // Detects nofollow meta robots tag and skips link extraction
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_META_NOFOLLOW"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_meta_nofollow")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.nofollow_detected(), true)
    }

    func testRobotsMetaNoindex() async throws {
        // Detects noindex meta robots tag in HTML page
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_META_NOINDEX"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_meta_noindex")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.noindex_detected(), true)
    }

    func testRobotsMissing404() async throws {
        // Missing robots.txt (404) allows all crawling
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_MISSING_404"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_missing_404")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_allowed(), true)
    }

    func testRobotsMultipleUserAgents() async throws {
        // Picks the most specific user-agent block from robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"SpecificBot\"}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_MULTIPLE_USER_AGENTS"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_multiple_user_agents")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_allowed(), true)
    }

    func testRobotsRequestRate() async throws {
        // Parses request-rate directive from robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"kreuzcrawl\"}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_REQUEST_RATE"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_request_rate")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.crawl_delay(), 5)
        XCTAssertEqual(result.is_allowed(), true)
    }

    func testRobotsSitemapDirective() async throws {
        // Discovers sitemap URL from Sitemap directive in robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_SITEMAP_DIRECTIVE"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_sitemap_directive")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_allowed(), true)
    }

    func testRobotsUserAgentSpecific() async throws {
        // Matches user-agent specific rules in robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true,\"user_agent\":\"KreuzcrawlBot\"}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_USER_AGENT_SPECIFIC"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_user_agent_specific")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_allowed(), false)
    }

    func testRobotsWildcardPaths() async throws {
        // Handles wildcard Disallow patterns in robots.txt
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_WILDCARD_PATHS"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_wildcard_paths")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_allowed(), false)
    }

    func testRobotsXRobotsTag() async throws {
        // Respects X-Robots-Tag HTTP header directives
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_ROBOTS_X_ROBOTS_TAG"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/robots_x_robots_tag")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual((result.x_robots_tag()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "noindex, nofollow")
        XCTAssertEqual(result.noindex_detected(), true)
        XCTAssertEqual(result.nofollow_detected(), true)
    }

}
