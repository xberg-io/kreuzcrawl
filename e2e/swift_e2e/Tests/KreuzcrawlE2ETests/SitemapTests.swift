// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bb11f9f81861f6fa24850d728f97432a8b5850355e96cc743a3b15633e409b3a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzcrawl
import RustBridge

/// E2e tests for category: sitemap.
final class SitemapTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "KREUZCRAWL_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testSitemapBasic() async throws {
        // Parses a standard urlset sitemap
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/sitemap_basic"
        let result = try await Kreuzcrawl.mapUrls(engine: engineObj, url: url)
        XCTAssertEqual(result.urls.count, 4)
    }

    func testSitemapCompressedGzip() async throws {
        // Parses a gzip-compressed sitemap file
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/sitemap_compressed_gzip"
        let result = try await Kreuzcrawl.mapUrls(engine: engineObj, url: url)
        XCTAssertEqual(result.urls.count, 3)
    }

    func testSitemapEmpty() async throws {
        // Handles empty sitemap gracefully
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/sitemap_empty"
        let result = try await Kreuzcrawl.mapUrls(engine: engineObj, url: url)
        XCTAssertEqual(result.urls.count, 0)
    }

    func testSitemapFromRobotsTxt() async throws {
        // Discovers sitemap via robots.txt Sitemap directive
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_SITEMAP_FROM_ROBOTS_TXT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/sitemap_from_robots_txt")
        let result = try await Kreuzcrawl.mapUrls(engine: engineObj, url: url)
        XCTAssertEqual(result.urls.count, 4)
    }

    func testSitemapIndex() async throws {
        // Follows sitemap index to discover child sitemaps
        let engineObj = try createEngine(config: nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_SITEMAP_INDEX"] ?? (AlefE2EMockServer.baseURL + "/fixtures/sitemap_index")
        let result = try await Kreuzcrawl.mapUrls(engine: engineObj, url: url)
        XCTAssertEqual(result.urls.count, 3)
    }

    func testSitemapLastmodFilter() async throws {
        // Filters sitemap URLs by lastmod date
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/sitemap_lastmod_filter"
        let result = try await Kreuzcrawl.mapUrls(engine: engineObj, url: url)
        XCTAssertEqual(result.urls.count, 4)
    }

    func testSitemapOnlyMode() async throws {
        // Uses sitemap URLs exclusively without following page links
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/sitemap_only_mode"
        let result = try await Kreuzcrawl.mapUrls(engine: engineObj, url: url)
        XCTAssertEqual(result.urls.count, 4)
    }

    func testSitemapXhtmlLinks() async throws {
        // Parses sitemap with XHTML namespace alternate links
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/sitemap_xhtml_links"
        let result = try await Kreuzcrawl.mapUrls(engine: engineObj, url: url)
        XCTAssertEqual(result.urls.count, 2)
    }

}
