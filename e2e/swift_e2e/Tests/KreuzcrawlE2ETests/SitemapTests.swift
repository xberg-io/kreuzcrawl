// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1bcb9feb4b313ad6620d5e967c0d1ef698733e5cd5324c5a033378da7300c19c
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
import Kreuzcrawl
import RustBridge

/// E2e tests for category: sitemap.
final class SitemapTests: XCTestCase {
    func testSitemapBasic() async throws {
        // Parses a standard urlset sitemap
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/sitemap_basic"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.urls().count, 4)
        XCTAssertEqual(result.has_lastmod(), true)
    }

    func testSitemapCompressedGzip() async throws {
        // Parses a gzip-compressed sitemap file
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/sitemap_compressed_gzip"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.urls().count, 3)
    }

    func testSitemapEmpty() async throws {
        // Handles empty sitemap gracefully
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/sitemap_empty"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.urls().count, 0)
    }

    func testSitemapFromRobotsTxt() async throws {
        // Discovers sitemap via robots.txt Sitemap directive
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_SITEMAP_FROM_ROBOTS_TXT"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/sitemap_from_robots_txt")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.urls().count, 4)
    }

    func testSitemapIndex() async throws {
        // Follows sitemap index to discover child sitemaps
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_SITEMAP_INDEX"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/sitemap_index")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.urls().count, 3)
    }

    func testSitemapLastmodFilter() async throws {
        // Filters sitemap URLs by lastmod date
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/sitemap_lastmod_filter"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.urls().count, 4)
        XCTAssertEqual(result.has_lastmod(), true)
    }

    func testSitemapOnlyMode() async throws {
        // Uses sitemap URLs exclusively without following page links
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/sitemap_only_mode"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.urls().count, 4)
    }

    func testSitemapXhtmlLinks() async throws {
        // Parses sitemap with XHTML namespace alternate links
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/sitemap_xhtml_links"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.urls().count, 2)
        XCTAssertEqual(result.has_lastmod(), false)
    }

}
