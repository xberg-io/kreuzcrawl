// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:201177ab1fb2fdec3556e24178fc176a8245e0c8a6a32a882f4cc3ded3e5ac8b
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzcrawl
import RustBridge

/// E2e tests for category: concurrent.
final class ConcurrentTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "KREUZCRAWL_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
        let _existing = ProcessInfo.processInfo.environment["SUT_URL"]
    }

    func testConcurrentBasic() async throws {
        // Concurrent crawling fetches all pages with max_concurrent workers
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":3,\"max_depth\":1}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_CONCURRENT_BASIC"] ?? (AlefE2EMockServer.baseURL + "/fixtures/concurrent_basic")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 6)
        XCTAssertGreaterThanOrEqual(result.pages().count, 6)
    }

    func testConcurrentDepthTwoFanOut() async throws {
        // Concurrent depth=2 crawl correctly fans out and deduplicates across levels
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":3,\"max_depth\":2}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_CONCURRENT_DEPTH_TWO_FAN_OUT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/concurrent_depth_two_fan_out")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 4)
    }

    func testConcurrentMaxPagesExact() async throws {
        // Concurrent crawling does not exceed max_pages limit even with high concurrency
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":5,\"max_depth\":1,\"max_pages\":3}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_CONCURRENT_MAX_PAGES_EXACT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/concurrent_max_pages_exact")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertLessThanOrEqual(result.pages().count, 3)
    }

    func testConcurrentPartialErrors() async throws {
        // Concurrent crawl handles partial failures gracefully
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":3,\"max_depth\":1}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_CONCURRENT_PARTIAL_ERRORS"] ?? (AlefE2EMockServer.baseURL + "/fixtures/concurrent_partial_errors")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertGreaterThanOrEqual(result.pages().count, 2)
    }

    func testConcurrentRespectsMaxPages() async throws {
        // Concurrent crawling respects max_pages limit
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":2,\"max_depth\":1,\"max_pages\":3}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_CONCURRENT_RESPECTS_MAX_PAGES"] ?? (AlefE2EMockServer.baseURL + "/fixtures/concurrent_respects_max_pages")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertLessThanOrEqual(result.pages().count, 3)
    }

}
