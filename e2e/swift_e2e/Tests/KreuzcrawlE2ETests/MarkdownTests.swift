// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:17d586c3ac3271b1cc9403d8d9eec7ec8b279be7db2a86f82deb76428d7c6e13
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
import Kreuzcrawl
import RustBridge

/// E2e tests for category: markdown.
final class MarkdownTests: XCTestCase {
    func testCitationsBalancedParens() async throws {
        // Citations correctly handle links inside parentheses with balanced rendering
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/citations_balanced_parens"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue((result.markdown()?.content().toString() ?? "").contains("("), "expected to contain: \("(")")
    }

    func testCitationsDuplicateUrls() async throws {
        // Citations deduplicates multiple links to the same URL
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/citations_duplicate_urls"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue(result.markdown()?.citations())
    }

    func testMarkdownBasicConversion() async throws {
        // HTML is always converted to markdown alongside raw HTML
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/markdown_basic_conversion"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        XCTAssertEqual((result.metadata().title()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "Test")
        XCTAssertGreaterThan(result.html().toString().count, 0, "expected non-empty value")
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue((result.markdown()?.content().toString() ?? "").contains("Hello World"), "expected to contain: \("Hello World")")
    }

    func testMarkdownCrawlAllPages() async throws {
        // All crawled pages have markdown field populated
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/markdown_crawl_all_pages"
        let result = try await crawl(engineObj, url)
        XCTAssertEqual(result.pages().count, 2)
    }

    func testMarkdownFitContent() async throws {
        // Fit markdown removes navigation and boilerplate content
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/markdown_fit_content"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
    }

    func testMarkdownHeadingsAndParagraphs() async throws {
        // Markdown conversion preserves heading hierarchy and paragraph text
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/markdown_headings_and_paragraphs"
        let result = try await scrape(engineObj, url)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue((result.markdown()?.content().toString() ?? "").contains("Main Title"), "expected to contain: \("Main Title")")
    }

    func testMarkdownLinksConverted() async throws {
        // HTML links are converted to markdown link syntax
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/markdown_links_converted"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        XCTAssertGreaterThan(result.html().toString().count, 0, "expected non-empty value")
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
        XCTAssertTrue((result.markdown()?.content().toString() ?? "").contains("Example"), "expected to contain: \("Example")")
    }

    func testMarkdownWithCitations() async throws {
        // Markdown includes citation conversion with numbered references
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/markdown_with_citations"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        XCTAssertGreaterThan((result.markdown()?.content().toString().count ?? 0), 0, "expected non-empty value")
    }

}
