// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9647f04b586a6ac08f0078e36ea483db02f8a7c33bf630ee47e07f31563a5ec4
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzcrawl
import RustBridge

/// E2e tests for category: content.
final class ContentTests: XCTestCase {
    func testContent204NoContent() async throws {
        // Handles 204 No Content response gracefully
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/content_204_no_content"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 204)
        XCTAssertEqual(result.html().toString().count, 0, "expected empty value")
    }

    func testContentCharsetIso8859() async throws {
        // Handles ISO-8859-1 encoded page correctly
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/content_charset_iso8859"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual((result.detectedCharset()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "iso-8859-1")
    }

    func testContentEmptyBody() async throws {
        // Handles 200 response with empty body gracefully
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/content_empty_body"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
    }

    func testContentGzipCompressed() async throws {
        // Handles response with Accept-Encoding gzip negotiation
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/content_gzip_compressed"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertGreaterThan(result.html().toString().count, 0, "expected non-empty value")
        XCTAssertEqual(result.statusCode(), 200)
    }

    func testContentLargePageLimit() async throws {
        // Respects max body size limit and truncates or skips oversized pages
        let engineConfig = try crawlConfigFromJson("{\"max_body_size\":1024,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/content_large_page_limit"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertLessThan(result.bodySize(), 1025)
    }

    func testContentMainOnly() async throws {
        // Extracts content with aggressive preprocessing, excluding nav, sidebar, footer
        let engineConfig = try crawlConfigFromJson("{\"content\":{\"preprocessing_preset\":\"aggressive\"},\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/content_main_only"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
    }

    func testContentPdfNoExtension() async throws {
        // Detects PDF content by Content-Type header when URL has no .pdf extension
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/content_pdf_no_extension"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.isPdf(), true)
    }

    func testContentRemoveTags() async throws {
        // Removes specified HTML elements by CSS selector before processing
        let engineConfig = try crawlConfigFromJson("{\"remove_tags\":[\"nav\",\"aside\",\"footer\"],\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/content_remove_tags"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertGreaterThan(result.html().toString().count, 0, "expected non-empty value")
    }

    func testContentUtf8Bom() async throws {
        // Handles UTF-8 content with BOM marker correctly
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/content_utf8_bom"
        let result = try await Kreuzcrawl.scrape(engine: engineObj, url: url)
        XCTAssertEqual((result.detectedCharset()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespacesAndNewlines), "utf-8")
        XCTAssertGreaterThan(result.html().toString().count, 0, "expected non-empty value")
    }

}
