// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:4efe1934db25304c4f30ec68ef965522ed616a662921ed1591b8336be68b7f2f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Kreuzcrawl
import RustBridge

/// E2e tests for category: redirect.
final class RedirectTests: XCTestCase {
    func testRedirect301Permanent() async throws {
        // Follows 301 permanent redirect and returns final page content
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_301_PERMANENT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_301_permanent")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/target".isEmpty || result.finalUrl().toString().contains("/target"), "expected to contain: \("/target")")
        XCTAssertEqual(result.redirectCount(), 1)
    }

    func testRedirect302Found() async throws {
        // Follows 302 Found redirect correctly
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_302_FOUND"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_302_found")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/found-target".isEmpty || result.finalUrl().toString().contains("/found-target"), "expected to contain: \("/found-target")")
        XCTAssertEqual(result.redirectCount(), 1)
    }

    func testRedirect303SeeOther() async throws {
        // Follows 303 See Other redirect (method changes to GET)
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_303_SEE_OTHER"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_303_see_other")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/see-other".isEmpty || result.finalUrl().toString().contains("/see-other"), "expected to contain: \("/see-other")")
        XCTAssertEqual(result.redirectCount(), 1)
    }

    func testRedirect307Temporary() async throws {
        // Follows 307 Temporary Redirect (preserves method)
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_307_TEMPORARY"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_307_temporary")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/temp-target".isEmpty || result.finalUrl().toString().contains("/temp-target"), "expected to contain: \("/temp-target")")
        XCTAssertEqual(result.redirectCount(), 1)
    }

    func testRedirect308Permanent() async throws {
        // Follows 308 Permanent Redirect (preserves method)
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_308_PERMANENT"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_308_permanent")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/perm-target".isEmpty || result.finalUrl().toString().contains("/perm-target"), "expected to contain: \("/perm-target")")
        XCTAssertEqual(result.redirectCount(), 1)
    }

    func testRedirectChain() async throws {
        // Follows a chain of redirects (301 -> 302 -> 200)
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_CHAIN"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_chain")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/step2".isEmpty || result.finalUrl().toString().contains("/step2"), "expected to contain: \("/step2")")
        XCTAssertEqual(result.redirectCount(), 2)
    }

    func testRedirectCrossDomain() async throws {
        // Reports cross-domain redirect target without following to external domain
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_CROSS_DOMAIN"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_cross_domain")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/external-redirect".isEmpty || result.finalUrl().toString().contains("/external-redirect"), "expected to contain: \("/external-redirect")")
        XCTAssertEqual(result.redirectCount(), 1)
    }

    func testRedirectLoop() async throws {
        // Detects redirect loop (A -> B -> A) and returns error
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_LOOP"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_loop")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        // skipped: field 'is_error' not available on result type
    }

    func testRedirectMaxExceeded() async throws {
        // Aborts when redirect count exceeds max_redirects limit
        let engineConfig = try crawlConfigFromJson("{\"max_redirects\":2,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_MAX_EXCEEDED"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_max_exceeded")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        // skipped: field 'is_error' not available on result type
    }

    func testRedirectMetaRefresh() async throws {
        // Follows HTML meta-refresh redirect to target page
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/redirect_meta_refresh"
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/target".isEmpty || result.finalUrl().toString().contains("/target"), "expected to contain: \("/target")")
        XCTAssertEqual(result.redirectCount(), 1)
    }

    func testRedirectRefreshHeader() async throws {
        // Handles HTTP Refresh header redirect
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_REFRESH_HEADER"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_refresh_header")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/refreshed".isEmpty || result.finalUrl().toString().contains("/refreshed"), "expected to contain: \("/refreshed")")
        XCTAssertEqual(result.redirectCount(), 1)
    }

    func testRedirectTo404() async throws {
        // Redirect target returns 404 Not Found
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_TO_404"] ?? (AlefE2EMockServer.baseURL + "/fixtures/redirect_to_404")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertTrue("/gone".isEmpty || result.finalUrl().toString().contains("/gone"), "expected to contain: \("/gone")")
        XCTAssertEqual(result.redirectCount(), 1)
        // skipped: field 'is_error' not available on result type
    }

}
