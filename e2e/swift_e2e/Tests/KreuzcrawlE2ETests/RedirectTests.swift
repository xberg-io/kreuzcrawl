// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ec0e250bcd2fab09eca1f426381edc3b1527ffa7879aa608e7c8d1f50a5d0277
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
import Kreuzcrawl
import RustBridge

/// E2e tests for category: redirect.
final class RedirectTests: XCTestCase {
    func testRedirect301Permanent() async throws {
        // Follows 301 permanent redirect and returns final page content
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_301_PERMANENT"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_301_permanent")
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/target"), "expected to contain: \("/target")")
        XCTAssertEqual(result.redirect_count(), 1)
    }

    func testRedirect302Found() async throws {
        // Follows 302 Found redirect correctly
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_302_FOUND"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_302_found")
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/found-target"), "expected to contain: \("/found-target")")
        XCTAssertEqual(result.redirect_count(), 1)
    }

    func testRedirect303SeeOther() async throws {
        // Follows 303 See Other redirect (method changes to GET)
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_303_SEE_OTHER"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_303_see_other")
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/see-other"), "expected to contain: \("/see-other")")
        XCTAssertEqual(result.redirect_count(), 1)
    }

    func testRedirect307Temporary() async throws {
        // Follows 307 Temporary Redirect (preserves method)
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_307_TEMPORARY"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_307_temporary")
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/temp-target"), "expected to contain: \("/temp-target")")
        XCTAssertEqual(result.redirect_count(), 1)
    }

    func testRedirect308Permanent() async throws {
        // Follows 308 Permanent Redirect (preserves method)
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_308_PERMANENT"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_308_permanent")
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/perm-target"), "expected to contain: \("/perm-target")")
        XCTAssertEqual(result.redirect_count(), 1)
    }

    func testRedirectChain() async throws {
        // Follows a chain of redirects (301 -> 302 -> 200)
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_CHAIN"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_chain")
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/step2"), "expected to contain: \("/step2")")
        XCTAssertEqual(result.redirect_count(), 2)
    }

    func testRedirectCrossDomain() async throws {
        // Reports cross-domain redirect target without following to external domain
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_CROSS_DOMAIN"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_cross_domain")
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/external-redirect"), "expected to contain: \("/external-redirect")")
        XCTAssertEqual(result.redirect_count(), 1)
    }

    func testRedirectLoop() async throws {
        // Detects redirect loop (A -> B -> A) and returns error
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_LOOP"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_loop")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_error(), true)
    }

    func testRedirectMaxExceeded() async throws {
        // Aborts when redirect count exceeds max_redirects limit
        let engineConfig = try crawlConfigFromJson("{\"max_redirects\":2,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_MAX_EXCEEDED"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_max_exceeded")
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.is_error(), true)
    }

    func testRedirectMetaRefresh() async throws {
        // Follows HTML meta-refresh redirect to target page
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_meta_refresh"
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/target"), "expected to contain: \("/target")")
        XCTAssertEqual(result.redirect_count(), 1)
    }

    func testRedirectRefreshHeader() async throws {
        // Handles HTTP Refresh header redirect
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_REFRESH_HEADER"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_refresh_header")
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/refreshed"), "expected to contain: \("/refreshed")")
        XCTAssertEqual(result.redirect_count(), 1)
    }

    func testRedirectTo404() async throws {
        // Redirect target returns 404 Not Found
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_REDIRECT_TO_404"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_to_404")
        let result = try await scrape(engineObj, url)
        XCTAssertTrue(result.final_url().toString().contains("/gone"), "expected to contain: \("/gone")")
        XCTAssertEqual(result.redirect_count(), 1)
        XCTAssertEqual(result.is_error(), true)
    }

}
