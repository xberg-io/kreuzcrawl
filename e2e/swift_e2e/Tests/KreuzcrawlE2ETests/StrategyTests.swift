// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:da91696c29af39d9ff9bd2b6b523b339ccf88854b4cb562dfb5d895a8ab99c0a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// swift-format-ignore-file

import XCTest
import Foundation
import Kreuzcrawl

/// E2e tests for category: strategy.
final class StrategyTests: XCTestCase {
    func testStrategyAdaptiveSaturation() async throws {
        // Adaptive strategy stops early when encountering saturation (duplicate content)
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":2,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_ADAPTIVE_SATURATION"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/strategy_adaptive_saturation")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertGreaterThanOrEqual(result.pages().count, 1)
    }

    func testStrategyAdaptiveWindow() async throws {
        // Adaptive strategy crawls more pages when content is diverse
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_ADAPTIVE_WINDOW"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/strategy_adaptive_window")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertGreaterThanOrEqual(result.pages().count, 3)
    }

    func testStrategyBestFirstSeed() async throws {
        // BestFirst strategy always processes the seed URL first
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_BEST_FIRST_SEED"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/strategy_best_first_seed")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 3)
        // skipped: field 'strategy.first_page_url_contains' not available on result type
    }

    func testStrategyBfsDefaultOrder() async throws {
        // BFS strategy visits pages in breadth-first order
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":2}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_BFS_DEFAULT_ORDER"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/strategy_bfs_default_order")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 5)
        // skipped: field 'strategy.crawl_order' not available on result type
    }

    func testStrategyDfsDepthFirst() async throws {
        // DFS strategy visits pages in depth-first order
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":2}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_DFS_DEPTH_FIRST"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/strategy_dfs_depth_first")
        let result = try await Kreuzcrawl.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 5)
        // skipped: field 'strategy.crawl_order' not available on result type
    }

}
