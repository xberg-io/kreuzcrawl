// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:49601f20590eb65839ec0e855462272c315ad6d4edb4933f4aab2e8c6b986b4f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import Kreuzcrawl
import RustBridge

/// E2e tests for category: encoding.
final class EncodingTests: XCTestCase {
    func testEncodingDoubleEncoded() async throws {
        // Handles double-encoded URL characters (%25C3%25B6)
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/encoding_double_encoded"
        let result = try await scrape(engineObj, url)
        XCTAssertGreaterThan(result.html().len(), 0, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.links().count, 1)
    }

    func testEncodingMixedCharsetPage() async throws {
        // Handles charset mismatch between HTTP header and HTML meta tag
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/encoding_mixed_charset_page"
        let result = try await scrape(engineObj, url)
        XCTAssertGreaterThan(result.html().len(), 0, "expected non-empty value")
    }

    func testEncodingPercentEncodedPath() async throws {
        // Handles percent-encoded spaces and characters in URL paths
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/encoding_percent_encoded_path"
        let result = try await scrape(engineObj, url)
        XCTAssertGreaterThan(result.html().len(), 0, "expected non-empty value")
        XCTAssertGreaterThanOrEqual(result.links().count, 2)
    }

    func testEncodingUnicodeUrl() async throws {
        // Handles Unicode characters in URLs (Hebrew, Japanese, Cyrillic)
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/encoding_unicode_url"
        let result = try await scrape(engineObj, url)
        XCTAssertGreaterThan(result.html().len(), 0, "expected non-empty value")
    }

}
