// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: validation

use kreuzcrawl::scrape;

#[tokio::test]
async fn test_validation_invalid_exclude_regex() {
    // Invalid regex in exclude_paths is rejected
    let engine_config: kreuzcrawl::CrawlConfig =
        serde_json::from_str("{\"exclude_paths\":[\"(unclosed\"]}").expect("config should parse");
    let engine = kreuzcrawl::create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = format!(
        "{}/fixtures/{}",
        std::env::var("MOCK_SERVER_URL").expect("MOCK_SERVER_URL not set"),
        "validation_invalid_exclude_regex"
    );
    let result = scrape(&engine, &url).await;
    assert!(result.is_err(), "expected call to fail");
    assert!(
        result.as_ref().unwrap_err().to_string().contains("exclude_path"),
        "error message mismatch"
    );
}

#[tokio::test]
async fn test_validation_invalid_include_regex() {
    // Invalid regex in include_paths is rejected
    let engine_config: kreuzcrawl::CrawlConfig =
        serde_json::from_str("{\"include_paths\":[\"[invalid\"]}").expect("config should parse");
    let engine = kreuzcrawl::create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = format!(
        "{}/fixtures/{}",
        std::env::var("MOCK_SERVER_URL").expect("MOCK_SERVER_URL not set"),
        "validation_invalid_include_regex"
    );
    let result = scrape(&engine, &url).await;
    assert!(result.is_err(), "expected call to fail");
    assert!(
        result.as_ref().unwrap_err().to_string().contains("include_path"),
        "error message mismatch"
    );
}

#[tokio::test]
async fn test_validation_invalid_retry_code() {
    // Retry code outside 100-599 is rejected
    let engine_config: kreuzcrawl::CrawlConfig =
        serde_json::from_str("{\"retry_codes\":[999]}").expect("config should parse");
    let engine = kreuzcrawl::create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = format!(
        "{}/fixtures/{}",
        std::env::var("MOCK_SERVER_URL").expect("MOCK_SERVER_URL not set"),
        "validation_invalid_retry_code"
    );
    let result = scrape(&engine, &url).await;
    assert!(result.is_err(), "expected call to fail");
    assert!(
        result.as_ref().unwrap_err().to_string().contains("retry code"),
        "error message mismatch"
    );
}

#[tokio::test]
async fn test_validation_max_pages_zero() {
    // max_pages=0 is rejected as invalid config
    let engine_config: kreuzcrawl::CrawlConfig =
        serde_json::from_str("{\"max_pages\":0}").expect("config should parse");
    let engine = kreuzcrawl::create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = format!(
        "{}/fixtures/{}",
        std::env::var("MOCK_SERVER_URL").expect("MOCK_SERVER_URL not set"),
        "validation_max_pages_zero"
    );
    let result = scrape(&engine, &url).await;
    assert!(result.is_err(), "expected call to fail");
    assert!(
        result.as_ref().unwrap_err().to_string().contains("max_pages"),
        "error message mismatch"
    );
}

#[tokio::test]
async fn test_validation_max_redirects_too_high() {
    // max_redirects > 100 is rejected as invalid config
    let engine_config: kreuzcrawl::CrawlConfig =
        serde_json::from_str("{\"max_redirects\":200}").expect("config should parse");
    let engine = kreuzcrawl::create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = format!(
        "{}/fixtures/{}",
        std::env::var("MOCK_SERVER_URL").expect("MOCK_SERVER_URL not set"),
        "validation_max_redirects_too_high"
    );
    let result = scrape(&engine, &url).await;
    assert!(result.is_err(), "expected call to fail");
    assert!(
        result.as_ref().unwrap_err().to_string().contains("max_redirects"),
        "error message mismatch"
    );
}

#[tokio::test]
async fn test_validation_timeout_zero() {
    // Zero request timeout is rejected as invalid config
    let engine_config: kreuzcrawl::CrawlConfig =
        serde_json::from_str("{\"request_timeout\":0}").expect("config should parse");
    let engine = kreuzcrawl::create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = format!(
        "{}/fixtures/{}",
        std::env::var("MOCK_SERVER_URL").expect("MOCK_SERVER_URL not set"),
        "validation_timeout_zero"
    );
    let result = scrape(&engine, &url).await;
    assert!(result.is_err(), "expected call to fail");
    assert!(
        result.as_ref().unwrap_err().to_string().contains("request_timeout"),
        "error message mismatch"
    );
}
