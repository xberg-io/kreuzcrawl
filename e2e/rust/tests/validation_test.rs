// Code generated by kreuzcrawl-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

use e2e_rust::helpers;

#[tokio::test]
async fn test_validation_invalid_exclude_regex() {
    // Invalid regex in exclude_paths is rejected
    let mock = helpers::setup_mock_server().await;
    let body = "<html></html>".to_owned();
    helpers::register_mock(&mock, "GET", "/", 200, &[], &body).await;

    let config = kreuzcrawl::CrawlConfig {
        exclude_paths: vec!["(unclosed".to_owned()],
        ..Default::default()
    };

    let err = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .err()
        .expect("build should fail with invalid config");
    assert!(
        err.to_string().contains("exclude_path"),
        "error should contain 'exclude_path', got: {}",
        err
    );
}

#[tokio::test]
async fn test_validation_invalid_include_regex() {
    // Invalid regex in include_paths is rejected
    let mock = helpers::setup_mock_server().await;
    let body = "<html></html>".to_owned();
    helpers::register_mock(&mock, "GET", "/", 200, &[], &body).await;

    let config = kreuzcrawl::CrawlConfig {
        include_paths: vec!["[invalid".to_owned()],
        ..Default::default()
    };

    let err = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .err()
        .expect("build should fail with invalid config");
    assert!(
        err.to_string().contains("include_path"),
        "error should contain 'include_path', got: {}",
        err
    );
}

#[tokio::test]
async fn test_validation_invalid_retry_code() {
    // Retry code outside 100-599 is rejected
    let mock = helpers::setup_mock_server().await;
    let body = "<html></html>".to_owned();
    helpers::register_mock(&mock, "GET", "/", 200, &[], &body).await;

    let config = kreuzcrawl::CrawlConfig {
        retry_codes: vec![999],
        ..Default::default()
    };

    let err = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .err()
        .expect("build should fail with invalid config");
    assert!(
        err.to_string().contains("retry code"),
        "error should contain 'retry code', got: {}",
        err
    );
}

#[tokio::test]
async fn test_validation_max_pages_zero() {
    // max_pages=0 is rejected as invalid config
    let mock = helpers::setup_mock_server().await;
    let body = "<html></html>".to_owned();
    helpers::register_mock(&mock, "GET", "/", 200, &[], &body).await;

    let config = kreuzcrawl::CrawlConfig {
        max_pages: Some(0),
        ..Default::default()
    };

    let err = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .err()
        .expect("build should fail with invalid config");
    assert!(
        err.to_string().contains("max_pages"),
        "error should contain 'max_pages', got: {}",
        err
    );
}

#[tokio::test]
async fn test_validation_max_redirects_too_high() {
    // max_redirects > 100 is rejected as invalid config
    let mock = helpers::setup_mock_server().await;
    let body = "<html></html>".to_owned();
    helpers::register_mock(&mock, "GET", "/", 200, &[], &body).await;

    let config = kreuzcrawl::CrawlConfig {
        max_redirects: 200,
        ..Default::default()
    };

    let err = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .err()
        .expect("build should fail with invalid config");
    assert!(
        err.to_string().contains("max_redirects"),
        "error should contain 'max_redirects', got: {}",
        err
    );
}

#[tokio::test]
async fn test_validation_timeout_zero() {
    // Zero request timeout is rejected as invalid config
    let mock = helpers::setup_mock_server().await;
    let body = "<html></html>".to_owned();
    helpers::register_mock(&mock, "GET", "/", 200, &[], &body).await;

    let config = kreuzcrawl::CrawlConfig {
        request_timeout: std::time::Duration::from_millis(0),
        ..Default::default()
    };

    let err = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .err()
        .expect("build should fail with invalid config");
    assert!(
        err.to_string().contains("request_timeout"),
        "error should contain 'request_timeout', got: {}",
        err
    );
}
