// Code generated by kreuzcrawl-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

use e2e_rust::helpers;
use kreuzcrawl::LinkType;

#[tokio::test]
async fn test_links_anchor_fragment() {
    // Identifies fragment-only links as anchor type
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/complex_links.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.links.iter().any(|l| l.link_type == LinkType::Anchor));
}

#[tokio::test]
async fn test_links_base_tag() {
    // Resolves relative URLs using base tag href
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/base_tag_page.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.links.len() >= 3);
    assert!(result.links.iter().any(|l| l.url.contains("example.com")));
}

#[tokio::test]
async fn test_links_document_types() {
    // Detects PDF, DOCX, XLSX links as document type
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/complex_links.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(
        result
            .links
            .iter()
            .any(|l| l.link_type == LinkType::Document)
    );
}

#[tokio::test]
async fn test_links_empty_href() {
    // Handles empty href attributes without errors
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/empty_href_links.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.links.len() >= 1);
    assert!(result.links.iter().any(|l| l.url.contains("/valid")));
}

#[tokio::test]
async fn test_links_internal_external_classification() {
    // Correctly classifies internal vs external links by domain
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/complex_links.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.links.len() >= 5);
    assert!(
        result
            .links
            .iter()
            .any(|l| l.link_type == LinkType::Internal)
    );
    assert!(
        result
            .links
            .iter()
            .any(|l| l.link_type == LinkType::External)
    );
}

#[tokio::test]
async fn test_links_mailto_javascript_skip() {
    // Skips mailto:, javascript:, and tel: scheme links
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/mailto_javascript_links.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.links.len() >= 1);
    assert!(result.links.iter().all(|l| !l.url.contains("mailto:")));
}

#[tokio::test]
async fn test_links_protocol_relative() {
    // Handles protocol-relative URLs (//example.com) correctly
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/protocol_relative_links.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.links.len() >= 2);
    assert!(result.links.iter().any(|l| l.url.starts_with("//")));
}

#[tokio::test]
async fn test_links_rel_attributes() {
    // Preserves rel=nofollow and rel=canonical attributes
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/complex_links.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.links.len() >= 1);
}

#[tokio::test]
async fn test_links_relative_parent() {
    // Resolves ../ and ./ relative parent path links correctly
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/relative_parent_links.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.links.len() >= 4);
}
