// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f5ffbcb55022f835fb2d4da58096f546138faf9a43a65660ae762132fc867418
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: encoding

use kreuzcrawl::create_engine;
use kreuzcrawl::scrape;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_encoding_double_encoded() {
    // Handles double-encoded URL characters (%25C3%25B6)
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_ENCODING_DOUBLE_ENCODED").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_ENCODING_DOUBLE_ENCODED")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "encoding_double_encoded"))
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert!(!result.html.is_empty(), "expected non-empty value");
    assert!(!result.links.is_empty(), "expected >= 1");
}

#[tokio::test]
async fn test_encoding_mixed_charset_page() {
    // Handles charset mismatch between HTTP header and HTML meta tag
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_ENCODING_MIXED_CHARSET_PAGE").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_ENCODING_MIXED_CHARSET_PAGE").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "encoding_mixed_charset_page"
            )
        })
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert!(!result.html.is_empty(), "expected non-empty value");
}

#[tokio::test]
async fn test_encoding_percent_encoded_path() {
    // Handles percent-encoded spaces and characters in URL paths
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_ENCODING_PERCENT_ENCODED_PATH").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_ENCODING_PERCENT_ENCODED_PATH").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "encoding_percent_encoded_path"
            )
        })
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert!(!result.html.is_empty(), "expected non-empty value");
    assert!(result.links.len() >= 2, "expected >= 2");
}

#[tokio::test]
async fn test_encoding_unicode_url() {
    // Handles Unicode characters in URLs (Hebrew, Japanese, Cyrillic)
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_ENCODING_UNICODE_URL").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_ENCODING_UNICODE_URL")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "encoding_unicode_url"))
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert!(!result.html.is_empty(), "expected non-empty value");
}
