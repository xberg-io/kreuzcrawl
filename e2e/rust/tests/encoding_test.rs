// Code generated by kreuzcrawl-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

use e2e_rust::helpers;

#[tokio::test]
async fn test_encoding_double_encoded() {
    // Handles double-encoded URL characters (%25C3%25B6)
    let mock = helpers::setup_mock_server().await;
    let body = "<html><body><a href=\"/path%25C3%25B6name\">Double Encoded</a><p>Content</p></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(!result.html.is_empty());
    assert!(result.links.len() >= 1);
}

#[tokio::test]
async fn test_encoding_mixed_charset_page() {
    // Handles charset mismatch between HTTP header and HTML meta tag
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/mixed_charset.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=iso-8859-1")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(!result.html.is_empty());
}

#[tokio::test]
async fn test_encoding_percent_encoded_path() {
    // Handles percent-encoded spaces and characters in URL paths
    let mock = helpers::setup_mock_server().await;
    let body = "<html><body><a href=\"/my%20page\">Spaced Page</a><a href=\"/caf%C3%A9\">Cafe</a><p>Content</p></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(!result.html.is_empty());
    assert!(result.links.len() >= 2);
}

#[tokio::test]
async fn test_encoding_unicode_url() {
    // Handles Unicode characters in URLs (Hebrew, Japanese, Cyrillic)
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/unicode_content.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(!result.html.is_empty());
}
