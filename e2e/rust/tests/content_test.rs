// Code generated by kreuzcrawl-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

use e2e_rust::helpers;

#[tokio::test]
async fn test_content_204_no_content() {
    // Handles 204 No Content response gracefully
    let mock = helpers::setup_mock_server().await;
    let body = String::new();
    helpers::register_mock(&mock, "GET", "/", 204, &[], &body).await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 204);
    assert!(result.html.is_empty());
}

#[tokio::test]
async fn test_content_charset_iso8859() {
    // Handles ISO-8859-1 encoded page correctly
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/charset_iso8859.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=iso-8859-1")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.detected_charset.as_deref(), Some("iso-8859-1"));
}

#[tokio::test]
async fn test_content_empty_body() {
    // Handles 200 response with empty body gracefully
    let mock = helpers::setup_mock_server().await;
    let body = "".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
}

#[tokio::test]
async fn test_content_gzip_compressed() {
    // Handles response with Accept-Encoding gzip negotiation
    let mock = helpers::setup_mock_server().await;
    let body = "<html><body><h1>Content that could be compressed</h1><p>The crawler should send Accept-Encoding: gzip and handle compressed responses.</p></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(!result.html.is_empty());
}

#[tokio::test]
async fn test_content_large_page_limit() {
    // Respects max body size limit and truncates or skips oversized pages
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/large_page.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        max_body_size: Some(1024),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.body_size <= 1024);
}

#[tokio::test]
async fn test_content_main_only() {
    // Extracts only main content area, excluding nav, sidebar, footer
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/main_content_page.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        main_content_only: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.main_content_only, true);
}

#[tokio::test]
async fn test_content_pdf_no_extension() {
    // Detects PDF content by Content-Type header when URL has no .pdf extension
    let mock = helpers::setup_mock_server().await;
    let body = "%PDF-1.4 fake pdf content".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "application/pdf")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_pdf, true);
}

#[tokio::test]
async fn test_content_remove_tags() {
    // Removes specified HTML elements by CSS selector before processing
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/main_content_page.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        remove_tags: vec!["nav".to_owned(), "aside".to_owned(), "footer".to_owned()],
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(!result.html.is_empty());
}

#[tokio::test]
async fn test_content_utf8_bom() {
    // Handles UTF-8 content with BOM marker correctly
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/utf8_bom.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(!result.html.is_empty());
    assert_eq!(result.detected_charset.as_deref(), Some("utf-8"));
}
