// Code generated by kreuzcrawl-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

use e2e_rust::helpers;

#[tokio::test]
async fn test_robots_allow_all() {
    // Permissive robots.txt allows all paths
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/allow_all.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, true);
}

#[tokio::test]
async fn test_robots_allow_override() {
    // Allow directive overrides Disallow for specific paths
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/allow_override.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, true);
}

#[tokio::test]
async fn test_robots_comments_handling() {
    // Correctly parses robots.txt with inline and line comments
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/with_comments.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        user_agent: Some("kreuzcrawl".to_owned()),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, true);
}

#[tokio::test]
async fn test_robots_crawl_delay() {
    // Respects crawl-delay directive from robots.txt
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/crawl_delay.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        user_agent: Some("kreuzcrawl".to_owned()),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.crawl_delay, Some(2));
}

#[tokio::test]
async fn test_robots_disallow_path() {
    // Robots.txt disallows specific paths
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/disallow_admin.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, false);
}

#[tokio::test]
async fn test_robots_meta_nofollow() {
    // Detects nofollow meta robots tag and skips link extraction
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/meta_nofollow.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.nofollow_detected, true);
}

#[tokio::test]
async fn test_robots_meta_noindex() {
    // Detects noindex meta robots tag in HTML page
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/meta_noindex.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.noindex_detected, true);
}

#[tokio::test]
async fn test_robots_missing_404() {
    // Missing robots.txt (404) allows all crawling
    let mock = helpers::setup_mock_server().await;
    let body = "Not Found".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        404,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        404,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, true);
}

#[tokio::test]
async fn test_robots_multiple_user_agents() {
    // Picks the most specific user-agent block from robots.txt
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/multiple_agents.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        user_agent: Some("SpecificBot".to_owned()),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, true);
}

#[tokio::test]
async fn test_robots_request_rate() {
    // Parses request-rate directive from robots.txt
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/request_rate.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        user_agent: Some("kreuzcrawl".to_owned()),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, true);
    assert_eq!(result.crawl_delay, Some(5));
}

#[tokio::test]
async fn test_robots_sitemap_directive() {
    // Discovers sitemap URL from Sitemap directive in robots.txt
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/with_sitemap.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, true);
}

#[tokio::test]
async fn test_robots_user_agent_specific() {
    // Matches user-agent specific rules in robots.txt
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/user_agent_specific.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        user_agent: Some("KreuzcrawlBot".to_owned()),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, false);
}

#[tokio::test]
async fn test_robots_wildcard_paths() {
    // Handles wildcard Disallow patterns in robots.txt
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("robots/wildcard_paths.txt");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[("content-type", "text/plain")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.is_allowed, false);
}

#[tokio::test]
async fn test_robots_x_robots_tag() {
    // Respects X-Robots-Tag HTTP header directives
    let mock = helpers::setup_mock_server().await;
    let body = "<html><body><h1>Page with X-Robots-Tag</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[
            ("x-robots-tag", "noindex, nofollow"),
            ("content-type", "text/html; charset=utf-8"),
        ],
        &body,
    )
    .await;
    helpers::register_mock(
        &mock,
        "GET",
        "/robots.txt",
        200,
        &[
            ("x-robots-tag", "noindex, nofollow"),
            ("content-type", "text/html; charset=utf-8"),
        ],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: true,
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.noindex_detected, true);
    assert_eq!(result.nofollow_detected, true);
    assert_eq!(result.x_robots_tag.as_deref(), Some("noindex, nofollow"));
}
