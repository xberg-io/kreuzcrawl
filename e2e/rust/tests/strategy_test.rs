// Code generated by kreuzcrawl-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

use e2e_rust::helpers;

#[tokio::test]
async fn test_strategy_best_first_seed() {
    // BestFirst strategy always processes the seed URL first
    let mock = helpers::setup_mock_server().await;
    let body_0 =
        "<html><body><a href=\"/page1\">Page 1</a><a href=\"/page2\">Page 2</a></body></html>"
            .to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html")],
        &body_0,
    )
    .await;
    let body_1 = "<html><body><h1>Page 1</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/page1",
        200,
        &[("content-type", "text/html")],
        &body_1,
    )
    .await;
    let body_2 = "<html><body><h1>Page 2</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/page2",
        200,
        &[("content-type", "text/html")],
        &body_2,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        max_depth: Some(1),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .strategy(kreuzcrawl::BestFirstStrategy)
        .build();
    let result = engine.crawl(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.pages.len(), 3);
    assert!(result.pages.first().unwrap().url.contains("/"));
}

#[tokio::test]
async fn test_strategy_bfs_default_order() {
    // BFS strategy visits pages in breadth-first order
    let mock = helpers::setup_mock_server().await;
    let body_0 = "<html><body><a href=\"/a\">A</a><a href=\"/b\">B</a></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html")],
        &body_0,
    )
    .await;
    let body_1 = "<html><body><a href=\"/a/1\">A1</a></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/a",
        200,
        &[("content-type", "text/html")],
        &body_1,
    )
    .await;
    let body_2 = "<html><body><a href=\"/b/1\">B1</a></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/b",
        200,
        &[("content-type", "text/html")],
        &body_2,
    )
    .await;
    let body_3 = "<html><body><h1>A1</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/a/1",
        200,
        &[("content-type", "text/html")],
        &body_3,
    )
    .await;
    let body_4 = "<html><body><h1>B1</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/b/1",
        200,
        &[("content-type", "text/html")],
        &body_4,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        max_depth: Some(2),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.crawl(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.pages.len(), 5);
    assert!(
        result.pages[0].url.contains("/"),
        "page 0 should contain '/', got '{}'",
        result.pages[0].url
    );
    assert!(
        result.pages[1].url.contains("/a"),
        "page 1 should contain '/a', got '{}'",
        result.pages[1].url
    );
    assert!(
        result.pages[2].url.contains("/b"),
        "page 2 should contain '/b', got '{}'",
        result.pages[2].url
    );
    assert!(
        result.pages[3].url.contains("/a/1"),
        "page 3 should contain '/a/1', got '{}'",
        result.pages[3].url
    );
    assert!(
        result.pages[4].url.contains("/b/1"),
        "page 4 should contain '/b/1', got '{}'",
        result.pages[4].url
    );
}

#[tokio::test]
async fn test_strategy_dfs_depth_first() {
    // DFS strategy visits pages in depth-first order
    let mock = helpers::setup_mock_server().await;
    let body_0 = "<html><body><a href=\"/a\">A</a><a href=\"/b\">B</a></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html")],
        &body_0,
    )
    .await;
    let body_1 = "<html><body><a href=\"/a/1\">A1</a></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/a",
        200,
        &[("content-type", "text/html")],
        &body_1,
    )
    .await;
    let body_2 = "<html><body><a href=\"/b/1\">B1</a></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/b",
        200,
        &[("content-type", "text/html")],
        &body_2,
    )
    .await;
    let body_3 = "<html><body><h1>A1</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/a/1",
        200,
        &[("content-type", "text/html")],
        &body_3,
    )
    .await;
    let body_4 = "<html><body><h1>B1</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/b/1",
        200,
        &[("content-type", "text/html")],
        &body_4,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        max_depth: Some(2),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .strategy(kreuzcrawl::DfsStrategy)
        .build();
    let result = engine.crawl(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.pages.len(), 5);
    assert!(
        result.pages[0].url.contains("/"),
        "page 0 should contain '/', got '{}'",
        result.pages[0].url
    );
    assert!(
        result.pages[1].url.contains("/b"),
        "page 1 should contain '/b', got '{}'",
        result.pages[1].url
    );
    assert!(
        result.pages[2].url.contains("/b/1"),
        "page 2 should contain '/b/1', got '{}'",
        result.pages[2].url
    );
    assert!(
        result.pages[3].url.contains("/a"),
        "page 3 should contain '/a', got '{}'",
        result.pages[3].url
    );
    assert!(
        result.pages[4].url.contains("/a/1"),
        "page 4 should contain '/a/1', got '{}'",
        result.pages[4].url
    );
}
