// Code generated by kreuzcrawl-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

use e2e_rust::helpers;

#[tokio::test]
async fn test_auth_basic_http() {
    // Sends HTTP Basic authentication header
    let mock = helpers::setup_mock_server().await;
    let body = "<html><body><h1>Authenticated Page</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        auth: Some(kreuzcrawl::AuthConfig::Basic {
            username: "testuser".to_owned(),
            password: "testpass".to_owned(),
        }),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.auth_header_sent);
    assert_eq!(result.status_code, 200);
}

#[tokio::test]
async fn test_auth_bearer_token() {
    // Sends Bearer token in Authorization header
    let mock = helpers::setup_mock_server().await;
    let body = "<html><body><h1>Bearer Auth Page</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        auth: Some(kreuzcrawl::AuthConfig::Bearer {
            token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.test".to_owned(),
        }),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.auth_header_sent);
    assert_eq!(result.status_code, 200);
}

#[tokio::test]
async fn test_auth_custom_header() {
    // Sends authentication via custom header (X-API-Key)
    let mock = helpers::setup_mock_server().await;
    let body = "<html><body><h1>API Key Auth Page</h1></body></html>".to_owned();
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        respect_robots_txt: false,
        auth: Some(kreuzcrawl::AuthConfig::Header {
            name: "X-API-Key".to_owned(),
            value: "sk-test-key-12345".to_owned(),
        }),
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert!(result.auth_header_sent);
    assert_eq!(result.status_code, 200);
}
