// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c775d3eecd441399f751cd484568703e9cce303599463ed0e2eb79b18ed53bc9
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
//! E2e tests for category: browser

use kreuzcrawl::create_engine;
use kreuzcrawl::scrape;
use kreuzcrawl::CrawlConfig;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_browser_config_auto_no_feature() {
    // Browser mode 'auto' without browser feature enabled does not use browser
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"mode\":\"auto\"}}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_CONFIG_AUTO_NO_FEATURE").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_CONFIG_AUTO_NO_FEATURE").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "browser_config_auto_no_feature"
            )
        })
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_config_never_mode() {
    // Browser mode 'never' prevents browser fallback even for SPA shell content
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"mode\":\"never\"}}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_CONFIG_NEVER_MODE").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_CONFIG_NEVER_MODE")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_config_never_mode"))
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_crawl_mode_always() {
    // Crawl with browser mode 'always' follows links using browser rendering
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"mode\":\"always\"},\"max_depth\":1,\"respect_robots_txt\":false}")
            .expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_CRAWL_MODE_ALWAYS").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_CRAWL_MODE_ALWAYS")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_crawl_mode_always"))
    });
    let _ = scrape(&engine, &url).await.expect("should succeed");
    // skipped: field 'pages.length' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_crawl_waf_fallback() {
    // Crawl with browser mode 'auto' falls back to browser when encountering WAF 403
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"mode\":\"auto\"},\"max_depth\":1,\"respect_robots_txt\":false}")
            .expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_CRAWL_WAF_FALLBACK").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_CRAWL_WAF_FALLBACK").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "browser_crawl_waf_fallback"
            )
        })
    });
    let _ = scrape(&engine, &url).await.expect("should succeed");
    // skipped: field 'pages.length' not available on result type
}

#[tokio::test]
async fn test_browser_detect_minimal_page() {
    // Does NOT flag a short but real content page as needing JS rendering
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_DETECT_MINIMAL_PAGE").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_DETECT_MINIMAL_PAGE").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "browser_detect_minimal_page"
            )
        })
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_detect_next_empty() {
    // Detects Next.js page with __NEXT_DATA__ but no rendered content as needing JS rendering
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_DETECT_NEXT_EMPTY").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_DETECT_NEXT_EMPTY")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_detect_next_empty"))
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_detect_next_rendered() {
    // Does NOT flag Next.js page with full SSR content as needing JS rendering
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_DETECT_NEXT_RENDERED").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_DETECT_NEXT_RENDERED").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "browser_detect_next_rendered"
            )
        })
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    // skipped: field 'html_not_empty' not available on result type
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_detect_normal_page() {
    // Does NOT flag a normal server-rendered page as needing JS rendering
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_DETECT_NORMAL_PAGE").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_DETECT_NORMAL_PAGE").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "browser_detect_normal_page"
            )
        })
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_detect_nuxt_shell() {
    // Detects Nuxt SPA shell with empty #__nuxt div as needing JS rendering
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_DETECT_NUXT_SHELL").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_DETECT_NUXT_SHELL")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_detect_nuxt_shell"))
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_detect_react_shell() {
    // Detects React SPA shell with empty #root div as needing JS rendering
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_DETECT_REACT_SHELL").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_DETECT_REACT_SHELL").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "browser_detect_react_shell"
            )
        })
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    // skipped: field 'html_not_empty' not available on result type
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_detect_vue_shell() {
    // Detects Vue SPA shell with empty #app div as needing JS rendering
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_DETECT_VUE_SHELL").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_DETECT_VUE_SHELL")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_detect_vue_shell"))
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_extra_wait() {
    // Browser extra_wait adds additional time after network_idle to ensure all async operations complete
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"extra_wait\":200,\"mode\":\"always\",\"wait\":\"network_idle\"}}")
            .expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_EXTRA_WAIT").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_EXTRA_WAIT")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_extra_wait"))
    });
    let _ = scrape(&engine, &url).await.expect("should succeed");
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_fallback_spa_render() {
    // Browser auto re-fetches SPA shell when JS rendering is detected
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"mode\":\"auto\"}}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_FALLBACK_SPA_RENDER").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_FALLBACK_SPA_RENDER").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "browser_fallback_spa_render"
            )
        })
    });
    let _ = scrape(&engine, &url).await.expect("should succeed");
    // skipped: field 'browser.js_render_hint' not available on result type
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_fallback_waf_blocked() {
    // Browser fallback triggers when WAF blocks the HTTP request (Cloudflare 403)
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"mode\":\"auto\"}}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_FALLBACK_WAF_BLOCKED").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_FALLBACK_WAF_BLOCKED").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "browser_fallback_waf_blocked"
            )
        })
    });
    let _ = scrape(&engine, &url).await.expect("should succeed");
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_mode_always() {
    // Browser mode 'always' uses browser even for normal server-rendered pages
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"mode\":\"always\"}}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_MODE_ALWAYS").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_MODE_ALWAYS")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_mode_always"))
    });
    let _ = scrape(&engine, &url).await.expect("should succeed");
    // skipped: field 'browser.browser_used' not available on result type
}

#[tokio::test]
async fn test_browser_profile_basic() {
    // Browser profile configuration persists and reuses browser state across crawl sessions
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"mode\":\"always\"},\"browser_profile\":\"test-profile\"}")
            .expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_PROFILE_BASIC").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_PROFILE_BASIC")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_profile_basic"))
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    // skipped: field 'browser.browser_used' not available on result type
    assert_eq!(result.status_code, 200, "equals assertion failed");
}

#[tokio::test]
async fn test_browser_wait_fixed() {
    // Browser wait strategy 'fixed' waits for a specific duration after page navigation
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"browser\":{\"extra_wait\":100,\"mode\":\"always\",\"wait\":\"fixed\"}}")
            .expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_WAIT_FIXED").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_WAIT_FIXED")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_wait_fixed"))
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    // skipped: field 'browser.browser_used' not available on result type
    assert_eq!(result.status_code, 200, "equals assertion failed");
}

#[tokio::test]
async fn test_browser_wait_selector() {
    // Browser wait strategy 'selector' waits for specific CSS selector before considering page loaded
    let engine_config: CrawlConfig = serde_json::from_str(
        "{\"browser\":{\"mode\":\"always\",\"wait\":\"selector\",\"wait_selector\":\"#content\"}}",
    )
    .expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_BROWSER_WAIT_SELECTOR").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_BROWSER_WAIT_SELECTOR")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "browser_wait_selector"))
    });
    let result = scrape(&engine, &url).await.expect("should succeed");
    // skipped: field 'browser.browser_used' not available on result type
    assert_eq!(result.status_code, 200, "equals assertion failed");
}
