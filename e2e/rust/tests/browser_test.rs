// Code generated by kreuzcrawl-e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

use e2e_rust::helpers;

#[tokio::test]
async fn test_browser_config_auto_no_feature() {
    // Browser mode 'auto' without browser feature enabled does not use browser
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/spa_react_shell.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        browser: kreuzcrawl::BrowserConfig {
            mode: kreuzcrawl::BrowserMode::Auto,
            ..Default::default()
        },
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(result.js_render_hint);
    assert!(!result.browser_used);
}

#[tokio::test]
async fn test_browser_config_never_mode() {
    // Browser mode 'never' prevents browser fallback even for SPA shell content
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/spa_react_shell.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        browser: kreuzcrawl::BrowserConfig {
            mode: kreuzcrawl::BrowserMode::Never,
            ..Default::default()
        },
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(result.js_render_hint);
    assert!(!result.browser_used);
}

#[tokio::test]
async fn test_browser_detect_minimal_page() {
    // Does NOT flag a short but real content page as needing JS rendering
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/spa_minimal_page.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(!result.js_render_hint);
    assert!(!result.browser_used);
}

#[tokio::test]
async fn test_browser_detect_next_empty() {
    // Detects Next.js page with __NEXT_DATA__ but no rendered content as needing JS rendering
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/spa_next_empty.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(result.js_render_hint);
    assert!(!result.browser_used);
}

#[tokio::test]
async fn test_browser_detect_next_rendered() {
    // Does NOT flag Next.js page with full SSR content as needing JS rendering
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/spa_next_rendered.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(!result.html.is_empty());
    assert!(!result.js_render_hint);
    assert!(!result.browser_used);
}

#[tokio::test]
async fn test_browser_detect_normal_page() {
    // Does NOT flag a normal server-rendered page as needing JS rendering
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/basic_page.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(!result.js_render_hint);
    assert!(!result.browser_used);
}

#[tokio::test]
async fn test_browser_detect_nuxt_shell() {
    // Detects Nuxt SPA shell with empty #__nuxt div as needing JS rendering
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/spa_nuxt_shell.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(result.js_render_hint);
    assert!(!result.browser_used);
}

#[tokio::test]
async fn test_browser_detect_react_shell() {
    // Detects React SPA shell with empty #root div as needing JS rendering
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/spa_react_shell.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(!result.html.is_empty());
    assert!(result.js_render_hint);
    assert!(!result.browser_used);
}

#[tokio::test]
async fn test_browser_detect_vue_shell() {
    // Detects Vue SPA shell with empty #app div as needing JS rendering
    let mock = helpers::setup_mock_server().await;
    let body = helpers::load_response_body("html/spa_vue_shell.html");
    helpers::register_mock(
        &mock,
        "GET",
        "/",
        200,
        &[("content-type", "text/html; charset=utf-8")],
        &body,
    )
    .await;

    let config = kreuzcrawl::CrawlConfig {
        ..Default::default()
    };

    let engine = kreuzcrawl::CrawlEngine::builder()
        .config(config.clone())
        .build()
        .unwrap();
    let result = engine.scrape(&mock.uri()).await;
    let result = result.expect("request should succeed");
    assert_eq!(result.status_code, 200);
    assert!(result.js_render_hint);
    assert!(!result.browser_used);
}
