// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f09626199f73e080e9cc94799e34800206647c019607292fef11877d11f5acb2
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code

import { describe, expect, it } from "vitest";
import {
	scrape,
	crawl,
	createEngine,
	WasmCrawlConfig,
	WasmAuthConfig,
	WasmBrowserConfig,
	WasmContentConfig,
	WasmProxyConfig,
} from "@kreuzberg/kreuzcrawl-wasm";

async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [
		_alefE2eText(item),
		_alefE2eText(record.kind),
		_alefE2eText(record.name),
		_alefE2eText(record.source),
		_alefE2eText(record.alias),
		_alefE2eText(record.text),
		_alefE2eText(record.signature),
		itemsText,
	];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}

describe("filter", () => {
	it("filter_bm25_crawl_integration: BM25 filter works during multi-page crawl, keeping relevant pages", async () => {
		const engineConfig = WasmCrawlConfig.default();
		engineConfig.maxConcurrent = 1;
		engineConfig.maxDepth = 1;
		const engine = createEngine(engineConfig);
		const url =
			process.env.MOCK_SERVER_FILTER_BM25_CRAWL_INTEGRATION ??
			`${process.env.MOCK_SERVER_URL}/fixtures/filter_bm25_crawl_integration`;
		await crawl(engine, url);
		// skipped: field 'filter.remaining_contain_keyword' not available on result type
	}, 30000);
	it("filter_bm25_empty_query: BM25 filter with empty query passes all pages through", async () => {
		const engineConfig = WasmCrawlConfig.default();
		engineConfig.maxDepth = 1;
		const engine = createEngine(engineConfig);
		const url =
			process.env.MOCK_SERVER_FILTER_BM25_EMPTY_QUERY ??
			`${process.env.MOCK_SERVER_URL}/fixtures/filter_bm25_empty_query`;
		await crawl(engine, url);
		// skipped: field 'crawl.pages_crawled' not available on result type
	}, 30000);
	it("filter_bm25_high_threshold: BM25 filter with very high threshold filters out all pages", async () => {
		const engineConfig = WasmCrawlConfig.default();
		engineConfig.maxDepth = 1;
		const engine = createEngine(engineConfig);
		const url =
			process.env.MOCK_SERVER_FILTER_BM25_HIGH_THRESHOLD ??
			`${process.env.MOCK_SERVER_URL}/fixtures/filter_bm25_high_threshold`;
		await scrape(engine, url);
		// skipped: field 'filter.pages_after_filter' not available on result type
	}, 30000);
	it("filter_bm25_relevant_pages: BM25 filter keeps only pages relevant to the query", async () => {
		const engineConfig = WasmCrawlConfig.default();
		engineConfig.maxDepth = 1;
		const engine = createEngine(engineConfig);
		const url =
			process.env.MOCK_SERVER_FILTER_BM25_RELEVANT_PAGES ??
			`${process.env.MOCK_SERVER_URL}/fixtures/filter_bm25_relevant_pages`;
		await scrape(engine, url);
		// skipped: field 'filter.remaining_contain_keyword' not available on result type
	}, 30000);
	it("filter_bm25_threshold_zero: BM25 filter with zero threshold passes all pages", async () => {
		const engineConfig = WasmCrawlConfig.default();
		engineConfig.maxDepth = 1;
		const engine = createEngine(engineConfig);
		const url =
			process.env.MOCK_SERVER_FILTER_BM25_THRESHOLD_ZERO ??
			`${process.env.MOCK_SERVER_URL}/fixtures/filter_bm25_threshold_zero`;
		await crawl(engine, url);
		// skipped: field 'crawl.pages_crawled' not available on result type
	}, 30000);
	it("filter_noop_crawl_all_kept: NoopFilter keeps all pages during a multi-page crawl", async () => {
		const engineConfig = WasmCrawlConfig.default();
		engineConfig.maxConcurrent = 1;
		engineConfig.maxDepth = 1;
		const engine = createEngine(engineConfig);
		const url =
			process.env.MOCK_SERVER_FILTER_NOOP_CRAWL_ALL_KEPT ??
			`${process.env.MOCK_SERVER_URL}/fixtures/filter_noop_crawl_all_kept`;
		await crawl(engine, url);
		// skipped: field 'filter.pages_after_filter' not available on result type
	}, 30000);
	it("filter_noop_passes_all: No content filter passes all crawled pages through", async () => {
		const engineConfig = WasmCrawlConfig.default();
		engineConfig.maxDepth = 1;
		const engine = createEngine(engineConfig);
		const url =
			process.env.MOCK_SERVER_FILTER_NOOP_PASSES_ALL ??
			`${process.env.MOCK_SERVER_URL}/fixtures/filter_noop_passes_all`;
		await crawl(engine, url);
		// skipped: field 'crawl.pages_crawled' not available on result type
	}, 30000);
});
