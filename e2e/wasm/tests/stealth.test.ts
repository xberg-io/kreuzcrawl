// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e580bfd264b31dee4811f765bf044eed5b554cf9ab71a4c37dd653b6e91414e1
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

import { describe, expect, it } from "vitest";
import {
  scrape,
  crawl,
  createEngine,
  WasmCrawlConfig,
  WasmAuthConfig,
  WasmBrowserConfig,
  WasmContentConfig,
  WasmProxyConfig,
} from "kreuzcrawl";
function _alefE2eText(value: unknown): string {
  return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
  if (item == null || typeof item !== "object") {
    return [_alefE2eText(item)];
  }
  const record = item as Record<string, unknown>;
  const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
  return [
    _alefE2eText(item),
    _alefE2eText(record.kind),
    _alefE2eText(record.name),
    _alefE2eText(record.source),
    _alefE2eText(record.alias),
    _alefE2eText(record.text),
    _alefE2eText(record.signature),
    itemsText,
  ];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
  if (fm == null) return "";
  if (typeof fm !== "object") return String(fm);
  const record = fm as Record<string, unknown>;
  const formatType = record.format_type;

  // FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
  // Extract the display string based on the variant type
  if (formatType === "image" && typeof record.image === "object") {
    const imageData = record.image as Record<string, unknown>;
    if (typeof imageData.format === "string") return imageData.format;
  }

  // Fallback: return format_type variant name
  if (typeof record.format_type === "string") return record.format_type;
  return "";
}

describe("stealth", () => {
  it("stealth_ua_rotation_config: User-agent rotation config is accepted and crawl succeeds", async () => {
    const engineConfig = WasmCrawlConfig.default();
    engineConfig.userAgents = ["Mozilla/5.0 (Windows NT 10.0)", "Chrome/120.0.0.0"];
    const engine = createEngine(engineConfig);
    const url = `${process.env.MOCK_SERVER_URL}/fixtures/stealth_ua_rotation_config`;
    const result = await scrape(engine, url);
    expect(Number(result.statusCode)).toBe(200);
  }, 30000);
  it("stealth_ua_rotation_round_robin: User-agent rotation cycles through multiple agents across multiple requests", async () => {
    const engineConfig = WasmCrawlConfig.default();
    engineConfig.maxDepth = 1;
    engineConfig.maxPages = 3;
    engineConfig.userAgents = [
      "Mozilla/5.0 (Windows NT 10.0; Win64; x64) TestAgent-1",
      "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15) TestAgent-2",
    ];
    const engine = createEngine(engineConfig);
    const url =
      process.env.MOCK_SERVER_STEALTH_UA_ROTATION_ROUND_ROBIN ??
      `${process.env.MOCK_SERVER_URL}/fixtures/stealth_ua_rotation_round_robin`;
    const result = await crawl(engine, url);
    expect(Number(result.pages.length)).toBeGreaterThanOrEqual(2);
  }, 30000);
  it("stealth_ua_rotation_single_domain: Custom user-agent string is applied for single domain crawl", async () => {
    const engineConfig = WasmCrawlConfig.default();
    engineConfig.maxDepth = 0;
    engineConfig.stayOnDomain = true;
    engineConfig.userAgents = ["Mozilla/5.0 TestBot/1.0 (+http://example.com/bot)"];
    const engine = createEngine(engineConfig);
    const url = `${process.env.MOCK_SERVER_URL}/fixtures/stealth_ua_rotation_single_domain`;
    const result = await crawl(engine, url);
    expect(Number(result.pages[0].statusCode)).toBe(200);
    expect(Number(result.pages.length)).toBe(1);
  }, 30000);
});
