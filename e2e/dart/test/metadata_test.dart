// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8f8f24a2e33ee678d1081d672e715d00af0c6ccb5f2222ebcd9b343b76f9cba0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// ignore_for_file: unused_local_variable

import 'package:test/test.dart';
import 'dart:io';
import 'package:kreuzcrawl/kreuzcrawl.dart';
import 'package:kreuzcrawl/src/kreuzcrawl_bridge_generated/frb_generated.dart'
    show RustLib;
import 'dart:async';
import 'dart:convert';

// E2e tests for category: metadata

String _alefE2eText(Object? value) {
  if (value == null) return '';
  // Check if it's an enum by examining its toString representation.
  final str = value.toString();
  if (str.contains('.')) {
    // Enum.toString() returns 'EnumName.variantName'. Extract the variant name.
    final parts = str.split('.');
    if (parts.length == 2) {
      final variantName = parts[1];
      // Convert camelCase variant names to snake_case for serde compatibility.
      // E.g. 'toolCalls' -> 'tool_calls', 'stop' -> 'stop'.
      return _camelToSnake(variantName);
    }
  }
  return str;
}

String _camelToSnake(String camel) {
  final buffer = StringBuffer();
  for (int i = 0; i < camel.length; i++) {
    final char = camel[i];
    if (char.contains(RegExp(r'[A-Z]'))) {
      if (i > 0) buffer.write('_');
      buffer.write(char.toLowerCase());
    } else {
      buffer.write(char);
    }
  }
  return buffer.toString();
}

Process? _mockServer;
String? _spawnedMockServerUrl;
final Map<String, String> _spawnedMockServers = {};

String _mockServerUrl() =>
    _spawnedMockServerUrl ??
    Platform.environment['MOCK_SERVER_URL'] ??
    'http://localhost:8080';

String _mockServerFor(String fixtureId, String envKey) =>
    _spawnedMockServers[fixtureId] ??
    Platform.environment[envKey] ??
    '${_mockServerUrl()}/fixtures/$fixtureId';

void main() {
  setUpAll(() async {
    await RustLib.init();
    if (Platform.environment['MOCK_SERVER_URL'] == null) {
      final _bin = Platform.script
          .resolve('../rust/target/release/mock-server')
          .toFilePath();
      final _fixturesDir = Platform.script
          .resolve('../../fixtures')
          .toFilePath();
      if (File(_bin).existsSync()) {
        _mockServer = await Process.start(_bin, [
          _fixturesDir,
        ], mode: ProcessStartMode.normal);
        final _ready = Completer<void>();
        _mockServer!.stdout
            .transform(utf8.decoder)
            .transform(const LineSplitter())
            .listen(
              (_line) {
                final _trimmed = _line.trim();
                if (_trimmed.startsWith('MOCK_SERVER_URL=')) {
                  _spawnedMockServerUrl = _trimmed.substring(
                    'MOCK_SERVER_URL='.length,
                  );
                } else if (_trimmed.startsWith('MOCK_SERVERS=')) {
                  final _json = _trimmed.substring('MOCK_SERVERS='.length);
                  try {
                    final _parsed = jsonDecode(_json) as Map<String, dynamic>;
                    _parsed.forEach((k, v) {
                      _spawnedMockServers[k] = v as String;
                    });
                  } catch (_) {}
                  if (!_ready.isCompleted) _ready.complete();
                } else if (_spawnedMockServerUrl != null) {
                  if (!_ready.isCompleted) _ready.complete();
                }
              },
              onDone: () {
                if (!_ready.isCompleted) _ready.complete();
              },
            );
        await _ready.future.timeout(
          const Duration(seconds: 30),
          onTimeout: () {},
        );
      }
    }
  });

  tearDownAll(() async {
    final server = _mockServer;
    if (server != null) {
      server.kill();
      await server.exitCode;
    }
  });

  test(
    'Extracts article:published_time, modified_time, author, section, and tags',
    () async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/metadata_article_times";
      final result = await KreuzcrawlBridge.scrape(engine, url);
      expect(result.statusCode, equals(200));
      // skipped: field 'article.published_time' not available on dart result type
      // skipped: field 'article.modified_time' not available on dart result type
      // skipped: field 'article.author' not available on dart result type
      // skipped: field 'article.section' not available on dart result type
      // skipped: field 'article.tags.length' not available on dart result type
    },
  );

  test('Extracts favicon link tags including apple-touch-icon', () async {
    final engine = await KreuzcrawlBridge.createEngine();
    final url = "${_mockServerUrl()}/fixtures/metadata_favicons";
    final result = await KreuzcrawlBridge.scrape(engine, url);
    expect(result.statusCode, equals(200));
    // skipped: field 'favicons.length' not available on dart result type
    // skipped: field 'favicons[].apple_touch' not available on dart result type
  });

  test('Extracts heading hierarchy (h1-h6) from HTML page', () async {
    final engine = await KreuzcrawlBridge.createEngine();
    final url = "${_mockServerUrl()}/fixtures/metadata_headings";
    final result = await KreuzcrawlBridge.scrape(engine, url);
    expect(result.statusCode, equals(200));
    // skipped: field 'headings.h1.length' not available on dart result type
    // skipped: field 'headings.h1[0].text' not available on dart result type
    // skipped: field 'headings.length' not available on dart result type
  });

  test('Extracts hreflang alternate link tags', () async {
    final engine = await KreuzcrawlBridge.createEngine();
    final url = "${_mockServerUrl()}/fixtures/metadata_hreflang";
    final result = await KreuzcrawlBridge.scrape(engine, url);
    expect(result.statusCode, equals(200));
    // skipped: field 'hreflang.length' not available on dart result type
    // skipped: field 'hreflang[].lang' not available on dart result type
  });

  test(
    'Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata',
    () async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/metadata_keywords_author";
      final result = await KreuzcrawlBridge.scrape(engine, url);
      expect(result.statusCode, equals(200));
      expect(
        result.metadata.title.toString().trim(),
        equals('Comprehensive Metadata Test Page'.toString().trim()),
      );
      expect(result.metadata.canonicalUrl, isNotNull);
      expect(result.metadata.keywords, isNotNull);
      expect(result.metadata.keywords, contains('rust'));
      expect(
        result.metadata.author.toString().trim(),
        equals('Jane Developer'.toString().trim()),
      );
      expect(result.metadata.viewport, isNotNull);
      expect(
        result.metadata.generator.toString().trim(),
        equals('kreuzcrawl/1.0'.toString().trim()),
      );
      expect(
        result.metadata.themeColor.toString().trim(),
        equals('#ff6600'.toString().trim()),
      );
      expect(
        result.metadata.robots.toString().trim(),
        equals('index, follow'.toString().trim()),
      );
      expect(
        result.metadata.htmlLang.toString().trim(),
        equals('en'.toString().trim()),
      );
      expect(
        result.metadata.htmlDir.toString().trim(),
        equals('ltr'.toString().trim()),
      );
    },
  );

  test('Extracts og:video, og:audio, and og:locale:alternate metadata', () async {
    final engine = await KreuzcrawlBridge.createEngine();
    final url = "${_mockServerUrl()}/fixtures/metadata_og_video_audio";
    final result = await KreuzcrawlBridge.scrape(engine, url);
    expect(result.statusCode, equals(200));
    expect(
      result.metadata.ogVideo.toString().trim(),
      equals('https://example.com/video.mp4'.toString().trim()),
    );
    expect(
      result.metadata.ogAudio.toString().trim(),
      equals('https://example.com/audio.mp3'.toString().trim()),
    );
    // skipped: field 'og.locale_alternate.length' not available on dart result type
  });

  test(
    'Extracts response metadata from HTTP headers (etag, server, content-language)',
    () async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/metadata_response_headers";
      final result = await KreuzcrawlBridge.scrape(engine, url);
      expect(result.statusCode, equals(200));
      // skipped: field 'response_headers.etag' not available on dart result type
      // skipped: field 'response_headers.last_modified' not available on dart result type
      // skipped: field 'response_headers.server' not available on dart result type
      // skipped: field 'response_headers.content_language' not available on dart result type
    },
  );

  test('Computes word count from visible page text', () async {
    final engine = await KreuzcrawlBridge.createEngine();
    final url = "${_mockServerUrl()}/fixtures/metadata_word_count";
    final result = await KreuzcrawlBridge.scrape(engine, url);
    expect(result.statusCode, equals(200));
    expect(result.metadata.wordCount, greaterThan(99));
    expect(result.metadata.wordCount, lessThan(301));
  });
}
