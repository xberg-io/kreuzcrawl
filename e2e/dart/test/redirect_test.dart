// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1e1cbe4718ff25d45891d2012df2899a3cf4a0b460ea68d767a774030bfdf380
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// ignore_for_file: unused_local_variable

import 'package:test/test.dart';
import 'dart:io';
import 'package:kreuzcrawl/kreuzcrawl.dart';
import 'package:kreuzcrawl/src/kreuzcrawl_bridge_generated/frb_generated.dart'
    show RustLib;

// E2e tests for category: redirect

String _alefE2eText(Object? value) {
  if (value == null) return '';
  // Check if it's an enum by examining its toString representation.
  final str = value.toString();
  if (str.contains('.')) {
    // Enum.toString() returns 'EnumName.variantName'. Extract the variant name.
    final parts = str.split('.');
    if (parts.length == 2) {
      final variantName = parts[1];
      // Convert camelCase variant names to snake_case for serde compatibility.
      // E.g. 'toolCalls' -> 'tool_calls', 'stop' -> 'stop'.
      return _camelToSnake(variantName);
    }
  }
  return str;
}

String _camelToSnake(String camel) {
  final buffer = StringBuffer();
  for (int i = 0; i < camel.length; i++) {
    final char = camel[i];
    if (char.contains(RegExp(r'[A-Z]'))) {
      if (i > 0) buffer.write('_');
      buffer.write(char.toLowerCase());
    } else {
      buffer.write(char);
    }
  }
  return buffer.toString();
}

void main() {
  setUpAll(() async {
    await RustLib.init();
  });

  test(
    'Follows 301 permanent redirect and returns final page content',
    () async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"respect_robots_txt":false}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          Platform.environment["MOCK_SERVER_REDIRECT_301_PERMANENT"] ??
          (Platform.environment["MOCK_SERVER_URL"]! +
              "/fixtures/redirect_301_permanent");
      final result = await KreuzcrawlBridge.crawl(engine, url);
      expect(result.finalUrl, contains('/target'));
      expect(result.redirectCount, equals(1));
    },
  );

  test('Follows 302 Found redirect correctly', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_REDIRECT_302_FOUND"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/redirect_302_found");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/found-target'));
    expect(result.redirectCount, equals(1));
  });

  test('Follows 303 See Other redirect (method changes to GET)', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_REDIRECT_303_SEE_OTHER"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/redirect_303_see_other");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/see-other'));
    expect(result.redirectCount, equals(1));
  });

  test('Follows 307 Temporary Redirect (preserves method)', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_REDIRECT_307_TEMPORARY"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/redirect_307_temporary");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/temp-target'));
    expect(result.redirectCount, equals(1));
  });

  test('Follows 308 Permanent Redirect (preserves method)', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_REDIRECT_308_PERMANENT"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/redirect_308_permanent");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/perm-target'));
    expect(result.redirectCount, equals(1));
  });

  test('Follows a chain of redirects (301 -> 302 -> 200)', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_REDIRECT_CHAIN"] ??
        (Platform.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_chain");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/step2'));
    expect(result.redirectCount, equals(2));
  });

  test(
    'Reports cross-domain redirect target without following to external domain',
    () async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"respect_robots_txt":false}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          Platform.environment["MOCK_SERVER_REDIRECT_CROSS_DOMAIN"] ??
          (Platform.environment["MOCK_SERVER_URL"]! +
              "/fixtures/redirect_cross_domain");
      final result = await KreuzcrawlBridge.crawl(engine, url);
      expect(result.finalUrl, contains('/external-redirect'));
      expect(result.redirectCount, equals(1));
    },
  );

  test('Detects redirect loop (A -> B -> A) and returns error', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_REDIRECT_LOOP"] ??
        (Platform.environment["MOCK_SERVER_URL"]! + "/fixtures/redirect_loop");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    // skipped: field 'is_error' not available on dart result type
  });

  test('Aborts when redirect count exceeds max_redirects limit', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"max_redirects":2,"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_REDIRECT_MAX_EXCEEDED"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/redirect_max_exceeded");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    // skipped: field 'is_error' not available on dart result type
  });

  test('Follows HTML meta-refresh redirect to target page', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/redirect_meta_refresh";
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/target'));
    expect(result.redirectCount, equals(1));
  });

  test('Handles HTTP Refresh header redirect', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_REDIRECT_REFRESH_HEADER"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/redirect_refresh_header");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/refreshed'));
    expect(result.redirectCount, equals(1));
  });

  test('Redirect target returns 404 Not Found', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_REDIRECT_TO_404"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/redirect_to_404");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/gone'));
    expect(result.redirectCount, equals(1));
    // skipped: field 'is_error' not available on dart result type
  });
}
