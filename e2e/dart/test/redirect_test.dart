// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7fa5a9b2a64f4e8748861595d59516936527d40614b29c06d8e061eddc867b05
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// ignore_for_file: unused_local_variable

import 'package:test/test.dart';
import 'dart:io';
import 'package:kreuzcrawl/kreuzcrawl.dart';
import 'package:kreuzcrawl/src/kreuzcrawl_bridge_generated/frb_generated.dart'
    show RustLib;
import 'dart:async';
import 'dart:convert';

// E2e tests for category: redirect

String _alefE2eText(Object? value) {
  if (value == null) return '';
  // Check if it's an enum by examining its toString representation.
  final str = value.toString();
  if (str.contains('.')) {
    // Enum.toString() returns 'EnumName.variantName'. Extract the variant name.
    final parts = str.split('.');
    if (parts.length == 2) {
      final variantName = parts[1];
      // Convert camelCase variant names to snake_case for serde compatibility.
      // E.g. 'toolCalls' -> 'tool_calls', 'stop' -> 'stop'.
      return _camelToSnake(variantName);
    }
  }
  return str;
}

String _camelToSnake(String camel) {
  final buffer = StringBuffer();
  for (int i = 0; i < camel.length; i++) {
    final char = camel[i];
    if (char.contains(RegExp(r'[A-Z]'))) {
      if (i > 0) buffer.write('_');
      buffer.write(char.toLowerCase());
    } else {
      buffer.write(char);
    }
  }
  return buffer.toString();
}

Process? _mockServer;
String? _spawnedMockServerUrl;
final Map<String, String> _spawnedMockServers = {};

String _mockServerUrl() =>
    _spawnedMockServerUrl ??
    Platform.environment['MOCK_SERVER_URL'] ??
    'http://localhost:8080';

String _mockServerFor(String fixtureId, String envKey) =>
    _spawnedMockServers[fixtureId] ??
    Platform.environment[envKey] ??
    '${_mockServerUrl()}/fixtures/$fixtureId';

void main() {
  setUpAll(() async {
    await RustLib.init();
    if (Platform.environment['MOCK_SERVER_URL'] == null) {
      final _bin = Platform.script
          .resolve('../rust/target/release/mock-server')
          .toFilePath();
      final _fixturesDir = Platform.script
          .resolve('../../fixtures')
          .toFilePath();
      if (File(_bin).existsSync()) {
        _mockServer = await Process.start(_bin, [
          _fixturesDir,
        ], mode: ProcessStartMode.normal);
        final _ready = Completer<void>();
        _mockServer!.stdout
            .transform(utf8.decoder)
            .transform(const LineSplitter())
            .listen(
              (_line) {
                final _trimmed = _line.trim();
                if (_trimmed.startsWith('MOCK_SERVER_URL=')) {
                  _spawnedMockServerUrl = _trimmed.substring(
                    'MOCK_SERVER_URL='.length,
                  );
                } else if (_trimmed.startsWith('MOCK_SERVERS=')) {
                  final _json = _trimmed.substring('MOCK_SERVERS='.length);
                  try {
                    final _parsed = jsonDecode(_json) as Map<String, dynamic>;
                    _parsed.forEach((k, v) {
                      _spawnedMockServers[k] = v as String;
                    });
                  } catch (_) {}
                  if (!_ready.isCompleted) _ready.complete();
                } else if (_spawnedMockServerUrl != null) {
                  if (!_ready.isCompleted) _ready.complete();
                }
              },
              onDone: () {
                if (!_ready.isCompleted) _ready.complete();
              },
            );
        await _ready.future.timeout(
          const Duration(seconds: 30),
          onTimeout: () {},
        );
      }
    }
  });

  tearDownAll(() async {
    final server = _mockServer;
    if (server != null) {
      server.kill();
      await server.exitCode;
    }
  });

  test(
    'Follows 301 permanent redirect and returns final page content',
    () async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"respect_robots_txt":false}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url = _mockServerFor(
        "redirect_301_permanent",
        "MOCK_SERVER_REDIRECT_301_PERMANENT",
      );
      final result = await KreuzcrawlBridge.crawl(engine, url);
      expect(result.finalUrl, contains('/target'));
      expect(result.redirectCount, equals(1));
    },
  );

  test('Follows 302 Found redirect correctly', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = _mockServerFor(
      "redirect_302_found",
      "MOCK_SERVER_REDIRECT_302_FOUND",
    );
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/found-target'));
    expect(result.redirectCount, equals(1));
  });

  test('Follows 303 See Other redirect (method changes to GET)', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = _mockServerFor(
      "redirect_303_see_other",
      "MOCK_SERVER_REDIRECT_303_SEE_OTHER",
    );
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/see-other'));
    expect(result.redirectCount, equals(1));
  });

  test('Follows 307 Temporary Redirect (preserves method)', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = _mockServerFor(
      "redirect_307_temporary",
      "MOCK_SERVER_REDIRECT_307_TEMPORARY",
    );
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/temp-target'));
    expect(result.redirectCount, equals(1));
  });

  test('Follows 308 Permanent Redirect (preserves method)', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = _mockServerFor(
      "redirect_308_permanent",
      "MOCK_SERVER_REDIRECT_308_PERMANENT",
    );
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/perm-target'));
    expect(result.redirectCount, equals(1));
  });

  test('Follows a chain of redirects (301 -> 302 -> 200)', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = _mockServerFor("redirect_chain", "MOCK_SERVER_REDIRECT_CHAIN");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/step2'));
    expect(result.redirectCount, equals(2));
  });

  test(
    'Reports cross-domain redirect target without following to external domain',
    () async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"respect_robots_txt":false}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url = _mockServerFor(
        "redirect_cross_domain",
        "MOCK_SERVER_REDIRECT_CROSS_DOMAIN",
      );
      final result = await KreuzcrawlBridge.crawl(engine, url);
      expect(result.finalUrl, contains('/external-redirect'));
      expect(result.redirectCount, equals(1));
    },
  );

  test('Detects redirect loop (A -> B -> A) and returns error', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = _mockServerFor("redirect_loop", "MOCK_SERVER_REDIRECT_LOOP");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    // skipped: field 'is_error' not available on dart result type
  });

  test('Aborts when redirect count exceeds max_redirects limit', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"max_redirects":2,"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = _mockServerFor(
      "redirect_max_exceeded",
      "MOCK_SERVER_REDIRECT_MAX_EXCEEDED",
    );
    final result = await KreuzcrawlBridge.crawl(engine, url);
    // skipped: field 'is_error' not available on dart result type
  });

  test('Follows HTML meta-refresh redirect to target page', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = "${_mockServerUrl()}/fixtures/redirect_meta_refresh";
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/target'));
    expect(result.redirectCount, equals(1));
  });

  test('Handles HTTP Refresh header redirect', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = _mockServerFor(
      "redirect_refresh_header",
      "MOCK_SERVER_REDIRECT_REFRESH_HEADER",
    );
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/refreshed'));
    expect(result.redirectCount, equals(1));
  });

  test('Redirect target returns 404 Not Found', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"respect_robots_txt":false}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url = _mockServerFor(
      "redirect_to_404",
      "MOCK_SERVER_REDIRECT_TO_404",
    );
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.finalUrl, contains('/gone'));
    expect(result.redirectCount, equals(1));
    // skipped: field 'is_error' not available on dart result type
  });
}
