// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3e74ad7b8dfd45964dcabcb6eaf367474cae523b9c537edd276441eb160bb1d4
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// ignore_for_file: unused_local_variable

import 'package:test/test.dart';
import 'dart:io';
import 'package:kreuzcrawl/kreuzcrawl.dart';
import 'package:kreuzcrawl/src/kreuzcrawl_bridge_generated/frb_generated.dart'
    show RustLib;

// E2e tests for category: validation
void main() {
  setUpAll(() async {
    await RustLib.init();
  });

  test(
    'Browser endpoint must be a valid ws:// or wss:// URL, not http://',
    () async {
      await expectLater(() async {
        final engineConfig = await createCrawlConfigFromJson(
          json:
              r'{"browser":{"endpoint":"http://not-websocket:3000","mode":"always"}}',
        );
        final engine = await KreuzcrawlBridge.createEngine(
          config: engineConfig,
        );
        final url =
            "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_browser_endpoint_invalid";
        return KreuzcrawlBridge.scrape(engine, url);
      }(), throwsA(anything));
    },
  );

  test('batch operation with empty batch_urls array is rejected', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_empty_batch_urls";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('auth object with empty username in basic auth is rejected', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"auth":{"password":"secret","type":"basic","username":""}}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_invalid_auth_config";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Invalid regex in exclude_paths is rejected', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"exclude_paths":["(unclosed"]}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_invalid_exclude_regex";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Invalid regex in include_paths is rejected', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"include_paths":["[invalid"]}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_invalid_include_regex";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('proxy with invalid URL like \'not-a-url\' is rejected', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"proxy":{"url":"not-a-url"}}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_invalid_proxy_url";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Retry code outside 100-599 is rejected', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"retry_codes":[999]}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_invalid_retry_code";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('max_concurrent=0 is rejected as invalid config (minimum is 1)', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"max_concurrent":0}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_max_concurrent_zero";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('max_depth=200 exceeds limit of 100', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"max_depth":200}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_max_depth_too_high";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('max_pages=0 is rejected as invalid config', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"max_pages":0}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_max_pages_zero";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('max_redirects > 100 is rejected as invalid config', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"max_redirects":200}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_max_redirects_too_high";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('max_body_size set to -1 is rejected as invalid config', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"max_body_size":0}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_negative_body_size";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Zero request timeout is rejected as invalid config', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"request_timeout":0}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          "${Platform.environment["MOCK_SERVER_URL"] ?? "http://localhost:8080"}/fixtures/validation_timeout_zero";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });
}
