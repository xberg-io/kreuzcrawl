// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:875098d7b6d5e27b9840485804dc3903d236e39b0aefd549dee6aa9ab6b0abd9
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// ignore_for_file: unused_local_variable

import 'package:test/test.dart';
import 'dart:io';
import 'package:kreuzcrawl/kreuzcrawl.dart';
import 'package:kreuzcrawl/src/kreuzcrawl_bridge_generated/frb_generated.dart'
    show RustLib;
import 'dart:async';
import 'dart:convert';

// E2e tests for category: error

String _alefE2eText(Object? value) {
  if (value == null) return '';
  // Check if it's an enum by examining its toString representation.
  final str = value.toString();
  if (str.contains('.')) {
    // Enum.toString() returns 'EnumName.variantName'. Extract the variant name.
    final parts = str.split('.');
    if (parts.length == 2) {
      final variantName = parts[1];
      // Convert camelCase variant names to snake_case for serde compatibility.
      // E.g. 'toolCalls' -> 'tool_calls', 'stop' -> 'stop'.
      return _camelToSnake(variantName);
    }
  }
  return str;
}

String _camelToSnake(String camel) {
  final buffer = StringBuffer();
  for (int i = 0; i < camel.length; i++) {
    final char = camel[i];
    if (char.contains(RegExp(r'[A-Z]'))) {
      if (i > 0) buffer.write('_');
      buffer.write(char.toLowerCase());
    } else {
      buffer.write(char);
    }
  }
  return buffer.toString();
}

Process? _mockServer;
String? _spawnedMockServerUrl;
final Map<String, String> _spawnedMockServers = {};

String _mockServerUrl() =>
    _spawnedMockServerUrl ??
    Platform.environment['MOCK_SERVER_URL'] ??
    'http://localhost:8080';

String _mockServerFor(String fixtureId, String envKey) =>
    _spawnedMockServers[fixtureId] ??
    Platform.environment[envKey] ??
    '${_mockServerUrl()}/fixtures/$fixtureId';

void main() {
  setUpAll(() async {
    await RustLib.init();
    if (Platform.environment['MOCK_SERVER_URL'] == null) {
      final _bin = Platform.script
          .resolve('../rust/target/release/mock-server')
          .toFilePath();
      final _fixturesDir = Platform.script
          .resolve('../../fixtures')
          .toFilePath();
      if (File(_bin).existsSync()) {
        _mockServer = await Process.start(_bin, [
          _fixturesDir,
        ], mode: ProcessStartMode.normal);
        final _ready = Completer<void>();
        _mockServer!.stdout
            .transform(utf8.decoder)
            .transform(const LineSplitter())
            .listen(
              (_line) {
                final _trimmed = _line.trim();
                if (_trimmed.startsWith('MOCK_SERVER_URL=')) {
                  _spawnedMockServerUrl = _trimmed.substring(
                    'MOCK_SERVER_URL='.length,
                  );
                } else if (_trimmed.startsWith('MOCK_SERVERS=')) {
                  final _json = _trimmed.substring('MOCK_SERVERS='.length);
                  try {
                    final _parsed = jsonDecode(_json) as Map<String, dynamic>;
                    _parsed.forEach((k, v) {
                      _spawnedMockServers[k] = v as String;
                    });
                  } catch (_) {}
                  if (!_ready.isCompleted) _ready.complete();
                } else if (_spawnedMockServerUrl != null) {
                  if (!_ready.isCompleted) _ready.complete();
                }
              },
              onDone: () {
                if (!_ready.isCompleted) _ready.complete();
              },
            );
        await _ready.future.timeout(
          const Duration(seconds: 30),
          onTimeout: () {},
        );
      }
    }
  });

  tearDownAll(() async {
    final server = _mockServer;
    if (server != null) {
      server.kill();
      await server.exitCode;
    }
  });

  test('Handles 401 Unauthorized response correctly', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_401_unauthorized";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Handles 403 Forbidden response correctly', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"browser":{"mode":"never"}}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url = "${_mockServerUrl()}/fixtures/error_403_forbidden";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Handles 404 response correctly', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_404_page";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Handles 408 Request Timeout response correctly', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_408_request_timeout";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Handles 410 Gone response correctly', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_410_gone";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Handles 500 server error', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_500_server";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Handles 502 Bad Gateway response correctly', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_502_bad_gateway";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test(
    'Browser launch fails when browser mode is always but browser is unavailable',
    () async {
      await expectLater(() async {
        final engineConfig = await createCrawlConfigFromJson(
          json: r'{"browser":{"mode":"always","timeout":1}}',
        );
        final engine = await KreuzcrawlBridge.createEngine(
          config: engineConfig,
        );
        final url = "${_mockServerUrl()}/fixtures/error_browser_launch_failure";
        return KreuzcrawlBridge.scrape(engine, url);
      }(), throwsA(anything));
    },
  );

  test('Browser page load times out', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"browser":{"mode":"always","timeout":1}}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url = "${_mockServerUrl()}/fixtures/error_browser_page_timeout";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Handles connection refused error gracefully', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_connection_refused";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Content-Length mismatch causes data loss error', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_data_loss_truncated";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Handles DNS resolution failure gracefully', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_dns_resolution";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test(
    'Scraping a URL that cannot be found returns an error containing the URL path',
    () async {
      await expectLater(() async {
        final engine = await KreuzcrawlBridge.createEngine();
        final url = "${_mockServerUrl()}/fixtures/error_empty_batch_urls";
        return KreuzcrawlBridge.scrape(engine, url);
      }(), throwsA(anything));
    },
  );

  test('Handles 200 with completely empty body gracefully', () async {
    final engine = await KreuzcrawlBridge.createEngine();
    final url = "${_mockServerUrl()}/fixtures/error_empty_response";
    final result = await KreuzcrawlBridge.scrape(engine, url);
    expect(result.html, anyOf(isNull, isEmpty));
  });

  test(
    'Proxy pointing to unreachable address causes connection error during scrape',
    () async {
      await expectLater(() async {
        final engineConfig = await createCrawlConfigFromJson(
          json: r'{"proxy":{"url":"http://127.0.0.1:1"}}',
        );
        final engine = await KreuzcrawlBridge.createEngine(
          config: engineConfig,
        );
        final url = "${_mockServerUrl()}/fixtures/error_invalid_proxy";
        return KreuzcrawlBridge.scrape(engine, url);
      }(), throwsA(anything));
    },
  );

  test('Handles incomplete or truncated HTTP response', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_partial_response";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Handles 429 rate limiting with Retry-After', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_rate_limited";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Retries request on 503 Service Unavailable response', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_retry_503";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test(
    'Implements exponential backoff when retrying failed requests',
    () async {
      await expectLater(() async {
        final engine = await KreuzcrawlBridge.createEngine();
        final url = "${_mockServerUrl()}/fixtures/error_retry_backoff";
        return KreuzcrawlBridge.scrape(engine, url);
      }(), throwsA(anything));
    },
  );

  test('Handles SSL certificate validation error', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_ssl_invalid_cert";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test(
    'Mock server delays response longer than request_timeout, surfacing a timeout error',
    () async {
      await expectLater(() async {
        final engineConfig = await createCrawlConfigFromJson(
          json: r'{"request_timeout":500}',
        );
        final engine = await KreuzcrawlBridge.createEngine(
          config: engineConfig,
        );
        final url = "${_mockServerUrl()}/fixtures/error_timeout";
        return KreuzcrawlBridge.scrape(engine, url);
      }(), throwsA(anything));
    },
  );

  test('Unsupported URL scheme (gopher) is rejected', () async {
    await expectLater(() async {
      final engine = await KreuzcrawlBridge.createEngine();
      final url = "${_mockServerUrl()}/fixtures/error_unsupported_scheme";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('Akamai WAF detection returns WafBlocked error', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"browser":{"mode":"never"}}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url = "${_mockServerUrl()}/fixtures/error_waf_akamai";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test('WAF challenge/block detection returns WafBlocked error', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"browser":{"mode":"never"}}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url = "${_mockServerUrl()}/fixtures/error_waf_blocked";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });

  test(
    'Detects WAF/bot protection false 403 (Cloudflare challenge page)',
    () async {
      await expectLater(() async {
        final engineConfig = await createCrawlConfigFromJson(
          json: r'{"browser":{"mode":"never"}}',
        );
        final engine = await KreuzcrawlBridge.createEngine(
          config: engineConfig,
        );
        final url = "${_mockServerUrl()}/fixtures/error_waf_false_403";
        return KreuzcrawlBridge.scrape(engine, url);
      }(), throwsA(anything));
    },
  );

  test('Imperva/Incapsula WAF detection', () async {
    await expectLater(() async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"browser":{"mode":"never"}}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url = "${_mockServerUrl()}/fixtures/error_waf_imperva";
      return KreuzcrawlBridge.scrape(engine, url);
    }(), throwsA(anything));
  });
}
