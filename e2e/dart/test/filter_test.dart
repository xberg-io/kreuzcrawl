// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:dc3e5e0288e2e20ecfa13db9ec5faee450b8ca5efa9b055d284e48db6e97a0ea
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
// ignore_for_file: unused_local_variable

import 'package:test/test.dart';
import 'dart:io';
import 'package:kreuzcrawl/kreuzcrawl.dart';
import 'package:kreuzcrawl/src/kreuzcrawl_bridge_generated/frb_generated.dart'
    show RustLib;

// E2e tests for category: filter

String _alefE2eText(Object? value) {
  if (value == null) return '';
  // Check if it's an enum by examining its toString representation.
  final str = value.toString();
  if (str.contains('.')) {
    // Enum.toString() returns 'EnumName.variantName'. Extract the variant name.
    final parts = str.split('.');
    if (parts.length == 2) {
      final variantName = parts[1];
      // Convert camelCase variant names to snake_case for serde compatibility.
      // E.g. 'toolCalls' -> 'tool_calls', 'stop' -> 'stop'.
      return _camelToSnake(variantName);
    }
  }
  return str;
}

String _camelToSnake(String camel) {
  final buffer = StringBuffer();
  for (int i = 0; i < camel.length; i++) {
    final char = camel[i];
    if (char.contains(RegExp(r'[A-Z]'))) {
      if (i > 0) buffer.write('_');
      buffer.write(char.toLowerCase());
    } else {
      buffer.write(char);
    }
  }
  return buffer.toString();
}

void main() {
  setUpAll(() async {
    await RustLib.init();
  });

  test(
    'BM25 filter works during multi-page crawl, keeping relevant pages',
    () async {
      final engineConfig = await createCrawlConfigFromJson(
        json: r'{"max_concurrent":1,"max_depth":1}',
      );
      final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
      final url =
          Platform.environment["MOCK_SERVER_FILTER_BM25_CRAWL_INTEGRATION"] ??
          (Platform.environment["MOCK_SERVER_URL"]! +
              "/fixtures/filter_bm25_crawl_integration");
      final result = await KreuzcrawlBridge.crawl(engine, url);
      // skipped: field 'filter.remaining_contain_keyword' not available on dart result type
    },
  );

  test('BM25 filter with empty query passes all pages through', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"max_depth":1}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_FILTER_BM25_EMPTY_QUERY"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/filter_bm25_empty_query");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.pages.length, equals(2));
  });

  test('BM25 filter with very high threshold filters out all pages', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"max_depth":1}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_FILTER_BM25_HIGH_THRESHOLD"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/filter_bm25_high_threshold");
    final result = await KreuzcrawlBridge.scrape(engine, url);
    // skipped: field 'filter.pages_after_filter' not available on dart result type
  });

  test('BM25 filter keeps only pages relevant to the query', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"max_depth":1}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_FILTER_BM25_RELEVANT_PAGES"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/filter_bm25_relevant_pages");
    final result = await KreuzcrawlBridge.scrape(engine, url);
    // skipped: field 'filter.remaining_contain_keyword' not available on dart result type
  });

  test('BM25 filter with zero threshold passes all pages', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"max_depth":1}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_FILTER_BM25_THRESHOLD_ZERO"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/filter_bm25_threshold_zero");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.pages.length, equals(2));
  });

  test('NoopFilter keeps all pages during a multi-page crawl', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"max_concurrent":1,"max_depth":1}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_FILTER_NOOP_CRAWL_ALL_KEPT"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/filter_noop_crawl_all_kept");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    // skipped: field 'filter.pages_after_filter' not available on dart result type
  });

  test('No content filter passes all crawled pages through', () async {
    final engineConfig = await createCrawlConfigFromJson(
      json: r'{"max_depth":1}',
    );
    final engine = await KreuzcrawlBridge.createEngine(config: engineConfig);
    final url =
        Platform.environment["MOCK_SERVER_FILTER_NOOP_PASSES_ALL"] ??
        (Platform.environment["MOCK_SERVER_URL"]! +
            "/fixtures/filter_noop_passes_all");
    final result = await KreuzcrawlBridge.crawl(engine, url);
    expect(result.pages.length, equals(3));
  });
}
