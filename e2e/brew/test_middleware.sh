#!/usr/bin/env bash
# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: middleware
set -euo pipefail

test_middleware_engine_crawl_with_defaults() {
    # Engine crawl with default middleware chain produces correct multi-page results
    local output
    output=$(kreuzcrawl scrape "${MOCK_SERVER_URL}/fixtures/middleware_engine_crawl_with_defaults" --format json)

    # skipped: field 'crawl.pages_crawled' not available on result type
    # skipped: field 'crawl.min_pages' not available on result type
}

test_middleware_noop_no_effect() {
    # Default middleware chain does not affect normal scraping
    local output
    output=$(kreuzcrawl scrape "${MOCK_SERVER_URL}/fixtures/middleware_noop_no_effect" --format json)

    local val_status_code
    val_status_code=$(echo "$output" | jq -r '.status_code')
    assert_equals "$val_status_code" '200' 'status_code'
    local val_metadata_title
    val_metadata_title=$(echo "$output" | jq -r '.metadata.title')
    assert_equals "$val_metadata_title" 'Middleware Test' 'metadata.title'
}

run_tests_middleware() {
    run_test test_middleware_engine_crawl_with_defaults
    run_test test_middleware_noop_no_effect
}
