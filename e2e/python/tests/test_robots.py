# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:716aade275523ceac8a1fa7307432c15938828dd45efa5e7f95fdbffb66d62ed
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef

"""E2e tests for category: robots."""

import os
import pytest  # noqa: F401
from kreuzcrawl import scrape, create_engine, CrawlConfig


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


def _alef_e2e_item_texts(item: object) -> tuple[str, ...]:
    raw_items = getattr(item, "items", None)
    items_text = (
        " ".join(str(value) for value in raw_items)
        if isinstance(raw_items, list)
        else ""
    )
    return (
        _alef_e2e_text(item),
        _alef_e2e_text(getattr(item, "kind", None)),
        _alef_e2e_text(getattr(item, "name", None)),
        _alef_e2e_text(getattr(item, "source", None)),
        _alef_e2e_text(getattr(item, "alias", None)),
        _alef_e2e_text(getattr(item, "text", None)),
        _alef_e2e_text(getattr(item, "signature", None)),
        items_text,
    )


@pytest.mark.asyncio
async def test_robots_allow_all() -> None:
    """Permissive robots.txt allows all paths."""
    engine_config = CrawlConfig(respect_robots_txt=True)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_ALLOW_ALL")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_allow_all"
    )

    result = await scrape(engine, url)
    assert result.is_allowed is True  # noqa: S101


@pytest.mark.asyncio
async def test_robots_allow_override() -> None:
    """Allow directive overrides Disallow for specific paths."""
    engine_config = CrawlConfig(respect_robots_txt=True)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_ALLOW_OVERRIDE")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_allow_override"
    )

    result = await scrape(engine, url)
    assert result.is_allowed is True  # noqa: S101


@pytest.mark.asyncio
async def test_robots_comments_handling() -> None:
    """Correctly parses robots.txt with inline and line comments."""
    engine_config = CrawlConfig(respect_robots_txt=True, user_agent="kreuzcrawl")
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_COMMENTS_HANDLING")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_comments_handling"
    )

    result = await scrape(engine, url)
    assert result.is_allowed is True  # noqa: S101


@pytest.mark.asyncio
async def test_robots_crawl_delay() -> None:
    """Respects crawl-delay directive from robots.txt."""
    engine_config = CrawlConfig(respect_robots_txt=True, user_agent="kreuzcrawl")
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_CRAWL_DELAY")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_crawl_delay"
    )

    result = await scrape(engine, url)
    assert result.crawl_delay == 2  # noqa: S101


@pytest.mark.asyncio
async def test_robots_disallow_path() -> None:
    """Robots.txt disallows specific paths."""
    engine_config = CrawlConfig(respect_robots_txt=True)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_DISALLOW_PATH")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_disallow_path"
    )

    result = await scrape(engine, url)
    assert result.is_allowed is False  # noqa: S101


@pytest.mark.asyncio
async def test_robots_meta_nofollow() -> None:
    """Detects nofollow meta robots tag and skips link extraction."""
    engine_config = CrawlConfig(respect_robots_txt=True)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_META_NOFOLLOW")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_meta_nofollow"
    )

    result = await scrape(engine, url)
    assert result.nofollow_detected is True  # noqa: S101


@pytest.mark.asyncio
async def test_robots_meta_noindex() -> None:
    """Detects noindex meta robots tag in HTML page."""
    engine_config = CrawlConfig(respect_robots_txt=True)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_META_NOINDEX")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_meta_noindex"
    )

    result = await scrape(engine, url)
    assert result.noindex_detected is True  # noqa: S101


@pytest.mark.asyncio
async def test_robots_missing_404() -> None:
    """Missing robots.txt (404) allows all crawling."""
    engine_config = CrawlConfig(respect_robots_txt=True)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_MISSING_404")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_missing_404"
    )

    result = await scrape(engine, url)
    assert result.is_allowed is True  # noqa: S101


@pytest.mark.asyncio
async def test_robots_multiple_user_agents() -> None:
    """Picks the most specific user-agent block from robots.txt."""
    engine_config = CrawlConfig(respect_robots_txt=True, user_agent="SpecificBot")
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_MULTIPLE_USER_AGENTS")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_multiple_user_agents"
    )

    result = await scrape(engine, url)
    assert result.is_allowed is True  # noqa: S101


@pytest.mark.asyncio
async def test_robots_request_rate() -> None:
    """Parses request-rate directive from robots.txt."""
    engine_config = CrawlConfig(respect_robots_txt=True, user_agent="kreuzcrawl")
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_REQUEST_RATE")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_request_rate"
    )

    result = await scrape(engine, url)
    assert result.crawl_delay == 5  # noqa: S101
    assert result.is_allowed is True  # noqa: S101


@pytest.mark.asyncio
async def test_robots_sitemap_directive() -> None:
    """Discovers sitemap URL from Sitemap directive in robots.txt."""
    engine_config = CrawlConfig(respect_robots_txt=True)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_SITEMAP_DIRECTIVE")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_sitemap_directive"
    )

    result = await scrape(engine, url)
    assert result.is_allowed is True  # noqa: S101


@pytest.mark.asyncio
async def test_robots_user_agent_specific() -> None:
    """Matches user-agent specific rules in robots.txt."""
    engine_config = CrawlConfig(respect_robots_txt=True, user_agent="KreuzcrawlBot")
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_USER_AGENT_SPECIFIC")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_user_agent_specific"
    )

    result = await scrape(engine, url)
    assert result.is_allowed is False  # noqa: S101


@pytest.mark.asyncio
async def test_robots_wildcard_paths() -> None:
    """Handles wildcard Disallow patterns in robots.txt."""
    engine_config = CrawlConfig(respect_robots_txt=True)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_WILDCARD_PATHS")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_wildcard_paths"
    )

    result = await scrape(engine, url)
    assert result.is_allowed is False  # noqa: S101


@pytest.mark.asyncio
async def test_robots_x_robots_tag() -> None:
    """Respects X-Robots-Tag HTTP header directives."""
    engine_config = CrawlConfig(respect_robots_txt=True)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ROBOTS_X_ROBOTS_TAG")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/robots_x_robots_tag"
    )

    result = await scrape(engine, url)
    assert result.x_robots_tag.strip() == "noindex, nofollow"  # noqa: S101
    assert result.noindex_detected is True  # noqa: S101
    assert result.nofollow_detected is True  # noqa: S101
