# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:6fb97317edd9ffc3ae866e22381b7ad87f3ff8745ddb79ac2d63d2f06e3c620b
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef

"""E2e tests for category: redirect."""

import os
import pytest  # noqa: F401
from kreuzcrawl import crawl, create_engine, CrawlConfig


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


def _alef_e2e_item_texts(item: object) -> tuple[str, ...]:
    raw_items = getattr(item, "items", None)
    items_text = (
        " ".join(str(value) for value in raw_items)
        if isinstance(raw_items, list)
        else ""
    )
    return (
        _alef_e2e_text(item),
        _alef_e2e_text(getattr(item, "kind", None)),
        _alef_e2e_text(getattr(item, "name", None)),
        _alef_e2e_text(getattr(item, "source", None)),
        _alef_e2e_text(getattr(item, "alias", None)),
        _alef_e2e_text(getattr(item, "text", None)),
        _alef_e2e_text(getattr(item, "signature", None)),
        items_text,
    )


@pytest.mark.asyncio
async def test_redirect_301_permanent() -> None:
    """Follows 301 permanent redirect and returns final page content."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_301_PERMANENT")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_301_permanent"
    )

    result = await crawl(engine, url)
    assert "/target" in result.final_url  # noqa: S101
    assert result.redirect_count == 1  # noqa: S101


@pytest.mark.asyncio
async def test_redirect_302_found() -> None:
    """Follows 302 Found redirect correctly."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_302_FOUND")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_302_found"
    )

    result = await crawl(engine, url)
    assert "/found-target" in result.final_url  # noqa: S101
    assert result.redirect_count == 1  # noqa: S101


@pytest.mark.asyncio
async def test_redirect_303_see_other() -> None:
    """Follows 303 See Other redirect (method changes to GET)."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_303_SEE_OTHER")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_303_see_other"
    )

    result = await crawl(engine, url)
    assert "/see-other" in result.final_url  # noqa: S101
    assert result.redirect_count == 1  # noqa: S101


@pytest.mark.asyncio
async def test_redirect_307_temporary() -> None:
    """Follows 307 Temporary Redirect (preserves method)."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_307_TEMPORARY")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_307_temporary"
    )

    result = await crawl(engine, url)
    assert "/temp-target" in result.final_url  # noqa: S101
    assert result.redirect_count == 1  # noqa: S101


@pytest.mark.asyncio
async def test_redirect_308_permanent() -> None:
    """Follows 308 Permanent Redirect (preserves method)."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_308_PERMANENT")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_308_permanent"
    )

    result = await crawl(engine, url)
    assert "/perm-target" in result.final_url  # noqa: S101
    assert result.redirect_count == 1  # noqa: S101


@pytest.mark.asyncio
async def test_redirect_chain() -> None:
    """Follows a chain of redirects (301 -> 302 -> 200)."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_CHAIN")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_chain"
    )

    result = await crawl(engine, url)
    assert "/step2" in result.final_url  # noqa: S101
    assert result.redirect_count == 2  # noqa: S101


@pytest.mark.asyncio
async def test_redirect_cross_domain() -> None:
    """Reports cross-domain redirect target without following to external domain."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_CROSS_DOMAIN")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_cross_domain"
    )

    result = await crawl(engine, url)
    assert "/external-redirect" in result.final_url  # noqa: S101
    assert result.redirect_count == 1  # noqa: S101


@pytest.mark.asyncio
async def test_redirect_loop() -> None:
    """Detects redirect loop (A -> B -> A) and returns error."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_LOOP")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_loop"
    )

    _ = await crawl(engine, url)
    # skipped: field 'is_error' not available on result type


@pytest.mark.asyncio
async def test_redirect_max_exceeded() -> None:
    """Aborts when redirect count exceeds max_redirects limit."""
    engine_config = CrawlConfig(max_redirects=2, respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_MAX_EXCEEDED")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_max_exceeded"
    )

    _ = await crawl(engine, url)
    # skipped: field 'is_error' not available on result type


@pytest.mark.asyncio
async def test_redirect_meta_refresh() -> None:
    """Follows HTML meta-refresh redirect to target page."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_meta_refresh"

    result = await crawl(engine, url)
    assert "/target" in result.final_url  # noqa: S101
    assert result.redirect_count == 1  # noqa: S101


@pytest.mark.asyncio
async def test_redirect_refresh_header() -> None:
    """Handles HTTP Refresh header redirect."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_REFRESH_HEADER")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_refresh_header"
    )

    result = await crawl(engine, url)
    assert "/refreshed" in result.final_url  # noqa: S101
    assert result.redirect_count == 1  # noqa: S101


@pytest.mark.asyncio
async def test_redirect_to_404() -> None:
    """Redirect target returns 404 Not Found."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_REDIRECT_TO_404")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/redirect_to_404"
    )

    result = await crawl(engine, url)
    assert "/gone" in result.final_url  # noqa: S101
    assert result.redirect_count == 1  # noqa: S101
    # skipped: field 'is_error' not available on result type
