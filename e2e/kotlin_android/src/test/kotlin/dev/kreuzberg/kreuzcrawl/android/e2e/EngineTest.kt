// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a9f527fe9e864e8aebf50f2da46f1aa3d4a30f14fc2fc7f2b7629db2239d93fc
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.kreuzcrawl.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import kotlinx.coroutines.flow.toList
import dev.kreuzberg.kreuzcrawl.android.Kreuzcrawl
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import dev.kreuzberg.kreuzcrawl.android.CrawlConfig

/** E2e tests for category: engine. */
class EngineTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("kcrawl_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load kcrawl_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testEngineBatchBasic() = runBlocking {
        // CrawlEngine with defaults batch scrapes like the free function
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/engine_batch_basic"
        val result = Kreuzcrawl.scrape(engine, url)
        // skipped: field 'batch.completed_count' not available on result type
        // skipped: field 'batch.total_count' not available on result type
    }

    @Test
    fun testEngineCrawlBasic() = runBlocking {
        // CrawlEngine with defaults crawls multiple pages like the free function
        val engineConfig = MAPPER.readValue("{\"max_depth\":1}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = System.getProperty("mockServer.engine_crawl_basic", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/engine_crawl_basic")
        val result = Kreuzcrawl.crawl(engine, url)
        // skipped: field 'crawl.pages_crawled' not available on result type
        // skipped: field 'crawl.min_pages' not available on result type
    }

    @Test
    fun testEngineMapBasic() = runBlocking {
        // CrawlEngine with defaults discovers URLs like the free function
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getProperty("mockServer.engine_map_basic", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/engine_map_basic")
        val result = Kreuzcrawl.mapUrls(engine, url)
        // skipped: field 'map.min_urls' not available on result type
    }

    @Test
    fun testEngineScrapeBasic() = runBlocking {
        // CrawlEngine with defaults scrapes a page identically to the free function
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getProperty("mockServer.engine_scrape_basic", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/engine_scrape_basic")
        val result = Kreuzcrawl.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals("text/html", result.contentType.trim())
        assertEquals("Engine Test", result.metadata.title.orEmpty().trim())
        assertTrue(result.metadata.description.orEmpty().contains("Testing the engine"), "expected to contain: " + "Testing the engine")
        assertTrue(result.links.size >= 1, "expected >= 1")
        assertTrue(result.metadata.headings?.size!! >= 1, "expected >= 1")
    }

    @Test
    fun testEngineStreamBasic() = runBlocking {
        // CrawlEngine with defaults streams events like the free function
        val engineConfig = MAPPER.readValue("{\"max_depth\":1}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = System.getProperty("mockServer.engine_stream_basic", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/engine_stream_basic")
        val result = Kreuzcrawl.crawlStream(engine, url)
        val chunks = result.toList()
        // streaming field 'stream.event_count_min': assertion type 'greater_than_or_equal' not rendered
    }

}
