// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1dae80dec968fb99d1b7b43dfca11bb89983c23bba3fd199d417e01a76145f00
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.kreuzcrawl.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.kreuzcrawl.android.Kreuzcrawl
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import dev.kreuzberg.kreuzcrawl.android.CrawlConfig

/** E2e tests for category: map. */
class MapTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("kcrawl_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load kcrawl_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testMapDiscoverUrls() = runBlocking {
        // Discovers all URLs on a site without fetching full content
        val engineConfig = MAPPER.readValue("{\"max_depth\":0,\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = System.getProperty("mockServer.map_discover_urls", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/map_discover_urls")
        val result = Kreuzcrawl.mapUrls(engine, url)
        assertTrue(result.urls.size >= 3, "expected >= 3")
    }

    @Test
    fun testMapExcludePatterns() = runBlocking {
        // Excludes URLs matching patterns from URL map
        val engineConfig = MAPPER.readValue("{\"exclude_paths\":[\"/private/.*\",\"/api/.*\"],\"max_depth\":0,\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = System.getProperty("mockServer.map_exclude_patterns", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/map_exclude_patterns")
        val result = Kreuzcrawl.mapUrls(engine, url)
        assertEquals(1, result.urls.size)
    }

    @Test
    fun testMapIncludeSubdomains() = runBlocking {
        // Includes subdomain URLs in URL map discovery; page has 1 local and 1 subdomain link
        val engineConfig = MAPPER.readValue("{\"allow_subdomains\":true,\"max_depth\":0,\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = System.getProperty("mockServer.map_include_subdomains", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/map_include_subdomains")
        val result = Kreuzcrawl.mapUrls(engine, url)
        assertTrue(result.urls.size >= 2, "expected >= 2")
    }

    @Test
    fun testMapLargeSitemap() = runBlocking {
        // Handles large sitemap with 100+ URLs
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/map_large_sitemap"
        val result = Kreuzcrawl.mapUrls(engine, url)
        assertTrue(result.urls.size >= 100, "expected >= 100")
    }

    @Test
    fun testMapLimitPagination() = runBlocking {
        // Limits map result count to specified maximum
        val engineConfig = MAPPER.readValue("{\"map_limit\":5,\"max_depth\":0,\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = System.getProperty("mockServer.map_limit_pagination", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/map_limit_pagination")
        val result = Kreuzcrawl.mapUrls(engine, url)
        assertTrue(result.urls.size <= 5, "expected <= 5")
    }

    @Test
    fun testMapSearchFilter() = runBlocking {
        // Filters map results by search keyword; 4 links in page but only 2 match 'blog'
        val engineConfig = MAPPER.readValue("{\"map_search\":\"blog\",\"max_depth\":0,\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = System.getProperty("mockServer.map_search_filter", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/map_search_filter")
        val result = Kreuzcrawl.mapUrls(engine, url)
        assertTrue(result.urls.size >= 2, "expected >= 2")
        assertTrue(result.urls.size <= 2, "expected <= 2")
    }

}
