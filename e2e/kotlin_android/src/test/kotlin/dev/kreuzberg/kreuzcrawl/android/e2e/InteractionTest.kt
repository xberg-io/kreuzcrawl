// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a9f527fe9e864e8aebf50f2da46f1aa3d4a30f14fc2fc7f2b7629db2239d93fc
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.kreuzcrawl.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.kreuzcrawl.android.Kreuzcrawl
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import dev.kreuzberg.kreuzcrawl.android.PageAction
import dev.kreuzberg.kreuzcrawl.android.CrawlConfig

/** E2e tests for category: interaction. */
class InteractionTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("kcrawl_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load kcrawl_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testInteractActionSequence() = runBlocking {
        // Execute a sequence of multiple actions (click, type, click) and verify all succeed with correct indices
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_action_sequence"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"selector\":\"#open\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#field\",\"text\":\"test_data\",\"type\":\"type\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#submit\",\"type\":\"click\"}", PageAction::class.java)))
        assertEquals(0, result.actionResults.first().actionIndex)
        assertEquals("click", result.actionResults.first().actionType.trim())
        assertEquals(true, result.actionResults.first().success)
        assertEquals(1, result.actionResults.get(1).actionIndex)
        assertEquals("type", result.actionResults.get(1).actionType.trim())
        assertEquals(true, result.actionResults.get(1).success)
        assertEquals(2, result.actionResults.get(2).actionIndex)
        assertEquals("click", result.actionResults.get(2).actionType.trim())
        assertEquals(true, result.actionResults.get(2).success)
    }

    @Test
    fun testInteractClickElement() = runBlocking {
        // Click a button element and verify success
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_click_element"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"selector\":\"#submit\",\"type\":\"click\"}", PageAction::class.java)))
        assertEquals(true, result.actionResults.first().success)
        assertEquals("click", result.actionResults.first().actionType.trim())
        assertEquals(0, result.actionResults.first().actionIndex)
    }

    @Test
    fun testInteractExecuteJs() = runBlocking {
        // Execute JavaScript that returns document.title and verify success with data
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_execute_js"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"script\":\"document.title\",\"type\":\"executeJs\"}", PageAction::class.java)))
        assertEquals(true, result.actionResults.first().success)
        assertEquals("executeJs", result.actionResults.first().actionType.trim())
        assertTrue(result.actionResults.first().data.orEmpty().contains("JS Test Page"), "expected to contain: " + "JS Test Page")
    }

    @Test
    fun testInteractInvalidSelector() = runBlocking {
        // Click on a non-existent selector and verify error in action_results
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_invalid_selector"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"selector\":\"#nonexistent\",\"type\":\"click\"}", PageAction::class.java)))
        assertEquals(false, result.actionResults.first().success)
        assertEquals("click", result.actionResults.first().actionType.trim())
        assertTrue(result.actionResults.first().error.orEmpty().contains("find click target"), "expected to contain: " + "find click target")
    }

    @Test
    fun testInteractMaxActionsExceeded() = runBlocking {
        // Submit 101 actions (exceeds MAX_ACTIONS=100) and verify validation error
        assertFailsWith<Exception> {
            val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
            val engine = Kreuzcrawl.createEngine(engineConfig)
            val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_max_actions_exceeded"
            Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#btn\",\"type\":\"click\"}", PageAction::class.java)))
        }
        Unit
    }

    @Test
    fun testInteractPressKey() = runBlocking {
        // Press a keyboard key (Enter) and verify success
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_press_key"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"selector\":\"#searchbox\",\"type\":\"click\"}", PageAction::class.java), MAPPER.readValue("{\"selector\":\"#searchbox\",\"text\":\"test\",\"type\":\"type\"}", PageAction::class.java), MAPPER.readValue("{\"key\":\"Enter\",\"type\":\"press\"}", PageAction::class.java)))
        assertEquals(true, result.actionResults.get(2).success)
        assertEquals("press", result.actionResults.get(2).actionType.trim())
        assertEquals(2, result.actionResults.get(2).actionIndex)
    }

    @Test
    fun testInteractScrapeDefault() = runBlocking {
        // Execute default Scrape action and verify extracted content
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_scrape_default"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"type\":\"scrape\"}", PageAction::class.java)))
        assertEquals(true, result.actionResults.first().success)
        assertEquals("scrape", result.actionResults.first().actionType.trim())
        assertTrue(result.actionResults.first().data != null, "expected non-empty value")
    }

    @Test
    fun testInteractScreenshot() = runBlocking {
        // Screenshot a specific element via selector and verify image data
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_screenshot"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"full_page\":false,\"type\":\"screenshot\"}", PageAction::class.java)))
        assertEquals(true, result.actionResults.first().success)
        assertEquals("screenshot", result.actionResults.first().actionType.trim())
        assertTrue(result.actionResults.first().data != null, "expected non-empty value")
    }

    @Test
    fun testInteractScreenshotFullPage() = runBlocking {
        // Screenshot full page and verify image data
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_screenshot_full_page"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"full_page\":true,\"type\":\"screenshot\"}", PageAction::class.java)))
        assertEquals(true, result.actionResults.first().success)
        assertEquals("screenshot", result.actionResults.first().actionType.trim())
        assertTrue(result.actionResults.first().data != null, "expected non-empty value")
    }

    @Test
    fun testInteractScrollDown() = runBlocking {
        // Scroll down on the page and verify success
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_scroll_down"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"amount\":500,\"direction\":\"down\",\"type\":\"scroll\"}", PageAction::class.java)))
        assertEquals(true, result.actionResults.first().success)
        assertEquals("scroll", result.actionResults.first().actionType.trim())
        assertEquals(0, result.actionResults.first().actionIndex)
    }

    @Test
    fun testInteractTypeInput() = runBlocking {
        // Type text into an input field and verify success
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_type_input"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"selector\":\"#username\",\"text\":\"john_doe\",\"type\":\"type\"}", PageAction::class.java)))
        assertEquals(true, result.actionResults.first().success)
        assertEquals("type", result.actionResults.first().actionType.trim())
        assertEquals(0, result.actionResults.first().actionIndex)
    }

    @Test
    fun testInteractWaitSelector() = runBlocking {
        // Wait for a CSS selector that exists on the page
        val engineConfig = MAPPER.readValue("{\"browser\":{\"mode\":\"always\"}}", CrawlConfig::class.java)
        val engine = Kreuzcrawl.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/interact_wait_selector"
        val result = Kreuzcrawl.interact(engine, url, listOf(MAPPER.readValue("{\"selector\":\"#content\",\"type\":\"wait\"}", PageAction::class.java)))
        assertEquals(true, result.actionResults.first().success)
        assertEquals("wait", result.actionResults.first().actionType.trim())
        assertEquals(0, result.actionResults.first().actionIndex)
    }

}
