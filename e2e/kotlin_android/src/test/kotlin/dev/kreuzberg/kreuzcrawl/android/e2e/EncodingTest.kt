// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:a51ab2ff8c1572afa532410ae5910674adf22744a2540d453879b03abff63781
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package dev.kreuzberg.kreuzcrawl.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import dev.kreuzberg.kreuzcrawl.android.Kreuzcrawl

/** E2e tests for category: encoding. */
class EncodingTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("KREUZCRAWL_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("kcrawl_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load kcrawl_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
    }
    @Test
    fun testEncodingDoubleEncoded() = runBlocking {
        // Handles double-encoded URL characters (%25C3%25B6)
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getProperty("mockServer.encoding_double_encoded", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/encoding_double_encoded")
        val result = Kreuzcrawl.scrape(engine, url)
        assertFalse(result.html.isEmpty(), "expected non-empty value")
        assertTrue(result.links.size >= 1, "expected >= 1")
    }

    @Test
    fun testEncodingMixedCharsetPage() = runBlocking {
        // Handles charset mismatch between HTTP header and HTML meta tag
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/encoding_mixed_charset_page"
        val result = Kreuzcrawl.scrape(engine, url)
        assertFalse(result.html.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testEncodingPercentEncodedPath() = runBlocking {
        // Handles percent-encoded spaces and characters in URL paths
        val engine = Kreuzcrawl.createEngine(null)
        val url = System.getProperty("mockServer.encoding_percent_encoded_path", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/encoding_percent_encoded_path")
        val result = Kreuzcrawl.scrape(engine, url)
        assertFalse(result.html.isEmpty(), "expected non-empty value")
        assertTrue(result.links.size >= 2, "expected >= 2")
    }

    @Test
    fun testEncodingUnicodeUrl() = runBlocking {
        // Handles Unicode characters in URLs (Hebrew, Japanese, Cyrillic)
        val engine = Kreuzcrawl.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/encoding_unicode_url"
        val result = Kreuzcrawl.scrape(engine, url)
        assertFalse(result.html.isEmpty(), "expected non-empty value")
    }

}
