// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cc01a1c094d3c52a5b52b4a912620f9a716410d5ef08dea3520ea2a80cd328ad
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
package io.xberg.crawlberg.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.crawlberg.android.Crawlberg
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import io.xberg.crawlberg.android.CrawlConfig

/** E2e tests for category: sitemap. */
class SitemapTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("cberg_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load cberg_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testSitemapBasic() = runBlocking {
        // Parses a standard urlset sitemap
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/sitemap_basic"
        val result = Crawlberg.mapUrls(engine, url)
        assertEquals(4, result.urls.size)
    }

    @Test
    fun testSitemapCompressedGzip() = runBlocking {
        // Parses a gzip-compressed sitemap file
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/sitemap_compressed_gzip"
        val result = Crawlberg.mapUrls(engine, url)
        assertEquals(3, result.urls.size)
    }

    @Test
    fun testSitemapEmpty() = runBlocking {
        // Handles empty sitemap gracefully
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/sitemap_empty"
        val result = Crawlberg.mapUrls(engine, url)
        assertEquals(0, result.urls.size)
    }

    @Test
    fun testSitemapFromRobotsTxt() = runBlocking {
        // Discovers sitemap via robots.txt Sitemap directive
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":true}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = System.getProperty("mockServer.sitemap_from_robots_txt", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/sitemap_from_robots_txt")
        val result = Crawlberg.mapUrls(engine, url)
        assertEquals(4, result.urls.size)
    }

    @Test
    fun testSitemapIndex() = runBlocking {
        // Follows sitemap index to discover child sitemaps
        val engine = Crawlberg.createEngine(null)
        val url = System.getProperty("mockServer.sitemap_index", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/sitemap_index")
        val result = Crawlberg.mapUrls(engine, url)
        assertEquals(3, result.urls.size)
    }

    @Test
    fun testSitemapLastmodFilter() = runBlocking {
        // Filters sitemap URLs by lastmod date
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/sitemap_lastmod_filter"
        val result = Crawlberg.mapUrls(engine, url)
        assertEquals(4, result.urls.size)
    }

    @Test
    fun testSitemapOnlyMode() = runBlocking {
        // Uses sitemap URLs exclusively without following page links
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/sitemap_only_mode"
        val result = Crawlberg.mapUrls(engine, url)
        assertEquals(4, result.urls.size)
    }

    @Test
    fun testSitemapXhtmlLinks() = runBlocking {
        // Parses sitemap with XHTML namespace alternate links
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/sitemap_xhtml_links"
        val result = Crawlberg.mapUrls(engine, url)
        assertEquals(2, result.urls.size)
    }

}
