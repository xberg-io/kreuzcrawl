// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:77819ad4d200946b6bce09cd7ecbbf2bd42926cc71914ca4b66af99575230d2d
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.kreuzcrawl.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.kreuzcrawl.Kreuzcrawl;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.kreuzcrawl.CrawlConfig;
import dev.kreuzberg.kreuzcrawl.ChatCompletionChunk;

/** E2e tests for category: stream. */
class StreamTest {
    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE);

    @Test
    void testBatchCrawlStreamEvents() throws Exception {
        // Batch crawl stream produces expected Page and Complete events
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":2}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        var result = Kreuzcrawl.batchCrawlStream(engine, null);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.length >= 4, "expected >= 4");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testBatchCrawlStreamPartialFailure() throws Exception {
        // Batch crawl stream emits both Page and Error events for mixed success/failure URLs
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        var result = Kreuzcrawl.batchCrawlStream(engine, null);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Error), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testCrawlStreamEvents() throws Exception {
        // Crawl stream produces page and complete events
        var engineConfig = MAPPER.readValue("{\"max_depth\":1,\"respect_robots_txt\":false}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/crawl_stream_events";
        var urlReq = new CrawlStreamRequest(url);
        var result = Kreuzcrawl.crawlStream(engine, urlReq);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.length >= 4, "expected >= 4");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testStreamDepthCrawl() throws Exception {
        // Stream produces events for multi-depth crawl with link following
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1,\"max_depth\":2}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_depth_crawl";
        var urlReq = new CrawlStreamRequest(url);
        var result = Kreuzcrawl.crawlStream(engine, urlReq);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.length >= 5, "expected >= 5");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testStreamErrorEventMidCrawl() throws Exception {
        // Stream emits error event when a page fails mid-crawl, but other pages succeed
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_error_event_mid_crawl";
        var urlReq = new CrawlStreamRequest(url);
        var result = Kreuzcrawl.crawlStream(engine, urlReq);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Error), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testStreamEventOrdering() throws Exception {
        // Stream ensures complete event arrives after all page events
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_event_ordering";
        var urlReq = new CrawlStreamRequest(url);
        var result = Kreuzcrawl.crawlStream(engine, urlReq);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.length >= 3, "expected >= 3");

    }


    @Test
    void testStreamLargeCrawl() throws Exception {
        // Stream handles crawl of 5+ pages with multiple events
        var engineConfig = MAPPER.readValue("{\"max_depth\":1,\"respect_robots_txt\":false}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_large_crawl";
        var urlReq = new CrawlStreamRequest(url);
        var result = Kreuzcrawl.crawlStream(engine, urlReq);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.length >= 6, "expected >= 6");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testStreamWithErrorEvent() throws Exception {
        // Stream emits page and complete events even when some pages fail
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1,\"max_depth\":1}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_with_error_event";
        var urlReq = new CrawlStreamRequest(url);
        var result = Kreuzcrawl.crawlStream(engine, urlReq);
        var chunks = new java.util.ArrayList<ChatCompletionChunk>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");
        assertTrue(chunks.length >= 2, "expected >= 2");

    }

}
