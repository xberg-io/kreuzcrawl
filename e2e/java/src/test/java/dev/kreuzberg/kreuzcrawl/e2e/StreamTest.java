// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2797a8f376fe333f29bd448e3ef46620267594db1a6c7a212e59e4bba3d06372
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef

package dev.kreuzberg.kreuzcrawl.e2e;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import dev.kreuzberg.kreuzcrawl.Kreuzcrawl;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module;
import dev.kreuzberg.kreuzcrawl.CrawlConfig;
import dev.kreuzberg.kreuzcrawl.CrawlConfig;
import java.util.Optional;
import dev.kreuzberg.kreuzcrawl.JsonUtil;
import dev.kreuzberg.kreuzcrawl.BatchCrawlStreamRequest;
import dev.kreuzberg.kreuzcrawl.CrawlEvent;
import dev.kreuzberg.kreuzcrawl.CrawlStreamRequest;

/** E2e tests for category: stream. */
class StreamTest {
    private static final ObjectMapper MAPPER = new ObjectMapper().registerModule(new Jdk8Module()).setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE);

    @Test
    void testBatchCrawlStreamEvents() throws Exception {
        // Batch crawl stream produces expected Page and Complete events
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":2}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String urlsBase = System.getProperty("mockServer.batch_crawl_stream_events", System.getenv().getOrDefault("MOCK_SERVER_BATCH_CRAWL_STREAM_EVENTS", (System.getProperty("mockServerUrl") != null ? System.getProperty("mockServerUrl") : (System.getenv("MOCK_SERVER_URL") != null ? System.getenv("MOCK_SERVER_URL") : "http://localhost:8000")) + "/fixtures/batch_crawl_stream_events"));
        java.util.List<String> urls = java.util.Arrays.stream(new String[]{"/page1", "/page2", "/page3"}).map(p -> p.startsWith("http") ? p : urlsBase + p).collect(java.util.stream.Collectors.toList());
        var urlsReq = new BatchCrawlStreamRequest(urls);
        var result = engine.batchCrawlStream(urlsReq);
        var chunks = new java.util.ArrayList<CrawlEvent>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 4, "expected >= 4");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testBatchCrawlStreamPartialFailure() throws Exception {
        // Batch crawl stream emits both Page and Error events for mixed success/failure URLs
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String urlsBase = System.getProperty("mockServer.batch_crawl_stream_partial_failure", System.getenv().getOrDefault("MOCK_SERVER_BATCH_CRAWL_STREAM_PARTIAL_FAILURE", (System.getProperty("mockServerUrl") != null ? System.getProperty("mockServerUrl") : (System.getenv("MOCK_SERVER_URL") != null ? System.getenv("MOCK_SERVER_URL") : "http://localhost:8000")) + "/fixtures/batch_crawl_stream_partial_failure"));
        java.util.List<String> urls = java.util.Arrays.stream(new String[]{"/success1", "/fail", "/success2"}).map(p -> p.startsWith("http") ? p : urlsBase + p).collect(java.util.stream.Collectors.toList());
        var urlsReq = new BatchCrawlStreamRequest(urls);
        var result = engine.batchCrawlStream(urlsReq);
        var chunks = new java.util.ArrayList<CrawlEvent>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Error), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testCrawlStreamEvents() throws Exception {
        // Crawl stream produces page and complete events
        var engineConfig = MAPPER.readValue("{\"max_depth\":1,\"respect_robots_txt\":false}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServer.crawl_stream_events", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/crawl_stream_events");
        var urlReq = new CrawlStreamRequest(url);
        var result = engine.crawlStream(urlReq);
        var chunks = new java.util.ArrayList<CrawlEvent>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 4, "expected >= 4");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testStreamDepthCrawl() throws Exception {
        // Stream produces events for multi-depth crawl with link following
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1,\"max_depth\":2}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServer.stream_depth_crawl", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_depth_crawl");
        var urlReq = new CrawlStreamRequest(url);
        var result = engine.crawlStream(urlReq);
        var chunks = new java.util.ArrayList<CrawlEvent>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 5, "expected >= 5");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testStreamErrorEventMidCrawl() throws Exception {
        // Stream emits error event when a page fails mid-crawl, but other pages succeed
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServer.stream_error_event_mid_crawl", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_error_event_mid_crawl");
        var urlReq = new CrawlStreamRequest(url);
        var result = engine.crawlStream(urlReq);
        var chunks = new java.util.ArrayList<CrawlEvent>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Error), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testStreamEventOrdering() throws Exception {
        // Stream ensures complete event arrives after all page events
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServer.stream_event_ordering", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_event_ordering");
        var urlReq = new CrawlStreamRequest(url);
        var result = engine.crawlStream(urlReq);
        var chunks = new java.util.ArrayList<CrawlEvent>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.size() >= 3, "expected >= 3");

    }


    @Test
    void testStreamLargeCrawl() throws Exception {
        // Stream handles crawl of 5+ pages with multiple events
        var engineConfig = MAPPER.readValue("{\"max_depth\":1,\"respect_robots_txt\":false}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServer.stream_large_crawl", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_large_crawl");
        var urlReq = new CrawlStreamRequest(url);
        var result = engine.crawlStream(urlReq);
        var chunks = new java.util.ArrayList<CrawlEvent>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.size() >= 6, "expected >= 6");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");

    }


    @Test
    void testStreamWithErrorEvent() throws Exception {
        // Stream emits page and complete events even when some pages fail
        var engineConfig = MAPPER.readValue("{\"max_concurrent\":1,\"max_depth\":1}", CrawlConfig.class);
        var engine = Kreuzcrawl.createEngine(engineConfig);
        String url = System.getProperty("mockServer.stream_with_error_event", System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL")) + "/fixtures/stream_with_error_event");
        var urlReq = new CrawlStreamRequest(url);
        var result = engine.crawlStream(urlReq);
        var chunks = new java.util.ArrayList<CrawlEvent>();
        var _it = result.iterator();
        while (_it.hasNext()) { chunks.add(_it.next()); }
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Page), "expected true");
        assertTrue(chunks.stream().anyMatch(e -> e instanceof CrawlEvent.Complete), "expected true");
        assertTrue(chunks.size() >= 2, "expected >= 2");

    }

}
