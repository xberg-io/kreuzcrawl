// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7d594a60a087e6fed6eb98ef6cf95cdec8f3013f84226cfef6669b8f17a6b169
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import Kreuzcrawl
import RustBridge

/// E2e tests for category: crawl.
final class CrawlTests: XCTestCase {
    func testContentBinarySkip() async throws {
        // Skips image and video content types gracefully
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/content_binary_skip"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.was_skipped(), true)
    }

    func testContentPdfLinkSkip() async throws {
        // Encounters PDF link and skips or marks as document type
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/content_pdf_link_skip"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.was_skipped(), true)
    }

    func testCrawlConcurrentDepth() async throws {
        // Concurrent crawl respects max_depth limit
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":3,\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_concurrent_depth"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
        // skipped: field 'stayed_on_domain' not available on result type
    }

    func testCrawlConcurrentLimit() async throws {
        // Respects max concurrent requests limit during crawl
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":2,\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_concurrent_limit"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlConcurrentMaxPages() async throws {
        // Concurrent crawl respects max_pages budget
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":4,\"max_depth\":1,\"max_pages\":3,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_concurrent_max_pages"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlCustomHeaders() async throws {
        // Sends custom headers on all crawl requests
        let engineConfig = try crawlConfigFromJson("{\"custom_headers\":{\"Accept-Language\":\"en-US\",\"X-Custom-Header\":\"test-value\"},\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_custom_headers"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlDepthOne() async throws {
        // Follows links one level deep from start page
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_depth_one"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
        // skipped: field 'stayed_on_domain' not available on result type
    }

    func testCrawlDepthPriority() async throws {
        // Crawls in breadth-first order, processing depth-0 pages before depth-1
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":2,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_depth_priority"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlDepthTwo() async throws {
        // Crawls 3 levels deep (depth 0, 1, 2)
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":2,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_depth_two"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlDepthTwoChain() async throws {
        // Depth=2 crawl follows a chain of links across three levels
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":2}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_depth_two_chain"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlDoubleSlashNormalization() async throws {
        // Normalizes double slashes in URL paths (//page to /page)
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_double_slash_normalization"
        let result = try await scrape(engineObj, url)
        // skipped: field 'unique_urls.length' not available on result type
    }

    func testCrawlEmptyPageNoLinks() async throws {
        // Crawl completes when child page has no outgoing links
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":2}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_empty_page_no_links"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlExcludePathPattern() async throws {
        // Skips URLs matching the exclude path pattern
        let engineConfig = try crawlConfigFromJson("{\"exclude_paths\":[\"/admin/.*\"],\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_exclude_path_pattern"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlExternalLinksIgnored() async throws {
        // External links are discovered but not followed when stay_on_domain is true
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1,\"stay_on_domain\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_external_links_ignored"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
        // skipped: field 'stayed_on_domain' not available on result type
    }

    func testCrawlFragmentStripping() async throws {
        // Strips #fragment from URLs for deduplication
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_fragment_stripping"
        let result = try await scrape(engineObj, url)
        // skipped: field 'unique_urls.length' not available on result type
    }

    func testCrawlIncludePathPattern() async throws {
        // Only follows URLs matching the include path pattern
        let engineConfig = try crawlConfigFromJson("{\"include_paths\":[\"/blog/.*\"],\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_include_path_pattern"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlMaxDepthZero() async throws {
        // max_depth=0 crawls only the seed page with no link following
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":0}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_max_depth_zero"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlMaxPages() async throws {
        // Stops crawling at page budget limit
        let engineConfig = try crawlConfigFromJson("{\"max_pages\":3,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_max_pages"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlMixedContentTypes() async throws {
        // Crawl handles links to non-HTML content types gracefully
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_mixed_content_types"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlMultipleRedirectsInTraversal() async throws {
        // Multiple linked pages with redirects are handled during crawl traversal
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_CRAWL_MULTIPLE_REDIRECTS_IN_TRAVERSAL"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_multiple_redirects_in_traversal")
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlQueryParamDedup() async throws {
        // Deduplicates URLs with same query params in different order
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_query_param_dedup"
        let result = try await scrape(engineObj, url)
        // skipped: field 'unique_urls.length' not available on result type
    }

    func testCrawlRedirectInTraversal() async throws {
        // Links that redirect are followed during crawl traversal
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_CRAWL_REDIRECT_IN_TRAVERSAL"] ?? (ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_redirect_in_traversal")
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlSelfLinkNoLoop() async throws {
        // Page linking to itself does not cause infinite crawl loop
        let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":1,\"max_depth\":1}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_self_link_no_loop"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlSinglePageNoLinks() async throws {
        // Crawling a page with no links returns only the seed page
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":2}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_single_page_no_links"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlStayOnDomain() async throws {
        // Does not follow external links when stay_on_domain is true
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false,\"stay_on_domain\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_stay_on_domain"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
        // skipped: field 'stayed_on_domain' not available on result type
    }

    func testCrawlSubdomainExclusion() async throws {
        // Stays on exact domain and skips subdomain links
        let engineConfig = try crawlConfigFromJson("{\"allow_subdomains\":false,\"max_depth\":1,\"respect_robots_txt\":false,\"stay_on_domain\":true}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_subdomain_exclusion"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
        // skipped: field 'stayed_on_domain' not available on result type
    }

    func testCrawlSubdomainInclusion() async throws {
        // Crawls subdomains when allow_subdomains is enabled
        let engineConfig = try crawlConfigFromJson("{\"allow_subdomains\":true,\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_subdomain_inclusion"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

    func testCrawlTrailingSlashDedup() async throws {
        // Deduplicates /page and /page/ as the same URL
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_trailing_slash_dedup"
        let result = try await scrape(engineObj, url)
        // skipped: field 'unique_urls.length' not available on result type
    }

    func testCrawlUrlDeduplication() async throws {
        // Deduplicates URLs that differ only by fragment or query params
        let engineConfig = try crawlConfigFromJson("{\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/crawl_url_deduplication"
        let result = try await scrape(engineObj, url)
        // skipped: field 'pages.length' not available on result type
    }

}
