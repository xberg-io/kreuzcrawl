// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:f4dfe437c13afdbbaa957c935d7a2db4b66e83d404c9a87eff0ca90bd57c0962
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import Foundation
import Kreuzcrawl
import RustBridge

/// E2e tests for category: metadata.
final class MetadataTests: XCTestCase {
    func testMetadataArticleTimes() async throws {
        // Extracts article:published_time, modified_time, author, section, and tags
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/metadata_article_times"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        // skipped: field 'article.published_time' not available on result type
        // skipped: field 'article.modified_time' not available on result type
        // skipped: field 'article.author' not available on result type
        // skipped: field 'article.section' not available on result type
        // skipped: field 'article.tags.length' not available on result type
    }

    func testMetadataFavicons() async throws {
        // Extracts favicon link tags including apple-touch-icon
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/metadata_favicons"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        // skipped: field 'favicons.length' not available on result type
        // skipped: field 'favicons[].apple_touch' not available on result type
    }

    func testMetadataHeadings() async throws {
        // Extracts heading hierarchy (h1-h6) from HTML page
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/metadata_headings"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        // skipped: field 'headings.h1.length' not available on result type
        // skipped: field 'headings.h1[0].text' not available on result type
        // skipped: field 'headings.length' not available on result type
    }

    func testMetadataHreflang() async throws {
        // Extracts hreflang alternate link tags
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/metadata_hreflang"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        // skipped: field 'hreflang.length' not available on result type
        // skipped: field 'hreflang[].lang' not available on result type
    }

    func testMetadataKeywordsAuthor() async throws {
        // Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/metadata_keywords_author"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        XCTAssertEqual((result.metadata().title()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespaces), "Comprehensive Metadata Test Page")
        XCTAssertNotNil(result.metadata().canonical_url(), "expected non-nil value")
        XCTAssertNotNil(result.metadata().keywords(), "expected non-nil value")
        XCTAssertTrue((result.metadata().keywords()?.toString() ?? "").contains("rust"), "expected to contain: \("rust")")
        XCTAssertEqual((result.metadata().author()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespaces), "Jane Developer")
        XCTAssertNotNil(result.metadata().viewport(), "expected non-nil value")
        XCTAssertEqual((result.metadata().generator()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespaces), "kreuzcrawl/1.0")
        XCTAssertEqual((result.metadata().theme_color()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespaces), "#ff6600")
        XCTAssertEqual((result.metadata().robots()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespaces), "index, follow")
        XCTAssertEqual((result.metadata().html_lang()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespaces), "en")
        XCTAssertEqual((result.metadata().html_dir()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespaces), "ltr")
    }

    func testMetadataOgVideoAudio() async throws {
        // Extracts og:video, og:audio, and og:locale:alternate metadata
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/metadata_og_video_audio"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        XCTAssertEqual((result.metadata().og_video()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespaces), "https://example.com/video.mp4")
        XCTAssertEqual((result.metadata().og_audio()?.toString() ?? "").trimmingCharacters(in: CharacterSet.whitespaces), "https://example.com/audio.mp3")
        // skipped: field 'og.locale_alternate.length' not available on result type
    }

    func testMetadataResponseHeaders() async throws {
        // Extracts response metadata from HTTP headers (etag, server, content-language)
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/metadata_response_headers"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        // skipped: field 'response_headers.etag' not available on result type
        // skipped: field 'response_headers.last_modified' not available on result type
        // skipped: field 'response_headers.server' not available on result type
        // skipped: field 'response_headers.content_language' not available on result type
    }

    func testMetadataWordCount() async throws {
        // Computes word count from visible page text
        let engineObj = try createEngine(nil)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_URL"]! + "/fixtures/metadata_word_count"
        let result = try await scrape(engineObj, url)
        XCTAssertEqual(result.status_code(), 200)
        // skipped: field 'computed.word_count' not available on result type
        // skipped: field 'computed.word_count' not available on result type
    }

}
