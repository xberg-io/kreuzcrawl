// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:0b5bd02536cb18ccfc813d6f6290c55d1d5dfab788fbf2d278c19e9684663b0b
// Re-generate with: alef generate
#![allow(missing_docs)]
#![allow(deprecated, dead_code, unused_imports, unused_variables)]
#![allow(
    clippy::default_trait_access,
    clippy::cast_possible_wrap,
    clippy::cast_possible_truncation,
    clippy::cast_sign_loss,
    clippy::just_underscores_and_digits,
    clippy::unused_unit,
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::too_many_arguments,
    clippy::map_identity,
    clippy::unnecessary_cast,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::useless_conversion,
    clippy::arc_with_non_send_sync,
    clippy::collapsible_if,
    clippy::clone_on_copy,
    clippy::should_implement_trait
)]
#![allow(
    clippy::unsafe_derive_deserialize,
    clippy::must_use_candidate,
    clippy::return_self_not_must_use,
    clippy::use_self,
    clippy::missing_const_for_fn,
    clippy::missing_errors_doc,
    clippy::needless_pass_by_value,
    clippy::doc_markdown,
    clippy::derive_partial_eq_without_eq,
    clippy::uninlined_format_args,
    clippy::redundant_clone,
    clippy::implicit_clone,
    clippy::redundant_closure_for_method_calls,
    clippy::wildcard_imports,
    clippy::option_if_let_else,
    clippy::too_many_lines
)]
#![allow(clippy::multiple_unsafe_ops_per_block)]
#![allow(unsafe_code)]

use pyo3::exceptions::PyRuntimeError;
use pyo3::prelude::*;
use std::collections::HashMap;
use std::sync::Arc;

/// Wrapper for trait visitor types (`Py<PyAny>`) that implements Clone.
///
/// `Py<PyAny>` is not Clone. This wrapper uses `Arc<Py<PyAny>>` internally for cheap cloning.
/// The .inner field is public for compatibility with generated code that needs to access
/// the underlying `Py<PyAny>` for trait dispatch.
#[derive(Debug)]
pub struct PyVisitorRef {
    pub inner: std::sync::Arc<pyo3::Py<pyo3::PyAny>>,
}

impl Clone for PyVisitorRef {
    fn clone(&self) -> Self {
        PyVisitorRef {
            inner: std::sync::Arc::clone(&self.inner),
        }
    }
}

impl From<pyo3::Py<pyo3::PyAny>> for PyVisitorRef {
    fn from(visitor: pyo3::Py<pyo3::PyAny>) -> Self {
        PyVisitorRef {
            inner: std::sync::Arc::new(visitor),
        }
    }
}

impl<'a, 'py> pyo3::FromPyObject<'a, 'py> for PyVisitorRef {
    type Error = pyo3::PyErr;

    fn extract(ob: pyo3::Borrowed<'a, 'py, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        Ok(PyVisitorRef {
            inner: std::sync::Arc::new(ob.to_owned().unbind()),
        })
    }
}

impl<'py> pyo3::conversion::IntoPyObject<'py> for PyVisitorRef {
    type Target = pyo3::PyAny;
    type Output = pyo3::Bound<'py, pyo3::PyAny>;
    type Error = std::convert::Infallible;

    fn into_pyobject(self, py: pyo3::Python<'py>) -> Result<Self::Output, Self::Error> {
        Ok((*self.inner).bind(py).clone())
    }
}

mod alef_json_str {
    use serde::{Deserialize, Deserializer};
    use serde_json::Value;
    pub fn deserialize<'de, D>(deserializer: D) -> Result<String, D::Error>
    where
        D: Deserializer<'de>,
    {
        let v = Value::deserialize(deserializer)?;
        Ok(match v {
            Value::String(s) => s,
            other => other.to_string(),
        })
    }
}

mod alef_json_str_opt {
    use serde::{Deserialize, Deserializer};
    use serde_json::Value;
    pub fn deserialize<'de, D>(deserializer: D) -> Result<Option<String>, D::Error>
    where
        D: Deserializer<'de>,
    {
        let v: Option<Value> = Option::deserialize(deserializer)?;
        Ok(v.and_then(|val| match val {
            Value::Null => None,
            Value::String(s) => Some(s),
            other => Some(other.to_string()),
        }))
    }
}

#[pyclass]
pub struct CrawlStreamIterator {
    receiver:
        Arc<tokio::sync::Mutex<tokio::sync::mpsc::Receiver<Result<kreuzcrawl::CrawlEvent, kreuzcrawl::CrawlError>>>>,
}

#[pymethods]
impl CrawlStreamIterator {
    fn __aiter__(slf: PyRef<'_, Self>) -> PyRef<'_, Self> {
        slf
    }

    fn __anext__<'py>(&self, py: Python<'py>) -> PyResult<Option<Bound<'py, PyAny>>> {
        let receiver = self.receiver.clone();
        pyo3_async_runtimes::tokio::future_into_py(py, async move {
            let mut rx = receiver.lock().await;
            match rx.recv().await {
                Some(Ok(chunk)) => Ok(CrawlEvent::from(chunk)),
                Some(Err(e)) => Err(crawl_error_to_py_err(e)),
                None => Err(pyo3::exceptions::PyStopAsyncIteration::new_err(())),
            }
        })
        .map(Some)
    }
}

#[pyclass]
pub struct BatchCrawlStreamIterator {
    receiver:
        Arc<tokio::sync::Mutex<tokio::sync::mpsc::Receiver<Result<kreuzcrawl::CrawlEvent, kreuzcrawl::CrawlError>>>>,
}

#[pymethods]
impl BatchCrawlStreamIterator {
    fn __aiter__(slf: PyRef<'_, Self>) -> PyRef<'_, Self> {
        slf
    }

    fn __anext__<'py>(&self, py: Python<'py>) -> PyResult<Option<Bound<'py, PyAny>>> {
        let receiver = self.receiver.clone();
        pyo3_async_runtimes::tokio::future_into_py(py, async move {
            let mut rx = receiver.lock().await;
            match rx.recv().await {
                Some(Ok(chunk)) => Ok(CrawlEvent::from(chunk)),
                Some(Err(e)) => Err(crawl_error_to_py_err(e)),
                None => Err(pyo3::exceptions::PyStopAsyncIteration::new_err(())),
            }
        })
        .map(Some)
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ExtractionMeta {
    /// Estimated cost of the LLM call in USD.
    #[pyo3(get)]
    pub cost: Option<f64>,
    /// Number of prompt (input) tokens consumed.
    #[pyo3(get)]
    pub prompt_tokens: Option<u64>,
    /// Number of completion (output) tokens generated.
    #[pyo3(get)]
    pub completion_tokens: Option<u64>,
    /// The model identifier used for extraction.
    #[pyo3(get)]
    pub model: Option<String>,
    /// Number of content chunks sent to the LLM.
    #[pyo3(get)]
    pub chunks_processed: usize,
}

#[pymethods]
impl ExtractionMeta {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (chunks_processed=Self::default().chunks_processed, cost=None, prompt_tokens=None, completion_tokens=None, model=None))]
    #[new]
    pub fn new(
        chunks_processed: usize,
        cost: Option<f64>,
        prompt_tokens: Option<u64>,
        completion_tokens: Option<u64>,
        model: Option<String>,
    ) -> Self {
        Self {
            cost,
            prompt_tokens,
            completion_tokens,
            model,
            chunks_processed,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ProxyConfig {
    /// Proxy URL (e.g. "http://proxy:8080", "socks5://proxy:1080").
    #[pyo3(get)]
    pub url: String,
    /// Optional username for proxy authentication.
    #[pyo3(get)]
    pub username: Option<String>,
    /// Optional password for proxy authentication.
    #[pyo3(get)]
    pub password: Option<String>,
}

#[pymethods]
impl ProxyConfig {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, username=None, password=None))]
    #[new]
    pub fn new(url: String, username: Option<String>, password: Option<String>) -> Self {
        Self {
            url,
            username,
            password,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ContentConfig {
    /// Output format: `"markdown"` (default), `"plain"`, `"djot"`.
    #[pyo3(get)]
    pub output_format: String,
    /// Preprocessing aggressiveness: `"minimal"`, `"standard"` (default), `"aggressive"`.
    ///
    /// - Minimal: only scripts/styles removed.
    /// - Standard: also removes nav, nav-hinted headers/footers/asides, forms.
    /// - Aggressive: removes all footers/asides unconditionally.
    #[pyo3(get)]
    pub preprocessing_preset: String,
    /// Remove navigation elements (nav, breadcrumbs, menus). Default: `true`.
    #[pyo3(get)]
    pub remove_navigation: bool,
    /// Remove form elements. Default: `true`.
    #[pyo3(get)]
    pub remove_forms: bool,
    /// HTML tag names to strip (render children only, remove the tag wrapper).
    /// Default: `["noscript"]`.
    #[pyo3(get)]
    pub strip_tags: Vec<String>,
    /// HTML tag names to preserve as raw HTML in output.
    #[pyo3(get)]
    pub preserve_tags: Vec<String>,
    /// CSS selectors for elements to exclude entirely (element + all content).
    ///
    /// Unlike `strip_tags` (which removes the wrapper but keeps children),
    /// excluded elements and all descendants are dropped. Supports CSS selectors:
    /// `.class`, `#id`, `[attribute]`, compound selectors.
    ///
    /// Example: `[".cookie-banner", "#ad-container", "[role='complementary']"]`
    #[pyo3(get)]
    pub exclude_selectors: Vec<String>,
    /// Skip image elements in output. Default: `false`.
    #[pyo3(get)]
    pub skip_images: bool,
    /// Max DOM traversal depth. Prevents stack overflow on deeply nested HTML.
    #[pyo3(get)]
    pub max_depth: Option<usize>,
    /// Enable line wrapping. Default: `false`.
    #[pyo3(get)]
    pub wrap: bool,
    /// Wrap width when `wrap` is enabled. Default: `80`.
    #[pyo3(get)]
    pub wrap_width: usize,
    /// Include document structure tree in output. Default: `true`.
    #[pyo3(get)]
    pub include_document_structure: bool,
}

#[pymethods]
impl ContentConfig {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (output_format=Self::default().output_format, preprocessing_preset=Self::default().preprocessing_preset, remove_navigation=Self::default().remove_navigation, remove_forms=Self::default().remove_forms, strip_tags=Self::default().strip_tags, preserve_tags=Self::default().preserve_tags, exclude_selectors=Self::default().exclude_selectors, skip_images=Self::default().skip_images, wrap=Self::default().wrap, wrap_width=Self::default().wrap_width, include_document_structure=Self::default().include_document_structure, max_depth=None))]
    #[new]
    pub fn new(
        output_format: String,
        preprocessing_preset: String,
        remove_navigation: bool,
        remove_forms: bool,
        strip_tags: Vec<String>,
        preserve_tags: Vec<String>,
        exclude_selectors: Vec<String>,
        skip_images: bool,
        wrap: bool,
        wrap_width: usize,
        include_document_structure: bool,
        max_depth: Option<usize>,
    ) -> Self {
        Self {
            output_format,
            preprocessing_preset,
            remove_navigation,
            remove_forms,
            strip_tags,
            preserve_tags,
            exclude_selectors,
            skip_images,
            max_depth,
            wrap,
            wrap_width,
            include_document_structure,
        }
    }

    #[allow(clippy::should_implement_trait)]
    #[staticmethod]
    #[pyo3(signature = ())]
    pub fn default() -> ContentConfig {
        kreuzcrawl::ContentConfig::default().into()
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct BrowserConfig {
    /// When to use the headless browser fallback.
    #[pyo3(get)]
    pub mode: BrowserMode,
    /// Browser backend used to render JavaScript-heavy pages.
    #[pyo3(get)]
    pub backend: BrowserBackend,
    /// CDP WebSocket endpoint for connecting to an external browser instance.
    #[pyo3(get)]
    pub endpoint: Option<String>,
    /// Timeout for browser page load and rendering (in milliseconds when serialized).
    #[pyo3(get)]
    pub timeout: Option<u64>,
    /// Wait strategy after browser navigation.
    #[pyo3(get)]
    pub wait: BrowserWait,
    /// CSS selector to wait for when `wait` is `Selector`.
    #[pyo3(get)]
    pub wait_selector: Option<String>,
    /// Extra time to wait after the wait condition is met.
    #[pyo3(get)]
    pub extra_wait: Option<u64>,
    /// Proxy for browser fetches. Overrides `CrawlConfig.proxy` when set.
    /// Native backend supports http/https only (no SOCKS5).
    #[pyo3(get)]
    pub proxy: Option<ProxyConfig>,
    /// URL patterns to block before the network request fires. Supports `*`
    /// wildcards. Useful for skipping ads/analytics/large images. Honored by
    /// `BrowserBackend.Native`; chromiumoxide ignores this field today.
    #[pyo3(get)]
    pub block_url_patterns: Vec<String>,
    /// JavaScript snippet evaluated after navigation completes.
    ///
    /// Scraping captures the native backend result in `ScrapeResult.browser.eval_result`.
    /// Interactions run this script before page actions on both browser backends but do
    /// not include the script result in `InteractionResult`.
    #[pyo3(get)]
    pub eval_script: Option<String>,
    /// User-agent used when fetching robots.txt. Defaults to `BrowserConfig.user_agent`
    /// (or kreuzcrawl's default) if unset. Native only.
    #[pyo3(get)]
    pub robots_user_agent: Option<String>,
    /// Capture the full network event stream into the result. Default false
    /// (only the document event is captured). Native only.
    #[pyo3(get)]
    pub capture_network_events: bool,
    /// Enable session affinity: reuse chromiumoxide Pages for same-domain
    /// requests so cookies + fingerprint + solved challenges persist.
    /// Default: true. When false, each request gets a fresh Page.
    #[pyo3(get)]
    pub session_affinity: bool,
}

#[pymethods]
impl BrowserConfig {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (mode=Self::default().mode, backend=Self::default().backend, timeout=None, wait=Self::default().wait, block_url_patterns=Self::default().block_url_patterns, capture_network_events=Self::default().capture_network_events, session_affinity=Self::default().session_affinity, endpoint=None, wait_selector=None, extra_wait=None, proxy=None, eval_script=None, robots_user_agent=None))]
    #[new]
    pub fn new(
        mode: BrowserMode,
        backend: BrowserBackend,
        timeout: Option<u64>,
        wait: BrowserWait,
        block_url_patterns: Vec<String>,
        capture_network_events: bool,
        session_affinity: bool,
        endpoint: Option<String>,
        wait_selector: Option<String>,
        extra_wait: Option<u64>,
        proxy: Option<ProxyConfig>,
        eval_script: Option<String>,
        robots_user_agent: Option<String>,
    ) -> Self {
        Self {
            mode,
            backend,
            endpoint,
            timeout,
            wait,
            wait_selector,
            extra_wait,
            proxy,
            block_url_patterns,
            eval_script,
            robots_user_agent,
            capture_network_events,
            session_affinity,
        }
    }

    #[allow(clippy::should_implement_trait)]
    #[staticmethod]
    #[pyo3(signature = ())]
    pub fn default() -> BrowserConfig {
        kreuzcrawl::BrowserConfig::default().into()
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
#[allow(clippy::similar_names)]
pub struct CrawlConfig {
    /// Maximum crawl depth (number of link hops from the start URL).
    #[pyo3(get)]
    pub max_depth: Option<usize>,
    /// Maximum number of pages to crawl.
    #[pyo3(get)]
    pub max_pages: Option<usize>,
    /// Maximum number of concurrent requests.
    #[pyo3(get)]
    pub max_concurrent: Option<usize>,
    /// Whether to respect robots.txt directives.
    #[pyo3(get)]
    pub respect_robots_txt: bool,
    /// When true, HTTP-level error responses (404 NotFound, 403 Forbidden, WAF blocks)
    /// are surfaced as `ScrapeResult` records with the matching `status_code` rather
    /// than raised as `CrawlError`. Default `false` preserves the historical
    /// throw-on-error contract for direct fetches. Independently of this flag,
    /// 404s reached at the end of a redirect chain are *always* surfaced softly —
    /// the user opted into redirect-following, so receiving a 404 there is part of
    /// the normal flow rather than an unexpected error.
    #[pyo3(get)]
    pub soft_http_errors: bool,
    /// Custom user-agent string.
    #[pyo3(get)]
    pub user_agent: Option<String>,
    /// Whether to restrict crawling to the same domain.
    #[pyo3(get)]
    pub stay_on_domain: bool,
    /// Whether to allow subdomains when `stay_on_domain` is true.
    #[pyo3(get)]
    pub allow_subdomains: bool,
    /// Regex patterns for paths to include during crawling.
    #[pyo3(get)]
    pub include_paths: Vec<String>,
    /// Regex patterns for paths to exclude during crawling.
    #[pyo3(get)]
    pub exclude_paths: Vec<String>,
    /// Custom HTTP headers to send with each request.
    #[pyo3(get)]
    pub custom_headers: HashMap<String, String>,
    /// Timeout for individual HTTP requests (in milliseconds when serialized).
    #[pyo3(get)]
    pub request_timeout: Option<u64>,
    /// Per-domain rate limit in milliseconds. When set, enforces a minimum delay
    /// between requests to the same domain. Defaults to 200ms when `None`.
    #[pyo3(get)]
    pub rate_limit_ms: Option<u64>,
    /// Maximum number of redirects to follow.
    #[pyo3(get)]
    pub max_redirects: usize,
    /// Number of retry attempts for failed requests.
    #[pyo3(get)]
    pub retry_count: usize,
    /// HTTP status codes that should trigger a retry.
    #[pyo3(get)]
    pub retry_codes: Vec<u16>,
    /// Whether to enable cookie handling.
    #[pyo3(get)]
    pub cookies_enabled: bool,
    /// Authentication configuration.
    #[pyo3(get)]
    pub auth: Option<AuthConfig>,
    /// Maximum response body size in bytes.
    #[pyo3(get)]
    pub max_body_size: Option<usize>,
    /// CSS selectors for tags to remove from HTML before processing.
    #[pyo3(get)]
    pub remove_tags: Vec<String>,
    /// Content extraction and conversion configuration.
    #[pyo3(get)]
    pub content: ContentConfig,
    /// Maximum number of URLs to return from a map operation.
    #[pyo3(get)]
    pub map_limit: Option<usize>,
    /// Search filter for map results (case-insensitive substring match on URLs).
    #[pyo3(get)]
    pub map_search: Option<String>,
    /// Whether to download assets (CSS, JS, images, etc.) from the page.
    #[pyo3(get)]
    pub download_assets: bool,
    /// Filter for asset categories to download.
    #[pyo3(get)]
    pub asset_types: Vec<AssetCategory>,
    /// Maximum size in bytes for individual asset downloads.
    #[pyo3(get)]
    pub max_asset_size: Option<usize>,
    /// Browser configuration.
    #[pyo3(get)]
    pub browser: BrowserConfig,
    /// Proxy configuration for HTTP requests.
    #[pyo3(get)]
    pub proxy: Option<ProxyConfig>,
    /// List of user-agent strings for rotation. If non-empty, overrides `user_agent`.
    #[pyo3(get)]
    pub user_agents: Vec<String>,
    /// Whether to capture a screenshot when using the browser.
    #[pyo3(get)]
    pub capture_screenshot: bool,
    /// Re-enqueue discovered `LinkType.Document` URLs into the crawl frontier so
    /// the crawl follows links *from* document pages (PDFs, etc.) as it would
    /// from HTML pages. Default: `false` (documents terminate at materialisation).
    #[pyo3(get)]
    pub follow_document_urls: bool,
    /// Maximum document-depth (from the seed URL through document links only)
    /// when `follow_document_urls` is true. `None` means inherit `max_depth`.
    /// Independent of `max_depth`: a document URL is enqueued only if BOTH the
    /// outer `max_depth` and (if set) `document_url_depth` permit it.
    #[pyo3(get)]
    pub document_url_depth: Option<u32>,
    /// Whether to download non-HTML documents (PDF, DOCX, images, code, etc.) instead of skipping them.
    #[pyo3(get)]
    pub download_documents: bool,
    /// Maximum size in bytes for document downloads. Defaults to 50 MB.
    #[pyo3(get)]
    pub document_max_size: Option<usize>,
    /// Allowlist of MIME types to download. If empty, uses built-in defaults.
    #[pyo3(get)]
    pub document_mime_types: Vec<String>,
    /// Path to write WARC output. If `None`, WARC output is disabled.
    #[pyo3(get)]
    pub warc_output: Option<String>,
    /// Named browser profile for persistent sessions (cookies, localStorage).
    #[pyo3(get)]
    pub browser_profile: Option<String>,
    /// Whether to save changes back to the browser profile on exit.
    #[pyo3(get)]
    pub save_browser_profile: bool,
}

#[pymethods]
impl CrawlConfig {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (respect_robots_txt=Self::default().respect_robots_txt, soft_http_errors=Self::default().soft_http_errors, stay_on_domain=Self::default().stay_on_domain, allow_subdomains=Self::default().allow_subdomains, include_paths=Self::default().include_paths, exclude_paths=Self::default().exclude_paths, custom_headers=Self::default().custom_headers, request_timeout=None, max_redirects=Self::default().max_redirects, retry_count=Self::default().retry_count, retry_codes=Self::default().retry_codes, cookies_enabled=Self::default().cookies_enabled, remove_tags=Self::default().remove_tags, content=None, download_assets=Self::default().download_assets, asset_types=Self::default().asset_types, browser=None, user_agents=Self::default().user_agents, capture_screenshot=Self::default().capture_screenshot, follow_document_urls=Self::default().follow_document_urls, download_documents=Self::default().download_documents, document_mime_types=Self::default().document_mime_types, save_browser_profile=Self::default().save_browser_profile, max_depth=None, max_pages=None, max_concurrent=None, user_agent=None, rate_limit_ms=None, auth=None, max_body_size=None, map_limit=None, map_search=None, max_asset_size=None, proxy=None, document_url_depth=None, document_max_size=None, warc_output=None, browser_profile=None))]
    #[new]
    pub fn new(
        respect_robots_txt: bool,
        soft_http_errors: bool,
        stay_on_domain: bool,
        allow_subdomains: bool,
        include_paths: Vec<String>,
        exclude_paths: Vec<String>,
        custom_headers: HashMap<String, String>,
        request_timeout: Option<u64>,
        max_redirects: usize,
        retry_count: usize,
        retry_codes: Vec<u16>,
        cookies_enabled: bool,
        remove_tags: Vec<String>,
        content: Option<ContentConfig>,
        download_assets: bool,
        asset_types: Vec<AssetCategory>,
        browser: Option<BrowserConfig>,
        user_agents: Vec<String>,
        capture_screenshot: bool,
        follow_document_urls: bool,
        download_documents: bool,
        document_mime_types: Vec<String>,
        save_browser_profile: bool,
        max_depth: Option<usize>,
        max_pages: Option<usize>,
        max_concurrent: Option<usize>,
        user_agent: Option<String>,
        rate_limit_ms: Option<u64>,
        auth: Option<AuthConfig>,
        max_body_size: Option<usize>,
        map_limit: Option<usize>,
        map_search: Option<String>,
        max_asset_size: Option<usize>,
        proxy: Option<ProxyConfig>,
        document_url_depth: Option<u32>,
        document_max_size: Option<usize>,
        warc_output: Option<String>,
        browser_profile: Option<String>,
    ) -> Self {
        Self {
            max_depth,
            max_pages,
            max_concurrent,
            respect_robots_txt,
            soft_http_errors,
            user_agent,
            stay_on_domain,
            allow_subdomains,
            include_paths,
            exclude_paths,
            custom_headers,
            request_timeout,
            rate_limit_ms,
            max_redirects,
            retry_count,
            retry_codes,
            cookies_enabled,
            auth,
            max_body_size,
            remove_tags,
            content: content.unwrap_or_else(|| Self::default().content),
            map_limit,
            map_search,
            download_assets,
            asset_types,
            max_asset_size,
            browser: browser.unwrap_or_else(|| Self::default().browser),
            proxy,
            user_agents,
            capture_screenshot,
            follow_document_urls,
            document_url_depth,
            download_documents,
            document_max_size,
            document_mime_types,
            warc_output,
            browser_profile,
            save_browser_profile,
        }
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = ())]
    pub fn validate(&self) -> PyResult<()> {
        #[allow(clippy::needless_update)]
        let core_self = kreuzcrawl::CrawlConfig {
            max_depth: self.max_depth,

            max_pages: self.max_pages,

            max_concurrent: self.max_concurrent,

            respect_robots_txt: self.respect_robots_txt,

            soft_http_errors: self.soft_http_errors,

            user_agent: self.user_agent.clone(),

            stay_on_domain: self.stay_on_domain,

            allow_subdomains: self.allow_subdomains,

            include_paths: self.include_paths.clone(),

            exclude_paths: self.exclude_paths.clone(),

            custom_headers: self
                .custom_headers
                .clone()
                .into_iter()
                .map(|(k, v)| (k.into(), v))
                .collect(),

            request_timeout: self
                .request_timeout
                .map(std::time::Duration::from_millis)
                .unwrap_or_default(),

            rate_limit_ms: self.rate_limit_ms,

            max_redirects: self.max_redirects,

            retry_count: self.retry_count,

            retry_codes: self.retry_codes.clone(),

            cookies_enabled: self.cookies_enabled,

            auth: self.auth.clone().map(Into::into),

            max_body_size: self.max_body_size,

            remove_tags: self.remove_tags.clone(),

            content: self.content.clone().into(),

            map_limit: self.map_limit,

            map_search: self.map_search.clone(),

            download_assets: self.download_assets,

            asset_types: self.asset_types.clone().into_iter().map(Into::into).collect(),

            max_asset_size: self.max_asset_size,

            browser: self.browser.clone().into(),

            proxy: self.proxy.clone().map(Into::into),

            user_agents: self.user_agents.clone(),

            capture_screenshot: self.capture_screenshot,

            follow_document_urls: self.follow_document_urls,

            document_url_depth: self.document_url_depth,

            download_documents: self.download_documents,

            document_max_size: self.document_max_size,

            document_mime_types: self.document_mime_types.clone(),

            warc_output: self.warc_output.clone().map(Into::into),

            browser_profile: self.browser_profile.clone(),

            save_browser_profile: self.save_browser_profile,

            ssrf: Default::default(),
            dispatch: Default::default(),
            ..Default::default()
        };
        let result = core_self
            .validate()
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        Ok(result)
    }

    #[allow(clippy::should_implement_trait)]
    #[staticmethod]
    #[pyo3(signature = ())]
    pub fn default() -> CrawlConfig {
        kreuzcrawl::CrawlConfig::default().into()
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct BrowserExtras {
    /// Return value of `BrowserConfig.eval_script`, if provided.
    #[pyo3(get)]
    #[serde(default, deserialize_with = "alef_json_str_opt::deserialize")]
    pub eval_result: Option<String>,
    /// Network events captured during page navigation (only populated when
    /// `BrowserConfig.capture_network_events` is true).
    #[pyo3(get)]
    pub network_events: Vec<ResponseMeta>,
    /// All non-expired cookies present in the browser's cookie jar after
    /// navigation completes (includes both prior cookies and server Set-Cookie).
    #[pyo3(get)]
    pub cookies: Vec<CookieInfo>,
}

#[pymethods]
impl BrowserExtras {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (network_events=Self::default().network_events, cookies=Self::default().cookies, eval_result=None))]
    #[new]
    pub fn new(network_events: Vec<ResponseMeta>, cookies: Vec<CookieInfo>, eval_result: Option<String>) -> Self {
        Self {
            eval_result,
            network_events,
            cookies,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct DownloadedDocument {
    /// The URL the document was fetched from.
    #[pyo3(get)]
    pub url: String,
    /// The MIME type from the Content-Type header.
    #[pyo3(get)]
    pub mime_type: String,
    /// Size of the document in bytes.
    #[pyo3(get)]
    pub size: usize,
    /// Filename extracted from Content-Disposition or URL path.
    #[pyo3(get)]
    pub filename: Option<String>,
    /// SHA-256 hex digest of the content.
    #[pyo3(get)]
    pub content_hash: String,
    /// Selected response headers.
    #[pyo3(get)]
    pub headers: HashMap<String, String>,
}

#[pymethods]
impl DownloadedDocument {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, mime_type=Self::default().mime_type, size=Self::default().size, content_hash=Self::default().content_hash, headers=Self::default().headers, filename=None))]
    #[new]
    pub fn new(
        url: String,
        mime_type: String,
        size: usize,
        content_hash: String,
        headers: HashMap<String, String>,
        filename: Option<String>,
    ) -> Self {
        Self {
            url,
            mime_type,
            size,
            filename,
            content_hash,
            headers,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct InteractionResult {
    /// Results from each executed action.
    #[pyo3(get)]
    pub action_results: Vec<ActionResult>,
    /// Final page HTML after all actions completed.
    #[pyo3(get)]
    pub final_html: String,
    /// Final page URL (may have changed due to navigation).
    #[pyo3(get)]
    pub final_url: String,
}

#[pymethods]
impl InteractionResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (action_results=Self::default().action_results, final_html=Self::default().final_html, final_url=Self::default().final_url))]
    #[new]
    pub fn new(action_results: Vec<ActionResult>, final_html: String, final_url: String) -> Self {
        Self {
            action_results,
            final_html,
            final_url,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ActionResult {
    /// Zero-based index of the action in the sequence.
    #[pyo3(get)]
    pub action_index: usize,
    /// The type of action that was executed.
    #[pyo3(get)]
    pub action_type: String,
    /// Whether the action completed successfully.
    #[pyo3(get)]
    pub success: bool,
    /// Action-specific return data (screenshot bytes, JS return value, scraped HTML).
    #[pyo3(get)]
    #[serde(default, deserialize_with = "alef_json_str_opt::deserialize")]
    pub data: Option<String>,
    /// Error message if the action failed.
    #[pyo3(get)]
    pub error: Option<String>,
}

#[pymethods]
impl ActionResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (action_index=Self::default().action_index, action_type=Self::default().action_type, success=Self::default().success, data=None, error=None))]
    #[new]
    pub fn new(
        action_index: usize,
        action_type: String,
        success: bool,
        data: Option<String>,
        error: Option<String>,
    ) -> Self {
        Self {
            action_index,
            action_type,
            success,
            data,
            error,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ScrapeResult {
    /// The HTTP status code of the response.
    #[pyo3(get)]
    pub status_code: u16,
    /// The final URL after following all redirects.
    #[pyo3(get)]
    pub final_url: String,
    /// The Content-Type header value.
    #[pyo3(get)]
    pub content_type: String,
    /// The HTML body of the response.
    #[pyo3(get)]
    pub html: String,
    /// The size of the response body in bytes.
    #[pyo3(get)]
    pub body_size: usize,
    /// Extracted metadata from the page.
    #[pyo3(get)]
    pub metadata: PageMetadata,
    /// Links found on the page.
    #[pyo3(get)]
    pub links: Vec<LinkInfo>,
    /// Images found on the page.
    #[pyo3(get)]
    pub images: Vec<ImageInfo>,
    /// Feed links found on the page.
    #[pyo3(get)]
    pub feeds: Vec<FeedInfo>,
    /// JSON-LD entries found on the page.
    #[pyo3(get)]
    pub json_ld: Vec<JsonLdEntry>,
    /// Whether the URL is allowed by robots.txt.
    #[pyo3(get)]
    pub is_allowed: bool,
    /// The crawl delay from robots.txt, in seconds.
    #[pyo3(get)]
    pub crawl_delay: Option<u64>,
    /// Whether a noindex directive was detected.
    #[pyo3(get)]
    pub noindex_detected: bool,
    /// Whether a nofollow directive was detected.
    #[pyo3(get)]
    pub nofollow_detected: bool,
    /// The X-Robots-Tag header value, if present.
    #[pyo3(get)]
    pub x_robots_tag: Option<String>,
    /// Whether the content is a PDF.
    #[pyo3(get)]
    pub is_pdf: bool,
    /// Whether the page was skipped (binary or PDF content).
    #[pyo3(get)]
    pub was_skipped: bool,
    /// The detected character set encoding.
    #[pyo3(get)]
    pub detected_charset: Option<String>,
    /// Whether an authentication header was sent with the request.
    #[pyo3(get)]
    pub auth_header_sent: bool,
    /// Response metadata extracted from HTTP headers.
    #[pyo3(get)]
    pub response_meta: Option<ResponseMeta>,
    /// Downloaded assets from the page.
    #[pyo3(get)]
    pub assets: Vec<DownloadedAsset>,
    /// Whether the page content suggests JavaScript rendering is needed.
    #[pyo3(get)]
    pub js_render_hint: bool,
    /// Whether the browser fallback was used to fetch this page.
    #[pyo3(get)]
    pub browser_used: bool,
    /// Markdown conversion of the page content.
    #[pyo3(get)]
    pub markdown: Option<MarkdownResult>,
    /// Structured data extracted by LLM. Populated when extraction is configured.
    #[pyo3(get)]
    #[serde(default, deserialize_with = "alef_json_str_opt::deserialize")]
    pub extracted_data: Option<String>,
    /// Metadata about the LLM extraction pass (cost, tokens, model).
    #[pyo3(get)]
    pub extraction_meta: Option<ExtractionMeta>,
    /// Downloaded non-HTML document (PDF, DOCX, image, code, etc.).
    #[pyo3(get)]
    pub downloaded_document: Option<DownloadedDocument>,
    /// Browser-specific extras (eval result, network events, cookies). Only
    /// populated when `BrowserBackend.Native` was used for this request.
    #[pyo3(get)]
    pub browser: Option<BrowserExtras>,
}

#[pymethods]
impl ScrapeResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (status_code=Self::default().status_code, final_url=Self::default().final_url, content_type=Self::default().content_type, html=Self::default().html, body_size=Self::default().body_size, metadata=None, links=Self::default().links, images=Self::default().images, feeds=Self::default().feeds, json_ld=Self::default().json_ld, is_allowed=Self::default().is_allowed, noindex_detected=Self::default().noindex_detected, nofollow_detected=Self::default().nofollow_detected, is_pdf=Self::default().is_pdf, was_skipped=Self::default().was_skipped, auth_header_sent=Self::default().auth_header_sent, assets=Self::default().assets, js_render_hint=Self::default().js_render_hint, browser_used=Self::default().browser_used, crawl_delay=None, x_robots_tag=None, detected_charset=None, response_meta=None, markdown=None, extracted_data=None, extraction_meta=None, downloaded_document=None, browser=None))]
    #[new]
    pub fn new(
        status_code: u16,
        final_url: String,
        content_type: String,
        html: String,
        body_size: usize,
        metadata: Option<PageMetadata>,
        links: Vec<LinkInfo>,
        images: Vec<ImageInfo>,
        feeds: Vec<FeedInfo>,
        json_ld: Vec<JsonLdEntry>,
        is_allowed: bool,
        noindex_detected: bool,
        nofollow_detected: bool,
        is_pdf: bool,
        was_skipped: bool,
        auth_header_sent: bool,
        assets: Vec<DownloadedAsset>,
        js_render_hint: bool,
        browser_used: bool,
        crawl_delay: Option<u64>,
        x_robots_tag: Option<String>,
        detected_charset: Option<String>,
        response_meta: Option<ResponseMeta>,
        markdown: Option<MarkdownResult>,
        extracted_data: Option<String>,
        extraction_meta: Option<ExtractionMeta>,
        downloaded_document: Option<DownloadedDocument>,
        browser: Option<BrowserExtras>,
    ) -> Self {
        Self {
            status_code,
            final_url,
            content_type,
            html,
            body_size,
            metadata: metadata.unwrap_or_else(|| Self::default().metadata),
            links,
            images,
            feeds,
            json_ld,
            is_allowed,
            crawl_delay,
            noindex_detected,
            nofollow_detected,
            x_robots_tag,
            is_pdf,
            was_skipped,
            detected_charset,
            auth_header_sent,
            response_meta,
            assets,
            js_render_hint,
            browser_used,
            markdown,
            extracted_data,
            extraction_meta,
            downloaded_document,
            browser,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CrawlPageResult {
    /// The original URL of the page.
    #[pyo3(get)]
    pub url: String,
    /// The normalized URL of the page.
    #[pyo3(get)]
    pub normalized_url: String,
    /// The HTTP status code of the response.
    #[pyo3(get)]
    pub status_code: u16,
    /// The Content-Type header value.
    #[pyo3(get)]
    pub content_type: String,
    /// The HTML body of the response.
    #[pyo3(get)]
    pub html: String,
    /// The size of the response body in bytes.
    #[pyo3(get)]
    pub body_size: usize,
    /// Extracted metadata from the page.
    #[pyo3(get)]
    pub metadata: PageMetadata,
    /// Links found on the page.
    #[pyo3(get)]
    pub links: Vec<LinkInfo>,
    /// Images found on the page.
    #[pyo3(get)]
    pub images: Vec<ImageInfo>,
    /// Feed links found on the page.
    #[pyo3(get)]
    pub feeds: Vec<FeedInfo>,
    /// JSON-LD entries found on the page.
    #[pyo3(get)]
    pub json_ld: Vec<JsonLdEntry>,
    /// The depth of this page from the start URL.
    #[pyo3(get)]
    pub depth: usize,
    /// Whether this page is on the same domain as the start URL.
    #[pyo3(get)]
    pub stayed_on_domain: bool,
    /// Whether this page was skipped (binary or PDF content).
    #[pyo3(get)]
    pub was_skipped: bool,
    /// Whether the content is a PDF.
    #[pyo3(get)]
    pub is_pdf: bool,
    /// The detected character set encoding.
    #[pyo3(get)]
    pub detected_charset: Option<String>,
    /// Markdown conversion of the page content.
    #[pyo3(get)]
    pub markdown: Option<MarkdownResult>,
    /// Structured data extracted by LLM. Populated when extraction is configured.
    #[pyo3(get)]
    #[serde(default, deserialize_with = "alef_json_str_opt::deserialize")]
    pub extracted_data: Option<String>,
    /// Metadata about the LLM extraction pass (cost, tokens, model).
    #[pyo3(get)]
    pub extraction_meta: Option<ExtractionMeta>,
    /// Downloaded non-HTML document (PDF, DOCX, image, code, etc.).
    #[pyo3(get)]
    pub downloaded_document: Option<DownloadedDocument>,
    /// Whether the browser fallback was used to fetch this page.
    #[pyo3(get)]
    pub browser_used: bool,
}

#[pymethods]
impl CrawlPageResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, normalized_url=Self::default().normalized_url, status_code=Self::default().status_code, content_type=Self::default().content_type, html=Self::default().html, body_size=Self::default().body_size, metadata=None, links=Self::default().links, images=Self::default().images, feeds=Self::default().feeds, json_ld=Self::default().json_ld, depth=Self::default().depth, stayed_on_domain=Self::default().stayed_on_domain, was_skipped=Self::default().was_skipped, is_pdf=Self::default().is_pdf, browser_used=Self::default().browser_used, detected_charset=None, markdown=None, extracted_data=None, extraction_meta=None, downloaded_document=None))]
    #[new]
    pub fn new(
        url: String,
        normalized_url: String,
        status_code: u16,
        content_type: String,
        html: String,
        body_size: usize,
        metadata: Option<PageMetadata>,
        links: Vec<LinkInfo>,
        images: Vec<ImageInfo>,
        feeds: Vec<FeedInfo>,
        json_ld: Vec<JsonLdEntry>,
        depth: usize,
        stayed_on_domain: bool,
        was_skipped: bool,
        is_pdf: bool,
        browser_used: bool,
        detected_charset: Option<String>,
        markdown: Option<MarkdownResult>,
        extracted_data: Option<String>,
        extraction_meta: Option<ExtractionMeta>,
        downloaded_document: Option<DownloadedDocument>,
    ) -> Self {
        Self {
            url,
            normalized_url,
            status_code,
            content_type,
            html,
            body_size,
            metadata: metadata.unwrap_or_else(|| Self::default().metadata),
            links,
            images,
            feeds,
            json_ld,
            depth,
            stayed_on_domain,
            was_skipped,
            is_pdf,
            detected_charset,
            markdown,
            extracted_data,
            extraction_meta,
            downloaded_document,
            browser_used,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CrawlResult {
    /// The list of crawled pages.
    #[pyo3(get)]
    pub pages: Vec<CrawlPageResult>,
    /// The final URL after following redirects.
    #[pyo3(get)]
    pub final_url: String,
    /// The number of redirects followed.
    #[pyo3(get)]
    pub redirect_count: usize,
    /// Whether any page was skipped during crawling.
    #[pyo3(get)]
    pub was_skipped: bool,
    /// An error message, if the crawl encountered an issue.
    #[pyo3(get)]
    pub error: Option<String>,
    /// Cookies collected during the crawl.
    #[pyo3(get)]
    pub cookies: Vec<CookieInfo>,
    /// Whether all crawled pages stayed on the same domain as the start URL.
    #[pyo3(get)]
    pub stayed_on_domain: bool,
    /// Whether the browser fallback was used for any page in this crawl.
    #[pyo3(get)]
    pub browser_used: bool,
}

#[pymethods]
impl CrawlResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (pages=Self::default().pages, final_url=Self::default().final_url, redirect_count=Self::default().redirect_count, was_skipped=Self::default().was_skipped, cookies=Self::default().cookies, stayed_on_domain=Self::default().stayed_on_domain, browser_used=Self::default().browser_used, error=None))]
    #[new]
    pub fn new(
        pages: Vec<CrawlPageResult>,
        final_url: String,
        redirect_count: usize,
        was_skipped: bool,
        cookies: Vec<CookieInfo>,
        stayed_on_domain: bool,
        browser_used: bool,
        error: Option<String>,
    ) -> Self {
        Self {
            pages,
            final_url,
            redirect_count,
            was_skipped,
            error,
            cookies,
            stayed_on_domain,
            browser_used,
        }
    }

    #[pyo3(signature = ())]
    pub fn unique_normalized_urls(&self) -> usize {
        let core_self = kreuzcrawl::CrawlResult {
            pages: self.pages.clone().into_iter().map(Into::into).collect(),

            final_url: self.final_url.clone(),

            redirect_count: self.redirect_count,

            was_skipped: self.was_skipped,

            error: self.error.clone(),

            cookies: self.cookies.clone().into_iter().map(Into::into).collect(),

            stayed_on_domain: self.stayed_on_domain,

            browser_used: self.browser_used,

            normalized_urls: Default::default(),
        };
        core_self.unique_normalized_urls()
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SitemapUrl {
    /// The URL.
    #[pyo3(get)]
    pub url: String,
    /// The last modification date, if present.
    #[pyo3(get)]
    pub lastmod: Option<String>,
    /// The change frequency, if present.
    #[pyo3(get)]
    pub changefreq: Option<String>,
    /// The priority, if present.
    #[pyo3(get)]
    pub priority: Option<String>,
}

#[pymethods]
impl SitemapUrl {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, lastmod=None, changefreq=None, priority=None))]
    #[new]
    pub fn new(url: String, lastmod: Option<String>, changefreq: Option<String>, priority: Option<String>) -> Self {
        Self {
            url,
            lastmod,
            changefreq,
            priority,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct MapResult {
    /// The list of discovered URLs.
    #[pyo3(get)]
    pub urls: Vec<SitemapUrl>,
}

#[pymethods]
impl MapResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (urls=Self::default().urls))]
    #[new]
    pub fn new(urls: Vec<SitemapUrl>) -> Self {
        Self { urls }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct MarkdownResult {
    /// Converted markdown text.
    #[pyo3(get)]
    pub content: String,
    /// Structured document tree with semantic nodes.
    #[pyo3(get)]
    #[serde(default, deserialize_with = "alef_json_str_opt::deserialize")]
    pub document_structure: Option<String>,
    /// Extracted tables with structured cell data.
    #[pyo3(get)]
    pub tables: Vec<String>,
    /// Non-fatal processing warnings.
    #[pyo3(get)]
    pub warnings: Vec<String>,
    /// Whether citation conversion was applied and produced at least one reference.
    ///
    /// `true` when the markdown contained inline links that were converted to
    /// numbered citation references. The converted content (with `[N]` markers)
    /// is available in `content`; the full reference list is accessible via
    /// `generate_citations` if needed separately.
    #[pyo3(get)]
    pub citations: bool,
    /// Content-filtered markdown optimized for LLM consumption.
    #[pyo3(get)]
    pub fit_content: Option<String>,
}

#[pymethods]
impl MarkdownResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (content=Self::default().content, tables=Self::default().tables, warnings=Self::default().warnings, citations=Self::default().citations, document_structure=None, fit_content=None))]
    #[new]
    pub fn new(
        content: String,
        tables: Vec<String>,
        warnings: Vec<String>,
        citations: bool,
        document_structure: Option<String>,
        fit_content: Option<String>,
    ) -> Self {
        Self {
            content,
            document_structure,
            tables,
            warnings,
            citations,
            fit_content,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
#[allow(clippy::similar_names)]
pub struct LinkInfo {
    /// The resolved URL of the link.
    #[pyo3(get)]
    pub url: String,
    /// The visible text of the link.
    #[pyo3(get)]
    pub text: String,
    /// The classification of the link.
    #[pyo3(get)]
    pub link_type: LinkType,
    /// The `rel` attribute value, if present.
    #[pyo3(get)]
    pub rel: Option<String>,
    /// Whether the link has `rel="nofollow"`.
    #[pyo3(get)]
    pub nofollow: bool,
}

#[pymethods]
impl LinkInfo {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, text=Self::default().text, link_type=Self::default().link_type, nofollow=Self::default().nofollow, rel=None))]
    #[new]
    pub fn new(url: String, text: String, link_type: LinkType, nofollow: bool, rel: Option<String>) -> Self {
        Self {
            url,
            text,
            link_type,
            rel,
            nofollow,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ImageInfo {
    /// The image URL.
    #[pyo3(get)]
    pub url: String,
    /// The alt text, if present.
    #[pyo3(get)]
    pub alt: Option<String>,
    /// The width attribute, if present and parseable.
    #[pyo3(get)]
    pub width: Option<u32>,
    /// The height attribute, if present and parseable.
    #[pyo3(get)]
    pub height: Option<u32>,
    /// The source of the image reference.
    #[pyo3(get)]
    pub source: ImageSource,
}

#[pymethods]
impl ImageInfo {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, source=Self::default().source, alt=None, width=None, height=None))]
    #[new]
    pub fn new(url: String, source: ImageSource, alt: Option<String>, width: Option<u32>, height: Option<u32>) -> Self {
        Self {
            url,
            alt,
            width,
            height,
            source,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct FeedInfo {
    /// The feed URL.
    #[pyo3(get)]
    pub url: String,
    /// The feed title, if present.
    #[pyo3(get)]
    pub title: Option<String>,
    /// The type of feed.
    #[pyo3(get)]
    pub feed_type: FeedType,
}

#[pymethods]
impl FeedInfo {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, feed_type=Self::default().feed_type, title=None))]
    #[new]
    pub fn new(url: String, feed_type: FeedType, title: Option<String>) -> Self {
        Self { url, title, feed_type }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct JsonLdEntry {
    /// The `@type` value from the JSON-LD object.
    #[pyo3(get)]
    pub schema_type: String,
    /// The `name` value, if present.
    #[pyo3(get)]
    pub name: Option<String>,
    /// The raw JSON-LD string.
    #[pyo3(get)]
    pub raw: String,
}

#[pymethods]
impl JsonLdEntry {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (schema_type=Self::default().schema_type, raw=Self::default().raw, name=None))]
    #[new]
    pub fn new(schema_type: String, raw: String, name: Option<String>) -> Self {
        Self { schema_type, name, raw }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CookieInfo {
    /// The cookie name.
    #[pyo3(get)]
    pub name: String,
    /// The cookie value.
    #[pyo3(get)]
    pub value: String,
    /// The cookie domain, if specified.
    #[pyo3(get)]
    pub domain: Option<String>,
    /// The cookie path, if specified.
    #[pyo3(get)]
    pub path: Option<String>,
}

#[pymethods]
impl CookieInfo {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (name=Self::default().name, value=Self::default().value, domain=None, path=None))]
    #[new]
    pub fn new(name: String, value: String, domain: Option<String>, path: Option<String>) -> Self {
        Self {
            name,
            value,
            domain,
            path,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct DownloadedAsset {
    /// The original URL of the asset.
    #[pyo3(get)]
    pub url: String,
    /// The SHA-256 content hash of the asset.
    #[pyo3(get)]
    pub content_hash: String,
    /// The MIME type from the Content-Type header.
    #[pyo3(get)]
    pub mime_type: Option<String>,
    /// The size of the asset in bytes.
    #[pyo3(get)]
    pub size: usize,
    /// The category of the asset.
    #[pyo3(get)]
    pub asset_category: AssetCategory,
    /// The HTML tag that referenced this asset (e.g., "link", "script", "img").
    #[pyo3(get)]
    pub html_tag: Option<String>,
}

#[pymethods]
impl DownloadedAsset {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, content_hash=Self::default().content_hash, size=Self::default().size, asset_category=Self::default().asset_category, mime_type=None, html_tag=None))]
    #[new]
    pub fn new(
        url: String,
        content_hash: String,
        size: usize,
        asset_category: AssetCategory,
        mime_type: Option<String>,
        html_tag: Option<String>,
    ) -> Self {
        Self {
            url,
            content_hash,
            mime_type,
            size,
            asset_category,
            html_tag,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ArticleMetadata {
    /// The article publication time.
    #[pyo3(get)]
    pub published_time: Option<String>,
    /// The article modification time.
    #[pyo3(get)]
    pub modified_time: Option<String>,
    /// The article author.
    #[pyo3(get)]
    pub author: Option<String>,
    /// The article section.
    #[pyo3(get)]
    pub section: Option<String>,
    /// The article tags.
    #[pyo3(get)]
    pub tags: Vec<String>,
}

#[pymethods]
impl ArticleMetadata {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (tags=Self::default().tags, published_time=None, modified_time=None, author=None, section=None))]
    #[new]
    pub fn new(
        tags: Vec<String>,
        published_time: Option<String>,
        modified_time: Option<String>,
        author: Option<String>,
        section: Option<String>,
    ) -> Self {
        Self {
            published_time,
            modified_time,
            author,
            section,
            tags,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct HreflangEntry {
    /// The language code (e.g., "en", "fr", "x-default").
    #[pyo3(get)]
    pub lang: String,
    /// The URL for this language variant.
    #[pyo3(get)]
    pub url: String,
}

#[pymethods]
impl HreflangEntry {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (lang=Self::default().lang, url=Self::default().url))]
    #[new]
    pub fn new(lang: String, url: String) -> Self {
        Self { lang, url }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
#[allow(clippy::similar_names)]
pub struct FaviconInfo {
    /// The icon URL.
    #[pyo3(get)]
    pub url: String,
    /// The `rel` attribute (e.g., "icon", "apple-touch-icon").
    #[pyo3(get)]
    pub rel: String,
    /// The `sizes` attribute, if present.
    #[pyo3(get)]
    pub sizes: Option<String>,
    /// The MIME type, if present.
    #[pyo3(get)]
    pub mime_type: Option<String>,
}

#[pymethods]
impl FaviconInfo {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, rel=Self::default().rel, sizes=None, mime_type=None))]
    #[new]
    pub fn new(url: String, rel: String, sizes: Option<String>, mime_type: Option<String>) -> Self {
        Self {
            url,
            rel,
            sizes,
            mime_type,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct HeadingInfo {
    /// The heading level (1-6).
    #[pyo3(get)]
    pub level: u8,
    /// The heading text content.
    #[pyo3(get)]
    pub text: String,
}

#[pymethods]
impl HeadingInfo {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (level=Self::default().level, text=Self::default().text))]
    #[new]
    pub fn new(level: u8, text: String) -> Self {
        Self { level, text }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ResponseMeta {
    /// The ETag header value.
    #[pyo3(get)]
    pub etag: Option<String>,
    /// The Last-Modified header value.
    #[pyo3(get)]
    pub last_modified: Option<String>,
    /// The Cache-Control header value.
    #[pyo3(get)]
    pub cache_control: Option<String>,
    /// The Server header value.
    #[pyo3(get)]
    pub server: Option<String>,
    /// The X-Powered-By header value.
    #[pyo3(get)]
    pub x_powered_by: Option<String>,
    /// The Content-Language header value.
    #[pyo3(get)]
    pub content_language: Option<String>,
    /// The Content-Encoding header value.
    #[pyo3(get)]
    pub content_encoding: Option<String>,
}

#[pymethods]
impl ResponseMeta {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (etag=None, last_modified=None, cache_control=None, server=None, x_powered_by=None, content_language=None, content_encoding=None))]
    #[new]
    pub fn new(
        etag: Option<String>,
        last_modified: Option<String>,
        cache_control: Option<String>,
        server: Option<String>,
        x_powered_by: Option<String>,
        content_language: Option<String>,
        content_encoding: Option<String>,
    ) -> Self {
        Self {
            etag,
            last_modified,
            cache_control,
            server,
            x_powered_by,
            content_language,
            content_encoding,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
#[allow(clippy::similar_names)]
pub struct PageMetadata {
    /// The page title from the `<title>` element.
    #[pyo3(get)]
    pub title: Option<String>,
    /// The meta description.
    #[pyo3(get)]
    pub description: Option<String>,
    /// The canonical URL from `<link rel="canonical">`.
    #[pyo3(get)]
    pub canonical_url: Option<String>,
    /// Keywords from `<meta name="keywords">`.
    #[pyo3(get)]
    pub keywords: Option<String>,
    /// Author from `<meta name="author">`.
    #[pyo3(get)]
    pub author: Option<String>,
    /// Viewport content from `<meta name="viewport">`.
    #[pyo3(get)]
    pub viewport: Option<String>,
    /// Theme color from `<meta name="theme-color">`.
    #[pyo3(get)]
    pub theme_color: Option<String>,
    /// Generator from `<meta name="generator">`.
    #[pyo3(get)]
    pub generator: Option<String>,
    /// Robots content from `<meta name="robots">`.
    #[pyo3(get)]
    pub robots: Option<String>,
    /// The `lang` attribute from the `<html>` element.
    #[pyo3(get)]
    pub html_lang: Option<String>,
    /// The `dir` attribute from the `<html>` element.
    #[pyo3(get)]
    pub html_dir: Option<String>,
    /// Open Graph title.
    #[pyo3(get)]
    pub og_title: Option<String>,
    /// Open Graph type.
    #[pyo3(get)]
    pub og_type: Option<String>,
    /// Open Graph image URL.
    #[pyo3(get)]
    pub og_image: Option<String>,
    /// Open Graph description.
    #[pyo3(get)]
    pub og_description: Option<String>,
    /// Open Graph URL.
    #[pyo3(get)]
    pub og_url: Option<String>,
    /// Open Graph site name.
    #[pyo3(get)]
    pub og_site_name: Option<String>,
    /// Open Graph locale.
    #[pyo3(get)]
    pub og_locale: Option<String>,
    /// Open Graph video URL.
    #[pyo3(get)]
    pub og_video: Option<String>,
    /// Open Graph audio URL.
    #[pyo3(get)]
    pub og_audio: Option<String>,
    /// Open Graph locale alternates.
    #[pyo3(get)]
    pub og_locale_alternates: Option<Vec<String>>,
    /// Twitter card type.
    #[pyo3(get)]
    pub twitter_card: Option<String>,
    /// Twitter title.
    #[pyo3(get)]
    pub twitter_title: Option<String>,
    /// Twitter description.
    #[pyo3(get)]
    pub twitter_description: Option<String>,
    /// Twitter image URL.
    #[pyo3(get)]
    pub twitter_image: Option<String>,
    /// Twitter site handle.
    #[pyo3(get)]
    pub twitter_site: Option<String>,
    /// Twitter creator handle.
    #[pyo3(get)]
    pub twitter_creator: Option<String>,
    /// Dublin Core title.
    #[pyo3(get)]
    pub dc_title: Option<String>,
    /// Dublin Core creator.
    #[pyo3(get)]
    pub dc_creator: Option<String>,
    /// Dublin Core subject.
    #[pyo3(get)]
    pub dc_subject: Option<String>,
    /// Dublin Core description.
    #[pyo3(get)]
    pub dc_description: Option<String>,
    /// Dublin Core publisher.
    #[pyo3(get)]
    pub dc_publisher: Option<String>,
    /// Dublin Core date.
    #[pyo3(get)]
    pub dc_date: Option<String>,
    /// Dublin Core type.
    #[pyo3(get)]
    pub dc_type: Option<String>,
    /// Dublin Core format.
    #[pyo3(get)]
    pub dc_format: Option<String>,
    /// Dublin Core identifier.
    #[pyo3(get)]
    pub dc_identifier: Option<String>,
    /// Dublin Core language.
    #[pyo3(get)]
    pub dc_language: Option<String>,
    /// Dublin Core rights.
    #[pyo3(get)]
    pub dc_rights: Option<String>,
    /// Article metadata from `article:*` Open Graph tags.
    #[pyo3(get)]
    pub article: Option<ArticleMetadata>,
    /// Hreflang alternate links.
    #[pyo3(get)]
    pub hreflangs: Option<Vec<HreflangEntry>>,
    /// Favicon and icon links.
    #[pyo3(get)]
    pub favicons: Option<Vec<FaviconInfo>>,
    /// Heading elements (h1-h6).
    #[pyo3(get)]
    pub headings: Option<Vec<HeadingInfo>>,
    /// Computed word count of the page body text.
    #[pyo3(get)]
    pub word_count: Option<usize>,
}

#[pymethods]
impl PageMetadata {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (title=None, description=None, canonical_url=None, keywords=None, author=None, viewport=None, theme_color=None, generator=None, robots=None, html_lang=None, html_dir=None, og_title=None, og_type=None, og_image=None, og_description=None, og_url=None, og_site_name=None, og_locale=None, og_video=None, og_audio=None, og_locale_alternates=None, twitter_card=None, twitter_title=None, twitter_description=None, twitter_image=None, twitter_site=None, twitter_creator=None, dc_title=None, dc_creator=None, dc_subject=None, dc_description=None, dc_publisher=None, dc_date=None, dc_type=None, dc_format=None, dc_identifier=None, dc_language=None, dc_rights=None, article=None, hreflangs=None, favicons=None, headings=None, word_count=None))]
    #[new]
    pub fn new(
        title: Option<String>,
        description: Option<String>,
        canonical_url: Option<String>,
        keywords: Option<String>,
        author: Option<String>,
        viewport: Option<String>,
        theme_color: Option<String>,
        generator: Option<String>,
        robots: Option<String>,
        html_lang: Option<String>,
        html_dir: Option<String>,
        og_title: Option<String>,
        og_type: Option<String>,
        og_image: Option<String>,
        og_description: Option<String>,
        og_url: Option<String>,
        og_site_name: Option<String>,
        og_locale: Option<String>,
        og_video: Option<String>,
        og_audio: Option<String>,
        og_locale_alternates: Option<Vec<String>>,
        twitter_card: Option<String>,
        twitter_title: Option<String>,
        twitter_description: Option<String>,
        twitter_image: Option<String>,
        twitter_site: Option<String>,
        twitter_creator: Option<String>,
        dc_title: Option<String>,
        dc_creator: Option<String>,
        dc_subject: Option<String>,
        dc_description: Option<String>,
        dc_publisher: Option<String>,
        dc_date: Option<String>,
        dc_type: Option<String>,
        dc_format: Option<String>,
        dc_identifier: Option<String>,
        dc_language: Option<String>,
        dc_rights: Option<String>,
        article: Option<ArticleMetadata>,
        hreflangs: Option<Vec<HreflangEntry>>,
        favicons: Option<Vec<FaviconInfo>>,
        headings: Option<Vec<HeadingInfo>>,
        word_count: Option<usize>,
    ) -> Self {
        Self {
            title,
            description,
            canonical_url,
            keywords,
            author,
            viewport,
            theme_color,
            generator,
            robots,
            html_lang,
            html_dir,
            og_title,
            og_type,
            og_image,
            og_description,
            og_url,
            og_site_name,
            og_locale,
            og_video,
            og_audio,
            og_locale_alternates,
            twitter_card,
            twitter_title,
            twitter_description,
            twitter_image,
            twitter_site,
            twitter_creator,
            dc_title,
            dc_creator,
            dc_subject,
            dc_description,
            dc_publisher,
            dc_date,
            dc_type,
            dc_format,
            dc_identifier,
            dc_language,
            dc_rights,
            article,
            hreflangs,
            favicons,
            headings,
            word_count,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CrawlStreamRequest {
    /// The seed URL to crawl.
    #[pyo3(get)]
    pub url: String,
}

#[pymethods]
impl CrawlStreamRequest {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url))]
    #[new]
    pub fn new(url: String) -> Self {
        Self { url }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct BatchCrawlStreamRequest {
    /// The seed URLs to crawl. Each URL is followed independently up to the
    /// engine's configured depth.
    #[pyo3(get)]
    pub urls: Vec<String>,
}

#[pymethods]
impl BatchCrawlStreamRequest {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (urls=Self::default().urls))]
    #[new]
    pub fn new(urls: Vec<String>) -> Self {
        Self { urls }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CitationResult {
    /// Markdown with links replaced by numbered citations.
    #[pyo3(get)]
    pub content: String,
    /// Numbered reference list: (index, url, text).
    #[pyo3(get)]
    pub references: Vec<CitationReference>,
}

#[pymethods]
impl CitationResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (content=Self::default().content, references=Self::default().references))]
    #[new]
    pub fn new(content: String, references: Vec<CitationReference>) -> Self {
        Self { content, references }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CitationReference {
    /// 1-based reference number as it appears in the source text.
    #[pyo3(get)]
    pub index: usize,
    /// Resolved absolute URL for this reference.
    #[pyo3(get)]
    pub url: String,
    /// Human-readable anchor text or title for the reference.
    #[pyo3(get)]
    pub text: String,
}

#[pymethods]
impl CitationReference {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (index=Self::default().index, url=Self::default().url, text=Self::default().text))]
    #[new]
    pub fn new(index: usize, url: String, text: String) -> Self {
        Self { index, url, text }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone)]
#[pyclass(unsendable, from_py_object)]
pub struct CrawlEngineHandle {
    inner: Arc<kreuzcrawl::CrawlEngineHandle>,
}

#[pymethods]
impl CrawlEngineHandle {
    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn crawl_stream<'py>(&self, py: Python<'py>, req: CrawlStreamRequest) -> PyResult<Bound<'py, PyAny>> {
        let inner = self.inner.clone();
        let core_req: kreuzcrawl::CrawlStreamRequest = req.into();
        let (tx, rx) = tokio::sync::mpsc::channel(32);
        pyo3_async_runtimes::tokio::get_runtime().spawn(async move {
            match inner.crawl_stream(core_req).await {
                Err(e) => {
                    let _ = tx.send(Err(e)).await;
                }
                Ok(mut stream) => {
                    while let Some(chunk) = futures::StreamExt::next(&mut stream).await {
                        if tx.send(chunk).await.is_err() {
                            break;
                        }
                    }
                }
            }
        });
        let iter = CrawlStreamIterator {
            receiver: Arc::new(tokio::sync::Mutex::new(rx)),
        };
        Ok(Bound::new(py, iter)?.into_any())
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn batch_crawl_stream<'py>(
        &self,
        py: Python<'py>,
        req: BatchCrawlStreamRequest,
    ) -> PyResult<Bound<'py, PyAny>> {
        let inner = self.inner.clone();
        let core_req: kreuzcrawl::BatchCrawlStreamRequest = req.into();
        let (tx, rx) = tokio::sync::mpsc::channel(32);
        pyo3_async_runtimes::tokio::get_runtime().spawn(async move {
            match inner.batch_crawl_stream(core_req).await {
                Err(e) => {
                    let _ = tx.send(Err(e)).await;
                }
                Ok(mut stream) => {
                    while let Some(chunk) = futures::StreamExt::next(&mut stream).await {
                        if tx.send(chunk).await.is_err() {
                            break;
                        }
                    }
                }
            }
        });
        let iter = BatchCrawlStreamIterator {
            receiver: Arc::new(tokio::sync::Mutex::new(rx)),
        };
        Ok(Bound::new(py, iter)?.into_any())
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct BatchScrapeResult {
    /// The URL that was scraped.
    #[pyo3(get)]
    pub url: String,
    /// The scrape result, if successful.
    #[pyo3(get)]
    pub result: Option<ScrapeResult>,
    /// The error message, if the scrape failed.
    #[pyo3(get)]
    pub error: Option<String>,
}

#[pymethods]
impl BatchScrapeResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, result=None, error=None))]
    #[new]
    pub fn new(url: String, result: Option<ScrapeResult>, error: Option<String>) -> Self {
        Self { url, result, error }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct BatchCrawlResult {
    /// The seed URL that was crawled.
    #[pyo3(get)]
    pub url: String,
    /// The crawl result, if successful.
    #[pyo3(get)]
    pub result: Option<CrawlResult>,
    /// The error message, if the crawl failed.
    #[pyo3(get)]
    pub error: Option<String>,
}

#[pymethods]
impl BatchCrawlResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (url=Self::default().url, result=None, error=None))]
    #[new]
    pub fn new(url: String, result: Option<CrawlResult>, error: Option<String>) -> Self {
        Self { url, result, error }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct BatchScrapeResults {
    /// Per-URL scrape results, in the order URLs were submitted.
    #[pyo3(get)]
    pub results: Vec<BatchScrapeResult>,
    /// Total number of URLs in the batch (equal to `results.len()`).
    #[pyo3(get)]
    pub total_count: usize,
    /// Number of URLs whose scrape succeeded (`error` is `None`).
    #[pyo3(get)]
    pub completed_count: usize,
    /// Number of URLs whose scrape failed (`error` is `Some`).
    #[pyo3(get)]
    pub failed_count: usize,
}

#[pymethods]
impl BatchScrapeResults {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (results=Self::default().results, total_count=Self::default().total_count, completed_count=Self::default().completed_count, failed_count=Self::default().failed_count))]
    #[new]
    pub fn new(
        results: Vec<BatchScrapeResult>,
        total_count: usize,
        completed_count: usize,
        failed_count: usize,
    ) -> Self {
        Self {
            results,
            total_count,
            completed_count,
            failed_count,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct BatchCrawlResults {
    /// Per-URL crawl results, in the order seed URLs were submitted.
    #[pyo3(get)]
    pub results: Vec<BatchCrawlResult>,
    /// Total number of seed URLs in the batch (equal to `results.len()`).
    #[pyo3(get)]
    pub total_count: usize,
    /// Number of seed URLs whose crawl succeeded (`error` is `None`).
    #[pyo3(get)]
    pub completed_count: usize,
    /// Number of seed URLs whose crawl failed (`error` is `Some`).
    #[pyo3(get)]
    pub failed_count: usize,
}

#[pymethods]
impl BatchCrawlResults {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (results=Self::default().results, total_count=Self::default().total_count, completed_count=Self::default().completed_count, failed_count=Self::default().failed_count))]
    #[new]
    pub fn new(
        results: Vec<BatchCrawlResult>,
        total_count: usize,
        completed_count: usize,
        failed_count: usize,
    ) -> Self {
        Self {
            results,
            total_count,
            completed_count,
            failed_count,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<Self>(&json_str).map_err(|e| pyo3::exceptions::PyValueError::new_err(e.to_string()))
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum BrowserMode {
    #[pyo3(name = "AUTO")]
    #[default]
    Auto = 0,
    #[pyo3(name = "ALWAYS")]
    Always = 1,
    #[pyo3(name = "NEVER")]
    Never = 2,
    #[pyo3(name = "STEALTH")]
    Stealth = 3,
}
#[pymethods]
impl BrowserMode {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "auto" => return Ok(Self::Auto),
                "always" => return Ok(Self::Always),
                "never" => return Ok(Self::Never),
                "stealth" => return Ok(Self::Stealth),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Auto),
                1 => return Ok(Self::Always),
                2 => return Ok(Self::Never),
                3 => return Ok(Self::Stealth),
                _ => {}
            }
        }
        let type_name = stringify!(BrowserMode);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize BrowserMode: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum BrowserWait {
    #[pyo3(name = "NETWORK_IDLE")]
    #[default]
    NetworkIdle = 0,
    #[pyo3(name = "SELECTOR")]
    Selector = 1,
    #[pyo3(name = "FIXED")]
    Fixed = 2,
}
#[pymethods]
impl BrowserWait {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "networkidle" => return Ok(Self::NetworkIdle),
                "network_idle" => return Ok(Self::NetworkIdle),
                "selector" => return Ok(Self::Selector),
                "fixed" => return Ok(Self::Fixed),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::NetworkIdle),
                1 => return Ok(Self::Selector),
                2 => return Ok(Self::Fixed),
                _ => {}
            }
        }
        let type_name = stringify!(BrowserWait);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize BrowserWait: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum BrowserBackend {
    #[pyo3(name = "CHROMIUMOXIDE")]
    #[default]
    Chromiumoxide = 0,
    #[pyo3(name = "NATIVE")]
    Native = 1,
}
#[pymethods]
impl BrowserBackend {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "chromiumoxide" => return Ok(Self::Chromiumoxide),
                "native" => return Ok(Self::Native),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Chromiumoxide),
                1 => return Ok(Self::Native),
                _ => {}
            }
        }
        let type_name = stringify!(BrowserBackend);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize BrowserBackend: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
pub struct AuthConfig {
    pub(crate) inner: kreuzcrawl::AuthConfig,
}

#[pymethods]
impl AuthConfig {
    #[new]
    #[pyo3(signature = (value=None, **kwargs))]
    fn new(
        py: Python<'_>,
        value: Option<&Bound<'_, pyo3::types::PyAny>>,
        kwargs: Option<&Bound<'_, pyo3::types::PyDict>>,
    ) -> PyResult<Self> {
        // Accept three call shapes:
        //   AuthConfig("variant_name")                     -> unit variant
        //   AuthConfig({"type": "variant", ...})           -> dict positional
        //   AuthConfig(type="variant", field=...)          -> kwargs (idiomatic)
        // Kwargs are assembled into a Python dict and re-serialized via json so
        // serde_json can deserialize into the tagged enum.
        let json_str: String = if let Some(v) = value {
            if let Ok(s) = v.extract::<String>() {
                serde_json::to_string(&s)
                    .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid AuthConfig: {e}")))?
            } else {
                let json_mod = py.import("json")?;
                json_mod.call_method1("dumps", (v,))?.extract()?
            }
        } else if let Some(kw) = kwargs {
            let json_mod = py.import("json")?;
            json_mod.call_method1("dumps", (kw,))?.extract()?
        } else {
            return Err(pyo3::exceptions::PyTypeError::new_err(
                "AuthConfig() requires a positional value or keyword arguments".to_string(),
            ));
        };
        let inner: kreuzcrawl::AuthConfig = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid AuthConfig: {e}")))?;
        Ok(Self { inner })
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(&self.inner)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize AuthConfig: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }

    #[getter]
    fn basic(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "basic" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn bearer(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "bearer" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn header(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "header" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn r#type(&self) -> pyo3::PyResult<String> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        json.get("type")
            .and_then(|v| v.as_str())
            .map(String::from)
            .ok_or_else(|| pyo3::exceptions::PyRuntimeError::new_err("type not found in serialized enum"))
    }
}

impl From<AuthConfig> for kreuzcrawl::AuthConfig {
    fn from(val: AuthConfig) -> Self {
        val.inner
    }
}

impl From<kreuzcrawl::AuthConfig> for AuthConfig {
    fn from(val: kreuzcrawl::AuthConfig) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for AuthConfig {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}

impl Default for AuthConfig {
    fn default() -> Self {
        Self {
            inner: Default::default(),
        }
    }
}

impl<'de> serde::Deserialize<'de> for AuthConfig {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = kreuzcrawl::AuthConfig::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum LinkType {
    #[pyo3(name = "INTERNAL")]
    #[default]
    Internal = 0,
    #[pyo3(name = "EXTERNAL")]
    External = 1,
    #[pyo3(name = "ANCHOR")]
    Anchor = 2,
    #[pyo3(name = "DOCUMENT")]
    Document = 3,
}
#[pymethods]
impl LinkType {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "internal" => return Ok(Self::Internal),
                "external" => return Ok(Self::External),
                "anchor" => return Ok(Self::Anchor),
                "document" => return Ok(Self::Document),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Internal),
                1 => return Ok(Self::External),
                2 => return Ok(Self::Anchor),
                3 => return Ok(Self::Document),
                _ => {}
            }
        }
        let type_name = stringify!(LinkType);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize LinkType: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ImageSource {
    #[pyo3(name = "IMG")]
    #[default]
    Img = 0,
    #[pyo3(name = "PICTURE_SOURCE")]
    PictureSource = 1,
    #[pyo3(name = "OG_IMAGE")]
    #[serde(rename = "og:image")]
    OgImage = 2,
    #[pyo3(name = "TWITTER_IMAGE")]
    #[serde(rename = "twitter:image")]
    TwitterImage = 3,
}
#[pymethods]
impl ImageSource {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "img" => return Ok(Self::Img),
                "picturesource" => return Ok(Self::PictureSource),
                "picture_source" => return Ok(Self::PictureSource),
                "ogimage" => return Ok(Self::OgImage),
                "og:image" => return Ok(Self::OgImage),
                "twitterimage" => return Ok(Self::TwitterImage),
                "twitter:image" => return Ok(Self::TwitterImage),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Img),
                1 => return Ok(Self::PictureSource),
                2 => return Ok(Self::OgImage),
                3 => return Ok(Self::TwitterImage),
                _ => {}
            }
        }
        let type_name = stringify!(ImageSource);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize ImageSource: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum FeedType {
    #[pyo3(name = "RSS")]
    #[default]
    Rss = 0,
    #[pyo3(name = "ATOM")]
    Atom = 1,
    #[pyo3(name = "JSON_FEED")]
    JsonFeed = 2,
}
#[pymethods]
impl FeedType {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "rss" => return Ok(Self::Rss),
                "atom" => return Ok(Self::Atom),
                "jsonfeed" => return Ok(Self::JsonFeed),
                "json_feed" => return Ok(Self::JsonFeed),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Rss),
                1 => return Ok(Self::Atom),
                2 => return Ok(Self::JsonFeed),
                _ => {}
            }
        }
        let type_name = stringify!(FeedType);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize FeedType: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum AssetCategory {
    #[pyo3(name = "DOCUMENT")]
    Document = 0,
    #[pyo3(name = "IMAGE")]
    #[default]
    Image = 1,
    #[pyo3(name = "AUDIO")]
    Audio = 2,
    #[pyo3(name = "VIDEO")]
    Video = 3,
    #[pyo3(name = "FONT")]
    Font = 4,
    #[pyo3(name = "STYLESHEET")]
    Stylesheet = 5,
    #[pyo3(name = "SCRIPT")]
    Script = 6,
    #[pyo3(name = "ARCHIVE")]
    Archive = 7,
    #[pyo3(name = "DATA")]
    Data = 8,
    #[pyo3(name = "OTHER")]
    Other = 9,
}
#[pymethods]
impl AssetCategory {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "document" => return Ok(Self::Document),
                "image" => return Ok(Self::Image),
                "audio" => return Ok(Self::Audio),
                "video" => return Ok(Self::Video),
                "font" => return Ok(Self::Font),
                "stylesheet" => return Ok(Self::Stylesheet),
                "script" => return Ok(Self::Script),
                "archive" => return Ok(Self::Archive),
                "data" => return Ok(Self::Data),
                "other" => return Ok(Self::Other),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Document),
                1 => return Ok(Self::Image),
                2 => return Ok(Self::Audio),
                3 => return Ok(Self::Video),
                4 => return Ok(Self::Font),
                5 => return Ok(Self::Stylesheet),
                6 => return Ok(Self::Script),
                7 => return Ok(Self::Archive),
                8 => return Ok(Self::Data),
                9 => return Ok(Self::Other),
                _ => {}
            }
        }
        let type_name = stringify!(AssetCategory);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize AssetCategory: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
pub struct CrawlEvent {
    pub(crate) inner: kreuzcrawl::CrawlEvent,
}

#[pymethods]
impl CrawlEvent {
    #[new]
    #[pyo3(signature = (value=None, **kwargs))]
    fn new(
        py: Python<'_>,
        value: Option<&Bound<'_, pyo3::types::PyAny>>,
        kwargs: Option<&Bound<'_, pyo3::types::PyDict>>,
    ) -> PyResult<Self> {
        // Accept three call shapes:
        //   CrawlEvent("variant_name")                     -> unit variant
        //   CrawlEvent({"type": "variant", ...})           -> dict positional
        //   CrawlEvent(type="variant", field=...)          -> kwargs (idiomatic)
        // Kwargs are assembled into a Python dict and re-serialized via json so
        // serde_json can deserialize into the tagged enum.
        let json_str: String = if let Some(v) = value {
            if let Ok(s) = v.extract::<String>() {
                serde_json::to_string(&s)
                    .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid CrawlEvent: {e}")))?
            } else {
                let json_mod = py.import("json")?;
                json_mod.call_method1("dumps", (v,))?.extract()?
            }
        } else if let Some(kw) = kwargs {
            let json_mod = py.import("json")?;
            json_mod.call_method1("dumps", (kw,))?.extract()?
        } else {
            return Err(pyo3::exceptions::PyTypeError::new_err(
                "CrawlEvent() requires a positional value or keyword arguments".to_string(),
            ));
        };
        let inner: kreuzcrawl::CrawlEvent = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid CrawlEvent: {e}")))?;
        Ok(Self { inner })
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(&self.inner)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize CrawlEvent: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }

    #[getter]
    fn page(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "page" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn error(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "error" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn complete(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "complete" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn r#type(&self) -> pyo3::PyResult<String> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        json.get("type")
            .and_then(|v| v.as_str())
            .map(String::from)
            .ok_or_else(|| pyo3::exceptions::PyRuntimeError::new_err("type not found in serialized enum"))
    }
}

impl From<CrawlEvent> for kreuzcrawl::CrawlEvent {
    fn from(val: CrawlEvent) -> Self {
        val.inner
    }
}

impl From<kreuzcrawl::CrawlEvent> for CrawlEvent {
    fn from(val: kreuzcrawl::CrawlEvent) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for CrawlEvent {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}

impl Default for CrawlEvent {
    fn default() -> Self {
        Self {
            inner: Default::default(),
        }
    }
}

impl<'de> serde::Deserialize<'de> for CrawlEvent {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = kreuzcrawl::CrawlEvent::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
pub struct PageAction {
    pub(crate) inner: kreuzcrawl::PageAction,
}

#[pymethods]
impl PageAction {
    #[new]
    #[pyo3(signature = (value=None, **kwargs))]
    fn new(
        py: Python<'_>,
        value: Option<&Bound<'_, pyo3::types::PyAny>>,
        kwargs: Option<&Bound<'_, pyo3::types::PyDict>>,
    ) -> PyResult<Self> {
        // Accept three call shapes:
        //   PageAction("variant_name")                     -> unit variant
        //   PageAction({"type": "variant", ...})           -> dict positional
        //   PageAction(type="variant", field=...)          -> kwargs (idiomatic)
        // Kwargs are assembled into a Python dict and re-serialized via json so
        // serde_json can deserialize into the tagged enum.
        let json_str: String = if let Some(v) = value {
            if let Ok(s) = v.extract::<String>() {
                serde_json::to_string(&s)
                    .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid PageAction: {e}")))?
            } else {
                let json_mod = py.import("json")?;
                json_mod.call_method1("dumps", (v,))?.extract()?
            }
        } else if let Some(kw) = kwargs {
            let json_mod = py.import("json")?;
            json_mod.call_method1("dumps", (kw,))?.extract()?
        } else {
            return Err(pyo3::exceptions::PyTypeError::new_err(
                "PageAction() requires a positional value or keyword arguments".to_string(),
            ));
        };
        let inner: kreuzcrawl::PageAction = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid PageAction: {e}")))?;
        Ok(Self { inner })
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(&self.inner)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize PageAction: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }

    #[getter]
    fn click(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "click" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn type_text(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "type_text" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn press(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "press" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn scroll(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "scroll" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn wait(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "wait" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn screenshot(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "screenshot" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn execute_js(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "execute_js" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn scrape(&self, py: Python<'_>) -> PyResult<Option<pyo3::Py<pyo3::types::PyDict>>> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        let tag_field = "type";
        let tag_value = json.get(tag_field).and_then(|v| v.as_str()).unwrap_or("");
        if tag_value != "scrape" {
            return Ok(None);
        }
        let json_str = json.to_string();
        let json_mod = py.import("json")?;
        let py_dict = json_mod
            .call_method1("loads", (&json_str,))?
            .cast_into::<pyo3::types::PyDict>()?;
        Ok(Some(py_dict.unbind()))
    }

    #[getter]
    fn r#type(&self) -> pyo3::PyResult<String> {
        let json =
            serde_json::to_value(&self.inner).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))?;
        json.get("type")
            .and_then(|v| v.as_str())
            .map(String::from)
            .ok_or_else(|| pyo3::exceptions::PyRuntimeError::new_err("type not found in serialized enum"))
    }
}

impl From<PageAction> for kreuzcrawl::PageAction {
    fn from(val: PageAction) -> Self {
        val.inner
    }
}

impl From<kreuzcrawl::PageAction> for PageAction {
    fn from(val: kreuzcrawl::PageAction) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for PageAction {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}

impl Default for PageAction {
    fn default() -> Self {
        Self {
            inner: Default::default(),
        }
    }
}

impl<'de> serde::Deserialize<'de> for PageAction {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = kreuzcrawl::PageAction::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ScrollDirection {
    #[pyo3(name = "UP")]
    Up = 0,
    #[pyo3(name = "DOWN")]
    #[default]
    Down = 1,
}
#[pymethods]
impl ScrollDirection {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "up" => return Ok(Self::Up),
                "down" => return Ok(Self::Down),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Up),
                1 => return Ok(Self::Down),
                _ => {}
            }
        }
        let type_name = stringify!(ScrollDirection);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("Failed to serialize ScrollDirection: {e}")))
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[pyfunction]
#[pyo3(signature = (markdown))]
pub fn generate_citations(markdown: String) -> CitationResult {
    kreuzcrawl::generate_citations(&markdown).into()
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (config=None))]
pub fn create_engine(config: Option<CrawlConfig>) -> PyResult<CrawlEngineHandle> {
    let config_core: Option<kreuzcrawl::CrawlConfig> = config.map(Into::into);

    kreuzcrawl::create_engine(config_core)
        .map(|val| CrawlEngineHandle { inner: Arc::new(val) })
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (engine, url))]
pub fn scrape<'py>(py: Python<'py>, engine: CrawlEngineHandle, url: String) -> PyResult<Bound<'py, PyAny>> {
    pyo3_async_runtimes::tokio::future_into_py(py, async move {
        let result = kreuzcrawl::scrape(&engine.inner, &url)
            .await
            .map_err(|e| PyErr::new::<PyRuntimeError, _>(e.to_string()))?;
        let wrapped_result: ScrapeResult = ScrapeResult::from(result);

        Ok(wrapped_result)
    })
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (engine, url))]
pub fn crawl<'py>(py: Python<'py>, engine: CrawlEngineHandle, url: String) -> PyResult<Bound<'py, PyAny>> {
    pyo3_async_runtimes::tokio::future_into_py(py, async move {
        let result = kreuzcrawl::crawl(&engine.inner, &url)
            .await
            .map_err(|e| PyErr::new::<PyRuntimeError, _>(e.to_string()))?;
        let wrapped_result: CrawlResult = CrawlResult::from(result);

        Ok(wrapped_result)
    })
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (engine, url))]
pub fn map_urls<'py>(py: Python<'py>, engine: CrawlEngineHandle, url: String) -> PyResult<Bound<'py, PyAny>> {
    pyo3_async_runtimes::tokio::future_into_py(py, async move {
        let result = kreuzcrawl::map_urls(&engine.inner, &url)
            .await
            .map_err(|e| PyErr::new::<PyRuntimeError, _>(e.to_string()))?;
        let wrapped_result: MapResult = MapResult::from(result);

        Ok(wrapped_result)
    })
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (engine, url, actions))]
pub fn interact<'py>(
    py: Python<'py>,
    engine: CrawlEngineHandle,
    url: String,
    actions: Vec<PageAction>,
) -> PyResult<Bound<'py, PyAny>> {
    let actions_core: Vec<_> = actions.into_iter().map(Into::into).collect();

    pyo3_async_runtimes::tokio::future_into_py(py, async move {
        let result = kreuzcrawl::interact(&engine.inner, &url, actions_core)
            .await
            .map_err(|e| PyErr::new::<PyRuntimeError, _>(e.to_string()))?;
        let wrapped_result: InteractionResult = InteractionResult::from(result);

        Ok(wrapped_result)
    })
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (engine, urls))]
pub fn batch_scrape<'py>(py: Python<'py>, engine: CrawlEngineHandle, urls: Vec<String>) -> PyResult<Bound<'py, PyAny>> {
    pyo3_async_runtimes::tokio::future_into_py(py, async move {
        let result = kreuzcrawl::batch_scrape(&engine.inner, urls)
            .await
            .map_err(|e| PyErr::new::<PyRuntimeError, _>(e.to_string()))?;
        let wrapped_result: BatchScrapeResults = BatchScrapeResults::from(result);

        Ok(wrapped_result)
    })
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (engine, urls))]
pub fn batch_crawl<'py>(py: Python<'py>, engine: CrawlEngineHandle, urls: Vec<String>) -> PyResult<Bound<'py, PyAny>> {
    pyo3_async_runtimes::tokio::future_into_py(py, async move {
        let result = kreuzcrawl::batch_crawl(&engine.inner, urls)
            .await
            .map_err(|e| PyErr::new::<PyRuntimeError, _>(e.to_string()))?;
        let wrapped_result: BatchCrawlResults = BatchCrawlResults::from(result);

        Ok(wrapped_result)
    })
}

// Error types
pyo3::create_exception!(_kreuzcrawl, NotFoundError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, UnauthorizedError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, ForbiddenError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, WafBlockedError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, CrawlTimeoutError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, RateLimitedError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, ServerError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, BadGatewayError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, GoneError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, CrawlConnectionError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, DnsError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, SslError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, DataLossError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, BrowserError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, BrowserTimeoutError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, InvalidConfigError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, UnsupportedError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, SsrfPolicyViolationError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, OtherError, pyo3::exceptions::PyException);
pyo3::create_exception!(_kreuzcrawl, CrawlError, pyo3::exceptions::PyException);

/// Convert a `kreuzcrawl::CrawlError` error to a Python exception.
fn crawl_error_to_py_err(e: kreuzcrawl::CrawlError) -> pyo3::PyErr {
    let msg = e.to_string();
    #[allow(unreachable_patterns)]
    match &e {
        kreuzcrawl::CrawlError::NotFound(..) => NotFoundError::new_err(msg),
        kreuzcrawl::CrawlError::Unauthorized(..) => UnauthorizedError::new_err(msg),
        kreuzcrawl::CrawlError::Forbidden(..) => ForbiddenError::new_err(msg),
        kreuzcrawl::CrawlError::WafBlocked { .. } => WafBlockedError::new_err(msg),
        kreuzcrawl::CrawlError::Timeout(..) => CrawlTimeoutError::new_err(msg),
        kreuzcrawl::CrawlError::RateLimited(..) => RateLimitedError::new_err(msg),
        kreuzcrawl::CrawlError::ServerError(..) => ServerError::new_err(msg),
        kreuzcrawl::CrawlError::BadGateway(..) => BadGatewayError::new_err(msg),
        kreuzcrawl::CrawlError::Gone(..) => GoneError::new_err(msg),
        kreuzcrawl::CrawlError::Connection(..) => CrawlConnectionError::new_err(msg),
        kreuzcrawl::CrawlError::Dns(..) => DnsError::new_err(msg),
        kreuzcrawl::CrawlError::Ssl(..) => SslError::new_err(msg),
        kreuzcrawl::CrawlError::DataLoss(..) => DataLossError::new_err(msg),
        kreuzcrawl::CrawlError::BrowserError(..) => BrowserError::new_err(msg),
        kreuzcrawl::CrawlError::BrowserTimeout(..) => BrowserTimeoutError::new_err(msg),
        kreuzcrawl::CrawlError::InvalidConfig(..) => InvalidConfigError::new_err(msg),
        kreuzcrawl::CrawlError::Unsupported(..) => UnsupportedError::new_err(msg),
        kreuzcrawl::CrawlError::SsrfPolicyViolation { .. } => SsrfPolicyViolationError::new_err(msg),
        kreuzcrawl::CrawlError::Other(..) => OtherError::new_err(msg),
        _ => CrawlError::new_err(msg),
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ExtractionMeta> for kreuzcrawl::ExtractionMeta {
    fn from(val: ExtractionMeta) -> Self {
        Self {
            cost: val.cost,
            prompt_tokens: val.prompt_tokens,
            completion_tokens: val.completion_tokens,
            model: val.model,
            chunks_processed: val.chunks_processed,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::ExtractionMeta> for ExtractionMeta {
    fn from(val: kreuzcrawl::ExtractionMeta) -> Self {
        Self {
            cost: val.cost,
            prompt_tokens: val.prompt_tokens,
            completion_tokens: val.completion_tokens,
            model: val.model.map(|v| v.to_string()),
            chunks_processed: val.chunks_processed,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ProxyConfig> for kreuzcrawl::ProxyConfig {
    fn from(val: ProxyConfig) -> Self {
        Self {
            url: val.url,
            username: val.username,
            password: val.password,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::ProxyConfig> for ProxyConfig {
    fn from(val: kreuzcrawl::ProxyConfig) -> Self {
        Self {
            url: val.url.to_string(),
            username: val.username.map(|v| v.to_string()),
            password: val.password.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ContentConfig> for kreuzcrawl::ContentConfig {
    fn from(val: ContentConfig) -> Self {
        Self {
            output_format: val.output_format,
            preprocessing_preset: val.preprocessing_preset,
            remove_navigation: val.remove_navigation,
            remove_forms: val.remove_forms,
            strip_tags: val.strip_tags.into_iter().collect(),
            preserve_tags: val.preserve_tags.into_iter().collect(),
            exclude_selectors: val.exclude_selectors.into_iter().collect(),
            skip_images: val.skip_images,
            max_depth: val.max_depth,
            wrap: val.wrap,
            wrap_width: val.wrap_width,
            include_document_structure: val.include_document_structure,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::ContentConfig> for ContentConfig {
    fn from(val: kreuzcrawl::ContentConfig) -> Self {
        Self {
            output_format: val.output_format.to_string(),
            preprocessing_preset: val.preprocessing_preset.to_string(),
            remove_navigation: val.remove_navigation,
            remove_forms: val.remove_forms,
            strip_tags: val.strip_tags.into_iter().collect(),
            preserve_tags: val.preserve_tags.into_iter().collect(),
            exclude_selectors: val.exclude_selectors.into_iter().collect(),
            skip_images: val.skip_images,
            max_depth: val.max_depth,
            wrap: val.wrap,
            wrap_width: val.wrap_width,
            include_document_structure: val.include_document_structure,
        }
    }
}

#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<BrowserConfig> for kreuzcrawl::BrowserConfig {
    fn from(val: BrowserConfig) -> Self {
        let mut __result = kreuzcrawl::BrowserConfig::default();
        __result.mode = val.mode.into();
        __result.backend = val.backend.into();
        __result.endpoint = val.endpoint;
        if let Some(__v) = val.timeout {
            __result.timeout = std::time::Duration::from_millis(__v);
        }
        __result.wait = val.wait.into();
        __result.wait_selector = val.wait_selector;
        __result.extra_wait = val.extra_wait.map(std::time::Duration::from_millis);
        __result.proxy = val.proxy.map(Into::into);
        __result.block_url_patterns = val.block_url_patterns.into_iter().collect();
        __result.eval_script = val.eval_script;
        __result.robots_user_agent = val.robots_user_agent;
        __result.capture_network_events = val.capture_network_events;
        __result.session_affinity = val.session_affinity;
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::BrowserConfig> for BrowserConfig {
    fn from(val: kreuzcrawl::BrowserConfig) -> Self {
        Self {
            mode: val.mode.into(),
            backend: val.backend.into(),
            endpoint: val.endpoint.map(|v| v.to_string()),
            timeout: Some(val.timeout.as_millis() as u64),
            wait: val.wait.into(),
            wait_selector: val.wait_selector.map(|v| v.to_string()),
            extra_wait: val.extra_wait.map(|d| d.as_millis() as u64),
            proxy: val.proxy.map(Into::into),
            block_url_patterns: val.block_url_patterns.into_iter().collect(),
            eval_script: val.eval_script.map(|v| v.to_string()),
            robots_user_agent: val.robots_user_agent.map(|v| v.to_string()),
            capture_network_events: val.capture_network_events,
            session_affinity: val.session_affinity,
        }
    }
}

#[allow(clippy::needless_update)]
#[allow(clippy::field_reassign_with_default, clippy::let_and_return)]
#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<CrawlConfig> for kreuzcrawl::CrawlConfig {
    fn from(val: CrawlConfig) -> Self {
        let mut __result = kreuzcrawl::CrawlConfig::default();
        __result.max_depth = val.max_depth;
        __result.max_pages = val.max_pages;
        __result.max_concurrent = val.max_concurrent;
        __result.respect_robots_txt = val.respect_robots_txt;
        __result.soft_http_errors = val.soft_http_errors;
        __result.user_agent = val.user_agent;
        __result.stay_on_domain = val.stay_on_domain;
        __result.allow_subdomains = val.allow_subdomains;
        __result.include_paths = val.include_paths.into_iter().collect();
        __result.exclude_paths = val.exclude_paths.into_iter().collect();
        __result.custom_headers = val
            .custom_headers
            .into_iter()
            .map(|(k, v)| (k.into(), v.into()))
            .collect();
        if let Some(__v) = val.request_timeout {
            __result.request_timeout = std::time::Duration::from_millis(__v);
        }
        __result.rate_limit_ms = val.rate_limit_ms;
        __result.max_redirects = val.max_redirects;
        __result.retry_count = val.retry_count;
        __result.retry_codes = val.retry_codes.into_iter().collect();
        __result.cookies_enabled = val.cookies_enabled;
        __result.auth = val.auth.map(Into::into);
        __result.max_body_size = val.max_body_size;
        __result.remove_tags = val.remove_tags.into_iter().collect();
        __result.content = val.content.into();
        __result.map_limit = val.map_limit;
        __result.map_search = val.map_search;
        __result.download_assets = val.download_assets;
        __result.asset_types = val.asset_types.into_iter().map(Into::into).collect();
        __result.max_asset_size = val.max_asset_size;
        __result.browser = val.browser.into();
        __result.proxy = val.proxy.map(Into::into);
        __result.user_agents = val.user_agents.into_iter().collect();
        __result.capture_screenshot = val.capture_screenshot;
        __result.follow_document_urls = val.follow_document_urls;
        __result.document_url_depth = val.document_url_depth;
        __result.download_documents = val.download_documents;
        __result.document_max_size = val.document_max_size;
        __result.document_mime_types = val.document_mime_types.into_iter().collect();
        __result.warc_output = val.warc_output.map(Into::into);
        __result.browser_profile = val.browser_profile;
        __result.save_browser_profile = val.save_browser_profile;
        __result
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::CrawlConfig> for CrawlConfig {
    fn from(val: kreuzcrawl::CrawlConfig) -> Self {
        Self {
            max_depth: val.max_depth,
            max_pages: val.max_pages,
            max_concurrent: val.max_concurrent,
            respect_robots_txt: val.respect_robots_txt,
            soft_http_errors: val.soft_http_errors,
            user_agent: val.user_agent.map(|v| v.to_string()),
            stay_on_domain: val.stay_on_domain,
            allow_subdomains: val.allow_subdomains,
            include_paths: val.include_paths.into_iter().collect(),
            exclude_paths: val.exclude_paths.into_iter().collect(),
            custom_headers: val
                .custom_headers
                .into_iter()
                .map(|(k, v)| (k.into(), v.into()))
                .collect(),
            request_timeout: Some(val.request_timeout.as_millis() as u64),
            rate_limit_ms: val.rate_limit_ms,
            max_redirects: val.max_redirects,
            retry_count: val.retry_count,
            retry_codes: val.retry_codes.into_iter().collect(),
            cookies_enabled: val.cookies_enabled,
            auth: val.auth.map(Into::into),
            max_body_size: val.max_body_size,
            remove_tags: val.remove_tags.into_iter().collect(),
            content: val.content.into(),
            map_limit: val.map_limit,
            map_search: val.map_search.map(|v| v.to_string()),
            download_assets: val.download_assets,
            asset_types: val.asset_types.into_iter().map(Into::into).collect(),
            max_asset_size: val.max_asset_size,
            browser: val.browser.into(),
            proxy: val.proxy.map(Into::into),
            user_agents: val.user_agents.into_iter().collect(),
            capture_screenshot: val.capture_screenshot,
            follow_document_urls: val.follow_document_urls,
            document_url_depth: val.document_url_depth,
            download_documents: val.download_documents,
            document_max_size: val.document_max_size,
            document_mime_types: val.document_mime_types.into_iter().collect(),
            warc_output: val.warc_output.map(|p| p.to_string_lossy().to_string()),
            browser_profile: val.browser_profile.map(|v| v.to_string()),
            save_browser_profile: val.save_browser_profile,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<BrowserExtras> for kreuzcrawl::BrowserExtras {
    fn from(val: BrowserExtras) -> Self {
        Self {
            eval_result: val.eval_result.as_ref().and_then(|s| serde_json::from_str(s).ok()),
            network_events: val.network_events.into_iter().map(Into::into).collect(),
            cookies: val.cookies.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::BrowserExtras> for BrowserExtras {
    fn from(val: kreuzcrawl::BrowserExtras) -> Self {
        Self {
            eval_result: val.eval_result.as_ref().map(ToString::to_string),
            network_events: val.network_events.into_iter().map(Into::into).collect(),
            cookies: val.cookies.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<DownloadedDocument> for kreuzcrawl::DownloadedDocument {
    fn from(val: DownloadedDocument) -> Self {
        Self {
            url: val.url,
            mime_type: val.mime_type.into(),
            content: Default::default(),
            size: val.size,
            filename: val.filename.map(Into::into),
            content_hash: val.content_hash.into(),
            headers: val.headers.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::DownloadedDocument> for DownloadedDocument {
    fn from(val: kreuzcrawl::DownloadedDocument) -> Self {
        Self {
            url: val.url.to_string(),
            mime_type: val.mime_type.to_string(),
            size: val.size,
            filename: val.filename.as_ref().map(|v| v.to_string()),
            content_hash: val.content_hash.to_string(),
            headers: val.headers.into_iter().map(|(k, v)| (k.into(), v.into())).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<InteractionResult> for kreuzcrawl::InteractionResult {
    fn from(val: InteractionResult) -> Self {
        Self {
            action_results: val.action_results.into_iter().map(Into::into).collect(),
            final_html: val.final_html,
            final_url: val.final_url,
            screenshot: Default::default(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::InteractionResult> for InteractionResult {
    fn from(val: kreuzcrawl::InteractionResult) -> Self {
        Self {
            action_results: val.action_results.into_iter().map(Into::into).collect(),
            final_html: val.final_html.to_string(),
            final_url: val.final_url.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ActionResult> for kreuzcrawl::ActionResult {
    fn from(val: ActionResult) -> Self {
        Self {
            action_index: val.action_index,
            action_type: val.action_type.into(),
            success: val.success,
            data: val.data.as_ref().and_then(|s| serde_json::from_str(s).ok()),
            error: val.error,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::ActionResult> for ActionResult {
    fn from(val: kreuzcrawl::ActionResult) -> Self {
        Self {
            action_index: val.action_index,
            action_type: val.action_type.to_string(),
            success: val.success,
            data: val.data.as_ref().map(ToString::to_string),
            error: val.error.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ScrapeResult> for kreuzcrawl::ScrapeResult {
    fn from(val: ScrapeResult) -> Self {
        Self {
            status_code: val.status_code,
            final_url: val.final_url,
            content_type: val.content_type,
            html: val.html,
            body_size: val.body_size,
            metadata: val.metadata.into(),
            links: val.links.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            feeds: val.feeds.into_iter().map(Into::into).collect(),
            json_ld: val.json_ld.into_iter().map(Into::into).collect(),
            is_allowed: val.is_allowed,
            crawl_delay: val.crawl_delay,
            noindex_detected: val.noindex_detected,
            nofollow_detected: val.nofollow_detected,
            x_robots_tag: val.x_robots_tag,
            is_pdf: val.is_pdf,
            was_skipped: val.was_skipped,
            detected_charset: val.detected_charset,
            auth_header_sent: val.auth_header_sent,
            response_meta: val.response_meta.map(Into::into),
            assets: val.assets.into_iter().map(Into::into).collect(),
            js_render_hint: val.js_render_hint,
            browser_used: val.browser_used,
            markdown: val.markdown.map(Into::into),
            extracted_data: val.extracted_data.as_ref().and_then(|s| serde_json::from_str(s).ok()),
            extraction_meta: val.extraction_meta.map(Into::into),
            screenshot: Default::default(),
            downloaded_document: val.downloaded_document.map(Into::into),
            browser: val.browser.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::ScrapeResult> for ScrapeResult {
    fn from(val: kreuzcrawl::ScrapeResult) -> Self {
        Self {
            status_code: val.status_code,
            final_url: val.final_url.to_string(),
            content_type: val.content_type.to_string(),
            html: val.html.to_string(),
            body_size: val.body_size,
            metadata: val.metadata.into(),
            links: val.links.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            feeds: val.feeds.into_iter().map(Into::into).collect(),
            json_ld: val.json_ld.into_iter().map(Into::into).collect(),
            is_allowed: val.is_allowed,
            crawl_delay: val.crawl_delay,
            noindex_detected: val.noindex_detected,
            nofollow_detected: val.nofollow_detected,
            x_robots_tag: val.x_robots_tag.map(|v| v.to_string()),
            is_pdf: val.is_pdf,
            was_skipped: val.was_skipped,
            detected_charset: val.detected_charset.map(|v| v.to_string()),
            auth_header_sent: val.auth_header_sent,
            response_meta: val.response_meta.map(Into::into),
            assets: val.assets.into_iter().map(Into::into).collect(),
            js_render_hint: val.js_render_hint,
            browser_used: val.browser_used,
            markdown: val.markdown.map(Into::into),
            extracted_data: val.extracted_data.as_ref().map(ToString::to_string),
            extraction_meta: val.extraction_meta.map(Into::into),
            downloaded_document: val.downloaded_document.map(Into::into),
            browser: val.browser.map(Into::into),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<CrawlPageResult> for kreuzcrawl::CrawlPageResult {
    fn from(val: CrawlPageResult) -> Self {
        Self {
            url: val.url,
            normalized_url: val.normalized_url,
            status_code: val.status_code,
            content_type: val.content_type,
            html: val.html,
            body_size: val.body_size,
            metadata: val.metadata.into(),
            links: val.links.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            feeds: val.feeds.into_iter().map(Into::into).collect(),
            json_ld: val.json_ld.into_iter().map(Into::into).collect(),
            depth: val.depth,
            stayed_on_domain: val.stayed_on_domain,
            was_skipped: val.was_skipped,
            is_pdf: val.is_pdf,
            detected_charset: val.detected_charset,
            markdown: val.markdown.map(Into::into),
            extracted_data: val.extracted_data.as_ref().and_then(|s| serde_json::from_str(s).ok()),
            extraction_meta: val.extraction_meta.map(Into::into),
            downloaded_document: val.downloaded_document.map(Into::into),
            browser_used: val.browser_used,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::CrawlPageResult> for CrawlPageResult {
    fn from(val: kreuzcrawl::CrawlPageResult) -> Self {
        Self {
            url: val.url.to_string(),
            normalized_url: val.normalized_url.to_string(),
            status_code: val.status_code,
            content_type: val.content_type.to_string(),
            html: val.html.to_string(),
            body_size: val.body_size,
            metadata: val.metadata.into(),
            links: val.links.into_iter().map(Into::into).collect(),
            images: val.images.into_iter().map(Into::into).collect(),
            feeds: val.feeds.into_iter().map(Into::into).collect(),
            json_ld: val.json_ld.into_iter().map(Into::into).collect(),
            depth: val.depth,
            stayed_on_domain: val.stayed_on_domain,
            was_skipped: val.was_skipped,
            is_pdf: val.is_pdf,
            detected_charset: val.detected_charset.map(|v| v.to_string()),
            markdown: val.markdown.map(Into::into),
            extracted_data: val.extracted_data.as_ref().map(ToString::to_string),
            extraction_meta: val.extraction_meta.map(Into::into),
            downloaded_document: val.downloaded_document.map(Into::into),
            browser_used: val.browser_used,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<CrawlResult> for kreuzcrawl::CrawlResult {
    fn from(val: CrawlResult) -> Self {
        Self {
            pages: val.pages.into_iter().map(Into::into).collect(),
            final_url: val.final_url,
            redirect_count: val.redirect_count,
            was_skipped: val.was_skipped,
            error: val.error,
            cookies: val.cookies.into_iter().map(Into::into).collect(),
            stayed_on_domain: val.stayed_on_domain,
            browser_used: val.browser_used,
            normalized_urls: Default::default(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::CrawlResult> for CrawlResult {
    fn from(val: kreuzcrawl::CrawlResult) -> Self {
        Self {
            pages: val.pages.into_iter().map(Into::into).collect(),
            final_url: val.final_url.to_string(),
            redirect_count: val.redirect_count,
            was_skipped: val.was_skipped,
            error: val.error.map(|v| v.to_string()),
            cookies: val.cookies.into_iter().map(Into::into).collect(),
            stayed_on_domain: val.stayed_on_domain,
            browser_used: val.browser_used,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<SitemapUrl> for kreuzcrawl::SitemapUrl {
    fn from(val: SitemapUrl) -> Self {
        Self {
            url: val.url,
            lastmod: val.lastmod,
            changefreq: val.changefreq,
            priority: val.priority,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::SitemapUrl> for SitemapUrl {
    fn from(val: kreuzcrawl::SitemapUrl) -> Self {
        Self {
            url: val.url.to_string(),
            lastmod: val.lastmod.map(|v| v.to_string()),
            changefreq: val.changefreq.map(|v| v.to_string()),
            priority: val.priority.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<MapResult> for kreuzcrawl::MapResult {
    fn from(val: MapResult) -> Self {
        Self {
            urls: val.urls.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::MapResult> for MapResult {
    fn from(val: kreuzcrawl::MapResult) -> Self {
        Self {
            urls: val.urls.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<MarkdownResult> for kreuzcrawl::MarkdownResult {
    fn from(val: MarkdownResult) -> Self {
        Self {
            content: val.content,
            document_structure: val
                .document_structure
                .as_ref()
                .and_then(|s| serde_json::from_str(s).ok()),
            tables: val
                .tables
                .into_iter()
                .filter_map(|s| serde_json::from_str(&s).ok())
                .collect(),
            warnings: val.warnings.into_iter().collect(),
            citations: val.citations,
            fit_content: val.fit_content,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::MarkdownResult> for MarkdownResult {
    fn from(val: kreuzcrawl::MarkdownResult) -> Self {
        Self {
            content: val.content.to_string(),
            document_structure: val.document_structure.as_ref().map(ToString::to_string),
            tables: val.tables.iter().map(ToString::to_string).collect(),
            warnings: val.warnings.into_iter().collect(),
            citations: val.citations,
            fit_content: val.fit_content.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<LinkInfo> for kreuzcrawl::LinkInfo {
    fn from(val: LinkInfo) -> Self {
        Self {
            url: val.url,
            text: val.text,
            link_type: val.link_type.into(),
            rel: val.rel,
            nofollow: val.nofollow,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::LinkInfo> for LinkInfo {
    fn from(val: kreuzcrawl::LinkInfo) -> Self {
        Self {
            url: val.url.to_string(),
            text: val.text.to_string(),
            link_type: val.link_type.into(),
            rel: val.rel.map(|v| v.to_string()),
            nofollow: val.nofollow,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ImageInfo> for kreuzcrawl::ImageInfo {
    fn from(val: ImageInfo) -> Self {
        Self {
            url: val.url,
            alt: val.alt,
            width: val.width,
            height: val.height,
            source: val.source.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::ImageInfo> for ImageInfo {
    fn from(val: kreuzcrawl::ImageInfo) -> Self {
        Self {
            url: val.url.to_string(),
            alt: val.alt.map(|v| v.to_string()),
            width: val.width,
            height: val.height,
            source: val.source.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<FeedInfo> for kreuzcrawl::FeedInfo {
    fn from(val: FeedInfo) -> Self {
        Self {
            url: val.url,
            title: val.title,
            feed_type: val.feed_type.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::FeedInfo> for FeedInfo {
    fn from(val: kreuzcrawl::FeedInfo) -> Self {
        Self {
            url: val.url.to_string(),
            title: val.title.map(|v| v.to_string()),
            feed_type: val.feed_type.into(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<JsonLdEntry> for kreuzcrawl::JsonLdEntry {
    fn from(val: JsonLdEntry) -> Self {
        Self {
            schema_type: val.schema_type,
            name: val.name,
            raw: val.raw,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::JsonLdEntry> for JsonLdEntry {
    fn from(val: kreuzcrawl::JsonLdEntry) -> Self {
        Self {
            schema_type: val.schema_type.to_string(),
            name: val.name.map(|v| v.to_string()),
            raw: val.raw.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<CookieInfo> for kreuzcrawl::CookieInfo {
    fn from(val: CookieInfo) -> Self {
        Self {
            name: val.name,
            value: val.value,
            domain: val.domain,
            path: val.path,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::CookieInfo> for CookieInfo {
    fn from(val: kreuzcrawl::CookieInfo) -> Self {
        Self {
            name: val.name.to_string(),
            value: val.value.to_string(),
            domain: val.domain.map(|v| v.to_string()),
            path: val.path.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<DownloadedAsset> for kreuzcrawl::DownloadedAsset {
    fn from(val: DownloadedAsset) -> Self {
        Self {
            url: val.url,
            content_hash: val.content_hash,
            mime_type: val.mime_type,
            size: val.size,
            asset_category: val.asset_category.into(),
            html_tag: val.html_tag,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::DownloadedAsset> for DownloadedAsset {
    fn from(val: kreuzcrawl::DownloadedAsset) -> Self {
        Self {
            url: val.url.to_string(),
            content_hash: val.content_hash.to_string(),
            mime_type: val.mime_type.map(|v| v.to_string()),
            size: val.size,
            asset_category: val.asset_category.into(),
            html_tag: val.html_tag.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ArticleMetadata> for kreuzcrawl::ArticleMetadata {
    fn from(val: ArticleMetadata) -> Self {
        Self {
            published_time: val.published_time,
            modified_time: val.modified_time,
            author: val.author,
            section: val.section,
            tags: val.tags.into_iter().collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::ArticleMetadata> for ArticleMetadata {
    fn from(val: kreuzcrawl::ArticleMetadata) -> Self {
        Self {
            published_time: val.published_time.map(|v| v.to_string()),
            modified_time: val.modified_time.map(|v| v.to_string()),
            author: val.author.map(|v| v.to_string()),
            section: val.section.map(|v| v.to_string()),
            tags: val.tags.into_iter().collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<HreflangEntry> for kreuzcrawl::HreflangEntry {
    fn from(val: HreflangEntry) -> Self {
        Self {
            lang: val.lang,
            url: val.url,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::HreflangEntry> for HreflangEntry {
    fn from(val: kreuzcrawl::HreflangEntry) -> Self {
        Self {
            lang: val.lang.to_string(),
            url: val.url.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<FaviconInfo> for kreuzcrawl::FaviconInfo {
    fn from(val: FaviconInfo) -> Self {
        Self {
            url: val.url,
            rel: val.rel,
            sizes: val.sizes,
            mime_type: val.mime_type,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::FaviconInfo> for FaviconInfo {
    fn from(val: kreuzcrawl::FaviconInfo) -> Self {
        Self {
            url: val.url.to_string(),
            rel: val.rel.to_string(),
            sizes: val.sizes.map(|v| v.to_string()),
            mime_type: val.mime_type.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<HeadingInfo> for kreuzcrawl::HeadingInfo {
    fn from(val: HeadingInfo) -> Self {
        Self {
            level: val.level,
            text: val.text,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::HeadingInfo> for HeadingInfo {
    fn from(val: kreuzcrawl::HeadingInfo) -> Self {
        Self {
            level: val.level,
            text: val.text.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ResponseMeta> for kreuzcrawl::ResponseMeta {
    fn from(val: ResponseMeta) -> Self {
        Self {
            etag: val.etag,
            last_modified: val.last_modified,
            cache_control: val.cache_control,
            server: val.server,
            x_powered_by: val.x_powered_by,
            content_language: val.content_language,
            content_encoding: val.content_encoding,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::ResponseMeta> for ResponseMeta {
    fn from(val: kreuzcrawl::ResponseMeta) -> Self {
        Self {
            etag: val.etag.map(|v| v.to_string()),
            last_modified: val.last_modified.map(|v| v.to_string()),
            cache_control: val.cache_control.map(|v| v.to_string()),
            server: val.server.map(|v| v.to_string()),
            x_powered_by: val.x_powered_by.map(|v| v.to_string()),
            content_language: val.content_language.map(|v| v.to_string()),
            content_encoding: val.content_encoding.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<PageMetadata> for kreuzcrawl::PageMetadata {
    fn from(val: PageMetadata) -> Self {
        Self {
            title: val.title,
            description: val.description,
            canonical_url: val.canonical_url,
            keywords: val.keywords,
            author: val.author,
            viewport: val.viewport,
            theme_color: val.theme_color,
            generator: val.generator,
            robots: val.robots,
            html_lang: val.html_lang,
            html_dir: val.html_dir,
            og_title: val.og_title,
            og_type: val.og_type,
            og_image: val.og_image,
            og_description: val.og_description,
            og_url: val.og_url,
            og_site_name: val.og_site_name,
            og_locale: val.og_locale,
            og_video: val.og_video,
            og_audio: val.og_audio,
            og_locale_alternates: val.og_locale_alternates.map(|v| v.into_iter().collect()),
            twitter_card: val.twitter_card,
            twitter_title: val.twitter_title,
            twitter_description: val.twitter_description,
            twitter_image: val.twitter_image,
            twitter_site: val.twitter_site,
            twitter_creator: val.twitter_creator,
            dc_title: val.dc_title,
            dc_creator: val.dc_creator,
            dc_subject: val.dc_subject,
            dc_description: val.dc_description,
            dc_publisher: val.dc_publisher,
            dc_date: val.dc_date,
            dc_type: val.dc_type,
            dc_format: val.dc_format,
            dc_identifier: val.dc_identifier,
            dc_language: val.dc_language,
            dc_rights: val.dc_rights,
            article: val.article.map(Into::into),
            hreflangs: val.hreflangs.map(|v| v.into_iter().map(Into::into).collect()),
            favicons: val.favicons.map(|v| v.into_iter().map(Into::into).collect()),
            headings: val.headings.map(|v| v.into_iter().map(Into::into).collect()),
            word_count: val.word_count,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::PageMetadata> for PageMetadata {
    fn from(val: kreuzcrawl::PageMetadata) -> Self {
        Self {
            title: val.title.map(|v| v.to_string()),
            description: val.description.map(|v| v.to_string()),
            canonical_url: val.canonical_url.map(|v| v.to_string()),
            keywords: val.keywords.map(|v| v.to_string()),
            author: val.author.map(|v| v.to_string()),
            viewport: val.viewport.map(|v| v.to_string()),
            theme_color: val.theme_color.map(|v| v.to_string()),
            generator: val.generator.map(|v| v.to_string()),
            robots: val.robots.map(|v| v.to_string()),
            html_lang: val.html_lang.map(|v| v.to_string()),
            html_dir: val.html_dir.map(|v| v.to_string()),
            og_title: val.og_title.map(|v| v.to_string()),
            og_type: val.og_type.map(|v| v.to_string()),
            og_image: val.og_image.map(|v| v.to_string()),
            og_description: val.og_description.map(|v| v.to_string()),
            og_url: val.og_url.map(|v| v.to_string()),
            og_site_name: val.og_site_name.map(|v| v.to_string()),
            og_locale: val.og_locale.map(|v| v.to_string()),
            og_video: val.og_video.map(|v| v.to_string()),
            og_audio: val.og_audio.map(|v| v.to_string()),
            og_locale_alternates: val.og_locale_alternates.map(|v| v.into_iter().collect()),
            twitter_card: val.twitter_card.map(|v| v.to_string()),
            twitter_title: val.twitter_title.map(|v| v.to_string()),
            twitter_description: val.twitter_description.map(|v| v.to_string()),
            twitter_image: val.twitter_image.map(|v| v.to_string()),
            twitter_site: val.twitter_site.map(|v| v.to_string()),
            twitter_creator: val.twitter_creator.map(|v| v.to_string()),
            dc_title: val.dc_title.map(|v| v.to_string()),
            dc_creator: val.dc_creator.map(|v| v.to_string()),
            dc_subject: val.dc_subject.map(|v| v.to_string()),
            dc_description: val.dc_description.map(|v| v.to_string()),
            dc_publisher: val.dc_publisher.map(|v| v.to_string()),
            dc_date: val.dc_date.map(|v| v.to_string()),
            dc_type: val.dc_type.map(|v| v.to_string()),
            dc_format: val.dc_format.map(|v| v.to_string()),
            dc_identifier: val.dc_identifier.map(|v| v.to_string()),
            dc_language: val.dc_language.map(|v| v.to_string()),
            dc_rights: val.dc_rights.map(|v| v.to_string()),
            article: val.article.map(Into::into),
            hreflangs: val.hreflangs.map(|v| v.into_iter().map(Into::into).collect()),
            favicons: val.favicons.map(|v| v.into_iter().map(Into::into).collect()),
            headings: val.headings.map(|v| v.into_iter().map(Into::into).collect()),
            word_count: val.word_count,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<CrawlStreamRequest> for kreuzcrawl::CrawlStreamRequest {
    fn from(val: CrawlStreamRequest) -> Self {
        Self { url: val.url }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::CrawlStreamRequest> for CrawlStreamRequest {
    fn from(val: kreuzcrawl::CrawlStreamRequest) -> Self {
        Self {
            url: val.url.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<BatchCrawlStreamRequest> for kreuzcrawl::BatchCrawlStreamRequest {
    fn from(val: BatchCrawlStreamRequest) -> Self {
        Self {
            urls: val.urls.into_iter().collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::BatchCrawlStreamRequest> for BatchCrawlStreamRequest {
    fn from(val: kreuzcrawl::BatchCrawlStreamRequest) -> Self {
        Self {
            urls: val.urls.into_iter().collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<CitationResult> for kreuzcrawl::CitationResult {
    fn from(val: CitationResult) -> Self {
        Self {
            content: val.content,
            references: val.references.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::CitationResult> for CitationResult {
    fn from(val: kreuzcrawl::CitationResult) -> Self {
        Self {
            content: val.content.to_string(),
            references: val.references.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<CitationReference> for kreuzcrawl::CitationReference {
    fn from(val: CitationReference) -> Self {
        Self {
            index: val.index,
            url: val.url,
            text: val.text,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::CitationReference> for CitationReference {
    fn from(val: kreuzcrawl::CitationReference) -> Self {
        Self {
            index: val.index,
            url: val.url.to_string(),
            text: val.text.to_string(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<BatchScrapeResult> for kreuzcrawl::BatchScrapeResult {
    fn from(val: BatchScrapeResult) -> Self {
        Self {
            url: val.url,
            result: val.result.map(Into::into),
            error: val.error,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::BatchScrapeResult> for BatchScrapeResult {
    fn from(val: kreuzcrawl::BatchScrapeResult) -> Self {
        Self {
            url: val.url.to_string(),
            result: val.result.map(Into::into),
            error: val.error.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<BatchCrawlResult> for kreuzcrawl::BatchCrawlResult {
    fn from(val: BatchCrawlResult) -> Self {
        Self {
            url: val.url,
            result: val.result.map(Into::into),
            error: val.error,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::BatchCrawlResult> for BatchCrawlResult {
    fn from(val: kreuzcrawl::BatchCrawlResult) -> Self {
        Self {
            url: val.url.to_string(),
            result: val.result.map(Into::into),
            error: val.error.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<BatchScrapeResults> for kreuzcrawl::BatchScrapeResults {
    fn from(val: BatchScrapeResults) -> Self {
        Self {
            results: val.results.into_iter().map(Into::into).collect(),
            total_count: val.total_count,
            completed_count: val.completed_count,
            failed_count: val.failed_count,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::BatchScrapeResults> for BatchScrapeResults {
    fn from(val: kreuzcrawl::BatchScrapeResults) -> Self {
        Self {
            results: val.results.into_iter().map(Into::into).collect(),
            total_count: val.total_count,
            completed_count: val.completed_count,
            failed_count: val.failed_count,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<BatchCrawlResults> for kreuzcrawl::BatchCrawlResults {
    fn from(val: BatchCrawlResults) -> Self {
        Self {
            results: val.results.into_iter().map(Into::into).collect(),
            total_count: val.total_count,
            completed_count: val.completed_count,
            failed_count: val.failed_count,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<kreuzcrawl::BatchCrawlResults> for BatchCrawlResults {
    fn from(val: kreuzcrawl::BatchCrawlResults) -> Self {
        Self {
            results: val.results.into_iter().map(Into::into).collect(),
            total_count: val.total_count,
            completed_count: val.completed_count,
            failed_count: val.failed_count,
        }
    }
}

impl From<BrowserMode> for kreuzcrawl::BrowserMode {
    fn from(val: BrowserMode) -> Self {
        match val {
            BrowserMode::Auto => Self::Auto,
            BrowserMode::Always => Self::Always,
            BrowserMode::Never => Self::Never,
            BrowserMode::Stealth => Self::Stealth,
        }
    }
}

impl From<kreuzcrawl::BrowserMode> for BrowserMode {
    fn from(val: kreuzcrawl::BrowserMode) -> Self {
        match val {
            kreuzcrawl::BrowserMode::Auto => Self::Auto,
            kreuzcrawl::BrowserMode::Always => Self::Always,
            kreuzcrawl::BrowserMode::Never => Self::Never,
            kreuzcrawl::BrowserMode::Stealth => Self::Stealth,
        }
    }
}

impl From<BrowserWait> for kreuzcrawl::BrowserWait {
    fn from(val: BrowserWait) -> Self {
        match val {
            BrowserWait::NetworkIdle => Self::NetworkIdle,
            BrowserWait::Selector => Self::Selector,
            BrowserWait::Fixed => Self::Fixed,
        }
    }
}

impl From<kreuzcrawl::BrowserWait> for BrowserWait {
    fn from(val: kreuzcrawl::BrowserWait) -> Self {
        match val {
            kreuzcrawl::BrowserWait::NetworkIdle => Self::NetworkIdle,
            kreuzcrawl::BrowserWait::Selector => Self::Selector,
            kreuzcrawl::BrowserWait::Fixed => Self::Fixed,
        }
    }
}

impl From<BrowserBackend> for kreuzcrawl::BrowserBackend {
    fn from(val: BrowserBackend) -> Self {
        match val {
            BrowserBackend::Chromiumoxide => Self::Chromiumoxide,
            BrowserBackend::Native => Self::Native,
        }
    }
}

impl From<kreuzcrawl::BrowserBackend> for BrowserBackend {
    fn from(val: kreuzcrawl::BrowserBackend) -> Self {
        match val {
            kreuzcrawl::BrowserBackend::Chromiumoxide => Self::Chromiumoxide,
            kreuzcrawl::BrowserBackend::Native => Self::Native,
        }
    }
}

impl From<LinkType> for kreuzcrawl::LinkType {
    fn from(val: LinkType) -> Self {
        match val {
            LinkType::Internal => Self::Internal,
            LinkType::External => Self::External,
            LinkType::Anchor => Self::Anchor,
            LinkType::Document => Self::Document,
        }
    }
}

impl From<kreuzcrawl::LinkType> for LinkType {
    fn from(val: kreuzcrawl::LinkType) -> Self {
        match val {
            kreuzcrawl::LinkType::Internal => Self::Internal,
            kreuzcrawl::LinkType::External => Self::External,
            kreuzcrawl::LinkType::Anchor => Self::Anchor,
            kreuzcrawl::LinkType::Document => Self::Document,
        }
    }
}

impl From<ImageSource> for kreuzcrawl::ImageSource {
    fn from(val: ImageSource) -> Self {
        match val {
            ImageSource::Img => Self::Img,
            ImageSource::PictureSource => Self::PictureSource,
            ImageSource::OgImage => Self::OgImage,
            ImageSource::TwitterImage => Self::TwitterImage,
        }
    }
}

impl From<kreuzcrawl::ImageSource> for ImageSource {
    fn from(val: kreuzcrawl::ImageSource) -> Self {
        match val {
            kreuzcrawl::ImageSource::Img => Self::Img,
            kreuzcrawl::ImageSource::PictureSource => Self::PictureSource,
            kreuzcrawl::ImageSource::OgImage => Self::OgImage,
            kreuzcrawl::ImageSource::TwitterImage => Self::TwitterImage,
        }
    }
}

impl From<FeedType> for kreuzcrawl::FeedType {
    fn from(val: FeedType) -> Self {
        match val {
            FeedType::Rss => Self::Rss,
            FeedType::Atom => Self::Atom,
            FeedType::JsonFeed => Self::JsonFeed,
        }
    }
}

impl From<kreuzcrawl::FeedType> for FeedType {
    fn from(val: kreuzcrawl::FeedType) -> Self {
        match val {
            kreuzcrawl::FeedType::Rss => Self::Rss,
            kreuzcrawl::FeedType::Atom => Self::Atom,
            kreuzcrawl::FeedType::JsonFeed => Self::JsonFeed,
        }
    }
}

impl From<AssetCategory> for kreuzcrawl::AssetCategory {
    fn from(val: AssetCategory) -> Self {
        match val {
            AssetCategory::Document => Self::Document,
            AssetCategory::Image => Self::Image,
            AssetCategory::Audio => Self::Audio,
            AssetCategory::Video => Self::Video,
            AssetCategory::Font => Self::Font,
            AssetCategory::Stylesheet => Self::Stylesheet,
            AssetCategory::Script => Self::Script,
            AssetCategory::Archive => Self::Archive,
            AssetCategory::Data => Self::Data,
            AssetCategory::Other => Self::Other,
        }
    }
}

impl From<kreuzcrawl::AssetCategory> for AssetCategory {
    fn from(val: kreuzcrawl::AssetCategory) -> Self {
        match val {
            kreuzcrawl::AssetCategory::Document => Self::Document,
            kreuzcrawl::AssetCategory::Image => Self::Image,
            kreuzcrawl::AssetCategory::Audio => Self::Audio,
            kreuzcrawl::AssetCategory::Video => Self::Video,
            kreuzcrawl::AssetCategory::Font => Self::Font,
            kreuzcrawl::AssetCategory::Stylesheet => Self::Stylesheet,
            kreuzcrawl::AssetCategory::Script => Self::Script,
            kreuzcrawl::AssetCategory::Archive => Self::Archive,
            kreuzcrawl::AssetCategory::Data => Self::Data,
            kreuzcrawl::AssetCategory::Other => Self::Other,
        }
    }
}

impl From<ScrollDirection> for kreuzcrawl::ScrollDirection {
    fn from(val: ScrollDirection) -> Self {
        match val {
            ScrollDirection::Up => Self::Up,
            ScrollDirection::Down => Self::Down,
        }
    }
}

impl From<kreuzcrawl::ScrollDirection> for ScrollDirection {
    fn from(val: kreuzcrawl::ScrollDirection) -> Self {
        match val {
            kreuzcrawl::ScrollDirection::Up => Self::Up,
            kreuzcrawl::ScrollDirection::Down => Self::Down,
        }
    }
}

#[pyfunction]
pub fn init_async_runtime() -> PyResult<()> {
    // Tokio runtime auto-initializes on first future_into_py call
    Ok(())
}

#[pymodule]
pub fn _kreuzcrawl(m: &Bound<'_, PyModule>) -> PyResult<()> {
    m.add_function(wrap_pyfunction!(init_async_runtime, m)?)?;
    m.add_class::<ExtractionMeta>()?;
    m.add_class::<ProxyConfig>()?;
    m.add_class::<ContentConfig>()?;
    m.add_class::<BrowserConfig>()?;
    m.add_class::<CrawlConfig>()?;
    m.add_class::<BrowserExtras>()?;
    m.add_class::<DownloadedDocument>()?;
    m.add_class::<InteractionResult>()?;
    m.add_class::<ActionResult>()?;
    m.add_class::<ScrapeResult>()?;
    m.add_class::<CrawlPageResult>()?;
    m.add_class::<CrawlResult>()?;
    m.add_class::<SitemapUrl>()?;
    m.add_class::<MapResult>()?;
    m.add_class::<MarkdownResult>()?;
    m.add_class::<LinkInfo>()?;
    m.add_class::<ImageInfo>()?;
    m.add_class::<FeedInfo>()?;
    m.add_class::<JsonLdEntry>()?;
    m.add_class::<CookieInfo>()?;
    m.add_class::<DownloadedAsset>()?;
    m.add_class::<ArticleMetadata>()?;
    m.add_class::<HreflangEntry>()?;
    m.add_class::<FaviconInfo>()?;
    m.add_class::<HeadingInfo>()?;
    m.add_class::<ResponseMeta>()?;
    m.add_class::<PageMetadata>()?;
    m.add_class::<CrawlStreamRequest>()?;
    m.add_class::<BatchCrawlStreamRequest>()?;
    m.add_class::<CitationResult>()?;
    m.add_class::<CitationReference>()?;
    m.add_class::<CrawlEngineHandle>()?;
    m.add_class::<BatchScrapeResult>()?;
    m.add_class::<BatchCrawlResult>()?;
    m.add_class::<BatchScrapeResults>()?;
    m.add_class::<BatchCrawlResults>()?;
    m.add_class::<BrowserMode>()?;
    m.add_class::<BrowserWait>()?;
    m.add_class::<BrowserBackend>()?;
    m.add_class::<AuthConfig>()?;
    m.add_class::<LinkType>()?;
    m.add_class::<ImageSource>()?;
    m.add_class::<FeedType>()?;
    m.add_class::<AssetCategory>()?;
    m.add_class::<CrawlEvent>()?;
    m.add_class::<PageAction>()?;
    m.add_class::<ScrollDirection>()?;
    m.add_function(wrap_pyfunction!(generate_citations, m)?)?;
    m.add_function(wrap_pyfunction!(create_engine, m)?)?;
    m.add_function(wrap_pyfunction!(scrape, m)?)?;
    m.add_function(wrap_pyfunction!(crawl, m)?)?;
    m.add_function(wrap_pyfunction!(map_urls, m)?)?;
    m.add_function(wrap_pyfunction!(interact, m)?)?;
    m.add_function(wrap_pyfunction!(batch_scrape, m)?)?;
    m.add_function(wrap_pyfunction!(batch_crawl, m)?)?;
    m.add("NotFoundError", m.py().get_type::<NotFoundError>())?;
    m.add("UnauthorizedError", m.py().get_type::<UnauthorizedError>())?;
    m.add("ForbiddenError", m.py().get_type::<ForbiddenError>())?;
    m.add("WafBlockedError", m.py().get_type::<WafBlockedError>())?;
    m.add("CrawlTimeoutError", m.py().get_type::<CrawlTimeoutError>())?;
    m.add("RateLimitedError", m.py().get_type::<RateLimitedError>())?;
    m.add("ServerError", m.py().get_type::<ServerError>())?;
    m.add("BadGatewayError", m.py().get_type::<BadGatewayError>())?;
    m.add("GoneError", m.py().get_type::<GoneError>())?;
    m.add("CrawlConnectionError", m.py().get_type::<CrawlConnectionError>())?;
    m.add("DnsError", m.py().get_type::<DnsError>())?;
    m.add("SslError", m.py().get_type::<SslError>())?;
    m.add("DataLossError", m.py().get_type::<DataLossError>())?;
    m.add("BrowserError", m.py().get_type::<BrowserError>())?;
    m.add("BrowserTimeoutError", m.py().get_type::<BrowserTimeoutError>())?;
    m.add("InvalidConfigError", m.py().get_type::<InvalidConfigError>())?;
    m.add("UnsupportedError", m.py().get_type::<UnsupportedError>())?;
    m.add(
        "SsrfPolicyViolationError",
        m.py().get_type::<SsrfPolicyViolationError>(),
    )?;
    m.add("OtherError", m.py().get_type::<OtherError>())?;
    m.add("CrawlError", m.py().get_type::<CrawlError>())?;
    Ok(())
}
