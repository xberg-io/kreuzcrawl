#ifndef KCRAWL_H
#define KCRAWL_H

#pragma once

/* This file is auto-generated by alef. DO NOT EDIT. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>
/* Opaque type forward declarations */
typedef struct KCRAWLActionResult KCRAWLActionResult;
typedef struct KCRAWLArticleMetadata KCRAWLArticleMetadata;
typedef struct KCRAWLAssetCategory KCRAWLAssetCategory;
typedef struct KCRAWLAuthConfig KCRAWLAuthConfig;
typedef struct KCRAWLBatchCrawlResult KCRAWLBatchCrawlResult;
typedef struct KCRAWLBatchScrapeResult KCRAWLBatchScrapeResult;
typedef struct KCRAWLBrowserConfig KCRAWLBrowserConfig;
typedef struct KCRAWLBrowserMode KCRAWLBrowserMode;
typedef struct KCRAWLBrowserWait KCRAWLBrowserWait;
typedef struct KCRAWLCachedPage KCRAWLCachedPage;
typedef struct KCRAWLCitationReference KCRAWLCitationReference;
typedef struct KCRAWLCitationResult KCRAWLCitationResult;
typedef struct KCRAWLCookieInfo KCRAWLCookieInfo;
typedef struct KCRAWLCrawlConfig KCRAWLCrawlConfig;
typedef struct KCRAWLCrawlEngineHandle KCRAWLCrawlEngineHandle;
typedef struct KCRAWLCrawlEvent KCRAWLCrawlEvent;
typedef struct KCRAWLCrawlPageResult KCRAWLCrawlPageResult;
typedef struct KCRAWLCrawlResult KCRAWLCrawlResult;
typedef struct KCRAWLDownloadedAsset KCRAWLDownloadedAsset;
typedef struct KCRAWLDownloadedDocument KCRAWLDownloadedDocument;
typedef struct KCRAWLExtractionMeta KCRAWLExtractionMeta;
typedef struct KCRAWLFaviconInfo KCRAWLFaviconInfo;
typedef struct KCRAWLFeedInfo KCRAWLFeedInfo;
typedef struct KCRAWLFeedType KCRAWLFeedType;
typedef struct KCRAWLHeadingInfo KCRAWLHeadingInfo;
typedef struct KCRAWLHreflangEntry KCRAWLHreflangEntry;
typedef struct KCRAWLImageInfo KCRAWLImageInfo;
typedef struct KCRAWLImageSource KCRAWLImageSource;
typedef struct KCRAWLInteractionResult KCRAWLInteractionResult;
typedef struct KCRAWLJsonLdEntry KCRAWLJsonLdEntry;
typedef struct KCRAWLLinkInfo KCRAWLLinkInfo;
typedef struct KCRAWLLinkType KCRAWLLinkType;
typedef struct KCRAWLMapResult KCRAWLMapResult;
typedef struct KCRAWLMarkdownResult KCRAWLMarkdownResult;
typedef struct KCRAWLPageMetadata KCRAWLPageMetadata;
typedef struct KCRAWLProxyConfig KCRAWLProxyConfig;
typedef struct KCRAWLResponseMeta KCRAWLResponseMeta;
typedef struct KCRAWLScrapeResult KCRAWLScrapeResult;
typedef struct KCRAWLSitemapUrl KCRAWLSitemapUrl;


/**
 * Return the last error code (0 means no error).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t kcrawl_last_error_code(void);

/**
 * Return the last error message. The pointer is valid until the next FFI call on this thread.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *kcrawl_last_error_context(void);

/**
 * Free a string previously returned by this library.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_free_string(char *ptr);

/**
 * Return the library version string. The pointer is static and must NOT be freed.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *kcrawl_version(void);

/**
 * Create a `ExtractionMeta` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_extraction_meta_free`.
 */
KCRAWLExtractionMeta *kcrawl_extraction_meta_from_json(const char *json);

/**
 * Free a `ExtractionMeta` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_extraction_meta_free(KCRAWLExtractionMeta *ptr);

/**
 * Get the `cost` field from a `ExtractionMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double kcrawl_extraction_meta_cost(const KCRAWLExtractionMeta *ptr);

/**
 * Get the `prompt_tokens` field from a `ExtractionMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t kcrawl_extraction_meta_prompt_tokens(const KCRAWLExtractionMeta *ptr);

/**
 * Get the `completion_tokens` field from a `ExtractionMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t kcrawl_extraction_meta_completion_tokens(const KCRAWLExtractionMeta *ptr);

/**
 * Get the `model` field from a `ExtractionMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_extraction_meta_model(const KCRAWLExtractionMeta *ptr);

/**
 * Get the `chunks_processed` field from a `ExtractionMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_extraction_meta_chunks_processed(const KCRAWLExtractionMeta *ptr);

/**
 * Create a `ProxyConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_proxy_config_free`.
 */
KCRAWLProxyConfig *kcrawl_proxy_config_from_json(const char *json);

/**
 * Free a `ProxyConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_proxy_config_free(KCRAWLProxyConfig *ptr);

/**
 * Get the `url` field from a `ProxyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_proxy_config_url(const KCRAWLProxyConfig *ptr);

/**
 * Get the `username` field from a `ProxyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_proxy_config_username(const KCRAWLProxyConfig *ptr);

/**
 * Get the `password` field from a `ProxyConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_proxy_config_password(const KCRAWLProxyConfig *ptr);

/**
 * Create a `BrowserConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_browser_config_free`.
 */
KCRAWLBrowserConfig *kcrawl_browser_config_from_json(const char *json);

/**
 * Free a `BrowserConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_browser_config_free(KCRAWLBrowserConfig *ptr);

/**
 * Get the `mode` field from a `BrowserConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLBrowserMode *kcrawl_browser_config_mode(const KCRAWLBrowserConfig *ptr);

/**
 * Get the `endpoint` field from a `BrowserConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_browser_config_endpoint(const KCRAWLBrowserConfig *ptr);

/**
 * Get the `timeout` field from a `BrowserConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t kcrawl_browser_config_timeout(const KCRAWLBrowserConfig *ptr);

/**
 * Get the `wait` field from a `BrowserConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLBrowserWait *kcrawl_browser_config_wait(const KCRAWLBrowserConfig *ptr);

/**
 * Get the `wait_selector` field from a `BrowserConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_browser_config_wait_selector(const KCRAWLBrowserConfig *ptr);

/**
 * Get the `extra_wait` field from a `BrowserConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t kcrawl_browser_config_extra_wait(const KCRAWLBrowserConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
KCRAWLBrowserConfig *kcrawl_browser_config_default(void);

/**
 * Create a `CrawlConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_crawl_config_free`.
 */
KCRAWLCrawlConfig *kcrawl_crawl_config_from_json(const char *json);

/**
 * Free a `CrawlConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_crawl_config_free(KCRAWLCrawlConfig *ptr);

/**
 * Get the `max_depth` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_config_max_depth(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `max_pages` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_config_max_pages(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `max_concurrent` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_config_max_concurrent(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `respect_robots_txt` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_config_respect_robots_txt(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `user_agent` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_user_agent(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `stay_on_domain` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_config_stay_on_domain(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `allow_subdomains` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_config_allow_subdomains(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `include_paths` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_include_paths(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `exclude_paths` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_exclude_paths(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `custom_headers` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_custom_headers(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `request_timeout` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t kcrawl_crawl_config_request_timeout(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `max_redirects` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_config_max_redirects(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `retry_count` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_config_retry_count(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `retry_codes` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_retry_codes(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `cookies_enabled` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_config_cookies_enabled(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `auth` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLAuthConfig *kcrawl_crawl_config_auth(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `max_body_size` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_config_max_body_size(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `main_content_only` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_config_main_content_only(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `remove_tags` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_remove_tags(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `map_limit` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_config_map_limit(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `map_search` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_map_search(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `download_assets` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_config_download_assets(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `asset_types` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_asset_types(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `max_asset_size` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_config_max_asset_size(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `browser` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLBrowserConfig *kcrawl_crawl_config_browser(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `proxy` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLProxyConfig *kcrawl_crawl_config_proxy(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `user_agents` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_user_agents(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `capture_screenshot` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_config_capture_screenshot(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `download_documents` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_config_download_documents(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `document_max_size` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_config_document_max_size(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `document_mime_types` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_document_mime_types(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `warc_output` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_warc_output(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `browser_profile` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_config_browser_profile(const KCRAWLCrawlConfig *ptr);

/**
 * Get the `save_browser_profile` field from a `CrawlConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_config_save_browser_profile(const KCRAWLCrawlConfig *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
KCRAWLCrawlConfig *kcrawl_crawl_config_default(void);

/**
 * Free a `DownloadedDocument` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_downloaded_document_free(KCRAWLDownloadedDocument *ptr);

/**
 * Get the `url` field from a `DownloadedDocument`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_downloaded_document_url(const KCRAWLDownloadedDocument *ptr);

/**
 * Get the `content` field from a `DownloadedDocument`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint8_t *kcrawl_downloaded_document_content(const KCRAWLDownloadedDocument *ptr,
                                            uintptr_t *out_len);

/**
 * Get the `size` field from a `DownloadedDocument`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_downloaded_document_size(const KCRAWLDownloadedDocument *ptr);

/**
 * Create a `InteractionResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_interaction_result_free`.
 */
KCRAWLInteractionResult *kcrawl_interaction_result_from_json(const char *json);

/**
 * Free a `InteractionResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_interaction_result_free(KCRAWLInteractionResult *ptr);

/**
 * Get the `action_results` field from a `InteractionResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_interaction_result_action_results(const KCRAWLInteractionResult *ptr);

/**
 * Get the `final_html` field from a `InteractionResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_interaction_result_final_html(const KCRAWLInteractionResult *ptr);

/**
 * Get the `final_url` field from a `InteractionResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_interaction_result_final_url(const KCRAWLInteractionResult *ptr);

/**
 * Get the `screenshot` field from a `InteractionResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint8_t *kcrawl_interaction_result_screenshot(const KCRAWLInteractionResult *ptr);

/**
 * Free a `ActionResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_action_result_free(KCRAWLActionResult *ptr);

/**
 * Get the `action_index` field from a `ActionResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_action_result_action_index(const KCRAWLActionResult *ptr);

/**
 * Get the `success` field from a `ActionResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_action_result_success(const KCRAWLActionResult *ptr);

/**
 * Get the `data` field from a `ActionResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_action_result_data(const KCRAWLActionResult *ptr);

/**
 * Get the `error` field from a `ActionResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_action_result_error(const KCRAWLActionResult *ptr);

/**
 * Create a `ScrapeResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_scrape_result_free`.
 */
KCRAWLScrapeResult *kcrawl_scrape_result_from_json(const char *json);

/**
 * Free a `ScrapeResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_scrape_result_free(KCRAWLScrapeResult *ptr);

/**
 * Get the `status_code` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint16_t kcrawl_scrape_result_status_code(const KCRAWLScrapeResult *ptr);

/**
 * Get the `content_type` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_content_type(const KCRAWLScrapeResult *ptr);

/**
 * Get the `html` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_html(const KCRAWLScrapeResult *ptr);

/**
 * Get the `body_size` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_scrape_result_body_size(const KCRAWLScrapeResult *ptr);

/**
 * Get the `metadata` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLPageMetadata *kcrawl_scrape_result_metadata(const KCRAWLScrapeResult *ptr);

/**
 * Get the `links` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_links(const KCRAWLScrapeResult *ptr);

/**
 * Get the `images` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_images(const KCRAWLScrapeResult *ptr);

/**
 * Get the `feeds` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_feeds(const KCRAWLScrapeResult *ptr);

/**
 * Get the `json_ld` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_json_ld(const KCRAWLScrapeResult *ptr);

/**
 * Get the `is_allowed` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_scrape_result_is_allowed(const KCRAWLScrapeResult *ptr);

/**
 * Get the `crawl_delay` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t kcrawl_scrape_result_crawl_delay(const KCRAWLScrapeResult *ptr);

/**
 * Get the `noindex_detected` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_scrape_result_noindex_detected(const KCRAWLScrapeResult *ptr);

/**
 * Get the `nofollow_detected` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_scrape_result_nofollow_detected(const KCRAWLScrapeResult *ptr);

/**
 * Get the `x_robots_tag` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_x_robots_tag(const KCRAWLScrapeResult *ptr);

/**
 * Get the `is_pdf` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_scrape_result_is_pdf(const KCRAWLScrapeResult *ptr);

/**
 * Get the `was_skipped` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_scrape_result_was_skipped(const KCRAWLScrapeResult *ptr);

/**
 * Get the `detected_charset` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_detected_charset(const KCRAWLScrapeResult *ptr);

/**
 * Get the `main_content_only` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_scrape_result_main_content_only(const KCRAWLScrapeResult *ptr);

/**
 * Get the `auth_header_sent` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_scrape_result_auth_header_sent(const KCRAWLScrapeResult *ptr);

/**
 * Get the `response_meta` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLResponseMeta *kcrawl_scrape_result_response_meta(const KCRAWLScrapeResult *ptr);

/**
 * Get the `assets` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_assets(const KCRAWLScrapeResult *ptr);

/**
 * Get the `js_render_hint` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_scrape_result_js_render_hint(const KCRAWLScrapeResult *ptr);

/**
 * Get the `browser_used` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_scrape_result_browser_used(const KCRAWLScrapeResult *ptr);

/**
 * Get the `markdown` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLMarkdownResult *kcrawl_scrape_result_markdown(const KCRAWLScrapeResult *ptr);

/**
 * Get the `extracted_data` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_scrape_result_extracted_data(const KCRAWLScrapeResult *ptr);

/**
 * Get the `extraction_meta` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLExtractionMeta *kcrawl_scrape_result_extraction_meta(const KCRAWLScrapeResult *ptr);

/**
 * Get the `screenshot` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint8_t *kcrawl_scrape_result_screenshot(const KCRAWLScrapeResult *ptr);

/**
 * Get the `downloaded_document` field from a `ScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLDownloadedDocument *kcrawl_scrape_result_downloaded_document(const KCRAWLScrapeResult *ptr);

/**
 * Create a `CrawlPageResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_crawl_page_result_free`.
 */
KCRAWLCrawlPageResult *kcrawl_crawl_page_result_from_json(const char *json);

/**
 * Free a `CrawlPageResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_crawl_page_result_free(KCRAWLCrawlPageResult *ptr);

/**
 * Get the `url` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_url(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `normalized_url` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_normalized_url(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `status_code` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint16_t kcrawl_crawl_page_result_status_code(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `content_type` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_content_type(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `html` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_html(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `body_size` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_page_result_body_size(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `metadata` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLPageMetadata *kcrawl_crawl_page_result_metadata(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `links` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_links(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `images` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_images(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `feeds` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_feeds(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `json_ld` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_json_ld(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `depth` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_page_result_depth(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `stayed_on_domain` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_page_result_stayed_on_domain(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `was_skipped` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_page_result_was_skipped(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `is_pdf` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_page_result_is_pdf(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `detected_charset` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_detected_charset(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `markdown` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLMarkdownResult *kcrawl_crawl_page_result_markdown(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `extracted_data` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_page_result_extracted_data(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `extraction_meta` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLExtractionMeta *kcrawl_crawl_page_result_extraction_meta(const KCRAWLCrawlPageResult *ptr);

/**
 * Get the `downloaded_document` field from a `CrawlPageResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLDownloadedDocument *kcrawl_crawl_page_result_downloaded_document(const KCRAWLCrawlPageResult *ptr);

/**
 * Create a `CrawlResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_crawl_result_free`.
 */
KCRAWLCrawlResult *kcrawl_crawl_result_from_json(const char *json);

/**
 * Free a `CrawlResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_crawl_result_free(KCRAWLCrawlResult *ptr);

/**
 * Get the `pages` field from a `CrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_result_pages(const KCRAWLCrawlResult *ptr);

/**
 * Get the `final_url` field from a `CrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_result_final_url(const KCRAWLCrawlResult *ptr);

/**
 * Get the `redirect_count` field from a `CrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_crawl_result_redirect_count(const KCRAWLCrawlResult *ptr);

/**
 * Get the `was_skipped` field from a `CrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_crawl_result_was_skipped(const KCRAWLCrawlResult *ptr);

/**
 * Get the `error` field from a `CrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_result_error(const KCRAWLCrawlResult *ptr);

/**
 * Get the `cookies` field from a `CrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_result_cookies(const KCRAWLCrawlResult *ptr);

/**
 * Get the `normalized_urls` field from a `CrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_crawl_result_normalized_urls(const KCRAWLCrawlResult *ptr);

/**
 * Create a `SitemapUrl` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_sitemap_url_free`.
 */
KCRAWLSitemapUrl *kcrawl_sitemap_url_from_json(const char *json);

/**
 * Free a `SitemapUrl` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_sitemap_url_free(KCRAWLSitemapUrl *ptr);

/**
 * Get the `url` field from a `SitemapUrl`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_sitemap_url_url(const KCRAWLSitemapUrl *ptr);

/**
 * Get the `lastmod` field from a `SitemapUrl`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_sitemap_url_lastmod(const KCRAWLSitemapUrl *ptr);

/**
 * Get the `changefreq` field from a `SitemapUrl`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_sitemap_url_changefreq(const KCRAWLSitemapUrl *ptr);

/**
 * Get the `priority` field from a `SitemapUrl`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_sitemap_url_priority(const KCRAWLSitemapUrl *ptr);

/**
 * Create a `MapResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_map_result_free`.
 */
KCRAWLMapResult *kcrawl_map_result_from_json(const char *json);

/**
 * Free a `MapResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_map_result_free(KCRAWLMapResult *ptr);

/**
 * Get the `urls` field from a `MapResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_map_result_urls(const KCRAWLMapResult *ptr);

/**
 * Create a `MarkdownResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_markdown_result_free`.
 */
KCRAWLMarkdownResult *kcrawl_markdown_result_from_json(const char *json);

/**
 * Free a `MarkdownResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_markdown_result_free(KCRAWLMarkdownResult *ptr);

/**
 * Get the `content` field from a `MarkdownResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_markdown_result_content(const KCRAWLMarkdownResult *ptr);

/**
 * Get the `document_structure` field from a `MarkdownResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_markdown_result_document_structure(const KCRAWLMarkdownResult *ptr);

/**
 * Get the `tables` field from a `MarkdownResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_markdown_result_tables(const KCRAWLMarkdownResult *ptr);

/**
 * Get the `warnings` field from a `MarkdownResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_markdown_result_warnings(const KCRAWLMarkdownResult *ptr);

/**
 * Get the `citations` field from a `MarkdownResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLCitationResult *kcrawl_markdown_result_citations(const KCRAWLMarkdownResult *ptr);

/**
 * Get the `fit_content` field from a `MarkdownResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_markdown_result_fit_content(const KCRAWLMarkdownResult *ptr);

/**
 * Create a `CachedPage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_cached_page_free`.
 */
KCRAWLCachedPage *kcrawl_cached_page_from_json(const char *json);

/**
 * Free a `CachedPage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_cached_page_free(KCRAWLCachedPage *ptr);

/**
 * Get the `url` field from a `CachedPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_cached_page_url(const KCRAWLCachedPage *ptr);

/**
 * Get the `status_code` field from a `CachedPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint16_t kcrawl_cached_page_status_code(const KCRAWLCachedPage *ptr);

/**
 * Get the `content_type` field from a `CachedPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_cached_page_content_type(const KCRAWLCachedPage *ptr);

/**
 * Get the `body` field from a `CachedPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_cached_page_body(const KCRAWLCachedPage *ptr);

/**
 * Get the `etag` field from a `CachedPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_cached_page_etag(const KCRAWLCachedPage *ptr);

/**
 * Get the `last_modified` field from a `CachedPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_cached_page_last_modified(const KCRAWLCachedPage *ptr);

/**
 * Get the `cached_at` field from a `CachedPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t kcrawl_cached_page_cached_at(const KCRAWLCachedPage *ptr);

/**
 * Create a `LinkInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_link_info_free`.
 */
KCRAWLLinkInfo *kcrawl_link_info_from_json(const char *json);

/**
 * Free a `LinkInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_link_info_free(KCRAWLLinkInfo *ptr);

/**
 * Get the `url` field from a `LinkInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_link_info_url(const KCRAWLLinkInfo *ptr);

/**
 * Get the `text` field from a `LinkInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_link_info_text(const KCRAWLLinkInfo *ptr);

/**
 * Get the `link_type` field from a `LinkInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLLinkType *kcrawl_link_info_link_type(const KCRAWLLinkInfo *ptr);

/**
 * Get the `rel` field from a `LinkInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_link_info_rel(const KCRAWLLinkInfo *ptr);

/**
 * Get the `nofollow` field from a `LinkInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t kcrawl_link_info_nofollow(const KCRAWLLinkInfo *ptr);

/**
 * Create a `ImageInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_image_info_free`.
 */
KCRAWLImageInfo *kcrawl_image_info_from_json(const char *json);

/**
 * Free a `ImageInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_image_info_free(KCRAWLImageInfo *ptr);

/**
 * Get the `url` field from a `ImageInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_image_info_url(const KCRAWLImageInfo *ptr);

/**
 * Get the `alt` field from a `ImageInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_image_info_alt(const KCRAWLImageInfo *ptr);

/**
 * Get the `width` field from a `ImageInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t kcrawl_image_info_width(const KCRAWLImageInfo *ptr);

/**
 * Get the `height` field from a `ImageInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t kcrawl_image_info_height(const KCRAWLImageInfo *ptr);

/**
 * Get the `source` field from a `ImageInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLImageSource *kcrawl_image_info_source(const KCRAWLImageInfo *ptr);

/**
 * Create a `FeedInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_feed_info_free`.
 */
KCRAWLFeedInfo *kcrawl_feed_info_from_json(const char *json);

/**
 * Free a `FeedInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_feed_info_free(KCRAWLFeedInfo *ptr);

/**
 * Get the `url` field from a `FeedInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_feed_info_url(const KCRAWLFeedInfo *ptr);

/**
 * Get the `title` field from a `FeedInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_feed_info_title(const KCRAWLFeedInfo *ptr);

/**
 * Get the `feed_type` field from a `FeedInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLFeedType *kcrawl_feed_info_feed_type(const KCRAWLFeedInfo *ptr);

/**
 * Create a `JsonLdEntry` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_json_ld_entry_free`.
 */
KCRAWLJsonLdEntry *kcrawl_json_ld_entry_from_json(const char *json);

/**
 * Free a `JsonLdEntry` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_json_ld_entry_free(KCRAWLJsonLdEntry *ptr);

/**
 * Get the `schema_type` field from a `JsonLdEntry`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_json_ld_entry_schema_type(const KCRAWLJsonLdEntry *ptr);

/**
 * Get the `name` field from a `JsonLdEntry`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_json_ld_entry_name(const KCRAWLJsonLdEntry *ptr);

/**
 * Get the `raw` field from a `JsonLdEntry`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_json_ld_entry_raw(const KCRAWLJsonLdEntry *ptr);

/**
 * Create a `CookieInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_cookie_info_free`.
 */
KCRAWLCookieInfo *kcrawl_cookie_info_from_json(const char *json);

/**
 * Free a `CookieInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_cookie_info_free(KCRAWLCookieInfo *ptr);

/**
 * Get the `name` field from a `CookieInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_cookie_info_name(const KCRAWLCookieInfo *ptr);

/**
 * Get the `value` field from a `CookieInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_cookie_info_value(const KCRAWLCookieInfo *ptr);

/**
 * Get the `domain` field from a `CookieInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_cookie_info_domain(const KCRAWLCookieInfo *ptr);

/**
 * Get the `path` field from a `CookieInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_cookie_info_path(const KCRAWLCookieInfo *ptr);

/**
 * Create a `DownloadedAsset` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_downloaded_asset_free`.
 */
KCRAWLDownloadedAsset *kcrawl_downloaded_asset_from_json(const char *json);

/**
 * Free a `DownloadedAsset` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_downloaded_asset_free(KCRAWLDownloadedAsset *ptr);

/**
 * Get the `url` field from a `DownloadedAsset`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_downloaded_asset_url(const KCRAWLDownloadedAsset *ptr);

/**
 * Get the `content_hash` field from a `DownloadedAsset`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_downloaded_asset_content_hash(const KCRAWLDownloadedAsset *ptr);

/**
 * Get the `mime_type` field from a `DownloadedAsset`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_downloaded_asset_mime_type(const KCRAWLDownloadedAsset *ptr);

/**
 * Get the `size` field from a `DownloadedAsset`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_downloaded_asset_size(const KCRAWLDownloadedAsset *ptr);

/**
 * Get the `asset_category` field from a `DownloadedAsset`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLAssetCategory *kcrawl_downloaded_asset_asset_category(const KCRAWLDownloadedAsset *ptr);

/**
 * Get the `html_tag` field from a `DownloadedAsset`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_downloaded_asset_html_tag(const KCRAWLDownloadedAsset *ptr);

/**
 * Create a `ArticleMetadata` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_article_metadata_free`.
 */
KCRAWLArticleMetadata *kcrawl_article_metadata_from_json(const char *json);

/**
 * Free a `ArticleMetadata` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_article_metadata_free(KCRAWLArticleMetadata *ptr);

/**
 * Get the `published_time` field from a `ArticleMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_article_metadata_published_time(const KCRAWLArticleMetadata *ptr);

/**
 * Get the `modified_time` field from a `ArticleMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_article_metadata_modified_time(const KCRAWLArticleMetadata *ptr);

/**
 * Get the `author` field from a `ArticleMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_article_metadata_author(const KCRAWLArticleMetadata *ptr);

/**
 * Get the `section` field from a `ArticleMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_article_metadata_section(const KCRAWLArticleMetadata *ptr);

/**
 * Get the `tags` field from a `ArticleMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_article_metadata_tags(const KCRAWLArticleMetadata *ptr);

/**
 * Create a `HreflangEntry` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_hreflang_entry_free`.
 */
KCRAWLHreflangEntry *kcrawl_hreflang_entry_from_json(const char *json);

/**
 * Free a `HreflangEntry` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_hreflang_entry_free(KCRAWLHreflangEntry *ptr);

/**
 * Get the `lang` field from a `HreflangEntry`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_hreflang_entry_lang(const KCRAWLHreflangEntry *ptr);

/**
 * Get the `url` field from a `HreflangEntry`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_hreflang_entry_url(const KCRAWLHreflangEntry *ptr);

/**
 * Create a `FaviconInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_favicon_info_free`.
 */
KCRAWLFaviconInfo *kcrawl_favicon_info_from_json(const char *json);

/**
 * Free a `FaviconInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_favicon_info_free(KCRAWLFaviconInfo *ptr);

/**
 * Get the `url` field from a `FaviconInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_favicon_info_url(const KCRAWLFaviconInfo *ptr);

/**
 * Get the `rel` field from a `FaviconInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_favicon_info_rel(const KCRAWLFaviconInfo *ptr);

/**
 * Get the `sizes` field from a `FaviconInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_favicon_info_sizes(const KCRAWLFaviconInfo *ptr);

/**
 * Get the `mime_type` field from a `FaviconInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_favicon_info_mime_type(const KCRAWLFaviconInfo *ptr);

/**
 * Create a `HeadingInfo` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_heading_info_free`.
 */
KCRAWLHeadingInfo *kcrawl_heading_info_from_json(const char *json);

/**
 * Free a `HeadingInfo` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_heading_info_free(KCRAWLHeadingInfo *ptr);

/**
 * Get the `level` field from a `HeadingInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint8_t kcrawl_heading_info_level(const KCRAWLHeadingInfo *ptr);

/**
 * Get the `text` field from a `HeadingInfo`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_heading_info_text(const KCRAWLHeadingInfo *ptr);

/**
 * Create a `ResponseMeta` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_response_meta_free`.
 */
KCRAWLResponseMeta *kcrawl_response_meta_from_json(const char *json);

/**
 * Free a `ResponseMeta` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_response_meta_free(KCRAWLResponseMeta *ptr);

/**
 * Get the `etag` field from a `ResponseMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_response_meta_etag(const KCRAWLResponseMeta *ptr);

/**
 * Get the `last_modified` field from a `ResponseMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_response_meta_last_modified(const KCRAWLResponseMeta *ptr);

/**
 * Get the `cache_control` field from a `ResponseMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_response_meta_cache_control(const KCRAWLResponseMeta *ptr);

/**
 * Get the `server` field from a `ResponseMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_response_meta_server(const KCRAWLResponseMeta *ptr);

/**
 * Get the `x_powered_by` field from a `ResponseMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_response_meta_x_powered_by(const KCRAWLResponseMeta *ptr);

/**
 * Get the `content_language` field from a `ResponseMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_response_meta_content_language(const KCRAWLResponseMeta *ptr);

/**
 * Get the `content_encoding` field from a `ResponseMeta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_response_meta_content_encoding(const KCRAWLResponseMeta *ptr);

/**
 * Create a `PageMetadata` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_page_metadata_free`.
 */
KCRAWLPageMetadata *kcrawl_page_metadata_from_json(const char *json);

/**
 * Free a `PageMetadata` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_page_metadata_free(KCRAWLPageMetadata *ptr);

/**
 * Get the `title` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_title(const KCRAWLPageMetadata *ptr);

/**
 * Get the `description` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_description(const KCRAWLPageMetadata *ptr);

/**
 * Get the `canonical_url` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_canonical_url(const KCRAWLPageMetadata *ptr);

/**
 * Get the `keywords` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_keywords(const KCRAWLPageMetadata *ptr);

/**
 * Get the `author` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_author(const KCRAWLPageMetadata *ptr);

/**
 * Get the `viewport` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_viewport(const KCRAWLPageMetadata *ptr);

/**
 * Get the `theme_color` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_theme_color(const KCRAWLPageMetadata *ptr);

/**
 * Get the `generator` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_generator(const KCRAWLPageMetadata *ptr);

/**
 * Get the `robots` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_robots(const KCRAWLPageMetadata *ptr);

/**
 * Get the `html_lang` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_html_lang(const KCRAWLPageMetadata *ptr);

/**
 * Get the `html_dir` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_html_dir(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_title` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_title(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_type` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_type(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_image` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_image(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_description` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_description(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_url` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_url(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_site_name` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_site_name(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_locale` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_locale(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_video` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_video(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_audio` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_audio(const KCRAWLPageMetadata *ptr);

/**
 * Get the `og_locale_alternates` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_og_locale_alternates(const KCRAWLPageMetadata *ptr);

/**
 * Get the `twitter_card` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_twitter_card(const KCRAWLPageMetadata *ptr);

/**
 * Get the `twitter_title` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_twitter_title(const KCRAWLPageMetadata *ptr);

/**
 * Get the `twitter_description` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_twitter_description(const KCRAWLPageMetadata *ptr);

/**
 * Get the `twitter_image` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_twitter_image(const KCRAWLPageMetadata *ptr);

/**
 * Get the `twitter_site` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_twitter_site(const KCRAWLPageMetadata *ptr);

/**
 * Get the `twitter_creator` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_twitter_creator(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_title` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_title(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_creator` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_creator(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_subject` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_subject(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_description` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_description(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_publisher` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_publisher(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_date` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_date(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_type` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_type(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_format` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_format(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_identifier` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_identifier(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_language` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_language(const KCRAWLPageMetadata *ptr);

/**
 * Get the `dc_rights` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_dc_rights(const KCRAWLPageMetadata *ptr);

/**
 * Get the `article` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLArticleMetadata *kcrawl_page_metadata_article(const KCRAWLPageMetadata *ptr);

/**
 * Get the `hreflangs` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_hreflangs(const KCRAWLPageMetadata *ptr);

/**
 * Get the `favicons` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_favicons(const KCRAWLPageMetadata *ptr);

/**
 * Get the `headings` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_page_metadata_headings(const KCRAWLPageMetadata *ptr);

/**
 * Get the `word_count` field from a `PageMetadata`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_page_metadata_word_count(const KCRAWLPageMetadata *ptr);

/**
 * Create a `CitationResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_citation_result_free`.
 */
KCRAWLCitationResult *kcrawl_citation_result_from_json(const char *json);

/**
 * Free a `CitationResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_citation_result_free(KCRAWLCitationResult *ptr);

/**
 * Get the `content` field from a `CitationResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_citation_result_content(const KCRAWLCitationResult *ptr);

/**
 * Get the `references` field from a `CitationResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_citation_result_references(const KCRAWLCitationResult *ptr);

/**
 * Create a `CitationReference` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_citation_reference_free`.
 */
KCRAWLCitationReference *kcrawl_citation_reference_from_json(const char *json);

/**
 * Free a `CitationReference` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_citation_reference_free(KCRAWLCitationReference *ptr);

/**
 * Get the `index` field from a `CitationReference`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uintptr_t kcrawl_citation_reference_index(const KCRAWLCitationReference *ptr);

/**
 * Get the `url` field from a `CitationReference`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_citation_reference_url(const KCRAWLCitationReference *ptr);

/**
 * Get the `text` field from a `CitationReference`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_citation_reference_text(const KCRAWLCitationReference *ptr);

/**
 * Free a `CrawlEngineHandle` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_crawl_engine_handle_free(KCRAWLCrawlEngineHandle *ptr);

/**
 * Create a `BatchScrapeResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_batch_scrape_result_free`.
 */
KCRAWLBatchScrapeResult *kcrawl_batch_scrape_result_from_json(const char *json);

/**
 * Free a `BatchScrapeResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_batch_scrape_result_free(KCRAWLBatchScrapeResult *ptr);

/**
 * Get the `url` field from a `BatchScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_batch_scrape_result_url(const KCRAWLBatchScrapeResult *ptr);

/**
 * Get the `result` field from a `BatchScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLScrapeResult *kcrawl_batch_scrape_result_result(const KCRAWLBatchScrapeResult *ptr);

/**
 * Get the `error` field from a `BatchScrapeResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_batch_scrape_result_error(const KCRAWLBatchScrapeResult *ptr);

/**
 * Create a `BatchCrawlResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `kcrawl_batch_crawl_result_free`.
 */
KCRAWLBatchCrawlResult *kcrawl_batch_crawl_result_from_json(const char *json);

/**
 * Free a `BatchCrawlResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void kcrawl_batch_crawl_result_free(KCRAWLBatchCrawlResult *ptr);

/**
 * Get the `url` field from a `BatchCrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_batch_crawl_result_url(const KCRAWLBatchCrawlResult *ptr);

/**
 * Get the `result` field from a `BatchCrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
KCRAWLCrawlResult *kcrawl_batch_crawl_result_result(const KCRAWLBatchCrawlResult *ptr);

/**
 * Get the `error` field from a `BatchCrawlResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *kcrawl_batch_crawl_result_error(const KCRAWLBatchCrawlResult *ptr);

/**
 * Convert an integer to a `BrowserMode` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t kcrawl_browser_mode_from_i32(int32_t value);

/**
 * Convert a `BrowserMode` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t kcrawl_browser_mode_from_str(const char *name);

/**
 * Convert an integer to a `BrowserWait` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t kcrawl_browser_wait_from_i32(int32_t value);

/**
 * Convert a `BrowserWait` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t kcrawl_browser_wait_from_str(const char *name);

/**
 * Convert an integer to a `AuthConfig` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t kcrawl_auth_config_from_i32(int32_t value);

/**
 * Convert a `AuthConfig` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t kcrawl_auth_config_from_str(const char *name);

/**
 * Convert an integer to a `LinkType` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t kcrawl_link_type_from_i32(int32_t value);

/**
 * Convert a `LinkType` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t kcrawl_link_type_from_str(const char *name);

/**
 * Convert an integer to a `ImageSource` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t kcrawl_image_source_from_i32(int32_t value);

/**
 * Convert a `ImageSource` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t kcrawl_image_source_from_str(const char *name);

/**
 * Convert an integer to a `FeedType` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t kcrawl_feed_type_from_i32(int32_t value);

/**
 * Convert a `FeedType` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t kcrawl_feed_type_from_str(const char *name);

/**
 * Convert an integer to a `AssetCategory` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t kcrawl_asset_category_from_i32(int32_t value);

/**
 * Convert a `AssetCategory` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t kcrawl_asset_category_from_str(const char *name);

/**
 * Create a new crawl engine with the given configuration.
 *
 * If `config` is `None`, uses [`CrawlConfig::default()`].
 * Returns an error if the configuration is invalid.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
KCRAWLCrawlEngineHandle *kcrawl_create_engine(const KCRAWLCrawlConfig *config);

/**
 * Scrape a single URL, returning extracted page data.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
KCRAWLScrapeResult *kcrawl_scrape(const KCRAWLCrawlEngineHandle *engine,
                                  const char *url);

/**
 * Crawl a website starting from `url`, following links up to the configured depth.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
KCRAWLCrawlResult *kcrawl_crawl(const KCRAWLCrawlEngineHandle *engine,
                                const char *url);

/**
 * Discover all pages on a website by following links and sitemaps.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
KCRAWLMapResult *kcrawl_map_urls(const KCRAWLCrawlEngineHandle *engine,
                                 const char *url);

/**
 * Scrape multiple URLs concurrently.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *kcrawl_batch_scrape(const KCRAWLCrawlEngineHandle *engine,
                          const char *urls);

/**
 * Crawl multiple seed URLs concurrently, each following links to configured depth.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *kcrawl_batch_crawl(const KCRAWLCrawlEngineHandle *engine,
                         const char *urls);

#endif  /* KCRAWL_H */
