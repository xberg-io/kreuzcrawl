// This file is auto-generated by alef. DO NOT EDIT.
// Re-generate with: alef generate
#![allow(dead_code, unused_imports, unused_variables, unused_mut)]
#![allow(
    clippy::too_many_arguments,
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::redundant_locals,
    dropping_references
)]

use std::cell::RefCell;
use std::ffi::{c_char, CStr, CString};

thread_local! {
    static LAST_ERROR_CODE: RefCell<i32> = const { RefCell::new(0) };
    static LAST_ERROR_CONTEXT: RefCell<Option<CString>> = const { RefCell::new(None) };
}

fn set_last_error(code: i32, message: &str) {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = code);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = CString::new(message).ok());
}

fn clear_last_error() {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = 0);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = None);
}

/// Return the last error code (0 means no error).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_last_error_code() -> i32 {
    LAST_ERROR_CODE.with_borrow(|c| *c)
}

/// Return the last error message. The pointer is valid until the next FFI call on this thread.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_last_error_context() -> *const c_char {
    LAST_ERROR_CONTEXT.with_borrow(|ctx| ctx.as_ref().map_or(std::ptr::null(), |c| c.as_ptr()))
}

/// Free a string previously returned by this library.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_free_string(ptr: *mut c_char) {
    if !ptr.is_null() {
        unsafe {
            drop(CString::from_raw(ptr));
        }
    }
}

/// Return the library version string. The pointer is static and must NOT be freed.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_version() -> *const c_char {
    static VERSION: &str = concat!(env!("CARGO_PKG_VERSION"), "\0");
    VERSION.as_ptr() as *const c_char
}

/// Create a `ExtractionMeta` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_extraction_meta_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_extraction_meta_from_json(json: *const c_char) -> *mut kreuzcrawl::ExtractionMeta {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::ExtractionMeta>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ExtractionMeta` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_extraction_meta_to_json(ptr: *const kreuzcrawl::ExtractionMeta) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ExtractionMeta` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_extraction_meta_free(ptr: *mut kreuzcrawl::ExtractionMeta) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `cost` field from a `ExtractionMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_extraction_meta_cost(ptr: *const kreuzcrawl::ExtractionMeta) -> f64 {
    if ptr.is_null() {
        return 0.0;
    }
    let obj = unsafe { &*ptr };
    match &obj.cost {
        Some(val) => *val,
        None => 0.0,
    }
}

/// Get the `prompt_tokens` field from a `ExtractionMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_extraction_meta_prompt_tokens(ptr: *const kreuzcrawl::ExtractionMeta) -> u64 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.prompt_tokens {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `completion_tokens` field from a `ExtractionMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_extraction_meta_completion_tokens(ptr: *const kreuzcrawl::ExtractionMeta) -> u64 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.completion_tokens {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `model` field from a `ExtractionMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_extraction_meta_model(ptr: *const kreuzcrawl::ExtractionMeta) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.model {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `chunks_processed` field from a `ExtractionMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_extraction_meta_chunks_processed(ptr: *const kreuzcrawl::ExtractionMeta) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.chunks_processed
}

/// Create a `ProxyConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_proxy_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_proxy_config_from_json(json: *const c_char) -> *mut kreuzcrawl::ProxyConfig {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::ProxyConfig>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ProxyConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_proxy_config_to_json(ptr: *const kreuzcrawl::ProxyConfig) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ProxyConfig` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_proxy_config_free(ptr: *mut kreuzcrawl::ProxyConfig) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `ProxyConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_proxy_config_url(ptr: *const kreuzcrawl::ProxyConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `username` field from a `ProxyConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_proxy_config_username(ptr: *const kreuzcrawl::ProxyConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.username {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `password` field from a `ProxyConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_proxy_config_password(ptr: *const kreuzcrawl::ProxyConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.password {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `BrowserConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_browser_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_from_json(json: *const c_char) -> *mut kreuzcrawl::BrowserConfig {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::BrowserConfig>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `BrowserConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_to_json(ptr: *const kreuzcrawl::BrowserConfig) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `BrowserConfig` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_free(ptr: *mut kreuzcrawl::BrowserConfig) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `mode` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_mode(
    ptr: *const kreuzcrawl::BrowserConfig,
) -> *mut kreuzcrawl::BrowserMode {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.mode.clone()))
}

/// Get the `endpoint` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_endpoint(
    ptr: *const kreuzcrawl::BrowserConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.endpoint {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `timeout` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_timeout(ptr: *const kreuzcrawl::BrowserConfig) -> u64 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.timeout.as_millis() as u64
}

/// Get the `wait` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_wait(
    ptr: *const kreuzcrawl::BrowserConfig,
) -> *mut kreuzcrawl::BrowserWait {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.wait.clone()))
}

/// Get the `wait_selector` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_wait_selector(
    ptr: *const kreuzcrawl::BrowserConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.wait_selector {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `extra_wait` field from a `BrowserConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_extra_wait(ptr: *const kreuzcrawl::BrowserConfig) -> u64 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.extra_wait {
        Some(val) => val.as_millis() as u64,
        None => 0,
    }
}

/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_config_default() -> *mut kreuzcrawl::BrowserConfig {
    clear_last_error();
    let result = kreuzcrawl::BrowserConfig::default();
    Box::into_raw(Box::new(result))
}

/// Create a `CrawlConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_crawl_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_from_json(json: *const c_char) -> *mut kreuzcrawl::CrawlConfig {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::CrawlConfig>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `CrawlConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_to_json(ptr: *const kreuzcrawl::CrawlConfig) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `CrawlConfig` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_free(ptr: *mut kreuzcrawl::CrawlConfig) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `max_depth` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_max_depth(ptr: *const kreuzcrawl::CrawlConfig) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.max_depth {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `max_pages` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_max_pages(ptr: *const kreuzcrawl::CrawlConfig) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.max_pages {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `max_concurrent` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_max_concurrent(ptr: *const kreuzcrawl::CrawlConfig) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.max_concurrent {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `respect_robots_txt` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_respect_robots_txt(ptr: *const kreuzcrawl::CrawlConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.respect_robots_txt as i32
}

/// Get the `user_agent` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_user_agent(ptr: *const kreuzcrawl::CrawlConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.user_agent {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `stay_on_domain` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_stay_on_domain(ptr: *const kreuzcrawl::CrawlConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.stay_on_domain as i32
}

/// Get the `allow_subdomains` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_allow_subdomains(ptr: *const kreuzcrawl::CrawlConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.allow_subdomains as i32
}

/// Get the `include_paths` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_include_paths(
    ptr: *const kreuzcrawl::CrawlConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.include_paths) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `exclude_paths` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_exclude_paths(
    ptr: *const kreuzcrawl::CrawlConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.exclude_paths) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `custom_headers` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_custom_headers(
    ptr: *const kreuzcrawl::CrawlConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.custom_headers) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `request_timeout` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_request_timeout(ptr: *const kreuzcrawl::CrawlConfig) -> u64 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.request_timeout.as_millis() as u64
}

/// Get the `max_redirects` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_max_redirects(ptr: *const kreuzcrawl::CrawlConfig) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.max_redirects
}

/// Get the `retry_count` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_retry_count(ptr: *const kreuzcrawl::CrawlConfig) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.retry_count
}

/// Get the `retry_codes` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_retry_codes(ptr: *const kreuzcrawl::CrawlConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.retry_codes) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `cookies_enabled` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_cookies_enabled(ptr: *const kreuzcrawl::CrawlConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.cookies_enabled as i32
}

/// Get the `auth` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_auth(ptr: *const kreuzcrawl::CrawlConfig) -> *mut kreuzcrawl::AuthConfig {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.auth {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `max_body_size` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_max_body_size(ptr: *const kreuzcrawl::CrawlConfig) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.max_body_size {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `main_content_only` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_main_content_only(ptr: *const kreuzcrawl::CrawlConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.main_content_only as i32
}

/// Get the `remove_tags` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_remove_tags(ptr: *const kreuzcrawl::CrawlConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.remove_tags) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `map_limit` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_map_limit(ptr: *const kreuzcrawl::CrawlConfig) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.map_limit {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `map_search` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_map_search(ptr: *const kreuzcrawl::CrawlConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.map_search {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `download_assets` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_download_assets(ptr: *const kreuzcrawl::CrawlConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.download_assets as i32
}

/// Get the `asset_types` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_asset_types(ptr: *const kreuzcrawl::CrawlConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.asset_types) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `max_asset_size` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_max_asset_size(ptr: *const kreuzcrawl::CrawlConfig) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.max_asset_size {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `browser` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_browser(
    ptr: *const kreuzcrawl::CrawlConfig,
) -> *mut kreuzcrawl::BrowserConfig {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.browser.clone()))
}

/// Get the `proxy` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_proxy(
    ptr: *const kreuzcrawl::CrawlConfig,
) -> *mut kreuzcrawl::ProxyConfig {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.proxy {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `user_agents` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_user_agents(ptr: *const kreuzcrawl::CrawlConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.user_agents) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `capture_screenshot` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_capture_screenshot(ptr: *const kreuzcrawl::CrawlConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.capture_screenshot as i32
}

/// Get the `download_documents` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_download_documents(ptr: *const kreuzcrawl::CrawlConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.download_documents as i32
}

/// Get the `document_max_size` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_document_max_size(ptr: *const kreuzcrawl::CrawlConfig) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.document_max_size {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `document_mime_types` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_document_mime_types(
    ptr: *const kreuzcrawl::CrawlConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.document_mime_types) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `warc_output` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_warc_output(ptr: *const kreuzcrawl::CrawlConfig) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.warc_output {
        Some(val) => match CString::new(val.to_string_lossy().to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `browser_profile` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_browser_profile(
    ptr: *const kreuzcrawl::CrawlConfig,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.browser_profile {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `save_browser_profile` field from a `CrawlConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_save_browser_profile(ptr: *const kreuzcrawl::CrawlConfig) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.save_browser_profile as i32
}

/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_default() -> *mut kreuzcrawl::CrawlConfig {
    clear_last_error();
    let result = kreuzcrawl::CrawlConfig::default();
    Box::into_raw(Box::new(result))
}

/// Validate the configuration, returning an error if any values are invalid.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_config_validate(this: *const kreuzcrawl::CrawlConfig) -> i32 {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return -1;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };
    let result = obj.validate();
    match result {
        Ok(()) => 0,
        Err(e) => {
            set_last_error(2, &e.to_string());
            -1
        }
    }
}

/// Create a `DownloadedDocument` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_downloaded_document_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_document_from_json(
    json: *const c_char,
) -> *mut kreuzcrawl::DownloadedDocument {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::DownloadedDocument>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `DownloadedDocument` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_document_to_json(ptr: *const kreuzcrawl::DownloadedDocument) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `DownloadedDocument` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_document_free(ptr: *mut kreuzcrawl::DownloadedDocument) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `DownloadedDocument`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_document_url(
    ptr: *const kreuzcrawl::DownloadedDocument,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `content` field from a `DownloadedDocument`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_document_content(
    ptr: *const kreuzcrawl::DownloadedDocument,
    out_len: *mut usize,
) -> *mut u8 {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    let data = &obj.content;
    if !out_len.is_null() {
        unsafe {
            *out_len = data.len();
        }
    }
    data.as_ptr() as *mut u8
}

/// Get the `size` field from a `DownloadedDocument`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_document_size(ptr: *const kreuzcrawl::DownloadedDocument) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.size
}

/// Create a `InteractionResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_interaction_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_interaction_result_from_json(
    json: *const c_char,
) -> *mut kreuzcrawl::InteractionResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::InteractionResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `InteractionResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_interaction_result_to_json(ptr: *const kreuzcrawl::InteractionResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `InteractionResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_interaction_result_free(ptr: *mut kreuzcrawl::InteractionResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `action_results` field from a `InteractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_interaction_result_action_results(
    ptr: *const kreuzcrawl::InteractionResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.action_results) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `final_html` field from a `InteractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_interaction_result_final_html(
    ptr: *const kreuzcrawl::InteractionResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.final_html.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `final_url` field from a `InteractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_interaction_result_final_url(
    ptr: *const kreuzcrawl::InteractionResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.final_url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `screenshot` field from a `InteractionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_interaction_result_screenshot(ptr: *const kreuzcrawl::InteractionResult) -> *mut u8 {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.screenshot {
        Some(val) => val.as_ptr() as *mut u8,
        None => std::ptr::null_mut(),
    }
}

/// Create a `ActionResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_action_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_action_result_from_json(json: *const c_char) -> *mut kreuzcrawl::ActionResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::ActionResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ActionResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_action_result_to_json(ptr: *const kreuzcrawl::ActionResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ActionResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_action_result_free(ptr: *mut kreuzcrawl::ActionResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `action_index` field from a `ActionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_action_result_action_index(ptr: *const kreuzcrawl::ActionResult) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.action_index
}

/// Get the `success` field from a `ActionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_action_result_success(ptr: *const kreuzcrawl::ActionResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.success as i32
}

/// Get the `data` field from a `ActionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_action_result_data(ptr: *const kreuzcrawl::ActionResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.data {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `error` field from a `ActionResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_action_result_error(ptr: *const kreuzcrawl::ActionResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.error {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `ScrapeResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_scrape_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_from_json(json: *const c_char) -> *mut kreuzcrawl::ScrapeResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::ScrapeResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ScrapeResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_to_json(ptr: *const kreuzcrawl::ScrapeResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ScrapeResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_free(ptr: *mut kreuzcrawl::ScrapeResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `status_code` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_status_code(ptr: *const kreuzcrawl::ScrapeResult) -> u16 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.status_code
}

/// Get the `content_type` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_content_type(
    ptr: *const kreuzcrawl::ScrapeResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.content_type.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `html` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_html(ptr: *const kreuzcrawl::ScrapeResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.html.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `body_size` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_body_size(ptr: *const kreuzcrawl::ScrapeResult) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.body_size
}

/// Get the `metadata` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_metadata(
    ptr: *const kreuzcrawl::ScrapeResult,
) -> *mut kreuzcrawl::PageMetadata {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.metadata.clone()))
}

/// Get the `links` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_links(ptr: *const kreuzcrawl::ScrapeResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.links) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `images` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_images(ptr: *const kreuzcrawl::ScrapeResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.images) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `feeds` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_feeds(ptr: *const kreuzcrawl::ScrapeResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.feeds) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `json_ld` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_json_ld(ptr: *const kreuzcrawl::ScrapeResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.json_ld) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `is_allowed` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_is_allowed(ptr: *const kreuzcrawl::ScrapeResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.is_allowed as i32
}

/// Get the `crawl_delay` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_crawl_delay(ptr: *const kreuzcrawl::ScrapeResult) -> u64 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.crawl_delay {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `noindex_detected` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_noindex_detected(ptr: *const kreuzcrawl::ScrapeResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.noindex_detected as i32
}

/// Get the `nofollow_detected` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_nofollow_detected(ptr: *const kreuzcrawl::ScrapeResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.nofollow_detected as i32
}

/// Get the `x_robots_tag` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_x_robots_tag(
    ptr: *const kreuzcrawl::ScrapeResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.x_robots_tag {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `is_pdf` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_is_pdf(ptr: *const kreuzcrawl::ScrapeResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.is_pdf as i32
}

/// Get the `was_skipped` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_was_skipped(ptr: *const kreuzcrawl::ScrapeResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.was_skipped as i32
}

/// Get the `detected_charset` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_detected_charset(
    ptr: *const kreuzcrawl::ScrapeResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.detected_charset {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `main_content_only` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_main_content_only(ptr: *const kreuzcrawl::ScrapeResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.main_content_only as i32
}

/// Get the `auth_header_sent` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_auth_header_sent(ptr: *const kreuzcrawl::ScrapeResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.auth_header_sent as i32
}

/// Get the `response_meta` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_response_meta(
    ptr: *const kreuzcrawl::ScrapeResult,
) -> *mut kreuzcrawl::ResponseMeta {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.response_meta {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `assets` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_assets(ptr: *const kreuzcrawl::ScrapeResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.assets) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `js_render_hint` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_js_render_hint(ptr: *const kreuzcrawl::ScrapeResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.js_render_hint as i32
}

/// Get the `browser_used` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_browser_used(ptr: *const kreuzcrawl::ScrapeResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.browser_used as i32
}

/// Get the `markdown` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_markdown(
    ptr: *const kreuzcrawl::ScrapeResult,
) -> *mut kreuzcrawl::MarkdownResult {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.markdown {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `extracted_data` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_extracted_data(
    ptr: *const kreuzcrawl::ScrapeResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.extracted_data {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `extraction_meta` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_extraction_meta(
    ptr: *const kreuzcrawl::ScrapeResult,
) -> *mut kreuzcrawl::ExtractionMeta {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.extraction_meta {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `screenshot` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_screenshot(ptr: *const kreuzcrawl::ScrapeResult) -> *mut u8 {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.screenshot {
        Some(val) => val.as_ptr() as *mut u8,
        None => std::ptr::null_mut(),
    }
}

/// Get the `downloaded_document` field from a `ScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape_result_downloaded_document(
    ptr: *const kreuzcrawl::ScrapeResult,
) -> *mut kreuzcrawl::DownloadedDocument {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.downloaded_document {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Create a `CrawlPageResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_crawl_page_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_from_json(json: *const c_char) -> *mut kreuzcrawl::CrawlPageResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::CrawlPageResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `CrawlPageResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_to_json(ptr: *const kreuzcrawl::CrawlPageResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `CrawlPageResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_free(ptr: *mut kreuzcrawl::CrawlPageResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_url(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `normalized_url` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_normalized_url(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.normalized_url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `status_code` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_status_code(ptr: *const kreuzcrawl::CrawlPageResult) -> u16 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.status_code
}

/// Get the `content_type` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_content_type(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.content_type.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `html` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_html(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.html.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `body_size` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_body_size(ptr: *const kreuzcrawl::CrawlPageResult) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.body_size
}

/// Get the `metadata` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_metadata(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut kreuzcrawl::PageMetadata {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.metadata.clone()))
}

/// Get the `links` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_links(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.links) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `images` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_images(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.images) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `feeds` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_feeds(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.feeds) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `json_ld` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_json_ld(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.json_ld) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `depth` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_depth(ptr: *const kreuzcrawl::CrawlPageResult) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.depth
}

/// Get the `stayed_on_domain` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_stayed_on_domain(ptr: *const kreuzcrawl::CrawlPageResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.stayed_on_domain as i32
}

/// Get the `was_skipped` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_was_skipped(ptr: *const kreuzcrawl::CrawlPageResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.was_skipped as i32
}

/// Get the `is_pdf` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_is_pdf(ptr: *const kreuzcrawl::CrawlPageResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.is_pdf as i32
}

/// Get the `detected_charset` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_detected_charset(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.detected_charset {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `markdown` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_markdown(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut kreuzcrawl::MarkdownResult {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.markdown {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `extracted_data` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_extracted_data(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.extracted_data {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `extraction_meta` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_extraction_meta(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut kreuzcrawl::ExtractionMeta {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.extraction_meta {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `downloaded_document` field from a `CrawlPageResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_page_result_downloaded_document(
    ptr: *const kreuzcrawl::CrawlPageResult,
) -> *mut kreuzcrawl::DownloadedDocument {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.downloaded_document {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Create a `CrawlResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_crawl_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_from_json(json: *const c_char) -> *mut kreuzcrawl::CrawlResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::CrawlResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `CrawlResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_to_json(ptr: *const kreuzcrawl::CrawlResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `CrawlResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_free(ptr: *mut kreuzcrawl::CrawlResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `pages` field from a `CrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_pages(ptr: *const kreuzcrawl::CrawlResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.pages) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `final_url` field from a `CrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_final_url(ptr: *const kreuzcrawl::CrawlResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.final_url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `redirect_count` field from a `CrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_redirect_count(ptr: *const kreuzcrawl::CrawlResult) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.redirect_count
}

/// Get the `was_skipped` field from a `CrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_was_skipped(ptr: *const kreuzcrawl::CrawlResult) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.was_skipped as i32
}

/// Get the `error` field from a `CrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_error(ptr: *const kreuzcrawl::CrawlResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.error {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `cookies` field from a `CrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_cookies(ptr: *const kreuzcrawl::CrawlResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.cookies) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `normalized_urls` field from a `CrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_normalized_urls(
    ptr: *const kreuzcrawl::CrawlResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.normalized_urls) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Returns the count of unique normalized URLs encountered during crawling.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_result_unique_normalized_urls(this: *const kreuzcrawl::CrawlResult) -> usize {
    clear_last_error();
    if this.is_null() {
        set_last_error(1, "Null pointer passed for self");
        return 0;
    }
    // SAFETY: null check above guarantees this is a valid pointer.
    let obj = unsafe { &*this };

    obj.unique_normalized_urls()
}

/// Create a `SitemapUrl` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_sitemap_url_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_sitemap_url_from_json(json: *const c_char) -> *mut kreuzcrawl::SitemapUrl {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::SitemapUrl>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `SitemapUrl` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_sitemap_url_to_json(ptr: *const kreuzcrawl::SitemapUrl) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `SitemapUrl` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_sitemap_url_free(ptr: *mut kreuzcrawl::SitemapUrl) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `SitemapUrl`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_sitemap_url_url(ptr: *const kreuzcrawl::SitemapUrl) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `lastmod` field from a `SitemapUrl`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_sitemap_url_lastmod(ptr: *const kreuzcrawl::SitemapUrl) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.lastmod {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `changefreq` field from a `SitemapUrl`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_sitemap_url_changefreq(ptr: *const kreuzcrawl::SitemapUrl) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.changefreq {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `priority` field from a `SitemapUrl`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_sitemap_url_priority(ptr: *const kreuzcrawl::SitemapUrl) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.priority {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `MapResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_map_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_map_result_from_json(json: *const c_char) -> *mut kreuzcrawl::MapResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::MapResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `MapResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_map_result_to_json(ptr: *const kreuzcrawl::MapResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `MapResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_map_result_free(ptr: *mut kreuzcrawl::MapResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `urls` field from a `MapResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_map_result_urls(ptr: *const kreuzcrawl::MapResult) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.urls) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `MarkdownResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_markdown_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_markdown_result_from_json(json: *const c_char) -> *mut kreuzcrawl::MarkdownResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::MarkdownResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `MarkdownResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_markdown_result_to_json(ptr: *const kreuzcrawl::MarkdownResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `MarkdownResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_markdown_result_free(ptr: *mut kreuzcrawl::MarkdownResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `content` field from a `MarkdownResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_markdown_result_content(
    ptr: *const kreuzcrawl::MarkdownResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.content.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `document_structure` field from a `MarkdownResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_markdown_result_document_structure(
    ptr: *const kreuzcrawl::MarkdownResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.document_structure {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `tables` field from a `MarkdownResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_markdown_result_tables(
    ptr: *const kreuzcrawl::MarkdownResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.tables) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `warnings` field from a `MarkdownResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_markdown_result_warnings(
    ptr: *const kreuzcrawl::MarkdownResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.warnings) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `citations` field from a `MarkdownResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_markdown_result_citations(
    ptr: *const kreuzcrawl::MarkdownResult,
) -> *mut kreuzcrawl::citations::CitationResult {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.citations {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `fit_content` field from a `MarkdownResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_markdown_result_fit_content(
    ptr: *const kreuzcrawl::MarkdownResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.fit_content {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `CachedPage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_cached_page_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_from_json(json: *const c_char) -> *mut kreuzcrawl::CachedPage {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::CachedPage>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `CachedPage` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_to_json(ptr: *const kreuzcrawl::CachedPage) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `CachedPage` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_free(ptr: *mut kreuzcrawl::CachedPage) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `CachedPage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_url(ptr: *const kreuzcrawl::CachedPage) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `status_code` field from a `CachedPage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_status_code(ptr: *const kreuzcrawl::CachedPage) -> u16 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.status_code
}

/// Get the `content_type` field from a `CachedPage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_content_type(ptr: *const kreuzcrawl::CachedPage) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.content_type.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `body` field from a `CachedPage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_body(ptr: *const kreuzcrawl::CachedPage) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.body.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `etag` field from a `CachedPage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_etag(ptr: *const kreuzcrawl::CachedPage) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.etag {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `last_modified` field from a `CachedPage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_last_modified(ptr: *const kreuzcrawl::CachedPage) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.last_modified {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `cached_at` field from a `CachedPage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cached_page_cached_at(ptr: *const kreuzcrawl::CachedPage) -> u64 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.cached_at
}

/// Create a `LinkInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_link_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_info_from_json(json: *const c_char) -> *mut kreuzcrawl::LinkInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::LinkInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `LinkInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_info_to_json(ptr: *const kreuzcrawl::LinkInfo) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `LinkInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_info_free(ptr: *mut kreuzcrawl::LinkInfo) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `LinkInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_info_url(ptr: *const kreuzcrawl::LinkInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `text` field from a `LinkInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_info_text(ptr: *const kreuzcrawl::LinkInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.text.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `link_type` field from a `LinkInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_info_link_type(ptr: *const kreuzcrawl::LinkInfo) -> *mut kreuzcrawl::LinkType {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.link_type.clone()))
}

/// Get the `rel` field from a `LinkInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_info_rel(ptr: *const kreuzcrawl::LinkInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.rel {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `nofollow` field from a `LinkInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_info_nofollow(ptr: *const kreuzcrawl::LinkInfo) -> i32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.nofollow as i32
}

/// Create a `ImageInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_image_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_info_from_json(json: *const c_char) -> *mut kreuzcrawl::ImageInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::ImageInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ImageInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_info_to_json(ptr: *const kreuzcrawl::ImageInfo) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ImageInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_info_free(ptr: *mut kreuzcrawl::ImageInfo) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `ImageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_info_url(ptr: *const kreuzcrawl::ImageInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `alt` field from a `ImageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_info_alt(ptr: *const kreuzcrawl::ImageInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.alt {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `width` field from a `ImageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_info_width(ptr: *const kreuzcrawl::ImageInfo) -> u32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.width {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `height` field from a `ImageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_info_height(ptr: *const kreuzcrawl::ImageInfo) -> u32 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.height {
        Some(val) => *val,
        None => 0,
    }
}

/// Get the `source` field from a `ImageInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_info_source(ptr: *const kreuzcrawl::ImageInfo) -> *mut kreuzcrawl::ImageSource {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.source.clone()))
}

/// Create a `FeedInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_feed_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_feed_info_from_json(json: *const c_char) -> *mut kreuzcrawl::FeedInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::FeedInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `FeedInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_feed_info_to_json(ptr: *const kreuzcrawl::FeedInfo) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `FeedInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_feed_info_free(ptr: *mut kreuzcrawl::FeedInfo) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `FeedInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_feed_info_url(ptr: *const kreuzcrawl::FeedInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `title` field from a `FeedInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_feed_info_title(ptr: *const kreuzcrawl::FeedInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.title {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `feed_type` field from a `FeedInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_feed_info_feed_type(ptr: *const kreuzcrawl::FeedInfo) -> *mut kreuzcrawl::FeedType {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.feed_type.clone()))
}

/// Create a `JsonLdEntry` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_json_ld_entry_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_json_ld_entry_from_json(json: *const c_char) -> *mut kreuzcrawl::JsonLdEntry {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::JsonLdEntry>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `JsonLdEntry` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_json_ld_entry_to_json(ptr: *const kreuzcrawl::JsonLdEntry) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `JsonLdEntry` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_json_ld_entry_free(ptr: *mut kreuzcrawl::JsonLdEntry) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `schema_type` field from a `JsonLdEntry`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_json_ld_entry_schema_type(
    ptr: *const kreuzcrawl::JsonLdEntry,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.schema_type.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `name` field from a `JsonLdEntry`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_json_ld_entry_name(ptr: *const kreuzcrawl::JsonLdEntry) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.name {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `raw` field from a `JsonLdEntry`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_json_ld_entry_raw(ptr: *const kreuzcrawl::JsonLdEntry) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.raw.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `CookieInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_cookie_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cookie_info_from_json(json: *const c_char) -> *mut kreuzcrawl::CookieInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::CookieInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `CookieInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cookie_info_to_json(ptr: *const kreuzcrawl::CookieInfo) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `CookieInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cookie_info_free(ptr: *mut kreuzcrawl::CookieInfo) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `name` field from a `CookieInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cookie_info_name(ptr: *const kreuzcrawl::CookieInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.name.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `value` field from a `CookieInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cookie_info_value(ptr: *const kreuzcrawl::CookieInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.value.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `domain` field from a `CookieInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cookie_info_domain(ptr: *const kreuzcrawl::CookieInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.domain {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `path` field from a `CookieInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_cookie_info_path(ptr: *const kreuzcrawl::CookieInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.path {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `DownloadedAsset` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_downloaded_asset_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_asset_from_json(json: *const c_char) -> *mut kreuzcrawl::DownloadedAsset {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::DownloadedAsset>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `DownloadedAsset` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_asset_to_json(ptr: *const kreuzcrawl::DownloadedAsset) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `DownloadedAsset` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_asset_free(ptr: *mut kreuzcrawl::DownloadedAsset) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `DownloadedAsset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_asset_url(ptr: *const kreuzcrawl::DownloadedAsset) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `content_hash` field from a `DownloadedAsset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_asset_content_hash(
    ptr: *const kreuzcrawl::DownloadedAsset,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.content_hash.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `mime_type` field from a `DownloadedAsset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_asset_mime_type(
    ptr: *const kreuzcrawl::DownloadedAsset,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.mime_type {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `size` field from a `DownloadedAsset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_asset_size(ptr: *const kreuzcrawl::DownloadedAsset) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.size
}

/// Get the `asset_category` field from a `DownloadedAsset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_asset_asset_category(
    ptr: *const kreuzcrawl::DownloadedAsset,
) -> *mut kreuzcrawl::AssetCategory {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    Box::into_raw(Box::new(obj.asset_category.clone()))
}

/// Get the `html_tag` field from a `DownloadedAsset`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_downloaded_asset_html_tag(
    ptr: *const kreuzcrawl::DownloadedAsset,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.html_tag {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `ArticleMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_article_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_article_metadata_from_json(json: *const c_char) -> *mut kreuzcrawl::ArticleMetadata {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::ArticleMetadata>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ArticleMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_article_metadata_to_json(ptr: *const kreuzcrawl::ArticleMetadata) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ArticleMetadata` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_article_metadata_free(ptr: *mut kreuzcrawl::ArticleMetadata) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `published_time` field from a `ArticleMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_article_metadata_published_time(
    ptr: *const kreuzcrawl::ArticleMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.published_time {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `modified_time` field from a `ArticleMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_article_metadata_modified_time(
    ptr: *const kreuzcrawl::ArticleMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.modified_time {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `author` field from a `ArticleMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_article_metadata_author(
    ptr: *const kreuzcrawl::ArticleMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.author {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `section` field from a `ArticleMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_article_metadata_section(
    ptr: *const kreuzcrawl::ArticleMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.section {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `tags` field from a `ArticleMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_article_metadata_tags(
    ptr: *const kreuzcrawl::ArticleMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.tags) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `HreflangEntry` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_hreflang_entry_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_hreflang_entry_from_json(json: *const c_char) -> *mut kreuzcrawl::HreflangEntry {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::HreflangEntry>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `HreflangEntry` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_hreflang_entry_to_json(ptr: *const kreuzcrawl::HreflangEntry) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `HreflangEntry` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_hreflang_entry_free(ptr: *mut kreuzcrawl::HreflangEntry) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `lang` field from a `HreflangEntry`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_hreflang_entry_lang(ptr: *const kreuzcrawl::HreflangEntry) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.lang.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `url` field from a `HreflangEntry`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_hreflang_entry_url(ptr: *const kreuzcrawl::HreflangEntry) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `FaviconInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_favicon_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_favicon_info_from_json(json: *const c_char) -> *mut kreuzcrawl::FaviconInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::FaviconInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `FaviconInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_favicon_info_to_json(ptr: *const kreuzcrawl::FaviconInfo) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `FaviconInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_favicon_info_free(ptr: *mut kreuzcrawl::FaviconInfo) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `FaviconInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_favicon_info_url(ptr: *const kreuzcrawl::FaviconInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `rel` field from a `FaviconInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_favicon_info_rel(ptr: *const kreuzcrawl::FaviconInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.rel.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `sizes` field from a `FaviconInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_favicon_info_sizes(ptr: *const kreuzcrawl::FaviconInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.sizes {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `mime_type` field from a `FaviconInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_favicon_info_mime_type(ptr: *const kreuzcrawl::FaviconInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.mime_type {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `HeadingInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_heading_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_heading_info_from_json(json: *const c_char) -> *mut kreuzcrawl::HeadingInfo {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::HeadingInfo>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `HeadingInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_heading_info_to_json(ptr: *const kreuzcrawl::HeadingInfo) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `HeadingInfo` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_heading_info_free(ptr: *mut kreuzcrawl::HeadingInfo) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `level` field from a `HeadingInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_heading_info_level(ptr: *const kreuzcrawl::HeadingInfo) -> u8 {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.level
}

/// Get the `text` field from a `HeadingInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_heading_info_text(ptr: *const kreuzcrawl::HeadingInfo) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.text.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `ResponseMeta` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_response_meta_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_from_json(json: *const c_char) -> *mut kreuzcrawl::ResponseMeta {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::ResponseMeta>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `ResponseMeta` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_to_json(ptr: *const kreuzcrawl::ResponseMeta) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `ResponseMeta` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_free(ptr: *mut kreuzcrawl::ResponseMeta) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `etag` field from a `ResponseMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_etag(ptr: *const kreuzcrawl::ResponseMeta) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.etag {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `last_modified` field from a `ResponseMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_last_modified(
    ptr: *const kreuzcrawl::ResponseMeta,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.last_modified {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `cache_control` field from a `ResponseMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_cache_control(
    ptr: *const kreuzcrawl::ResponseMeta,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.cache_control {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `server` field from a `ResponseMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_server(ptr: *const kreuzcrawl::ResponseMeta) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.server {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `x_powered_by` field from a `ResponseMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_x_powered_by(
    ptr: *const kreuzcrawl::ResponseMeta,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.x_powered_by {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `content_language` field from a `ResponseMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_content_language(
    ptr: *const kreuzcrawl::ResponseMeta,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.content_language {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `content_encoding` field from a `ResponseMeta`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_response_meta_content_encoding(
    ptr: *const kreuzcrawl::ResponseMeta,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.content_encoding {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `PageMetadata` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_page_metadata_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_from_json(json: *const c_char) -> *mut kreuzcrawl::PageMetadata {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::PageMetadata>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `PageMetadata` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_to_json(ptr: *const kreuzcrawl::PageMetadata) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `PageMetadata` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_free(ptr: *mut kreuzcrawl::PageMetadata) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `title` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_title(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.title {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `description` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_description(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.description {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `canonical_url` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_canonical_url(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.canonical_url {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `keywords` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_keywords(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.keywords {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `author` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_author(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.author {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `viewport` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_viewport(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.viewport {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `theme_color` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_theme_color(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.theme_color {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `generator` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_generator(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.generator {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `robots` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_robots(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.robots {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `html_lang` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_html_lang(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.html_lang {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `html_dir` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_html_dir(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.html_dir {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_title` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_title(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_title {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_type` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_type(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_type {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_image` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_image(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_image {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_description` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_description(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_description {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_url` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_url(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_url {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_site_name` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_site_name(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_site_name {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_locale` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_locale(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_locale {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_video` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_video(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_video {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_audio` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_audio(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_audio {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `og_locale_alternates` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_og_locale_alternates(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.og_locale_alternates {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `twitter_card` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_twitter_card(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.twitter_card {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `twitter_title` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_twitter_title(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.twitter_title {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `twitter_description` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_twitter_description(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.twitter_description {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `twitter_image` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_twitter_image(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.twitter_image {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `twitter_site` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_twitter_site(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.twitter_site {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `twitter_creator` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_twitter_creator(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.twitter_creator {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_title` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_title(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_title {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_creator` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_creator(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_creator {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_subject` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_subject(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_subject {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_description` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_description(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_description {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_publisher` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_publisher(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_publisher {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_date` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_date(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_date {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_type` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_type(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_type {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_format` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_format(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_format {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_identifier` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_identifier(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_identifier {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_language` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_language(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_language {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `dc_rights` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_dc_rights(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.dc_rights {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `article` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_article(
    ptr: *const kreuzcrawl::PageMetadata,
) -> *mut kreuzcrawl::ArticleMetadata {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.article {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `hreflangs` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_hreflangs(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.hreflangs {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `favicons` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_favicons(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.favicons {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `headings` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_headings(ptr: *const kreuzcrawl::PageMetadata) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.headings {
        Some(val) => match serde_json::to_string(&val) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => std::ptr::null_mut(),
            },
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Get the `word_count` field from a `PageMetadata`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_page_metadata_word_count(ptr: *const kreuzcrawl::PageMetadata) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    match &obj.word_count {
        Some(val) => *val,
        None => 0,
    }
}

/// Create a `CitationResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_citation_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_result_from_json(json: *const c_char) -> *mut kreuzcrawl::CitationResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::CitationResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `CitationResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_result_to_json(ptr: *const kreuzcrawl::CitationResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `CitationResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_result_free(ptr: *mut kreuzcrawl::CitationResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `content` field from a `CitationResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_result_content(
    ptr: *const kreuzcrawl::CitationResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.content.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `references` field from a `CitationResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_result_references(
    ptr: *const kreuzcrawl::CitationResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match serde_json::to_string(&obj.references) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Create a `CitationReference` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_citation_reference_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_reference_from_json(
    json: *const c_char,
) -> *mut kreuzcrawl::CitationReference {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::CitationReference>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `CitationReference` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_reference_to_json(ptr: *const kreuzcrawl::CitationReference) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `CitationReference` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_reference_free(ptr: *mut kreuzcrawl::CitationReference) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `index` field from a `CitationReference`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_reference_index(ptr: *const kreuzcrawl::CitationReference) -> usize {
    if ptr.is_null() {
        return 0;
    }
    let obj = unsafe { &*ptr };
    obj.index
}

/// Get the `url` field from a `CitationReference`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_reference_url(
    ptr: *const kreuzcrawl::CitationReference,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `text` field from a `CitationReference`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_citation_reference_text(
    ptr: *const kreuzcrawl::CitationReference,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.text.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Free a `CrawlEngineHandle` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_engine_handle_free(ptr: *mut kreuzcrawl::CrawlEngineHandle) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Create a `BatchScrapeResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_batch_scrape_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_scrape_result_from_json(
    json: *const c_char,
) -> *mut kreuzcrawl::BatchScrapeResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::BatchScrapeResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `BatchScrapeResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_scrape_result_to_json(ptr: *const kreuzcrawl::BatchScrapeResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `BatchScrapeResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_scrape_result_free(ptr: *mut kreuzcrawl::BatchScrapeResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `BatchScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_scrape_result_url(
    ptr: *const kreuzcrawl::BatchScrapeResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `result` field from a `BatchScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_scrape_result_result(
    ptr: *const kreuzcrawl::BatchScrapeResult,
) -> *mut kreuzcrawl::ScrapeResult {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.result {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `error` field from a `BatchScrapeResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_scrape_result_error(
    ptr: *const kreuzcrawl::BatchScrapeResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.error {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Create a `BatchCrawlResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `kcrawl_batch_crawl_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_crawl_result_from_json(json: *const c_char) -> *mut kreuzcrawl::BatchCrawlResult {
    clear_last_error();
    if json.is_null() {
        set_last_error(1, "Null pointer passed for JSON string");
        return std::ptr::null_mut();
    }
    let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in JSON string");
            return std::ptr::null_mut();
        }
    };
    match serde_json::from_str::<kreuzcrawl::BatchCrawlResult>(c_str) {
        Ok(val) => Box::into_raw(Box::new(val)),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Serialize a `BatchCrawlResult` to a JSON string. Returns null on failure.
/// # Safety
/// `ptr` must be a valid, non-null pointer returned by a `kcrawl` function.
/// The returned string must be freed with `kcrawl_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_crawl_result_to_json(ptr: *const kreuzcrawl::BatchCrawlResult) -> *mut c_char {
    clear_last_error();
    if ptr.is_null() {
        set_last_error(1, "Null pointer passed to to_json");
        return std::ptr::null_mut();
    }
    let val = unsafe { &*ptr };
    match serde_json::to_string(val) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(e) => {
                set_last_error(2, &e.to_string());
                std::ptr::null_mut()
            }
        },
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Free a `BatchCrawlResult` handle.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_crawl_result_free(ptr: *mut kreuzcrawl::BatchCrawlResult) {
    if !ptr.is_null() {
        unsafe {
            drop(Box::from_raw(ptr));
        }
    }
}

/// Get the `url` field from a `BatchCrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_crawl_result_url(
    ptr: *const kreuzcrawl::BatchCrawlResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match CString::new(obj.url.to_string()) {
        Ok(cs) => cs.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

/// Get the `result` field from a `BatchCrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_crawl_result_result(
    ptr: *const kreuzcrawl::BatchCrawlResult,
) -> *mut kreuzcrawl::CrawlResult {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.result {
        Some(val) => Box::into_raw(Box::new(val.clone())),
        None => std::ptr::null_mut(),
    }
}

/// Get the `error` field from a `BatchCrawlResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_crawl_result_error(
    ptr: *const kreuzcrawl::BatchCrawlResult,
) -> *mut std::ffi::c_char {
    if ptr.is_null() {
        return std::ptr::null_mut();
    }
    let obj = unsafe { &*ptr };
    match &obj.error {
        Some(val) => match CString::new(val.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        None => std::ptr::null_mut(),
    }
}

/// Convert an integer to a `BrowserMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_mode_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Auto
        1 => 1, // Always
        2 => 2, // Never
        _ => {
            set_last_error(1, "Invalid BrowserMode variant");
            -1
        }
    }
}

/// Convert a `BrowserMode` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_mode_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Auto" => 0,
        "Always" => 1,
        "Never" => 2,
        _ => {
            set_last_error(1, "Unknown BrowserMode variant");
            -1
        }
    }
}

/// Convert an integer to a `BrowserWait` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_wait_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // NetworkIdle
        1 => 1, // Selector
        2 => 2, // Fixed
        _ => {
            set_last_error(1, "Invalid BrowserWait variant");
            -1
        }
    }
}

/// Convert a `BrowserWait` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_browser_wait_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "NetworkIdle" => 0,
        "Selector" => 1,
        "Fixed" => 2,
        _ => {
            set_last_error(1, "Unknown BrowserWait variant");
            -1
        }
    }
}

/// Convert an integer to a `AuthConfig` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_auth_config_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Basic
        1 => 1, // Bearer
        2 => 2, // Header
        _ => {
            set_last_error(1, "Invalid AuthConfig variant");
            -1
        }
    }
}

/// Convert a `AuthConfig` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_auth_config_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Basic" => 0,
        "Bearer" => 1,
        "Header" => 2,
        _ => {
            set_last_error(1, "Unknown AuthConfig variant");
            -1
        }
    }
}

/// Convert an integer to a `LinkType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_type_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Internal
        1 => 1, // External
        2 => 2, // Anchor
        3 => 3, // Document
        _ => {
            set_last_error(1, "Invalid LinkType variant");
            -1
        }
    }
}

/// Convert a `LinkType` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_link_type_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Internal" => 0,
        "External" => 1,
        "Anchor" => 2,
        "Document" => 3,
        _ => {
            set_last_error(1, "Unknown LinkType variant");
            -1
        }
    }
}

/// Convert an integer to a `ImageSource` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_source_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Img
        1 => 1, // PictureSource
        2 => 2, // OgImage
        3 => 3, // TwitterImage
        _ => {
            set_last_error(1, "Invalid ImageSource variant");
            -1
        }
    }
}

/// Convert a `ImageSource` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_image_source_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Img" => 0,
        "PictureSource" => 1,
        "OgImage" => 2,
        "TwitterImage" => 3,
        _ => {
            set_last_error(1, "Unknown ImageSource variant");
            -1
        }
    }
}

/// Convert an integer to a `FeedType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_feed_type_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Rss
        1 => 1, // Atom
        2 => 2, // JsonFeed
        _ => {
            set_last_error(1, "Invalid FeedType variant");
            -1
        }
    }
}

/// Convert a `FeedType` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_feed_type_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Rss" => 0,
        "Atom" => 1,
        "JsonFeed" => 2,
        _ => {
            set_last_error(1, "Unknown FeedType variant");
            -1
        }
    }
}

/// Convert an integer to a `AssetCategory` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_asset_category_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Document
        1 => 1, // Image
        2 => 2, // Audio
        3 => 3, // Video
        4 => 4, // Font
        5 => 5, // Stylesheet
        6 => 6, // Script
        7 => 7, // Archive
        8 => 8, // Data
        9 => 9, // Other
        _ => {
            set_last_error(1, "Invalid AssetCategory variant");
            -1
        }
    }
}

/// Convert a `AssetCategory` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_asset_category_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Document" => 0,
        "Image" => 1,
        "Audio" => 2,
        "Video" => 3,
        "Font" => 4,
        "Stylesheet" => 5,
        "Script" => 6,
        "Archive" => 7,
        "Data" => 8,
        "Other" => 9,
        _ => {
            set_last_error(1, "Unknown AssetCategory variant");
            -1
        }
    }
}

/// Convert an integer to a `CrawlEvent` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_event_from_i32(value: i32) -> i32 {
    match value {
        0 => 0, // Page
        1 => 1, // Error
        2 => 2, // Complete
        _ => {
            set_last_error(1, "Invalid CrawlEvent variant");
            -1
        }
    }
}

/// Convert a `CrawlEvent` variant name (C string) to its integer value. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl_event_from_str(name: *const c_char) -> i32 {
    if name.is_null() {
        set_last_error(1, "Null pointer passed for enum name");
        return -1;
    }
    let s = match unsafe { CStr::from_ptr(name) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in enum name");
            return -1;
        }
    };
    match s {
        "Page" => 0,
        "Error" => 1,
        "Complete" => 2,
        _ => {
            set_last_error(1, "Unknown CrawlEvent variant");
            -1
        }
    }
}

fn get_ffi_runtime() -> &'static tokio::runtime::Runtime {
    use std::sync::OnceLock;
    static RUNTIME: OnceLock<tokio::runtime::Runtime> = OnceLock::new();
    RUNTIME.get_or_init(|| tokio::runtime::Runtime::new().expect("Failed to create tokio runtime"))
}

/// Create a new crawl engine with the given configuration.
///
/// If `config` is `None`, uses [`CrawlConfig::default()`].
/// Returns an error if the configuration is invalid.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_create_engine(
    config: *const kreuzcrawl::CrawlConfig,
) -> *mut kreuzcrawl::CrawlEngineHandle {
    clear_last_error();
    let config_rs = if config.is_null() {
        None
    } else {
        Some(unsafe { &*config }.clone())
    };
    let result = kreuzcrawl::create_engine(config_rs);
    match result {
        Ok(val) => Box::into_raw(Box::new(val.clone())),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Scrape a single URL, returning extracted page data.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_scrape(
    engine: *const kreuzcrawl::CrawlEngineHandle,
    url: *const std::ffi::c_char,
) -> *mut kreuzcrawl::ScrapeResult {
    clear_last_error();
    if engine.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'engine'");
        return std::ptr::null_mut();
    }
    let engine_rs = unsafe { &*engine }.clone();
    if url.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'url'");
        return std::ptr::null_mut();
    }
    let url_rs = match unsafe { CStr::from_ptr(url) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'url'");
            return std::ptr::null_mut();
        }
    };
    let result = get_ffi_runtime().block_on(async { kreuzcrawl::scrape(&engine_rs, &url_rs).await });
    match result {
        Ok(val) => Box::into_raw(Box::new(val.clone())),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Crawl a website starting from `url`, following links up to the configured depth.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_crawl(
    engine: *const kreuzcrawl::CrawlEngineHandle,
    url: *const std::ffi::c_char,
) -> *mut kreuzcrawl::CrawlResult {
    clear_last_error();
    if engine.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'engine'");
        return std::ptr::null_mut();
    }
    let engine_rs = unsafe { &*engine }.clone();
    if url.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'url'");
        return std::ptr::null_mut();
    }
    let url_rs = match unsafe { CStr::from_ptr(url) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'url'");
            return std::ptr::null_mut();
        }
    };
    let result = get_ffi_runtime().block_on(async { kreuzcrawl::crawl(&engine_rs, &url_rs).await });
    match result {
        Ok(val) => Box::into_raw(Box::new(val.clone())),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Discover all pages on a website by following links and sitemaps.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_map_urls(
    engine: *const kreuzcrawl::CrawlEngineHandle,
    url: *const std::ffi::c_char,
) -> *mut kreuzcrawl::MapResult {
    clear_last_error();
    if engine.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'engine'");
        return std::ptr::null_mut();
    }
    let engine_rs = unsafe { &*engine }.clone();
    if url.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'url'");
        return std::ptr::null_mut();
    }
    let url_rs = match unsafe { CStr::from_ptr(url) }.to_str() {
        Ok(s) => s.to_string(),
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'url'");
            return std::ptr::null_mut();
        }
    };
    let result = get_ffi_runtime().block_on(async { kreuzcrawl::map_urls(&engine_rs, &url_rs).await });
    match result {
        Ok(val) => Box::into_raw(Box::new(val.clone())),
        Err(e) => {
            set_last_error(2, &e.to_string());
            std::ptr::null_mut()
        }
    }
}

/// Scrape multiple URLs concurrently.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_scrape(
    engine: *const kreuzcrawl::CrawlEngineHandle,
    urls: *const std::ffi::c_char,
) -> *mut std::ffi::c_char {
    clear_last_error();
    if engine.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'engine'");
        return std::ptr::null_mut();
    }
    let engine_rs = unsafe { &*engine }.clone();
    if urls.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'urls'");
        return std::ptr::null_mut();
    }
    let urls_rs_str = match unsafe { CStr::from_ptr(urls) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'urls'");
            return std::ptr::null_mut();
        }
    };
    let urls_rs = match serde_json::from_str(urls_rs_str) {
        Ok(v) => v,
        Err(e) => {
            set_last_error(2, &e.to_string());
            return std::ptr::null_mut();
        }
    };
    let result = get_ffi_runtime().block_on(async { kreuzcrawl::batch_scrape(&engine_rs, urls_rs).await });
    match serde_json::to_string(&result) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}

/// Crawl multiple seed URLs concurrently, each following links to configured depth.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn kcrawl_batch_crawl(
    engine: *const kreuzcrawl::CrawlEngineHandle,
    urls: *const std::ffi::c_char,
) -> *mut std::ffi::c_char {
    clear_last_error();
    if engine.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'engine'");
        return std::ptr::null_mut();
    }
    let engine_rs = unsafe { &*engine }.clone();
    if urls.is_null() {
        set_last_error(1, "Null pointer passed for parameter 'urls'");
        return std::ptr::null_mut();
    }
    let urls_rs_str = match unsafe { CStr::from_ptr(urls) }.to_str() {
        Ok(s) => s,
        Err(_) => {
            set_last_error(1, "Invalid UTF-8 in parameter 'urls'");
            return std::ptr::null_mut();
        }
    };
    let urls_rs = match serde_json::from_str(urls_rs_str) {
        Ok(v) => v,
        Err(e) => {
            set_last_error(2, &e.to_string());
            return std::ptr::null_mut();
        }
    };
    let result = get_ffi_runtime().block_on(async { kreuzcrawl::batch_crawl(&engine_rs, urls_rs).await });
    match serde_json::to_string(&result) {
        Ok(s) => match CString::new(s) {
            Ok(cs) => cs.into_raw(),
            Err(_) => std::ptr::null_mut(),
        },
        Err(_) => std::ptr::null_mut(),
    }
}
