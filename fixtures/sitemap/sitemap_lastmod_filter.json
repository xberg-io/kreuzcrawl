{
	"id": "sitemap_lastmod_filter",
	"description": "Filters sitemap URLs by lastmod date",
	"tags": ["sitemap", "lastmod", "map"],
	"input": {
		"mock_responses": [
			{
				"status_code": 200,
				"headers": {
					"content-type": "application/xml"
				},
				"body_file": "xml/sitemap_with_lastmod.xml"
			}
		],
		"config": {
			"respect_robots_txt": false
		}
	},
	"assertions": [
		{
			"type": "equals",
			"field": "urls.length",
			"value": 4
		}
	]
}
