{
	"id": "validation_invalid_auth_config",
	"description": "auth object with empty username in basic auth is rejected",
	"tags": ["validation"],
	"input": {
		"mock_responses": [
			{
				"status_code": 200,
				"body_inline": "<html></html>"
			}
		],
		"config": {
			"auth": {
				"type": "basic",
				"username": "",
				"password": "secret"
			}
		}
	},
	"assertions": [
		{
			"type": "error",
			"value": "auth"
		}
	]
}
