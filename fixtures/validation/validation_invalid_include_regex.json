{
	"id": "validation_invalid_include_regex",
	"description": "Invalid regex in include_paths is rejected",
	"tags": [
		"validation"
	],
	"input": {
		"mock_responses": [
			{
				"status_code": 200,
				"body_inline": "<html></html>"
			}
		],
		"config": {
			"include_paths": [
				"[invalid"
			]
		}
	},
	"assertions": [
		{
			"type": "error",
			"value": "include_path"
		}
	]
}
